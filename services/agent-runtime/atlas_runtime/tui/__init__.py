"""ATLAS native terminal workbench (Phase 10.6) — package marker only.

No re-exports here yet; submodules (`capabilities`, `theme`, `session_select`,
`header`, ...) are imported directly by callers and by later waves' app wiring.
"""
