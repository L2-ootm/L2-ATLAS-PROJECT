"""ATLAS golden workflows — deterministic orchestrators (Phase 10.0.5).

See `atlas_runtime.golden_workflow_service` for the shared orchestrator-core
helpers (ensure_golden_run, record_artifact, emit_workflow_event) that every
workflow module in this package builds on.
"""
