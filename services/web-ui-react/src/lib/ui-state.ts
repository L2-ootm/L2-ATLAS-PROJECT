// Shared cockpit shell UI constants. The sidebar is position:fixed, so the
// layout tracks its width to keep main content from being occluded when expanded.
export const SIDEBAR_WIDTH_COLLAPSED = 64;
export const SIDEBAR_WIDTH_EXPANDED = 224;
export const SIDEBAR_STORAGE_KEY = 'atlas-sidebar-expanded';
