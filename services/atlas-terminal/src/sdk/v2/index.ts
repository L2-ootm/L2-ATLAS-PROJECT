// ATLAS: donor SDK v2 entry trimmed to the client surface. The donor's
// createOpencodeAppServer (self-hosted server spawn) is intentionally not
// vendored — ATLAS is the only backend. See ATTRIBUTION.md.
export * from "./client.js"

export * as data from "./data.js"
