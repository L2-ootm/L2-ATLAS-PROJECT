{
	"name": "atlas-terminal",
	"version": "0.0.1",
	"private": true,
	"description": "ATLAS terminal surface (STAGE 0) — MiMo-Code MIT presentation donor over the ATLAS gateway via the fetch-adapter seam. See docs/plans/2026-07-03-mimo-donor-tui-refactor-plan.md.",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"dev": "bun run --conditions=browser src/main.tsx",
		"smoke": "bun run --conditions=browser src/main.tsx --smoke",
		"test": "bun test",
		"typecheck": "bunx tsc --noEmit"
	},
	"dependencies": {
		"@opentui/core": "0.1.101",
		"@opentui/solid": "0.1.101",
		"solid-js": "1.9.9"
	},
	"devDependencies": {
		"@tsconfig/bun": "1.0.7",
		"@types/bun": "1.3.3",
		"typescript": "5.9.2"
	}
}
