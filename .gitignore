# External repo audit copies — never commit
_EXTERNAL_REPOS/

# Visual brainstorming companion (local only)
.superpowers/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.env
*.env

# Secrets and state
auth.json
*.db
sessions/
*.key
*.pem

# OS
.DS_Store
Thumbs.db

# Local-only files
docs/private/


# Twenty CRM sidecar — fetched upstream files + local secrets (script-managed, never commit)
infra/compose/twenty/

# Generated test/coverage artifacts (PRODUCTION_REPO_STRUCTURE §4.5)
.coverage
coverage.json
coverage_branch.json
htmlcov/
.pytest_cache/

# Playwright CLI session logs (console/page snapshots, audit jsonl) — local only
.playwright-cli/

# Rust build output
target/

# Runtime artifacts directory (kept via artifacts/.gitignore)
artifacts/*
!artifacts/.gitignore
