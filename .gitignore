# External repo audit copies — never commit
_EXTERNAL_REPOS/

# Local debug/session docs — never commit
.debug/

# Visual brainstorming companion (local only)
.superpowers/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.env
*.env

# JavaScript / frontend dependencies and generated output
node_modules/
packages/atlas-cli/*.tgz
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.svelte-kit/
.next/
.vercel/

# Build output — do not commit new generated bundles unless explicitly approved
build/
dist/

# Secrets and state
auth.json
*.db
sessions/
*.key
*.pem

# OS
.DS_Store
Thumbs.db

# Local-only files
docs/private/
CLAUDE-FABLE-5.md
.mimoignore

# Internal planning and session handoffs are not part of the public product
.planning/
HANDOFF.md
l2_atlas_30_day_mass_adoption_wedge_plan.md

# Local tool state and generated scratch
**/.mimocode/
.ops/
*.backup

# Internal Discord development material
services/discord-bot/dev-utils/

# Internal Cashflow research and generated briefing material
services/cashflow/research/
services/cashflow/docs/compose/
services/cashflow/BRIEF.md
services/cashflow/scripts/generate-brief-pdf.py

# Twenty CRM sidecar — fetched upstream files + local secrets (script-managed, never commit)
infra/compose/twenty/

# Generated test/coverage artifacts (PRODUCTION_REPO_STRUCTURE §4.5)
.coverage
coverage.json
coverage_branch.json
htmlcov/
.pytest_cache/

# Playwright CLI session logs (console/page snapshots, audit jsonl) — local only
.playwright-cli/

# Rust build output
target/

# Runtime artifacts directory (kept via artifacts/.gitignore)
artifacts/*
!artifacts/.gitignore

# UAT / Playwright screenshot evidence (transient, not archived)
output/

# Local dev wrapper — contains an absolute path to this machine's venv python
atlas.cmd

# Local Claude Code state (plans, worktrees, settings) — never publish
.claude/

# Kilo/ULTRAPLAN tool scratch (superseded planning drafts) — never publish
.kilo/

# Vendored Discord sidecar (services/discord-bot) — runtime state the bot regenerates.
# Secrets (.env), *.db, __pycache__, .venv, .pytest_cache are already covered above.
services/discord-bot/chroma_db/
services/discord-bot/data/
