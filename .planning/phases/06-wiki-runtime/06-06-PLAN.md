---
phase: 06-wiki-runtime
plan: 06
type: execute
wave: 4
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md
autonomous: true
requirements:
  - WIKI-01
  - WIKI-02
  - WIKI-03
  - WIKI-04
  - WIKI-05
  - AUDIT-03

must_haves:
  truths:
    - "Full wiki-runtime test suite passes at >= 80% branch coverage"
    - "docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md exists with 4 open design questions"
    - "No graph implementation code exists in Phase 6 — research doc only"
    - "atlas-core tests still pass"
    - "atlas-runtime tests still pass"
  artifacts:
    - path: "docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md"
      provides: "Graph memory design questions for v2.0 (no code)"
      contains: "GRAPH"
  key_links:
    - from: "docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md"
      to: "docs/architecture/AGENT_MEMORY_FRAMEWORK_STRATEGY.md"
      via: "extends Layer 4 graph memory design questions (v2.0 scope)"
      pattern: "graph"
---

<objective>
Run the full Phase 6 test suite to confirm coverage gate, then write the graph memory research notes document.

Purpose: All six REQ-IDs (WIKI-01..05 + AUDIT-03) are proven by the test suite passing at >= 80% branch coverage. The graph memory research doc satisfies the D-019 requirement to document Layer 4 design questions in Phase 6 (no implementation). This plan runs in Wave 4 parallel to 06-05 — no file overlap.

Output: Full suite green at >= 80% coverage. docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md with 4 structured design questions.
</objective>

<execution_context>
@C:/Users/Davi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Davi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-wiki-runtime/06-RESEARCH.md
@.planning/phases/06-wiki-runtime/CONTEXT.md

<interfaces>
From 06-RESEARCH.md §"Graph Memory Research (Out of Scope — Document Only)":
The four design questions to document:
1. How should mission → run → artifact → source relationships be represented as a graph? (nodes: Mission, Run, Artifact, Source, WikiPage; edges: produced_by, referenced_by, sourced_from)
2. What graph schema serves "which decisions led to this wiki page?" (tracing AuditEvent → WikiPage → Source chain)
3. Can Graphify-style extraction produce meaningful edges from existing ATLAS artifacts? (existing .planning/graphs/ pattern)
4. What storage backend: dedicated SQLite graph table (adjacency list) vs. embedded graph library vs. Graphify .json format?

From CONTEXT.md §"Graph-memory research (no implementation)":
"These questions go into docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md (to be created). No graph code is implemented in Phase 6."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Phase 6 coverage gate</name>
  <files>none — verification-only task, no files modified</files>
  <read_first>
    - services/wiki-runtime/tests/ (to understand what tests are present before running)
  </read_first>
  <action>
    Run the complete wiki-runtime test suite with coverage gate. Do not modify any production files — this task is verification only.

    Command: pytest services/wiki-runtime/tests/ --cov=atlas_wiki --cov-report=term-missing --cov-fail-under=80 -v

    If the suite fails:
    - If a test fails due to a missing stub from 06-02 (wiki_service stub not yet replaced by 06-03): note the failure clearly in the SUMMARY — this plan depends on 06-03 being complete first
    - If a test fails due to a coverage threshold not met: identify the uncovered branches and note them in the SUMMARY for gap-closure
    - Do NOT add production code in this task to fix coverage — report gaps only

    Also run atlas-core and atlas-runtime suites to confirm no regressions from schema changes in 06-01 and CLI changes in 06-05:
    - pytest packages/atlas-core/ -x -q
    - pytest services/agent-runtime/tests/ -x -q
  </action>
  <verify>
    <automated>cd /c/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT && pytest services/wiki-runtime/tests/ --cov=atlas_wiki --cov-report=term-missing --cov-fail-under=80 -q 2>&1 | tail -20</automated>
  </verify>
  <done>pytest services/wiki-runtime/tests/ exits 0 with --cov-fail-under=80. All 3 test files (test_wiki_service.py, test_provenance_service.py, test_cli.py) contribute. pytest packages/atlas-core/ -x -q exits 0. pytest services/agent-runtime/tests/ -x -q exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md</name>
  <files>docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md</files>
  <read_first>
    - docs/architecture/AGENT_MEMORY_FRAMEWORK_STRATEGY.md (to reference Layer 4 context correctly)
    - .planning/phases/06-wiki-runtime/06-RESEARCH.md §"Graph Memory Research" (the four questions to document)
  </read_first>
  <action>
    Create docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md as a Markdown research document.

    Structure:
    - Title: "# ATLAS Graph Memory — Design Questions (v2.0 Scope)"
    - Metadata block: "**Phase:** 6 (research only — no implementation)", "**Relates to:** D-019 Memory Framework Layer 4", "**Status:** Open questions for v2.0"
    - Brief paragraph: This document captures design questions for ATLAS graph memory (Layer 4 of the D-019 diverse memory framework). Phase 6 implements Layers 2 and 3 (Wiki + optional semantic retrieval). Layer 4 graph memory is scoped to v2.0. These questions frame the design space without committing to an implementation.
    - Section "## Design Questions"

    Four numbered questions (from RESEARCH.md — expand each with 3-5 bullet sub-points):

    Q1: Node/edge schema for mission → run → artifact → source relationships
    - What are the natural node types? (Mission, Run, AuditEvent, Artifact, Source, WikiPage)
    - What are the natural edge types? (produced_by, referenced_by, sourced_from, triggered_by)
    - Can these edges be derived from existing FK relationships in the SQLite schema, or do they require a separate extraction pass?

    Q2: "Which decisions led to this wiki page?" traceability
    - Tracing chain: AuditEvent → WikiPage → Source
    - What metadata must be stored at write time to reconstruct this chain later?
    - How does MemoryProvenance (Phase 6) serve as the entry point for this trace?

    Q3: Graphify-style extraction from existing ATLAS artifacts
    - ATLAS already uses .planning/graphs/graph.json (Graphify format)
    - Can the same extraction pipeline produce edges from wiki pages, audit events, and source documents?
    - What entity resolution is needed to link Graphify-extracted terms to Mission/Source UUIDs?

    Q4: Storage backend options
    - Option A: SQLite adjacency list table (nodes + edges tables) — consistent with D-003, no new dependency
    - Option B: Embedded graph library (networkx, igraph) — richer algorithms, Python-only, not persistent
    - Option C: Extend Graphify .json format — consistent with existing .planning/graphs/ usage
    - Recommendation criterion: pick the option that adds least new infrastructure while supporting "which decisions led to this wiki page?" query

    - Section "## Out of Scope (Phase 6)"
    - Short list: graph schema implementation, graph extraction pipeline, graph query API, graph visualization

    - Section "## Next Steps (v2.0)"
    - Short list: answer Q4 via spike, design node/edge schema, implement extraction from AuditEvent + WikiPage rows, expose via Phase 7 API extension

    Do not include any code blocks, import statements, or implementation details. This is a research questions document only.
  </action>
  <verify>
    <automated>cd /c/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT && python -c "import pathlib; content = pathlib.Path('docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md').read_text(); assert 'Q1' in content or 'Design Questions' in content; assert 'Out of Scope' in content; print('graph notes OK')"</automated>
  </verify>
  <done>docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md exists with all 4 design questions, "Out of Scope (Phase 6)" section, and "Next Steps (v2.0)" section. File contains no Python code blocks or import statements. File references D-019 and MemoryProvenance correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research doc → production code | This doc is research only — no implementation decisions locked in it; all graph work is v2.0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Information Disclosure | Graph memory research doc revealing ATLAS architecture | accept | Document is internal; single-operator v1.0; no external exposure |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
cd /c/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT
# Full Phase 6 suite with coverage
pytest services/wiki-runtime/tests/ \
  --cov=atlas_wiki --cov-report=term-missing --cov-fail-under=80 -q

# No regressions in dependent packages
pytest packages/atlas-core/ -x -q
pytest services/agent-runtime/tests/ -x -q

# Research doc exists
python -c "
import pathlib
p = pathlib.Path('docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md')
assert p.exists(), 'File missing'
text = p.read_text()
for term in ['Design Questions', 'Out of Scope', 'Next Steps', 'D-019', 'MemoryProvenance']:
    assert term in text, f'Missing: {term}'
print('graph notes verified OK')
"
```
</verification>

<success_criteria>
1. pytest services/wiki-runtime/tests/ exits 0 with --cov-fail-under=80.
2. pytest packages/atlas-core/ -x -q exits 0 (no schema regression).
3. pytest services/agent-runtime/tests/ -x -q exits 0 (no CLI regression).
4. docs/research/GRAPH_MEMORY_RESEARCH_NOTES.md exists with Design Questions, Out of Scope, Next Steps sections.
5. Graph research doc contains no code blocks or implementation decisions.
</success_criteria>

<output>
Create .planning/phases/06-wiki-runtime/06-06-SUMMARY.md when done.
</output>
