---
phase: 08-cockpit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs
  - native/atlas-core-rs/crates/atlas-gateway/src/db.rs
autonomous: true
requirements:
  - COCKPIT-02
  - COCKPIT-03
  - COCKPIT-04
  - COCKPIT-05

must_haves:
  truths:
    - "POST /v1/wiki/pages creates a wiki page via atlas CLI dispatch and returns the created page"
    - "PUT /v1/wiki/pages/{slug} updates a wiki page and returns the updated page"
    - "GET /v1/wiki/pages/{slug} returns a single wiki page with body and provenance fields"
    - "GET /v1/models returns the model registry from the database"
    - "POST /v1/missions/{id}/cancel cancels an active run via atlas CLI and returns updated run"
    - "All new endpoints return correct HTTP status codes (201/200/404/422)"
  artifacts:
    - path: "native/atlas-core-rs/crates/atlas-gateway/src/lib.rs"
      provides: "Extended gateway with wiki write + models + cancel endpoints"
      contains: "wiki_create"
    - path: "native/atlas-core-rs/crates/atlas-gateway/src/db.rs"
      provides: "DB read functions for wiki page detail and model registry"
      contains: "get_wiki_page"
  key_links:
    - from: "native/atlas-core-rs/crates/atlas-gateway/src/lib.rs"
      to: "dispatch_atlas"
      via: "wiki create/update/cancel handlers call dispatch_atlas"
      pattern: "dispatch_atlas"
    - from: "native/atlas-core-rs/crates/atlas-gateway/src/db.rs"
      to: "wiki_pages table"
      via: "get_wiki_page SELECT slug,title,body,layer,created_at,updated_at"
      pattern: "get_wiki_page"
---

<objective>
Extend the Phase 7 atlas-gateway with the write-dispatch and detail endpoints Phase 8 cockpit surfaces require but that were not in scope for Phase 7: wiki create/update, single wiki page detail, model registry read, and run cancel.

Purpose: Surface 3 (wiki browser) needs create/update, Surface 4 (model panel) needs the model registry, Surface 2 (run monitoring) needs cancel. These are small Rust additions following the established D-022 dispatch pattern — writes via `atlas` CLI, reads direct SQLite.

Output: Recompiled atlas-gateway binary (debug) with five new endpoints. No new business logic — writes delegate to atlas CLI exactly as create_mission and start_run do.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-cockpit/CONTEXT.md
@.planning/phases/08-cockpit/08-UI-SPEC.md

<interfaces>
<!-- Existing gateway patterns to replicate exactly -->

From native/atlas-core-rs/crates/atlas-gateway/src/lib.rs:

Dispatch pattern (copy for all write handlers):
  dispatch_atlas(&state.atlas_cmd, &["subcommand", "--flag", "value"]).await?

Existing write handlers follow this structure:
  1. extract body fields
  2. call dispatch_atlas with atlas subcommand args
  3. call blocking(move || db::read_back_fn(&path, &id)).await?
  4. return 201 + JSON body

Existing route registration:
  .route("/v1/missions", get(missions_list).post(create_mission))
  .route("/v1/missions/{id}", get(mission_detail))
  .route("/v1/missions/{id}/run", post(start_run))

From native/atlas-core-rs/crates/atlas-gateway/src/db.rs:

wiki_pages table columns (from 0001_core.sql):
  slug TEXT PRIMARY KEY, title TEXT, body TEXT, layer INTEGER,
  created_at TEXT, updated_at TEXT

model_registry table (from 0003 migration, D-017):
  query db table "model_registry" — columns: model_id, provider, tier, health, policy, updated_at

Atlas CLI commands for new dispatch endpoints:
  wiki create:  atlas wiki update --slug {slug} --title {title} --body {body} --layer {layer}
  wiki update:  atlas wiki update --slug {slug} --title {title} --body {body} --layer {layer}
  run cancel:   atlas mission cancel {mission_id}
  (Note: atlas wiki update is an upsert — use same command for create and update)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add wiki page detail + model registry reads to db.rs</name>
  <files>
    native/atlas-core-rs/crates/atlas-gateway/src/db.rs
  </files>
  <read_first>
    - native/atlas-core-rs/crates/atlas-gateway/src/db.rs (read entire file — understand open_ro, row mappers, existing patterns before editing)
    - infra/migrations/0001_core.sql (verify wiki_pages column names)
    - infra/migrations/0003_model_registry.sql (verify model_registry column names — if file does not exist, query the database schema directly using: sqlite3 ~/.atlas/atlas.db ".schema model_registry")
  </read_first>
  <action>
    Add these two read functions to db.rs following the existing row-mapper pattern:

    get_wiki_page(path: &Path, slug: &str) -> Result&lt;Option&lt;Value&gt;, DbError&gt;:
    SELECT slug, title, body, layer, created_at, updated_at FROM wiki_pages WHERE slug = ?1.
    Return None on QueryReturnedNoRows. The JSON Value must include all six fields.
    Also include provenance if the wiki_provenance table exists (LEFT JOIN or separate query — check schema first). If provenance table absent, include "provenance": null in the response.

    list_models(path: &Path, limit: i64) -> Result&lt;Vec&lt;Value&gt;, DbError&gt;:
    SELECT model_id, provider, tier, health, policy, updated_at FROM model_registry ORDER BY tier, model_id LIMIT ?1.
    If the model_registry table does not exist (query returns error with "no such table"), return Ok(vec![]) rather than DbError — the gateway must not 503 when no models are registered.
  </action>
  <verify>
    <automated>cd native/atlas-core-rs && cargo build -p atlas-gateway 2>&1 | grep -E "^error" | head -10; echo "build exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - native/atlas-core-rs/crates/atlas-gateway/src/db.rs contains "pub fn get_wiki_page"
    - native/atlas-core-rs/crates/atlas-gateway/src/db.rs contains "pub fn list_models"
    - cargo build -p atlas-gateway exits 0 with no compile errors
    - get_wiki_page returns Ok(None) for unknown slug (not DbError::Failed)
    - list_models returns Ok(vec![]) when model_registry table absent (no such table error swallowed)
  </acceptance_criteria>
  <done>Two new db read functions compile cleanly. No existing functions modified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add wiki write handlers + models + cancel endpoints to lib.rs</name>
  <files>
    native/atlas-core-rs/crates/atlas-gateway/src/lib.rs
  </files>
  <read_first>
    - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs (read entire file — understand CreateMissionBody, dispatch_atlas, start_run, app() router before editing)
    - native/atlas-core-rs/crates/atlas-gateway/src/db.rs (after Task 1 — need get_wiki_page and list_models signatures)
  </read_first>
  <action>
    Add five new handlers and register five new routes. Follow existing patterns exactly:

    1. wiki_page_detail (GET /v1/wiki/pages/{slug}):
    Path param slug. Call blocking(|| db::get_wiki_page(&path, &slug)).await?. Return 200 { page: ... } or ApiError::NotFound("wiki page").

    2. CreateWikiPageBody struct: { slug: String, title: String, body: String, layer: Option&lt;i64&gt; }.
    wiki_create (POST /v1/wiki/pages): dispatch_atlas args: &["wiki", "update", "--slug", &slug, "--title", &title, "--body", &body, "--layer", &layer_str]. layer defaults to 2 if absent. After dispatch, call db::get_wiki_page to read back. Return 201 { page: ... }.

    3. UpdateWikiPageBody struct: { title: Option&lt;String&gt;, body: Option&lt;String&gt;, layer: Option&lt;i64&gt; }.
    wiki_update (PUT /v1/wiki/pages/{slug}): read current page first to get existing title/body/layer for merge. Dispatch atlas wiki update with merged values. Read back and return 200 { page: ... }. If page not found on pre-read, return 404.

    4. models_list (GET /v1/models): call db::list_models(&path, limit). Return { models: [...], count: N }.

    5. cancel_run (POST /v1/missions/{id}/cancel): dispatch_atlas args: &["mission", "cancel", &id]. On success, attempt to read the most recent run for this mission via db::get_mission and return { mission: ..., message: "run cancelled" }. If atlas returns non-zero, return ApiError::Internal with stderr.

    Register in app() router:
      .route("/v1/wiki/pages", get(wiki_pages).post(wiki_create))
      .route("/v1/wiki/pages/{slug}", get(wiki_page_detail).put(wiki_update))
      .route("/v1/models", get(models_list))
      .route("/v1/missions/{id}/cancel", post(cancel_run))
    (The existing .route("/v1/wiki/pages", get(wiki_pages)) must be merged to get(wiki_pages).post(wiki_create) — do not add a duplicate route.)
  </action>
  <verify>
    <automated>cd native/atlas-core-rs && cargo build -p atlas-gateway 2>&1 | grep -E "^error" | head -10; echo "build exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs contains "async fn wiki_create"
    - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs contains "async fn wiki_update"
    - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs contains "async fn wiki_page_detail"
    - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs contains "async fn models_list"
    - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs contains "async fn cancel_run"
    - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs contains "/v1/wiki/pages/{slug}" and "/v1/models" and "/v1/missions/{id}/cancel"
    - cargo build -p atlas-gateway exits 0
    - No duplicate route registrations for /v1/wiki/pages (must be single .route() call)
  </acceptance_criteria>
  <done>Gateway compiles with all five new endpoints. All writes delegate to atlas CLI via dispatch_atlas. Binary rebuilt in debug mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP body → dispatch_atlas | Title/intent/body strings from cockpit are passed as CLI args to atlas subprocess |
| atlas subprocess | Writes go through atlas policy engine — policy.py still guards workspace boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | wiki_create/wiki_update body inputs | mitigate | Inputs passed as explicit named CLI args (--title, --body, --slug) — not interpolated into shell string; tokio::process::Command args are OS-level argv, not shell injection surface |
| T-08-06 | Tampering | cancel_run dispatch | mitigate | Mission ID comes from URL path param (typed String), passed as positional CLI arg — no shell interpolation |
| T-08-07 | Denial of Service | wiki body size unbounded | accept | Loopback-only v1.0 single-operator; no size limit imposed for MVP; add in Phase 10 hardening |
| T-08-08 | Information Disclosure | model_registry read | accept | Local database, loopback-only, no credentials stored in model_registry (D-017: key management is Phase 10) |
| T-08-SC | Tampering | cargo build (no new dependencies added) | accept | No new crates added in this plan; existing Cargo.lock unchanged |
</threat_model>

<verification>
After both tasks complete:
- cargo build -p atlas-gateway exits 0
- cargo test -p atlas-gateway exits 0 (existing tests must not regress)
- GET /v1/wiki/pages/{slug} returns 404 for unknown slug (not 500)
- GET /v1/models returns { models: [], count: 0 } when no model_registry table (gateway does not 503)
- All five routes appear in the router (grep "/v1/wiki/pages/{slug}" and "/v1/models" and "cancel" in lib.rs)
</verification>

<success_criteria>
Gateway binary compiles with wiki create/update/detail, model registry, and run cancel endpoints. All writes delegate to atlas CLI following D-022 pattern. No existing tests broken.
</success_criteria>

<output>
Create `.planning/phases/08-cockpit/08-02-SUMMARY.md` when done.
</output>
