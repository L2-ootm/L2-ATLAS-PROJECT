---
phase: 08-cockpit
plan: 06
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
files_modified:
  - services/web-ui/src/routes/models/+page.svelte
  - services/web-ui/src/lib/components/ModelRow.svelte
  - services/web-ui/src/lib/api.ts
autonomous: false
requirements:
  - COCKPIT-06

must_haves:
  truths:
    - "Model registry table renders from GET /v1/models with MODEL ID, PROVIDER, TIER, HEALTH, POLICY columns"
    - "HEALTH column uses StatusBadge (HEALTHY=succeeded green, DEGRADED=warning amber, DOWN=failed crimson)"
    - "POLICY column renders text badge (PREFERRED / FALLBACK / DISABLED) in JetBrains Mono 12px"
    - "No edit controls or toggle controls — explicitly read-only (grayed note: MUTATION CONTROLS: PHASE 10)"
    - "Routing Policy block shows key-value pairs from model registry data in JetBrains Mono 12px"
    - "Audit Visibility block shows last 10 RUN ID → MODEL ID pairs linked to /runs/{id}"
    - "Empty state renders GATEWAY message if gateway is unreachable"
    - "Cockpit initial page load < 2s (adapter-static static build verified)"
    - "No Electron in package.json"
    - "npm run dev starts and all four routes render without console errors in Chrome/Firefox"
  artifacts:
    - path: "services/web-ui/src/routes/models/+page.svelte"
      provides: "Model registry surface — read-only (COCKPIT-06)"
      contains: "MODEL REGISTRY"
    - path: "services/web-ui/src/lib/components/ModelRow.svelte"
      provides: "Single model registry table row"
      contains: "PREFERRED"
    - path: "services/web-ui/src/lib/api.ts"
      provides: "Extended with listModels function"
      contains: "listModels"
  key_links:
    - from: "services/web-ui/src/routes/models/+page.svelte"
      to: "listModels in api.ts"
      via: "called on mount"
      pattern: "listModels"
---

<objective>
Implement Surface 4: the read-only provider/model registry panel, then run the end-to-end verification checkpoint confirming all four surfaces load correctly and COCKPIT-06 (< 2s load) is met.

Purpose: The model panel gives the operator visibility into which models are configured, their routing policy, and which model handled each recent run. It is explicitly read-only in v1.0 — key management and mutation controls are Phase 10.

Output: /models route rendering model registry table (read-only), routing policy block, audit visibility block. API client extended with listModels. Final verification checkpoint confirms all surfaces functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cockpit/08-UI-SPEC.md
@.planning/phases/08-cockpit/08-01-SUMMARY.md
@.planning/phases/08-cockpit/08-02-SUMMARY.md
@.planning/phases/08-cockpit/08-03-SUMMARY.md
@.planning/phases/08-cockpit/08-04-SUMMARY.md
@.planning/phases/08-cockpit/08-05-SUMMARY.md

<interfaces>
<!-- New gateway endpoint from Plan 02 -->
GET /v1/models → { models: ModelEntry[], count: number }
ModelEntry { model_id: string; provider: string; tier: string; health: string; policy: string; updated_at: string }
(health values from db: "healthy" | "degraded" | "down" — map to StatusBadge: healthy→SUCCEEDED, degraded→PARTIAL, down→FAILED)

Audit visibility data source: GET /v1/runs/{id} has run.model_id if available — but the gateway doesn't store model_id on runs yet. Use GET /v1/missions?limit=10, then for each mission's latest run show: run_id → "N/A" if model_id absent, or use run's summary field. Alternative: show last 10 runs from GET /v1/wiki/search is wrong. Correct approach: call GET /v1/missions (returns missions with runs via mission_detail), collect last 10 runs across all missions ordered by started_at desc. For each run show RUN ID → run.session_id (which maps to a model session) or just RUN ID → mission title. Per UI spec: "RUN ID → MODEL ID". If model_id is not in the run response shape, display "RUN ID → —" with a note that model routing audit requires Phase 7 model_id field extension. This is acceptable in v1.0.

Design spec values from 08-UI-SPEC.md (Surface 4):
  Panel header: HudLabel "MODEL REGISTRY" + no action button + grayed note "(MUTATION CONTROLS: PHASE 10)"
  Model table: DATA TABLE, columns MODEL ID | PROVIDER | TIER | HEALTH | POLICY — all JetBrains Mono 12px
  HEALTH → StatusBadge (HEALTHY/DEGRADED/DOWN = succeeded/partial/failed colors)
  POLICY text badge: mono 12px, PREFERRED (#00FF94) / FALLBACK (#FFD600) / DISABLED (#505050)
  Routing Policy block: GlassPanel below table, HudLabel "ROUTING POLICY", key-value pairs JetBrains Mono 12px "KEY: value"
  Audit Visibility block: HudLabel "AUDIT VISIBILITY", last 10 entries "RUN ID → MODEL ID", linked, secondary ghost link "VIEW ALL IN RUNS"
  Empty state: "MODEL REGISTRY EMPTY. Ensure the Phase 7 gateway is running on 127.0.0.1:8484."
  Topo: model registry → --l2-topo-glow-info on cursor over model table
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Model registry surface + api.ts listModels extension</name>
  <files>
    services/web-ui/src/lib/api.ts
    services/web-ui/src/lib/components/ModelRow.svelte
    services/web-ui/src/routes/models/+page.svelte
  </files>
  <read_first>
    - services/web-ui/src/lib/api.ts (current state after Plans 01+05 — read before appending)
    - .planning/phases/08-cockpit/08-UI-SPEC.md (Surface 4: complete spec)
    - services/web-ui/src/lib/components/StatusBadge.svelte (for HEALTH column mapping)
  </read_first>
  <action>
    api.ts additions (append — do not replace file):
    - Add ModelEntry interface: { model_id: string; provider: string; tier: string; health: string; policy: string; updated_at: string }.
    - Add listModels(): Promise&lt;{ models: ModelEntry[]; count: number }&gt; — GET /v1/models, returns empty { models: [], count: 0 } if gateway returns 503 or 404 (do not throw, so models page degrades gracefully).

    ModelRow.svelte: Props: model: ModelEntry. Renders tr with height 48px min, padding 0. Row border-bottom 1px solid rgba(255,255,255,0.05). Row hover rgba(255,255,255,0.03) 80ms transition. All cells: JetBrains Mono 12px font. Columns: MODEL ID (#A0A0A0), PROVIDER (#A0A0A0), TIER (#A0A0A0), HEALTH (StatusBadge — map health: "healthy"→"SUCCEEDED", "degraded"→"PARTIAL"/"DEGRADED", "down"→"FAILED"; use StatusBadge with the mapped status string), POLICY (styled span: "PREFERRED" → color #00FF94; "FALLBACK" → color #FFD600; "DISABLED" → color #505050; font-family var(--l2-font-mono), font-size 12px, text-transform uppercase). No edit controls, no toggles anywhere in this row.

    models/+page.svelte: State: models: ModelEntry[], loading, error. On mount: call listModels(). If error (network): set error to "GATEWAY OFFLINE — 127.0.0.1:8484 not responding. Start the atlas-gateway process." Section header: HudLabel "MODEL REGISTRY" + right side: Inter 400 12px muted "(MUTATION CONTROLS: PHASE 10)" in color rgba(255,255,255,0.20).

    Model table: GlassPanel wrapping table. Header row (background #050505): HUD labels "MODEL ID" | "PROVIDER" | "TIER" | "HEALTH" | "POLICY". Each data row: ModelRow. Empty state (models.length === 0 after load): "MODEL REGISTRY EMPTY. Ensure the Phase 7 gateway is running on 127.0.0.1:8484." HudLabel + body Inter 400.

    Routing Policy block: GlassPanel below table, margin-top 24px. HudLabel "ROUTING POLICY". Derive key-value pairs from models data: group by tier, for each tier show "TASK_CLASS_{TIER.toUpperCase()}: {model.model_id}" where policy is "PREFERRED". If no PREFERRED model for a tier, omit. Render as list of spans: each line "KEY: value" in JetBrains Mono 12px #A0A0A0. If models empty: single line "NO POLICY CONFIGURED" in JetBrains Mono 12px rgba(255,255,255,0.40).

    Audit Visibility block: GlassPanel below routing policy. HudLabel "AUDIT VISIBILITY". Load last 10 runs by calling listMissions(10) — this returns missions with no run detail. To get runs, call getMission for each (up to 3 missions, take their run arrays, sort by started_at desc, take first 10). For each run show: span run.id (JetBrains Mono 12px #A0A0A0, linked to /runs/{run.id} using anchor) + " → " + span (JetBrains Mono 12px #505050) showing run.session_id ?? "—". If no missions loaded, show "NO AUDIT DATA". At bottom: secondary ghost link "VIEW ALL IN RUNS" navigating to /runs.

    Topo: on mouseenter of model table container, activate topo glow (--l2-topo-glow-info). Use same pattern as other surfaces.
  </action>
  <verify>
    <automated>cd services/web-ui && npm run check 2>&1 | grep -E "^Error|error\[" | head -10 ; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - services/web-ui/src/lib/api.ts contains "export async function listModels" and "ModelEntry"
    - services/web-ui/src/lib/components/ModelRow.svelte contains "PREFERRED" and "FALLBACK" and "DISABLED" and "StatusBadge"
    - services/web-ui/src/routes/models/+page.svelte contains "MODEL REGISTRY" and "MUTATION CONTROLS: PHASE 10" and "ROUTING POLICY" and "AUDIT VISIBILITY"
    - services/web-ui/src/routes/models/+page.svelte does NOT contain any "button" element with text "edit", "delete", "add", or "configure" (read-only enforcement)
    - npm run check exits 0
    - npm run build exits 0 and produces build/ static files
  </acceptance_criteria>
  <done>Model registry surface rendered as read-only data display. All four surface routes exist and build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01–06 complete. SvelteKit cockpit with four surfaces:
    - /missions — mission list + create form (COCKPIT-01, COCKPIT-05)
    - /runs/[id] — SSE audit stream + completed run trail (COCKPIT-02, COCKPIT-03)
    - /wiki — wiki browser + create/edit (COCKPIT-04)
    - /models — read-only model registry (COCKPIT-06 read side)
    - atlas-gateway extended with wiki write + models + cancel endpoints (Plan 02)
    - All L2 Systems design tokens, typography, colors, and spacing per 08-UI-SPEC.md
  </what-built>
  <how-to-verify>
    1. Start the gateway: cd native/atlas-core-rs && cargo run -p atlas-gateway
       Confirm: http://127.0.0.1:8484/health returns {"status":"ok"}

    2. Start the cockpit: cd services/web-ui && npm run dev
       Confirm: http://localhost:5173 loads and redirects to /missions

    3. Check COCKPIT-06 performance: Open Chrome DevTools → Network tab. Hard reload /missions.
       Confirm: Initial page load (DOMContentLoaded) &lt; 2000ms

    4. Verify Surface 1 (Missions):
       - Mission list renders with glass panel table and HUD label column headers
       - Status badges are colored correctly (RUNNING=cyan, SUCCEEDED=green, FAILED=crimson)
       - Click CREATE MISSION: modal opens with TITLE input, INTENT textarea, CREATE MISSION + DISCARD buttons
       - Submit a test mission: appears in list without page reload

    5. Verify Surface 2 (Runs): Navigate to /missions/{id} for a mission that has a run. Click the run.
       - Run header shows ID (mono), status badge, timestamps
       - For active runs: LIVE badge pulses
       - For completed runs: audit event rows render with correct color coding (tool calls teal, LLM calls violet, errors crimson)
       - EXPORT JSONL button visible for completed runs

    6. Verify Surface 3 (Wiki): Navigate to /wiki
       - Two-column layout: page list left (280px), viewer right
       - Search bar at top with Search icon
       - Creating a page via CREATE PAGE form works (if gateway is running and atlas CLI available)

    7. Verify Surface 4 (Models): Navigate to /models
       - MODEL REGISTRY header with "(MUTATION CONTROLS: PHASE 10)" note
       - No edit controls, no buttons in the table
       - ROUTING POLICY and AUDIT VISIBILITY blocks present

    8. Verify cross-surface: Sidebar collapse toggle works. Active nav item is cyan.
       Wordmark "L2 // SYSTEMS" visible in expanded sidebar.

    9. Check No Electron: grep -c "electron" services/web-ui/package.json (should output 0)

    10. Check adapter-static: cat services/web-ui/svelte.config.js | grep "adapter-static" (must be present)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass, or describe which surfaces have issues.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cockpit → gateway | All four surfaces call loopback gateway; read-only for models surface |
| static build → filesystem | adapter-static emits HTML/JS/CSS files; no server component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-20 | Information Disclosure | Audit Visibility block | accept | Shows RUN IDs and session_id values — no secrets, no PII; loopback operator tool |
| T-08-21 | Elevation of Privilege | Read-only model surface | mitigate | No mutation controls rendered in v1.0; Plan explicitly verifies no edit buttons in acceptance criteria |
| T-08-22 | Denial of Service | Parallel getMission calls in Audit Visibility | mitigate | Cap at 3 getMission calls; total fetch budget bounded |
| T-08-SC | Tampering | No new npm packages | accept | No new packages; static build artifact reviewed in checkpoint |
</threat_model>

<verification>
Final phase verification after checkpoint approval:
- npm run build exits 0 — adapter-static produces build/ (no SSR, no server routes)
- grep -c "electron" services/web-ui/package.json outputs 0
- grep "adapter-static" services/web-ui/svelte.config.js matches
- All six COCKPIT REQ-IDs covered: COCKPIT-01 (mission list), COCKPIT-02 (live SSE), COCKPIT-03 (completed trail), COCKPIT-04 (wiki browser), COCKPIT-05 (create mission), COCKPIT-06 (< 2s load, no Electron)
- cargo test -p atlas-gateway exits 0 (gateway regression-free after Plan 02 additions)
</verification>

<success_criteria>
COCKPIT-06: Cockpit loads in &lt; 2 seconds on localhost (adapter-static, no SSR, code-split routes, no Electron). All four surfaces render correctly. Operator can complete the full loop: create mission → launch run → watch audit stream → browse wiki — from the cockpit alone.
</success_criteria>

<output>
Create `.planning/phases/08-cockpit/08-06-SUMMARY.md` when done.
</output>
