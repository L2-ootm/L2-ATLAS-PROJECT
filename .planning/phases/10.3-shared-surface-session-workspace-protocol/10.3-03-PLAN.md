---
phase: 10.3-shared-surface-session-workspace-protocol
plan: 03
type: tdd
wave: 2
depends_on: ["10.3-01"]
files_modified:
  - services/agent-runtime/atlas_runtime/surface_events.py
  - services/agent-runtime/tests/test_surface_events.py
autonomous: true
requirements: [SURF-04, AGNT-01, AUD-01]
user_setup: []

must_haves:
  truths:
    - "A single pure normalizer maps every AuditEvent.event_type to a SurfaceEvent discriminated kind"
    - "The terminal RunOutcome becomes a completion SurfaceEvent carrying its status/summary/stop_reason"
    - "SurfaceEvents carry a monotonic per-session seq assigned in normalizer order, enabling reconnect gap-detection (replay seq > last_seq)"
    - "The normalizer is a read-only projection over immutable audit_events rows — no new event bus (AGNT-01)"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/surface_events.py"
      provides: "normalize_surface_events() pure projection + event_type→kind map"
      contains: "def normalize_surface_events"
  key_links:
    - from: "surface_events.normalize_surface_events"
      to: "AuditEvent / RunOutcome"
      via: "pure projection → SurfaceEvent[kind] with seq"
      pattern: "SurfaceEvent\\("
    - from: "surface_events"
      to: "audit_service.get_events_for_run"
      via: "ORDER BY timestamp ASC immutable read source"
      pattern: "get_events_for_run"
---

<objective>
Deliver the single normalized event contract that all surfaces (WebUI via gateway SSE, TUI via an
in-process iterator) will consume. One pure function projects immutable `audit_events` rows plus the
terminal `RunOutcome` into a `SurfaceEvent` stream with a discriminated `kind` and a monotonic
per-session `seq`. Phase 10.3 scope is the contract + normalizer + conformance tests only — TUI/WebUI
rendering is wired in 10.6/10.7.

Purpose: SURF-04 (one normalized stream covering text, reasoning, tool calls/results, tasks/subagents,
retries, retrieval, approvals, errors, completion) + AGNT-01 (read-projection, no new bus) + AUD-01
(events derive from the audited ledger).
Output: `surface_events.py` and `test_surface_events.py` (kind-coverage matrix + monotonic-seq +
completion-carries-RunOutcome).

Depends on plan 01 for the `SurfaceEvent`/`EventKind` types and the new `core.py` audit literals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-CONTEXT.md
@.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-PATTERNS.md
@.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-RESEARCH.md

<interfaces>
<!-- Contracts the executor consumes directly. -->

From plan 01 — packages/atlas-core/atlas_core/schemas/surface_session.py:
- `EventKind = Literal["text","reasoning","tool_call","tool_result","task","retry","retrieval","approval","error","completion"]`
- `SurfaceEvent(_FrozenContract)`: session_id: str, seq: int, kind: EventKind, run_id: str | None, occurred_at: str (ISO), payload_json: str = "{}" (JSON string, NOT a dict — D-013).

Read source — services/agent-runtime/atlas_runtime/audit_service.py:
- `get_events_for_run(conn, run_id)` (lines ~201-206): `SELECT * FROM audit_events WHERE run_id=? ORDER BY timestamp ASC` → list[AuditEvent]. Immutable + time-ordered → deterministic seq.

Completion source — services/agent-runtime/atlas_runtime/agents/base.py:
- frozen `RunOutcome` (lines 22-38): status, summary, evidence, inferences, uncertainties, stop_reason.

Input domain — packages/atlas-core/atlas_core/schemas/core.py:
- `AuditEvent.event_type` Literal (the full set including the six members appended in plan 01).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: normalize_surface_events pure projection + event_type→kind map</name>
  <files>services/agent-runtime/atlas_runtime/surface_events.py, services/agent-runtime/tests/test_surface_events.py</files>
  <read_first>
    - services/agent-runtime/atlas_runtime/audit_service.py (get_events_for_run lines 201-206)
    - services/agent-runtime/atlas_runtime/agents/base.py (RunOutcome lines 22-38)
    - packages/atlas-core/atlas_core/schemas/surface_session.py (SurfaceEvent/EventKind — from plan 01)
    - services/agent-runtime/tests/test_tool_capability_conformance.py (parametrized-matrix convention)
    - .planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-PATTERNS.md (surface_events.py section; event_type→kind map)
  </read_first>
  <behavior>
    - normalize_surface_events maps llm_call→text or reasoning (split on data payload shape), tool_call/tool_requested→tool_call, tool_completed→tool_result, tool_failed→error, subagent_run→task, approval→approval, failure→error, artifact/wiki_update/memory_change→retrieval, surface_session_*/run_cancelled/permission_transition→activity-appropriate kinds.
    - given a terminal RunOutcome, the LAST emitted SurfaceEvent has kind="completion" and its payload_json encodes status/summary/stop_reason.
    - seq is strictly increasing starting at the same base for a session across calls; replaying with last_seq returns only events with seq > last_seq.
    - the function performs NO writes and constructs no pub/sub — it returns a tuple[SurfaceEvent, ...].
    - every member of EventKind is reachable from at least one audit event_type (conformance coverage).
  </behavior>
  <action>
    Create `surface_events.py` with `_KIND_MAP: dict[str, EventKind]` covering every `AuditEvent.event_type` member, and `normalize_surface_events(audit_events, run_outcome=None, *, session_id, start_seq=0) -> tuple[SurfaceEvent, ...]`. Iterate the already time-ordered audit events (caller passes the result of `get_events_for_run`, ORDER BY timestamp ASC), assign `seq` incrementally from `start_seq`, map `event_type → kind` via `_KIND_MAP` (for `llm_call`, inspect the event `data` payload to split `text` vs `reasoning`), and set `occurred_at` from the audit timestamp ISO string and `payload_json` from the (already-redacted) audit data JSON string (D-013 — keep it a string, do not parse to a dict in the public field). If `run_outcome` is provided, append a final `SurfaceEvent(kind="completion", ...)` whose `payload_json` JSON-encodes `status`/`summary`/`stop_reason` from the frozen `RunOutcome`. Provide a `replay_since(events, last_seq)` helper returning only `seq > last_seq`. Do NOT add a new event bus, do NOT write to the DB, do NOT author state in any transport (AGNT-01 / D-022). Write `test_surface_events.py` mirroring the conformance-matrix convention: a parametrized `test_normalizer_maps_every_audit_type`, `test_completion_event_carries_run_outcome`, `test_seq_is_monotonic_per_session`, `test_replay_since_returns_only_newer`, and `test_every_event_kind_is_reachable` (assert each EventKind appears in `_KIND_MAP.values()` or the completion path).
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_surface_events.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest services/agent-runtime/tests/test_surface_events.py -q` exits 0.
    - A parametrized test asserts every audit event_type maps to its expected kind (full SURF-04 enumeration).
    - A test asserts the terminal completion event carries the RunOutcome status/summary/stop_reason.
    - A test asserts seq is strictly monotonic and `replay_since` returns only seq > last_seq.
    - `grep -n "def normalize_surface_events" services/agent-runtime/atlas_runtime/surface_events.py` returns a match; `grep -c "conn.execute\\|INSERT\\|UPDATE" services/agent-runtime/atlas_runtime/surface_events.py` (filtered with `grep -v '^#'`) is 0 (pure read-projection, no writes).
  </acceptance_criteria>
  <done>One pure normalizer maps every audit type to a discriminated SurfaceEvent kind, appends the RunOutcome completion, assigns monotonic per-session seq, and supports reconnect replay; conformance suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit ledger → surface event stream | Persisted audit rows cross into a client-facing normalized projection |
| normalizer → transports (SSE / iterator) | Same projection consumed by multiple surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.3-EVT-BUS | Tampering / Elevation | new event bus temptation | mitigate | normalizer is a pure read-projection over immutable `audit_events`; no pub/sub, no DB writes (AGNT-01) — enforced by a no-writes grep gate |
| T-10.3-EVT-AUTH | Information Disclosure / Elevation | cross-surface event delivery | mitigate | SurfaceEvent carries no decision authority; permission routing deferred to 10.5; gateway only forwards normalizer output (D-022) |
| T-10.3-EVT-LEAK | Information Disclosure | payload_json from audit data | mitigate | audit `data` is already redacted by `emit`/SECRET_PATTERNS at write time; normalizer reuses the redacted string and never re-derives raw payloads |
| T-10.3-EVT-REPLAY | Repudiation / replay confusion | reconnect gap-detection | mitigate | monotonic per-session seq + `replay_since(last_seq)` gives deterministic gap detection over immutable, time-ordered rows |
| T-10.3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — stdlib only |
</threat_model>

<verification>
- `python -m pytest services/agent-runtime/tests/test_surface_events.py -q` green.
- No-writes gate: `grep -v '^#' services/agent-runtime/atlas_runtime/surface_events.py | grep -c "INSERT\|UPDATE\|DELETE"` returns 0.
</verification>

<success_criteria>
- SURF-04: one normalizer covers the full event enumeration with a discriminated kind and monotonic seq.
- AGNT-01: pure read-projection, no new runtime/bus.
- AUD-01: events derive from the audited, redacted ledger.
</success_criteria>

<output>
Create `.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-03-SUMMARY.md` when done.
</output>
