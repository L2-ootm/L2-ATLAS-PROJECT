---
phase: 10.3-shared-surface-session-workspace-protocol
plan: 05
status: complete
completed: 2026-06-25
requirements: [SURF-05, AUD-01, AGNT-01]
---

# Plan 10.3-05 Summary — surface session resume

## What shipped

`surface_session_service.resume_session(conn, lock, session_id, *, owner_token, owner_pid=None)`:
1. **Fail-closed contract replay first** — `agent_contract_service.replay_contract` for the
   session's run, asserting the stored prompt/tool-catalog/context-policy versions match the
   immutable 10.2 `RunContractSnapshot`. `ContractCompatibilityError` (drift) / `LookupError`
   (missing) propagate BEFORE any transition, so a mismatch leaves the session suspended.
2. **Re-bind ownership** — fresh `owner_token` + refreshed `heartbeat_at` so the plan-04
   reconciliation sweep treats the resumed session as owned-alive.
3. **Guarded transitions** `suspended → resuming → active`; the active transition emits the
   audited `surface_session_resumed` event (AUD-01).
4. Surface/workspace/mission-run-session identity preserved (only state/ownership/heartbeat change).

Reuses the existing contract replay — no new runtime, no new snapshot format (AGNT-01). This
closes the checker-flagged dead end: the `resuming` state and `surface_session_resumed` literal
(plan 01) now have an implementing path.

## Verification

- `python -m pytest services/agent-runtime/tests/test_surface_session_resume.py -q` → **5 passed**
  (identity preservation, version-mismatch fail-closed, not-reclaimed-after-resume, completed→resume
  raises ValueError, audited resumed event).
- Full agent-runtime suite → **504 passed, 1 failed (pre-existing `claude_agent_sdk` env), 2 skipped**.
- grep gate: `def resume_session` / `replay_contract` / `surface_session_resumed` all present.

## Commits

- `90b080a` test(10.3-05) · `e2e97fc` feat(10.3-05)
