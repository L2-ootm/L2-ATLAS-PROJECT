---
phase: 10.3-shared-surface-session-workspace-protocol
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/atlas-core/atlas_core/schemas/surface_session.py
  - packages/atlas-core/atlas_core/schemas/core.py
  - infra/migrations/0016_surface_sessions.sql
  - services/agent-runtime/atlas_runtime/surface_session_service.py
  - services/agent-runtime/tests/test_surface_session_service.py
  - services/agent-runtime/tests/conftest.py
autonomous: true
requirements: [SURF-01, AUD-01, AGNT-01]
user_setup: []

must_haves:
  truths:
    - "A surface session row persists every SURF-01 identity field (surface kind/instance, workspace kind, project/root, mission/run/session, agent/model, permission mode, prompt/context/tool-catalog versions, lifecycle state)"
    - "SurfaceSession is constructed and validated before any SQL write; unknown surface/workspace kinds are rejected by the frozen model"
    - "Every lifecycle transition is guarded by a SELECT-status precondition and emits an audit event carrying session_id"
    - "Terminal session states (completed/failed/reclaimed) cannot be mutated"
  artifacts:
    - path: "packages/atlas-core/atlas_core/schemas/surface_session.py"
      provides: "Frozen SurfaceSession + SurfaceEvent + SessionState + EventKind"
      contains: "class SurfaceSession"
    - path: "infra/migrations/0016_surface_sessions.sql"
      provides: "surface_sessions table mirroring SurfaceSession fields 1:1"
      contains: "CREATE TABLE IF NOT EXISTS surface_sessions"
    - path: "services/agent-runtime/atlas_runtime/surface_session_service.py"
      provides: "create/get/list/transition lifecycle state machine"
      contains: "_ALLOWED_FROM"
  key_links:
    - from: "surface_session_service.py"
      to: "audit_service.emit"
      via: "emit-after-lock for each transition"
      pattern: "emit\\("
    - from: "surface_session_service.py"
      to: "surface_sessions table"
      via: "with lock: with conn: SELECT-precondition + write"
      pattern: "with lock"
---

<objective>
Establish the persisted `SurfaceSession` entity, its 1:1 migration, and the lifecycle state-machine
service. This is the spine all other 10.3 work consumes: plan 03 imports the `SurfaceEvent`/`EventKind`
types and the new audit `event_type` literals defined here; plan 04 extends this service with
reconciliation and cancellation transitions.

Purpose: SURF-01 (session identity persistence) + AUD-01 (every lifecycle change audited) using the
existing `run_service` write shape and `audit_service.emit` bus — no new runtime (AGNT-01).
Output: `surface_session.py` (frozen models), `0016_surface_sessions.sql`, `surface_session_service.py`,
`core.py` audit-literal extension, and the `test_surface_session_service.py` suite + `surface_session`
conftest fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-CONTEXT.md
@.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-PATTERNS.md
@.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-RESEARCH.md

<interfaces>
<!-- Contracts the executor consumes directly — no codebase exploration needed. -->

Reuse from packages/atlas-core/atlas_core/schemas/agent_contract.py (do NOT redefine):
- `_FrozenContract` (BaseModel, model_config frozen=True, str_strip_whitespace=True, extra="forbid")
- `SurfaceIdentity(_FrozenContract)`: kind: SurfaceKind (cli|tui|webui|api|native|test), session_id: str
- `WorkspaceIdentity(_FrozenContract)`: kind: WorkspaceKind (project|directory|global), root: str, project_id: str | None
- `SurfaceKind`, `WorkspaceKind`, `PermissionMode`, `ModelIdentity` Literals/models — import, do not redefine.

Reuse from packages/atlas-core/atlas_core/schemas/core.py:
- `Run` (lines 218-237): the exact id-gen + field_serializer shape to copy:
  `id: str = Field(default_factory=lambda: str(uuid.uuid4()))`;
  `@field_serializer(...)` returning `None if dt is None else dt.isoformat()`.
- `AuditEvent.event_type` Literal (lines ~255-274): the list to append new members to.

Service analog — services/agent-runtime/atlas_runtime/run_service.py:
- `start_run` (line 31): Pydantic-first construct+validate → `with lock: with conn:` SELECT-precondition + INSERT → emit AFTER lock release.
- `complete_run` (line 96): the guarded transition shape (`if row[0] != "running": raise ValueError`).

Audit bus — services/agent-runtime/atlas_runtime/audit_service.py:
- `emit(conn, lock, *, run_id, event_type, session_id=..., data=...)` (line 63) — re-acquires `lock` internally; redacts `data` via SECRET_PATTERNS. NEVER call inside the write lock (Pitfall 6 deadlock).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Frozen SurfaceSession / SurfaceEvent models + migration 0016 + audit literals</name>
  <files>packages/atlas-core/atlas_core/schemas/surface_session.py, packages/atlas-core/atlas_core/schemas/core.py, infra/migrations/0016_surface_sessions.sql</files>
  <read_first>
    - packages/atlas-core/atlas_core/schemas/core.py (Run lines 218-237 for id-gen + field_serializer; AuditEvent.event_type Literal lines ~255-274)
    - packages/atlas-core/atlas_core/schemas/agent_contract.py (lines 50-51 _FrozenContract; lines 94-124 SurfaceIdentity/WorkspaceIdentity/PermissionMode/ModelIdentity)
    - infra/migrations/0015_agent_contract_snapshots.sql (table + BEFORE UPDATE trigger template; confirm next number is 0016)
    - .planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-PATTERNS.md (surface_session.py + 0016 sections)
  </read_first>
  <behavior>
    - SurfaceSession constructs from valid SurfaceIdentity + WorkspaceIdentity + ModelIdentity; round-trips through model_dump.
    - Unknown surface kind or workspace kind raises pydantic ValidationError (extra="forbid", Literal-constrained).
    - SurfaceEvent rejects a dict payload field (payload_json must be a str, D-013).
    - SessionState default is "starting"; id auto-generates as a uuid4 string.
    - datetime fields serialize to ISO-8601 strings (created_at/updated_at/heartbeat_at).
  </behavior>
  <action>
    Create `surface_session.py` defining: `SessionState = Literal["starting","active","suspended","resuming","cancelling","completed","failed","reclaimed"]`; `EventKind = Literal["text","reasoning","tool_call","tool_result","task","retry","retrieval","approval","error","completion"]`; frozen `SurfaceSession` composing `SurfaceIdentity surface`, `WorkspaceIdentity workspace`, plus `agent: str`, `model: ModelIdentity`, `permission_mode: PermissionMode`, `prompt_version: str`, `tool_catalog_version: str`, `context_policy_version: str`, `state: SessionState = "starting"`, `owner_token: str = ""`, `owner_pid: int | None = None`, `heartbeat_at`/`created_at`/`updated_at` datetimes, `mission_id: str | None`, `run_id: str | None`; copy the `id` default_factory and `field_serializer` shape verbatim from core.py `Run`. Define frozen `SurfaceEvent(_FrozenContract)` with `session_id: str`, `seq: int`, `kind: EventKind`, `run_id: str | None = None`, `occurred_at: str`, `payload_json: str = "{}"` (JSON string per D-013, never a dict). In `core.py`, APPEND to the `AuditEvent.event_type` Literal (do not reorder existing members) the members `surface_session_started`, `surface_session_suspended`, `surface_session_resumed`, `surface_session_reclaimed`, `run_cancelled`, `permission_transition` (no schema migration — column is TEXT, Pattern 8 / AUD-01). Create `0016_surface_sessions.sql` mirroring SurfaceSession fields 1:1 as TEXT/INTEGER columns (`id` PRIMARY KEY, `surface_kind`, `surface_session_id`, `workspace_kind`, `workspace_root`, `project_id`, `mission_id`, `run_id`, `agent`, `model_provider`, `model_id`, `permission_mode`, `prompt_version`, `tool_catalog_version`, `context_policy_version`, `state`, `owner_token`, `owner_pid` INTEGER, `heartbeat_at`, `created_at`, `updated_at`) using `CREATE TABLE IF NOT EXISTS`; keep the session→run link SOFT (no FK on runs.session_id, mirror tool_approvals per RESEARCH OQ-3); add a `surface_sessions_terminal_immutable` `BEFORE UPDATE` trigger `WHEN OLD.state IN ('completed','failed','reclaimed')` raising ABORT — keep the `BEGIN ... END` body intact (db.py uses executescript; never `;`-split trigger bodies).
  </action>
  <verify>
    <automated>python -m pytest packages/atlas-core/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest packages/atlas-core/tests -q` exits 0 with the new SurfaceSession/SurfaceEvent assertions green.
    - `grep -n "surface_session_started" packages/atlas-core/atlas_core/schemas/core.py` returns a match inside the AuditEvent.event_type Literal.
    - `infra/migrations/0016_surface_sessions.sql` contains `CREATE TABLE IF NOT EXISTS surface_sessions` and a `BEFORE UPDATE` trigger; no `runs.session_id` FK is declared.
  </acceptance_criteria>
  <done>SurfaceSession/SurfaceEvent frozen models exist and validate; migration 0016 mirrors fields 1:1 with terminal-immutability trigger and soft run link; six new audit literals appended to core.py.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: surface_session_service lifecycle state machine + conftest fixture</name>
  <files>services/agent-runtime/atlas_runtime/surface_session_service.py, services/agent-runtime/tests/test_surface_session_service.py, services/agent-runtime/tests/conftest.py</files>
  <read_first>
    - services/agent-runtime/atlas_runtime/run_service.py (start_run line 31, complete_run line 96, cancel_run line 161 — the TOCTOU-safe write + emit-after-lock shape)
    - services/agent-runtime/atlas_runtime/audit_service.py (emit signature line 63; redaction)
    - services/agent-runtime/tests/conftest.py (db/run_id/lock/mission_id fixtures lines 26-77 — mirror the minimal-row insertion for a surface_session fixture)
    - packages/atlas-core/atlas_core/schemas/surface_session.py (from Task 1)
  </read_first>
  <behavior>
    - create_session: constructs+validates SurfaceSession BEFORE INSERT; persists all SURF-01 fields; emits a surface_session_started audit event with session_id.
    - get_session returns the persisted row as a SurfaceSession; list_sessions returns inserted sessions.
    - transition starting→active→suspended→resuming→active is allowed; an illegal jump (e.g. completed→active) raises ValueError.
    - every successful transition emits exactly one audit event carrying session_id and the target state.
    - a transition into a terminal state is rejected by the DB trigger if the row is already terminal.
  </behavior>
  <action>
    Create `surface_session_service.py` mirroring `run_service.py`. Define `_ALLOWED_FROM: dict[SessionState, frozenset]` encoding `starting→{active,failed}`, `active→{suspended,cancelling,completed,failed}`, `suspended→{resuming,cancelling,reclaimed,failed}`, `resuming→{active,failed}`, `cancelling→{cancelled? no — use completed|failed}`, with `completed`/`failed`/`reclaimed` terminal (empty outgoing). Implement `create_session(conn, lock, *, surface, workspace, agent, model, permission_mode, prompt_version, tool_catalog_version, context_policy_version, mission_id=None, run_id=None, owner_token="", owner_pid=None) -> SurfaceSession`: construct+validate the model, then `with lock: with conn:` INSERT, release, then `emit(... event_type="surface_session_started", session_id=session.id, data={...})`. Implement `get_session`, `list_sessions`, and `transition_session(conn, lock, session_id, target_state, *, run_id=None) -> None`: inside `with lock: with conn:` SELECT current state, raise ValueError if row missing or `current not in _resolve_allowed(target_state)` (a state is reachable iff target ∈ _ALLOWED_FROM[current]); UPDATE state + updated_at; release lock; then emit the matching `event_type` (`surface_session_suspended`/`surface_session_resumed`/etc.) with session_id. NEVER emit inside the lock (Pitfall 6). Add a `surface_session` fixture to conftest.py mirroring the `run_id` fixture: insert one minimal `surface_sessions` row and yield its id. Write `test_surface_session_service.py` asserting the behaviors above (create persists all fields, illegal transition raises, each transition audits, terminal immutability).
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_surface_session_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest services/agent-runtime/tests/test_surface_session_service.py -q` exits 0.
    - A test asserts `create_session` rejects nothing valid AND that an illegal transition (e.g. completed→active) raises ValueError.
    - A test asserts an `audit_events` row with `event_type LIKE 'surface_session_%'` and the session_id exists after a transition (AUD-01).
    - `grep -n "_ALLOWED_FROM" services/agent-runtime/atlas_runtime/surface_session_service.py` returns a match.
  </acceptance_criteria>
  <done>create/get/list/transition implemented with guarded transitions + emit-after-lock; conftest surface_session fixture added; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| surface client → session service | Untrusted surface/workspace identity fields cross into persisted authority state |
| service → audit bus | Lifecycle/identity payloads cross into the persisted audit ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.3-01-VAL | Tampering | SurfaceSession INSERT | mitigate | Frozen Pydantic model (`extra="forbid"`, Literal kinds) validates before any SQL; parameterized INSERT only (no f-string SQL) |
| T-10.3-01-IMMUT | Tampering / Repudiation | terminal session rows | mitigate | `surface_sessions_terminal_immutable` BEFORE UPDATE trigger raises ABORT when OLD.state is terminal (DB-enforced, survives buggy callers) |
| T-10.3-01-AUDLEAK | Information Disclosure | audit emit of session identity | mitigate | route all audit through `audit_service.emit` which redacts via SECRET_PATTERNS before persistence (AUD-01) |
| T-10.3-ORPHAN | Elevation of Privilege | session ownership fields | accept (this plan) | owner_token/owner_pid/heartbeat_at columns are defined here; the reconciliation sweep that enforces them ships in plan 04 |
| T-10.3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; psutil decision deferred to plan 04 per RESEARCH (stdlib default) |
</threat_model>

<verification>
- `python -m pytest packages/atlas-core/tests -q` green (new frozen models).
- `python -m pytest services/agent-runtime/tests/test_surface_session_service.py -q` green.
- Migration 0016 applies cleanly under the conftest all-migrations in-memory DB (the fixture applies every `infra/migrations/*.sql` in order).
</verification>

<success_criteria>
- SURF-01: a surface session row persists every required identity field, validated before INSERT.
- AUD-01: every lifecycle transition emits a redacted audit event carrying session_id.
- AGNT-01: no new AgentRuntime/provider/tool-executor/memory backend introduced.
- Terminal sessions are DB-immutable; session→run link is soft (no FK).
</success_criteria>

<output>
Create `.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-01-SUMMARY.md` when done.
</output>
