---
phase: 03-research-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md
autonomous: true
requirements:
  - RESEARCH-02

must_haves:
  truths:
    - "docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md exists with at least 10 numbered open questions"
    - "The document defines a clear MVP boundary for what is in v2 scope vs out of scope"
    - "The document contains a research brief ready for a future deep-dive agent to execute"
    - "D-007 lock is explicitly acknowledged — CRM is v2 only, not v1"
    - "The document contains no schema DDL, API endpoint definitions, or v1 feature decisions"
  artifacts:
    - path: "docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md"
      provides: "Scoped intake brief for CRM/Pulse/Channels v2 work — closes D-010"
      contains: "## Open Questions"
  key_links:
    - from: "docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md"
      to: "docs/decisions/2026-06-04_DECISION_REGISTER.md"
      via: "D-007 and D-010 references in the document"
      pattern: "D-007"
---

<objective>
Write docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md — a scoped research intake document that closes D-010 (missing CRM/Pulse/Channels research) by defining open questions, an MVP boundary, and a research brief for a future deep-dive agent.

Purpose: Phase 3's goal is to close all open research gaps before the build phases begin. D-010 is the second open gap. This document does NOT design CRM — it surfaces what must be decided and defines the boundary between v1 (irrelevant) and v2 (planned but undesigned).

Output:
- docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md (new)
</objective>

<execution_context>
@C:\Users\Davi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Davi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-research-closure/03-RESEARCH.md
@.planning/phases/03-research-closure/CONTEXT.md

<interfaces>
<!-- Key facts the executor needs. Extracted from source documents. -->

Locked decisions relevant to this document:
- D-007 (locked): CRM/relationship runtime is NOT first implementation surface. MVP priority order ends at cockpit; minimal CRM primitives are item 6. Status: locked for MVP.
- D-009 (locked): STT/TTS/overlay is not a first MVP blocker — do NOT scope it into this research.
- D-010 (open): A dedicated CRM/Pulse/Channels report still needs to be run; existing reports only partially cover it. THIS plan closes D-010.

From REQUIREMENTS.md — Future Requirements (Milestone 2+):
- PULSE-01: User sees a periodic briefing of repo state, inboxes, deadlines, and wiki health.
- PULSE-02: Heartbeat monitors run on cron and emit AuditEvents.
- CRM-01: Contact/Organization/Opportunity models exist in SQLite with audit trail linkage.
- CRM-02: CRM entities linkable to missions.

Pre-analyzed open questions from 03-RESEARCH.md (must all appear):
CRM:
1. Should ATLAS CRM link to missions by mission_id FK, or should CRM entities be wiki pages with a crm: namespace?
2. What is the minimum viable Contact schema that doesn't constrain future extension (Twenty-style metadata fields)?
3. Should CRM records be audited via the AuditEvent bus, or have a separate change log?
4. Does ATLAS CRM need duplicate detection at v2, or is that post-v2?
5. What import path enables populating the CRM from existing contacts (CSV, Google Contacts, vCard)?

Pulse:
6. What exactly triggers a Pulse briefing — time schedule, mission completion, or explicit request?
7. What is the output format: markdown wiki page, push notification, CLI print, or all three?
8. What data sources does Pulse aggregate: wiki freshness, open missions, calendar (if integrated), repo state?
9. How does Pulse interact with the audit stream — does it produce AuditEvents, consume them, or both?

Channels:
10. Which messaging channels are in v2 scope: WhatsApp only, Discord only, or both?
11. For WhatsApp: official Meta Business API vs unofficial client library — which is ToS-safe for the target use case?
12. What is the approval flow for outbound channel messages — every message requires human approval or only first contact?
13. How are channel conversations stored: as wiki pages, as AuditEvents, or in a separate channels table?
14. What privacy model governs conversation storage and retention?

Prior research artifacts to synthesize from:
- docs/research/DEEP_RESEARCH_BACKLOG.md — R5 (Pulse), R6 (CRM/Twenty), R7 (WhatsApp/Channels)
- docs/research/2026-06-04_RESEARCH_SYNTHESIS.md — cross-report CRM/Pulse/Channels state
- Twenty CRM 2.0: MCP server native, TypeScript SDK, PostgreSQL — too heavy for ATLAS v1 SQLite stack.
  Research recommends NOT forking Twenty CRM; start with minimal AI-native CRM primitives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md</name>
  <files>docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md</files>

  <read_first>
    - .planning/phases/03-research-closure/03-RESEARCH.md — sections "RESEARCH-02 Pre-Analysis: CRM/Pulse/Channels Scope", open questions list, MVP boundary, "What Is Known from Prior Research"
    - .planning/phases/03-research-closure/CONTEXT.md — D-007 (locked), D-009 (locked), D-010 (open); "What NOT to Build" section
    - .planning/REQUIREMENTS.md — Future Requirements section (PULSE-01, PULSE-02, CRM-01, CRM-02)
    - docs/decisions/2026-06-04_DECISION_REGISTER.md — D-007 and D-010 full entries
    - docs/research/DEEP_RESEARCH_BACKLOG.md — R5, R6, R7 definitions (if file exists; skip if absent)
  </read_first>

  <action>
    Create docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md with the following sections in order:

    ## 1. Purpose and Scope
    State that this document closes D-010 (missing CRM/Pulse/Channels research). Explicitly state:
    - CRM is v2 scope only (D-007 locked). This document does NOT design CRM for v1.
    - Pulse/heartbeat monitors depend on Phase 4 (AuditEvent bus) and Phase 6 (Wiki runtime) — cannot be built until both phases complete.
    - Channels (WhatsApp/Discord) are listed as "too risky before runtime is proven" in REQUIREMENTS.md. Research must answer ToS and approval questions before any implementation begins.
    - STT/TTS/overlay is explicitly OUT OF SCOPE (D-009 locked).
    State which v2 milestone requirements are addressed: PULSE-01, PULSE-02, CRM-01, CRM-02.

    ## 2. What We Know
    Synthesize the state of prior research for each of the three areas. Prose format (not bullets-only). Include:

    **CRM:**
    - Research recommends NOT forking Twenty CRM. Twenty 2.0 uses PostgreSQL + TypeScript SDK + MCP server — too heavy for the ATLAS v1 SQLite stack.
    - ATLAS CRM should start with minimal AI-native primitives: Contact, Organization, Opportunity — linked to wiki and missions per CRM-01/CRM-02.
    - The core data model minimum: Contact (name, email, org, notes, mission_ids), Organization (name, contacts), Opportunity (org, status, value, mission_ids). This is the starting-point definition, not a locked schema.

    **Pulse:**
    - Pulse = periodic briefing aggregating: repo state, open missions, wiki health, inbox state, deadlines.
    - Heartbeat monitors run on cron and emit AuditEvents (PULSE-02).
    - Pulse cannot be scoped for v1 because its inputs (audit stream from Phase 4, wiki from Phase 6, mission lifecycle from Phase 5) do not exist until Phase 6 completes.
    - Phase dependency chain: Phase 4 → Phase 5 → Phase 6 → Pulse.

    **Channels:**
    - L2-BOT has Discord and channel management patterns (from DEEP_RESEARCH_BACKLOG.md R7).
    - WhatsApp has ToS risk. Official Meta Business API vs unofficial Baileys library is an unresolved question.
    - Listed as "too risky before runtime is proven" in REQUIREMENTS.md out-of-scope section.
    - The risk is not technical difficulty but regulatory/ToS compliance for outbound messaging at scale.

    ## 3. Open Questions
    List all open questions as a numbered list. Minimum 14 questions (use all 14 from the pre-analysis in 03-RESEARCH.md). Group under three sub-headings: ### CRM Open Questions, ### Pulse Open Questions, ### Channels Open Questions. Each question should be a single sentence ending in a question mark. Do NOT answer the questions. State only what is unknown and why it matters.

    After the list, add a note: "These questions must be resolved before any v2 CRM/Pulse/Channels implementation begins. The research brief in Section 5 provides the scope for a future deep-dive agent to answer them."

    ## 4. MVP Boundary
    Use a table with columns: Feature | v1 Scope | v2 Scope | Notes.

    Rows:
    - CRM Contact model | OUT — not in MVP | IN (CRM-01) | Linked to missions by mission_id or wiki namespace (open question)
    - CRM Organization model | OUT | IN (CRM-01) | Part of minimal CRM primitive set
    - CRM Opportunity model | OUT | IN (CRM-01) | Part of minimal CRM primitive set
    - CRM linkage to missions | OUT | IN (CRM-02) | Linkage model is open question
    - CRM duplicate detection | OUT | OUT (post-v2) | Too complex for initial v2 scope
    - CRM import (CSV/vCard) | OUT | TBD | Depends on open question resolution
    - Pulse briefing (cron-triggered) | OUT — requires Phase 4+5+6 | IN (PULSE-01, PULSE-02) | Cannot build before Phase 6 completes
    - Pulse output format | OUT | TBD | Open question to resolve
    - WhatsApp integration | OUT — ToS risk unresolved | TBD for v2 | Requires explicit Meta API decision first
    - Discord integration | OUT | TBD for v2 | Existing L2-BOT patterns available
    - Multi-channel simultaneously | OUT | OUT (post-v2) | One channel only at v2
    - Automated outbound without approval | OUT | OUT | Hard constraint — human approval always required
    - STT/TTS/overlay | OUT (D-009) | Milestone 2 | Separate from CRM/Pulse/Channels

    Below the table, add a prose note: "The v2 scope above is the intended scope definition for Milestone v2.0, not a commitment. Each IN item requires the corresponding open questions to be resolved before a build phase begins."

    ## 5. Research Brief
    Write a structured brief for a future deep-dive agent. The brief must be self-contained — a future agent with no prior project context should be able to execute it. Structure:

    ### Research Brief: CRM/Pulse/Channels Deep Dive (v2 Milestone)

    **Mission:** Resolve the open questions in Section 3 sufficiently to produce implementation-ready decisions for ATLAS Milestone v2.0.

    **Constraints (non-negotiable):**
    - CRM: ATLAS uses SQLite, not PostgreSQL. Do NOT design for Postgres or Twenty CRM's data model.
    - CRM: Must be Pydantic v2 compatible (D-012) — schema source of truth is atlas_core.schemas.
    - Channels/WhatsApp: Do NOT recommend unofficial Baileys library without a ToS analysis. The Meta Business API is the starting assumption.
    - STT/TTS/overlay: OUT OF SCOPE (D-009).
    - Electron: OUT OF SCOPE (D-005).

    **Research tasks (prioritized):**
    1. CRM linkage model: Evaluate FK mission_id column vs wiki-namespace entity representation. Produce a recommendation with tradeoffs.
    2. Contact schema: Propose a minimum viable Contact schema that supports future extension (metadata/custom field pattern). Reference Twenty 2.0's extension approach as a prior art comparison point — do NOT copy their schema.
    3. CRM audit integration: Determine whether CRM records should emit AuditEvents via the Phase 4 bus or maintain a separate change log. Recommend with rationale.
    4. Pulse trigger and format: Recommend one trigger mechanism (cron/event/explicit) and one output format (wiki page/CLI/notification) for PULSE-01. Both can be extended later — pick the simplest first.
    5. WhatsApp ToS analysis: Produce a legal/ToS risk assessment of the official Meta Business API for L2's use case. Answer: which account tier is required, what volume limits apply, and what approval flow is mandated for outbound messages.
    6. Channel storage model: Recommend whether channel conversations are stored as wiki pages, AuditEvents, or a separate channels table. Justify the choice against the Phase 2 schema.

    **Deliverables:**
    - A decision record for each of the 6 research tasks above (one recommendation per task, with rationale and tradeoffs).
    - Updated REQUIREMENTS.md with v2 requirement IDs (CRM-01 through CRM-N, PULSE-01 through PULSE-N, CHANNELS-01 if applicable) once the scope is confirmed.

    **NOT in scope for this research brief:**
    - Any v1 implementation.
    - Multi-tenant CRM.
    - Postgres migration.
    - More than one channel integration.
    - Automated outbound messaging.
    - STT/TTS (D-009).

    ## 6. Phase Dependencies
    State the prerequisite phase chain that must complete before any CRM/Pulse/Channels feature can be built:
    - Phase 4 (ATLAS Event Bus) — AuditEvent bus required by Pulse and CRM audit integration
    - Phase 5 (Mission & Run Lifecycle) — mission lifecycle required for CRM-to-mission linkage
    - Phase 6 (LLM Wiki Runtime) — wiki runtime required for Pulse data aggregation and optional CRM wiki-namespace model
    - Phase 7 (API Gateway) — typed REST API required for CRM/Channels API surface
    All of Phases 4–7 must be complete before v2 CRM/Pulse work begins.

    ## 7. Sources
    List the source documents synthesized in this intake: DEEP_RESEARCH_BACKLOG.md (R5, R6, R7), REQUIREMENTS.md (Future Requirements section), docs/decisions/2026-06-04_DECISION_REGISTER.md (D-007, D-010), 03-RESEARCH.md (RESEARCH-02 pre-analysis).

    Format: Standard markdown. Level-2 headings for major sections, level-3 for sub-sections. Numbered list for open questions. Table for MVP boundary. NO code blocks containing DDL, schema definitions, or API endpoint definitions — this is a scoping document, not a design document.
  </action>

  <verify>
    File docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md must:
    - exist at that exact path
    - contain the heading "## Open Questions" or "## 3. Open Questions"
    - contain the heading "## MVP Boundary" or "## 4. MVP Boundary"
    - contain the heading "## Research Brief" or "## 5. Research Brief"
    - contain "D-007" at least once
    - contain at least 10 numbered items in the Open Questions section (verify by counting lines matching pattern "^\d+\. ")
    - NOT contain "CREATE TABLE" or "CREATE INDEX" (no DDL)
    - NOT contain "def " or "class " (no Python code stubs)
    - NOT contain "v1" as a CRM feature label (CRM is v2 only per D-007)
  </verify>

  <acceptance_criteria>
    - File exists at docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md
    - Contains "## Open Questions" section (or equivalent level-2 heading)
    - Contains "## MVP Boundary" section (or equivalent level-2 heading) with a table
    - Contains "## Research Brief" section (or equivalent level-2 heading) with structured tasks
    - At least 14 numbered open questions present (covering CRM, Pulse, and Channels)
    - Explicitly references D-007 and states CRM is v2 scope only
    - Explicitly states STT/TTS is out of scope (D-009)
    - MVP boundary table includes rows for CRM Contact, Pulse briefing, WhatsApp integration, and STT/TTS
    - Research brief contains numbered research tasks and a "NOT in scope" list
    - No DDL (CREATE TABLE, etc.), no Python class definitions, no API endpoint definitions present
    - Phase dependency chain (Phases 4 → 5 → 6 → 7) is stated
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor write access | Executor writes one markdown file to the repo working tree — no user input, no network calls, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md | accept | New file creation; no existing content to corrupt; git diff after creation confirms scope |
| T-03-02-02 | Information Disclosure | CRM_PULSE_CHANNELS_DEEP_DIVE.md | accept | Internal architectural research document; no secrets, no PII, no credentials; documents only open questions and scope boundaries |
| T-03-02-03 | Spoofing | D-007/D-010 references | mitigate | Executor must read docs/decisions/2026-06-04_DECISION_REGISTER.md before writing; D-007 and D-010 entries must be cited accurately — do not paraphrase decision status |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages; no package manager operations are performed |

**Security note:** This is a documentation-only plan. No code is executed, no user input is processed, no authentication surfaces are introduced, and no external services are called. ASVS L1 categories V2–V6 are not applicable. The only integrity risk is the document drifting into implementation design (schema DDL, API definitions) which would violate D-007's scope boundary — mitigated by explicit acceptance criteria checks for the absence of DDL.
</threat_model>

<verification>
After the task completes, verify the following phase-level conditions:

1. docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md exists
2. grep "## Open Questions" docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md returns a result
3. grep "## MVP Boundary" docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md returns a result
4. grep "## Research Brief" docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md returns a result
5. grep -c "^[0-9]\+\. " docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md returns a number >= 10
6. grep "D-007" docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md returns at least one line
7. grep "CREATE TABLE" docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md returns no output
</verification>

<success_criteria>
RESEARCH-02 is satisfied when:
- docs/research/CRM_PULSE_CHANNELS_DEEP_DIVE.md exists
- It contains at least 14 numbered open questions grouped under CRM, Pulse, and Channels
- It contains a clear MVP boundary table distinguishing v1 (OUT), v2 (IN/TBD), and post-v2 items
- It contains a self-contained research brief that a future agent can execute without prior project context
- It explicitly acknowledges D-007 (CRM is v2, not v1) and D-009 (STT/TTS out of scope)
- It contains no schema DDL, no Python class definitions, and no API endpoint implementations
</success_criteria>

<output>
Create .planning/phases/03-research-closure/03-02-SUMMARY.md when done.
</output>
