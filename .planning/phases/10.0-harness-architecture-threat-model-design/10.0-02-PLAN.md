---
phase: 10.0-harness-architecture-threat-model-design
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/migrations/0004_registry_v2.sql
  - packages/atlas-core/atlas_core/schemas/registry_v2.py
  - packages/atlas-core/atlas_core/schemas/__init__.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A reviewer can read 0004_registry_v2.sql and confirm three new additive tables with the composite PK, source-scoped deactivation, and distinct status/auth_status, without the legacy model_registry table being dropped"
    - "The Pydantic models Provider/ModelV2/RoutePolicy mirror the DDL field-for-field and carry no secret material (auth_status is derived, not stored secret)"
    - "A future executor reading the migration knows route_policy is schema-only with no v1.1 reader/writer, the compat VIEW is a 10.3-cutover artifact, and FK is intentionally not enforced"
  artifacts:
    - path: "infra/migrations/0004_registry_v2.sql"
      provides: "provider_registry, model_registry_v2, route_policy additive DDL"
      contains: "PRIMARY KEY(model_id, provider_id, source)"
    - path: "packages/atlas-core/atlas_core/schemas/registry_v2.py"
      provides: "Provider / ModelV2 / RoutePolicy frozen Pydantic v2 models mirroring the DDL"
      contains: "class ModelV2"
    - path: "packages/atlas-core/atlas_core/schemas/__init__.py"
      provides: "barrel re-export of the new registry models"
      contains: "ModelV2"
  key_links:
    - from: "packages/atlas-core/atlas_core/schemas/registry_v2.py"
      to: "infra/migrations/0004_registry_v2.sql"
      via: "DDL-mirrors-Pydantic 1:1 (D-012)"
      pattern: "model_registry_v2"
---

<objective>
Draft the `0004_registry_v2.sql` additive migration (provider/model_v2/route tables) and the mirrored Pydantic v2 schema direction, committing the provider/model/route schema before the 10.3 discovery pipeline and gateway reader-cutover build against it. De-risks PROV-01 and MOD-01/03/04/05 (owned by 10.3).

Purpose: the migration + Pydantic models are the contract 10.3 implements. The composite key gives idempotent multi-source discovery; source-scoped deactivation isolates providers; distinct status/auth_status keeps reachability orthogonal to credential state. Drafting both now prevents later migration churn (route_policy) and locks the column-for-column shape.

Output: `infra/migrations/0004_registry_v2.sql`, `packages/atlas-core/atlas_core/schemas/registry_v2.py`, updated `schemas/__init__.py` barrel export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.0-harness-architecture-threat-model-design/10.0-CONTEXT.md
@.planning/phases/10.0-harness-architecture-threat-model-design/10.0-RESEARCH.md
@.planning/phases/10.0-harness-architecture-threat-model-design/10.0-PATTERNS.md
@.planning/phases/10.0-harness-architecture-threat-model-design/10.0-VALIDATION.md

<interfaces>
DDL house style to copy column-for-column (from infra/migrations/0003_model_registry.sql):

```sql
-- 0003: model registry for the AI router (D-017).
-- Mirrors atlas_runtime/model_registry.py ensure_schema(); both are
-- CREATE TABLE IF NOT EXISTS so apply order is idempotent.
-- The Rust gateway (D-022) reads this table directly for /models.

CREATE TABLE IF NOT EXISTS model_registry (
    model_id    TEXT PRIMARY KEY,
    provider    TEXT NOT NULL DEFAULT '',
    source      TEXT NOT NULL,
    first_seen  TEXT NOT NULL,
    last_seen   TEXT NOT NULL,
    active      INTEGER NOT NULL DEFAULT 1
);
```

Pydantic model shape to copy (from packages/atlas-core/atlas_core/schemas/core.py — frozen v2, Literal enums, ISO field_serializer, JSON-string blobs not dict):

```python
class Mission(BaseModel):
    model_config = ConfigDict(frozen=True, str_strip_whitespace=True)
    id: str = Field(default_factory=lambda: str(uuid.uuid4()))
    status: Literal["pending", "running", "succeeded", "failed", "cancelled"] = "pending"
    created_at: datetime.datetime = Field(default_factory=lambda: datetime.datetime.now(datetime.timezone.utc))
    @field_serializer("created_at", "updated_at")
    def serialize_dt(self, dt): return None if dt is None else dt.isoformat()
```

Conventions (D-012/D-013): all timestamps `TEXT NOT NULL` ISO-8601 (never DATETIME); booleans `INTEGER`; JSON blobs `TEXT ... DEFAULT '{}'` (Pydantic field = `str = "{}"`, never `dict[str, Any]`); pragmas declared ONCE in 0001 (do not redeclare in 0004); enums = `Literal[...]` not Python Enum.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft 0004_registry_v2.sql (embeds LANDMINE 4 + additive/no-DROP + VIEW note + no-FK note)</name>
  <files>infra/migrations/0004_registry_v2.sql</files>
  <read_first>
    - infra/migrations/0003_model_registry.sql (closest analog — same file family; copy the header + CREATE TABLE IF NOT EXISTS + column-alignment style)
    - infra/migrations/0001_core.sql (pragma-declared-once + multi-table + JSON-blob `TEXT DEFAULT '{}'` conventions)
    - services/agent-runtime/atlas_runtime/model_registry.py (the ensure_schema()-parity convention and the v1 source-scoped soft-delete the UPSERT pattern replaces; reference only — not edited here)
    - native/atlas-core-rs/crates/atlas-gateway/src/db.rs (list_models() selects named columns model_id, provider, source, first_seen, last_seen, active FROM model_registry — this is why the legacy table must NOT be dropped and why the compat VIEW is a drop-in only at cutover)
    - .planning/phases/10.0-harness-architecture-threat-model-design/10.0-RESEARCH.md "Area 3" (composite PK, UPSERT preserving first_seen, source-scoped deactivation, VIEW option, FK open question) and 10.0-PATTERNS.md "0004" section + Landmines table
    - the file being written: infra/migrations/0004_registry_v2.sql (new)
  </read_first>
  <action>
    Create infra/migrations/0004_registry_v2.sql as a purely ADDITIVE migration matching the 0003/0001 DDL house style (header comment `-- 0004: provider/model_v2/route registry (PROV-01, MOD-03/04/05).` + a `-- Mirrors atlas_core.schemas.registry_v2 / the 10.3 v2 discovery ensure_schema(); CREATE TABLE IF NOT EXISTS so apply order is idempotent.` note). Define exactly three tables, column-for-column per seed §8 / CONTEXT Area 3, with aligned columns, ISO-8601 `TEXT` timestamps, `TEXT ... DEFAULT '{}'` JSON blobs:
    1. `provider_registry(provider_id TEXT PRIMARY KEY, display_name TEXT, auth_type TEXT, default_base_url TEXT, api_modes_json TEXT NOT NULL DEFAULT '{}', source TEXT, status TEXT, last_checked TEXT, last_error TEXT)`.
    2. `model_registry_v2(model_id TEXT, provider_id TEXT, source TEXT, api_mode TEXT, status TEXT, auth_status TEXT, context_window INTEGER, metadata_json TEXT NOT NULL DEFAULT '{}', first_seen TEXT NOT NULL, last_seen TEXT NOT NULL, deactivated_at TEXT, PRIMARY KEY(model_id, provider_id, source))` — composite PK as a table-level constraint (last item in the column list); `status` and `auth_status` are DISTINCT columns; `deactivated_at` is a nullable timestamp for source-scoped soft-delete.
    3. `route_policy(task_class TEXT PRIMARY KEY, provider_id TEXT, model_id TEXT, fallback_policy_json TEXT NOT NULL DEFAULT '{}', updated_at TEXT)`.
    Add `CREATE INDEX IF NOT EXISTS` on `model_registry_v2(source)` and `model_registry_v2(source, deactivated_at)` (support the source-scoped deactivation pass).
    Do NOT redeclare pragmas (set once in 0001). Do NOT add an FTS5 table.
    EMBED, as SQL comments:
    - The hard rule: this migration is ADDITIVE and never removes the legacy `0003` `model_registry` table; the Rust gateway reads `model_registry` directly until the 10.3 reader cutover. (Phrase this comment so the literal token `DROP TABLE model_registry` never appears on any line — e.g. write "never DROPs/ALTERs the legacy model_registry table" — so the no-DROP gate cannot self-trip on its own warning comment.)
    - LANDMINE 4: `route_policy` is SCHEMA-ONLY in v1.1 — no reader/writer; routing enforcement / `atlas route` CLI is v1.2+ (ROUTE-01/02 deferred). Mark explicitly so a future executor does not assume it is wired.
    - The compat-VIEW design note as a COMMENTED option (not live DDL): a `CREATE VIEW model_registry AS SELECT model_id, provider_id AS provider, source, first_seen, last_seen, CASE WHEN deactivated_at IS NULL THEN 1 ELSE 0 END AS active FROM model_registry_v2;` is the recommended 10.3 cutover mechanism (drop-in for the gateway's named-column read) but cannot coexist with the legacy table of the same name, so it lands only AT the cutover.
    - The idempotent UPSERT contract (as a comment, justifying the DDL): `INSERT ... ON CONFLICT(model_id, provider_id, source) DO UPDATE SET ... last_seen = excluded.last_seen, deactivated_at = NULL` with `first_seen` intentionally NOT in the SET clause (preserves the original). The UPSERT itself is owned by 10.3.
    - The no-FK note: `model_registry_v2.provider_id` / `route_policy.provider_id` are NOT enforced as foreign keys in v1.1 (discovery may write a model before its provider row exists); the logical relationship is documented, hard FK deferred to v1.2.
  </action>
  <acceptance_criteria>
    - File infra/migrations/0004_registry_v2.sql exists.
    - `grep -c 'PRIMARY KEY(model_id, provider_id, source)' infra/migrations/0004_registry_v2.sql` >= 1.
    - Defines all three tables: grep matches `CREATE TABLE IF NOT EXISTS provider_registry`, `model_registry_v2`, `route_policy`.
    - Both `status` and `auth_status` columns present in model_registry_v2; `deactivated_at` present.
    - HARD RULE enforced (executable DDL only, comment lines excluded): `grep -v '^[[:space:]]*--' infra/migrations/0004_registry_v2.sql | grep -c 'DROP TABLE model_registry'` == 0 AND no executable `ALTER TABLE model_registry`. (Comment lines are filtered so the embedded no-DROP warning note cannot self-trip the gate.)
    - route_policy marked schema-only (grep -i 'schema-only' or 'no reader/writer' or 'v1.2').
    - Compat VIEW present only as a comment, never as live executable DDL outside a comment line (the `CREATE VIEW model_registry` text appears only on `--`-prefixed lines).
    - UPSERT contract documented: grep matches `ON CONFLICT` and `first_seen` with a "not in SET"/"preserve" note.
    - No-FK note present (grep -i 'foreign key' or 'FK' near 'v1.2'/'not enforced').
  </acceptance_criteria>
  <verify>
    <automated>test -f infra/migrations/0004_registry_v2.sql && grep -q 'PRIMARY KEY(model_id, provider_id, source)' infra/migrations/0004_registry_v2.sql && grep -q 'provider_registry' infra/migrations/0004_registry_v2.sql && grep -q 'model_registry_v2' infra/migrations/0004_registry_v2.sql && grep -q 'route_policy' infra/migrations/0004_registry_v2.sql && grep -q 'auth_status' infra/migrations/0004_registry_v2.sql && grep -q 'deactivated_at' infra/migrations/0004_registry_v2.sql && [ "$(grep -v '^[[:space:]]*--' infra/migrations/0004_registry_v2.sql | grep -c 'DROP TABLE model_registry')" -eq 0 ] && grep -q 'ON CONFLICT' infra/migrations/0004_registry_v2.sql && echo PASS</automated>
  </verify>
  <done>0004_registry_v2.sql drafted: three additive tables with composite PK, source-scoped deactivation, distinct status/auth_status, indexes, and the embedded no-DROP rule + LANDMINE 4 + VIEW note + UPSERT contract + no-FK note.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror Pydantic v2 schema direction (Provider / ModelV2 / RoutePolicy)</name>
  <files>packages/atlas-core/atlas_core/schemas/registry_v2.py, packages/atlas-core/atlas_core/schemas/__init__.py</files>
  <read_first>
    - packages/atlas-core/atlas_core/schemas/core.py (the canonical frozen Pydantic v2 shape: model_config frozen + str_strip_whitespace, Literal enums, @field_serializer ISO timestamps, JSON-string blob fields like AuditEvent.data, SECRET_PATTERNS, Optional[datetime] for nullable timestamps like Run.finished_at)
    - packages/atlas-core/atlas_core/schemas/__init__.py (the alphabetical barrel re-export + __all__ pattern to extend)
    - infra/migrations/0004_registry_v2.sql (the DDL these models must mirror 1:1 — written in Task 1)
    - .planning/phases/10.0-harness-architecture-threat-model-design/10.0-PATTERNS.md "Pydantic models" section (house conventions + the credential-boundary landmine)
    - the files being written: packages/atlas-core/atlas_core/schemas/registry_v2.py (new), packages/atlas-core/atlas_core/schemas/__init__.py (extend)
  </read_first>
  <action>
    Create packages/atlas-core/atlas_core/schemas/registry_v2.py with three frozen Pydantic v2 models mirroring the 0004 DDL field-for-field (name-for-name, type-intent-for-type: TEXT->str, INTEGER->int, `TEXT DEFAULT '{}'`->`str = "{}"`). Follow core.py conventions exactly: `from __future__ import annotations`; `model_config = ConfigDict(frozen=True, str_strip_whitespace=True)` on every model; `Literal[...]` unions for enum-like columns (NOT Python Enum); `@field_serializer` returning `.isoformat()` for datetime fields; `Optional[datetime.datetime] = None` for nullable timestamps (`deactivated_at`, `last_checked`).
    - `Provider`: fields `provider_id` (natural str key, NOT a uuid default-factory), `display_name`, `auth_type`, `default_base_url`, `api_modes_json: str = "{}"`, `source`, `status`, `last_checked: Optional[datetime] = None`, `last_error`.
    - `ModelV2`: identity is the NATURAL composite (`model_id`, `provider_id`, `source`) — do NOT auto-generate a uuid id. Fields: `model_id`, `provider_id`, `source`, `api_mode`, `status` (Literal vocabulary: available/offline/unsupported/...), `auth_status` (Literal vocabulary: available/auth_present/needs_api_key/needs_login/offline/rate_limited/...), `context_window: int`, `metadata_json: str = "{}"`, `first_seen: datetime`, `last_seen: datetime`, `deactivated_at: Optional[datetime] = None`.
    - `RoutePolicy`: `task_class` (natural str key), `provider_id`, `model_id`, `fallback_policy_json: str = "{}"`, `updated_at: datetime`.
    - CREDENTIAL BOUNDARY (landmine): no model carries secret material. `auth_status` is a DERIVED status Literal computed at discovery time from the file auth store — document this in the model docstring; only `provider_id` + derived status live here, never a key.
    - Add a module docstring stating "DDL in infra/migrations/0004_registry_v2.sql mirrors these fields 1:1 (D-012)."
    Then extend packages/atlas-core/atlas_core/schemas/__init__.py to re-export `Provider`, `ModelV2`, `RoutePolicy` and add them to `__all__`, preserving the existing alphabetical block.
  </action>
  <acceptance_criteria>
    - File packages/atlas-core/atlas_core/schemas/registry_v2.py exists with `class Provider`, `class ModelV2`, `class RoutePolicy`.
    - Each class uses `ConfigDict(frozen=True, str_strip_whitespace=True)`.
    - ModelV2 has fields model_id, provider_id, source, api_mode, status, auth_status, context_window, metadata_json, first_seen, last_seen, deactivated_at (grep each) and does NOT use a uuid default-factory for identity.
    - JSON blob fields are `str` (grep `api_modes_json: str`, `metadata_json: str`, `fallback_policy_json: str`), never `dict`.
    - auth_status documented as derived/never-a-secret (grep -i 'derived' or 'never' near 'auth_status'/'secret').
    - __init__.py re-exports and __all__-lists ModelV2 (grep ModelV2 in __init__.py).
    - Module imports cleanly: `python -c "from atlas_core.schemas import Provider, ModelV2, RoutePolicy"` succeeds (run from the package's import root, e.g. packages/atlas-core or wherever atlas_core is importable).
  </acceptance_criteria>
  <verify>
    <automated>test -f packages/atlas-core/atlas_core/schemas/registry_v2.py && grep -q 'class Provider' packages/atlas-core/atlas_core/schemas/registry_v2.py && grep -q 'class ModelV2' packages/atlas-core/atlas_core/schemas/registry_v2.py && grep -q 'class RoutePolicy' packages/atlas-core/atlas_core/schemas/registry_v2.py && grep -q 'frozen=True' packages/atlas-core/atlas_core/schemas/registry_v2.py && grep -q 'auth_status' packages/atlas-core/atlas_core/schemas/registry_v2.py && grep -q 'metadata_json: str' packages/atlas-core/atlas_core/schemas/registry_v2.py && grep -q 'ModelV2' packages/atlas-core/atlas_core/schemas/__init__.py && (cd packages/atlas-core && python -c "from atlas_core.schemas import Provider, ModelV2, RoutePolicy" ) && echo PASS</automated>
  </verify>
  <done>registry_v2.py committed with Provider/ModelV2/RoutePolicy mirroring the DDL 1:1, frozen, JSON-string blobs, derived auth_status (no secrets), and re-exported from the schemas barrel; imports cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovery writer -> SQLite registry | the registry must reference provider_id + derived auth_status only; never store secret material |
| Rust gateway reader -> model_registry table | the gateway reads the legacy table directly; the migration must not break that read path mid-milestone |

## STRIDE Threat Register

This plan ships a drafted DDL file + Pydantic models (no executing runtime). Residual surface is schema/document correctness.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.0-06 | Information Disclosure | secret leakage into SQLite registry | mitigate | Credential boundary: Provider/ModelV2 carry no secret; auth_status is derived; registry references provider_id only (SEC-01) |
| T-10.0-07 | DoS / Availability | gateway read path broken by migration | mitigate | Migration is additive; no DROP/ALTER of legacy model_registry; gateway keeps reading it until 10.3 cutover (verified named-column read in db.rs) |
| T-10.0-08 | Tampering | cross-source deactivation collision | mitigate | Composite PK + source-scoped deactivated_at design (MOD-04) isolates each source's rows |
| T-10.0-09 | Tampering | dead/unwired schema assumed live | accept | route_policy is schema-only in v1.1 (LANDMINE 4); marked explicitly in the migration comment; accepted because creating it now avoids a v1.2 migration churn |

No package-manager installs occur in this plan. No `high` residual threat introduced: a drafted `.sql` and Pydantic models execute nothing in 10.0; the additive-migration design keeps the gateway green.
</threat_model>

<verification>
- 0004_registry_v2.sql exists with all three tables, the composite PK, distinct status/auth_status, source-scoped deactivated_at, indexes, and embeds the no-DROP rule + LANDMINE 4 + VIEW note + UPSERT contract + no-FK note.
- `grep -c 'DROP TABLE model_registry' infra/migrations/0004_registry_v2.sql` == 0.
- registry_v2.py defines Provider/ModelV2/RoutePolicy mirroring the DDL 1:1, frozen, JSON-string blobs, derived auth_status; re-exported and imports cleanly.
</verification>

<success_criteria>
The additive `0004` migration and the mirrored Pydantic schema direction are committed, de-risking PROV-01 / MOD-03/04/05, with the legacy table untouched and all Area-3 landmines embedded.
</success_criteria>

<output>
Create `.planning/phases/10.0-harness-architecture-threat-model-design/10.0-02-SUMMARY.md` when done.
</output>
