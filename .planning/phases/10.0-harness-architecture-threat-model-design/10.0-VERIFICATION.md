---
phase: 10.0-harness-architecture-threat-model-design
verified: 2026-06-15T22:30:00Z
status: passed
score: 8/8
overrides_applied: 0
re_verification: null
gaps: []
deferred: []
human_verification: []
---

# Phase 10.0: Harness Architecture & Threat-Model Design — Verification Report

**Phase Goal:** Commit the auth-store layout, adapter boundary, registry schema, and security threat models before any harness code is written.
**Verified:** 2026-06-15T22:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

This is a DESIGN / DOCUMENTS-ONLY phase (precedent: v1.0 Phase 7). It ships NO runtime code except one additive SQL migration and Pydantic schemas. Acceptance is design-document correctness (grep gates) and schema/migration validity — not feature behavior.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Auth-store layout decided and documented (flat `~/.atlas/auth.json`, ATLAS_HOME-aware resolver, single-function change for profiles) | VERIFIED | `AUTH_STORE_DESIGN.md` L53–65 commits the layout; `auth_store_path()` resolver named; grep: ATLAS_HOME, auth.json, PASS |
| 2 | Adapter boundary documented: ATLAS adapter in `services/agent-runtime/`; `foundation/` changes are extension-points only; DIVERGENCE_LOG naming reconciled | VERIFIED | `ADAPTER_BOUNDARY_DESIGN.md` §§Goal/Composition/One-way; DIV-F-* labelled incorrect and superseded; D-LOG-002 back-filled in DIVERGENCE_LOG.md |
| 3 | `0004_registry_v2.sql` schema drafted (provider/model_v2/route tables) with composite key and source-scoped deactivation | VERIFIED | File exists; `PRIMARY KEY(model_id, provider_id, source)`, `deactivated_at`, `auth_status` all present; no executable DROP TABLE model_registry |
| 4 | OAuth-callback and native-IPC threat-model drafts written; fallback-cascade spec committed | VERIFIED | All 5 docs exist; all per-task grep gates PASS (confirmed directly below) |
| 5 | Every landmine (1–7) is embedded in the relevant artifact as a load-bearing caveat | VERIFIED | L1/L2 in AUTH_STORE_DESIGN; L3 in ADAPTER_BOUNDARY_DESIGN + DIVERGENCE_LOG; L4 in 0004 SQL comment; L5 in NATIVE_IPC; L6+L7 in FALLBACK_CASCADE |
| 6 | All docs carry Phase 7 shape ("owns no v1 REQ-IDs; de-risks <IDs>") | VERIFIED | grep "owns no v1 REQ-IDs" PASS on all 5 docs |
| 7 | Cross-references are coherent: adapter references cascade; schemas mirror DDL; IPC doc references loopback HTTP gateway | VERIFIED | ADAPTER_BOUNDARY_DESIGN has 3 grep hits for FALLBACK_CASCADE; registry_v2.py carries "DDL in 0004_registry_v2.sql mirrors these fields 1:1 (D-012)"; NATIVE_IPC_THREAT_MODEL references 127.0.0.1:8484 and marks it NOT an IPC surface |
| 8 | Hand-off contracts present in all docs (target consuming phase named explicitly) | VERIFIED | Hand-off sections present in AUTH_STORE (→10.1), ADAPTER_BOUNDARY (→10.2), FALLBACK_CASCADE (→10.2), OAUTH (→future spike), NATIVE_IPC (→10.5) |

**Score:** 8/8 truths verified

---

### Landmine Coverage Detail

| Landmine | Location | Key text | Verified |
|----------|----------|----------|---------|
| LANDMINE 1 (os.replace non-atomicity on Windows) | `AUTH_STORE_DESIGN.md` §LANDMINE 1 | "not guaranteed to be atomic ... non-atomic CopyFile ... cross-process lock — not the rename — is the no-corruption guarantee" | VERIFIED |
| LANDMINE 2 (OS-handle advisory lock, not presence-based) | `AUTH_STORE_DESIGN.md` §LANDMINE 2 | "fcntl.flock ... msvcrt.locking on an open file descriptor ... NOT a presence-based lock ... deadlocks after a crash" | VERIFIED |
| LANDMINE 3 (DIVERGENCE_LOG naming; DIV-F incorrect) | `ADAPTER_BOUNDARY_DESIGN.md` §LANDMINE 3 + `DIVERGENCE_LOG.md` D-LOG-002 | "DIV-F-002 ... incorrect and superseded ... canonical id scheme D-LOG-NNN ... back-filled as D-LOG-002" | VERIFIED |
| LANDMINE 4 (route_policy schema-only in v1.1) | `0004_registry_v2.sql` SQL comment block | "SCHEMA-ONLY IN v1.1 — no reader/writer is wired in this milestone" | VERIFIED |
| LANDMINE 5 (PTY-as-byte-channel hard gate) | `NATIVE_IPC_THREAT_MODEL.md` §5 | "transports keystrokes and raw bytes ... MUST NEVER transport a command string or argv from the webview" | VERIFIED |
| LANDMINE 6 (400 requires body inspection; ambiguous→HALT) | `FALLBACK_CASCADE.md` §LANDMINE 6 | "cannot be classified from status code alone ... inspect the error body ... Safe default when ambiguous: HALT" | VERIFIED |
| LANDMINE 7 (non-conformant providers; garbled→CASCADE) | `FALLBACK_CASCADE.md` §LANDMINE 7 | "garbled ... non-conforming response ... treated as a CASCADE-class transport failure" | VERIFIED |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docs/architecture/AUTH_STORE_DESIGN.md` | Auth-store layout, resolver, atomic-write+lock, perms, secrets-at-rest | VERIFIED | 221 lines; all Plan 01 Task 1 grep gates PASS; commit f6a29ec |
| `docs/architecture/ADAPTER_BOUNDARY_DESIGN.md` | Adapter/foundation boundary, 5 responsibilities, DIVERGENCE_LOG reconciliation | VERIFIED | All Plan 01 Task 2 grep gates PASS; commit 0c3378e |
| `docs/architecture/FALLBACK_CASCADE.md` | Canonical HALT/CASCADE error-classification table | VERIFIED | All Plan 01 Task 3 grep gates PASS; commit 093ffdb |
| `foundation/atlas-hermes/DIVERGENCE_LOG.md` (D-LOG-002) | Numbered divergence record for atlas_audit shim | VERIFIED | D-LOG-002 heading present; atlas_audit entry present; commit 0c3378e |
| `infra/migrations/0004_registry_v2.sql` | provider_registry, model_registry_v2, route_policy additive DDL | VERIFIED | All Plan 02 Task 1 grep gates PASS; no executable DROP TABLE; commit 1cc3c9f |
| `packages/atlas-core/atlas_core/schemas/registry_v2.py` | Provider/ModelV2/RoutePolicy frozen Pydantic v2 models | VERIFIED | All Plan 02 Task 2 grep gates PASS; commit 6721835 |
| `packages/atlas-core/atlas_core/schemas/__init__.py` | Barrel re-export of the 3 new registry models | VERIFIED | ModelV2/Provider/RoutePolicy imported and in __all__; import verified by orchestrator |
| `docs/security/OAUTH_CALLBACK_THREAT_MODEL.md` | Deferred-OAuth callback gate spec | VERIFIED | All Plan 03 Task 1 grep gates PASS (PKCE, S256, hmac.compare_digest, constant-time, SEC-05); commit 31831a6 |
| `docs/security/NATIVE_IPC_THREAT_MODEL.md` | Tauri 2 IPC command inventory + PTY-byte-channel hard gate | VERIFIED | All Plan 03 Task 2 grep gates PASS (pty_open/write, deny-by-default, NAT-03, 127.0.0.1); commit 4573947 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ADAPTER_BOUNDARY_DESIGN.md` | `FALLBACK_CASCADE.md` | Cross-reference "cascade is adapter-driven" | VERIFIED | 3 grep hits for FALLBACK_CASCADE in ADAPTER doc; cascade ownership explicitly stated as adapter responsibility (b) |
| `registry_v2.py` | `0004_registry_v2.sql` | DDL-mirrors-Pydantic 1:1 (D-012) | VERIFIED | Module docstring: "DDL in infra/migrations/0004_registry_v2.sql mirrors these fields 1:1 (D-012)"; field-for-field match confirmed by read |
| `NATIVE_IPC_THREAT_MODEL.md` | `native/atlas-core-rs/crates/atlas-gateway` | Loopback HTTP posture reference (127.0.0.1, not an IPC surface) | VERIFIED | §4 explicitly states "atlas-gateway binds exclusively to 127.0.0.1 (port 8484)" and "is NOT an IPC surface" |
| `AUTH_STORE_DESIGN.md` | `ADAPTER_BOUNDARY_DESIGN.md` | Credential boundary (secrets only in file store; adapter resolves from auth store) | VERIFIED | ADAPTER §(a) references AUTH_STORE_DESIGN.md explicitly; credential boundary section in AUTH doc cross-references registry |
| `0004_registry_v2.sql` | Legacy `model_registry` (0003) | Additive-only, gateway compat preserved | VERIFIED | Comment block lines 4–7 state the no-DROP rule; compat VIEW is comment-only (line 135: `-- CREATE VIEW model_registry AS`) |

---

### Data-Flow Trace (Level 4)

Not applicable. Phase 10.0 ships no runtime code with data flows. The migration and Pydantic schemas are inert artifacts (DDL + type definitions); no component renders dynamic data from them in this phase.

---

### Behavioral Spot-Checks

Not applicable. No new runnable entry points in this phase (design/schema-only). The orchestrator confirmed: atlas-core schema import OK; 33/33 atlas-core pytest pass; all 4 migrations apply cleanly on fresh SQLite.

---

### Probe Execution

No probes declared or applicable for this design-only phase.

---

### Requirements Coverage

Phase 10.0 owns no v1 REQ-IDs by design (explicit ROADMAP entry: "Requirements: none (design/enabling phase — precedent: v1.0 Phase 7)"). All three plans carry `requirements: []`. This is not a gap — it matches the ROADMAP contract.

The phase de-risks the following REQ-IDs owned by downstream phases:

| Downstream Phase | De-risked REQ-IDs | Document |
|-----------------|-------------------|----------|
| 10.1 | AUTH-01, AUTH-02, SEC-03 | AUTH_STORE_DESIGN.md |
| 10.2 | AGNT-01..06, AGNT-02, AGNT-03, AUD-01 | ADAPTER_BOUNDARY_DESIGN.md, FALLBACK_CASCADE.md |
| 10.3 | PROV-01, MOD-01, MOD-03, MOD-04, MOD-05 | 0004_registry_v2.sql, registry_v2.py |
| 10.5 | SEC-04, NAT-03 | NATIVE_IPC_THREAT_MODEL.md |
| Future spike | SEC-05 | OAUTH_CALLBACK_THREAT_MODEL.md |

---

### Anti-Patterns Found

None. Scan across all 8 produced artifacts returned zero hits for TBD, FIXME, XXX, PLACEHOLDER, TODO, HACK, or unresolved debt markers. No stub patterns detected (no runtime code was produced — design documents by nature are not stub-screened for empty handlers or return-null). No console.log, no hardcoded empty data in rendering paths.

---

### Human Verification Required

None. This phase produces only design documents and an inert schema/migration. All acceptance criteria are programmatically verifiable via grep gates, which were confirmed above. Visual/UX/behavioral checks do not apply.

---

### Gaps Summary

No gaps. All 8 must-have truths are VERIFIED. All 9 artifacts are present, substantive, and cross-referenced correctly. All 7 landmines are embedded in the correct documents with the required wording. All per-task automated grep gates PASS. No debt markers. No stubs. Commit history confirms all 7 commits exist in the repository.

---

## ROADMAP Success Criteria — Verdict

| SC | Criterion | Status |
|----|-----------|--------|
| 1 | Auth-store layout decided and documented (flat `~/.atlas/auth.json`, single resolver, profile-ready) | VERIFIED |
| 2 | Adapter boundary documented (services/agent-runtime, extension-points only, no foundation imports reversed) | VERIFIED |
| 3 | `0004_registry_v2.sql` drafted (provider/model_v2/route, composite key, source-scoped deactivation) | VERIFIED |
| 4 | OAuth-callback and native-IPC threat-model drafts written; fallback-cascade spec committed | VERIFIED |

**Overall: PASS** — Phase 10.0 goal achieved. All downstream phases (10.1–10.5) inherit reviewed, landmine-embedded specs rather than blank pages.

---

_Verified: 2026-06-15T22:30:00Z_
_Verifier: Claude (gsd-verifier)_
