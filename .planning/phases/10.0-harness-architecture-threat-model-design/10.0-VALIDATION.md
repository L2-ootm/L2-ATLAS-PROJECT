---
phase: 10.0
slug: harness-architecture-threat-model-design
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-15
---

# Phase 10.0 — Validation Strategy

> Per-phase validation contract. **10.0 ships design artifacts (documents + drafted DDL), not runtime code.** Validation here = checker/reviewer assertions over the committed documents and SQL, NOT unit tests of behavior. The runtime test suites for these designs are authored downstream (10.1 auth, 10.2 adapter/fallback, 10.3 registry, 10.5 IPC) — surfaced in "Downstream Wave 0" below so the implementing phases inherit them.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | none for 10.0 — design/document review (downstream phases use `pytest` + `cargo test`, already green per STATE.md) |
| **Config file** | none — no test code authored in 10.0 |
| **Quick run command** | `gsd-sdk query roadmap.get-phase 10.0` + document review of the 10.0 deliverables |
| **Full suite command** | n/a (10.0 ships no executable code) |
| **Estimated runtime** | n/a |

---

## Sampling Rate

- **After every task commit:** Reviewer confirms the deliverable's assertion row (below) holds.
- **After every plan wave:** Re-read the committed artifact end-to-end against its assertion row.
- **Before `/gsd-verify-work`:** Every assertion row marked ✅.
- **Max feedback latency:** document review — immediate.

---

## Per-Task Verification Map (design-deliverable assertions)

| Task ID | Deliverable | Requirement de-risked | Threat Ref | Assertion (checker confirms) | Test Type | File Exists | Status |
|---------|-------------|-----------------------|------------|------------------------------|-----------|-------------|--------|
| 10.0-auth | Auth-store design doc | AUTH-01/02, SEC-03 | — | Single path resolver + `ATLAS_HOME` override; temp+`os.replace`+OS-handle-lock+fsync named; **Windows `os.replace` non-atomicity caveat recorded**; `0600` + `icacls /inheritance:r /grant:r`; "lock, not rename, is the no-corruption guarantee" stated | doc review | ⬜ | ⬜ pending |
| 10.0-ddl | `0004_registry_v2.sql` draft | PROV-01, MOD-03/04/05 | — | 3 tables; `model_registry_v2` PK=`(model_id,provider_id,source)`; `deactivated_at` present; `status` AND `auth_status` both present; **does NOT DROP `model_registry`**; idempotent UPSERT preserving `first_seen` documented | DDL review | ⬜ | ⬜ pending |
| 10.0-pyd | Pydantic schema direction | MOD-* (D-012) | — | `Provider`/`ModelV2`/`RoutePolicy` field types mirror the DDL | doc review | ⬜ | ⬜ pending |
| 10.0-adapter | Adapter-boundary design doc | AGNT-01..06 | — | Composition (import+instantiate `AIAgent`); one-way dep; 5 adapter responsibilities listed; `atlas_audit` emission path named; "wrap not rewrite" hard gate; DIVERGENCE_LOG naming reconciled (LANDMINE 3) | doc review | ⬜ | ⬜ pending |
| 10.0-oauth | OAuth-callback threat-model draft | SEC-05 | T-10.0-OAUTH | PKCE-S256, constant-time `state` validation, one-shot `127.0.0.1:0` listener, exact redirect match, no-secret-in-logs listed as required gates; OAuth marked deferred | doc review | ⬜ | ⬜ pending |
| 10.0-ipc | Native-IPC threat-model draft | SEC-04, NAT-03 | T-10.0-IPC | Every planned IPC command as `{command,params,caller,privilege,threat,mitigation}`; PTY-byte-channel hard gate; Tauri 2 deny-by-default capability model referenced | doc review | ⬜ | ⬜ pending |
| 10.0-cascade | Fallback-cascade error table | AGNT-02/03, AUD-01 | — | OpenAI-first then health-ordered; 401/403 HALT; network/5xx/429 CASCADE; 404 model-scoped; 400 HALT-with-body-inspection (LANDMINE 6); refusal≠error; bounded attempts; provider-used surfaced | doc review | ⬜ | ⬜ pending |

*Status: ⬜ pending · ✅ confirmed · ❌ missing/incorrect*

---

## Wave 0 Requirements

*Existing infrastructure covers 10.0 — no test code is authored in a design phase. No framework install needed.*

### Downstream Wave 0 (inherited by implementing phases — NOT authored in 10.0)

- [ ] **10.1** `test_auth_store.py` — roundtrip, concurrent cross-process lock, malformed-file-safe, redaction, Windows path, `icacls` perms, `~/.codex` byte-identity (seed §13).
- [ ] **10.2** `test_adapter_fallback.py` — each error-class → HALT/CASCADE; provider-used surfaced; audit `provider_fallback{from,to,reason}` emitted.
- [ ] **10.3** `test_registry_v2.py` — idempotent UPSERT preserves `first_seen`; source-scoped deactivation; `status`≠`auth_status`; v1-row backward compat; gateway VIEW cutover.
- [ ] **10.5** `test_ipc_allowlist` — capability allowlist deny-by-default; PTY rejects command strings.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| All 10.0 deliverables | (design-only) | 10.0 ships documents + drafted DDL, not runnable behavior; correctness is established by review against the Per-Task assertion rows, not by executing code | For each row above, open the committed artifact and confirm the assertion text holds verbatim |

---

## Validation Sign-Off

- [ ] Every Per-Task assertion row confirmed ✅ against its committed artifact
- [ ] All 7 RESEARCH landmines reflected in the relevant deliverable
- [ ] `0004_registry_v2.sql` draft does not DROP the legacy `model_registry` table
- [ ] Both threat-model drafts present with their hard-gate statements
- [ ] Downstream Wave 0 list carried into 10.1/10.2/10.3/10.5 planning
- [x] `nyquist_compliant: true` set (design-review validation contract, no runtime tests in 10.0)

**Approval:** pending
