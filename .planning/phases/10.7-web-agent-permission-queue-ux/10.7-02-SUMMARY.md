# 10.7-02 Summary: Deterministic Policy and Hardline Authority

**Completed:** 2026-06-29  
**Requirements:** WEB-04, WEB-05  
**Status:** Green

## Delivered

- Replaced the fixed read/write/shell decision with one deterministic evaluator implementing:
  hardline → master deny → master preset/rules → narrowing profile → scoped allow → default.
- Added manual, smart, and full-autonomy behavior with deterministic explain receipts.
- Added an immutable, versioned hardline corpus for filesystem-root destruction, block devices,
  shutdown/reboot, and fork bombs, with benign near-match tests.
- Added canonical workspace confinement and trusted explicit ATLAS maintenance scope.
- Integrated the evaluator at `tool_service.invoke`, including:
  - denial without an executable approval;
  - immediate execution for policy allows;
  - existing broker queue for asks;
  - exact session-scoped allow rule consumption; and
  - receipt provenance in audit events and approval records.
- Added additive migration `0018_permission_policy_receipts.sql` with nullable
  `tool_approvals.policy_receipt`; pre-migration rows load with `None`.
- Added bounded adapter waits with one-second heartbeat/cancellation checks and terminal rejection
  on silence, timeout, cancellation, or required-channel disconnect.

## Review Incorporation

- The migration strategy is now concrete and tested.
- Go compatibility is locked to explicit gateway-upgrade remediation, never an actionable global
  fallback.
- React test bootstrap cost is explicit in Plan 05.
- Plan 07 now audits dependency manifests across Python, Rust, Go, and React and runs the complete
  test/build gate rather than only `git diff --check`.
- The prior Rust formatting changes were already isolated in commit `7893ada`.

## TDD and Verification Evidence

- RED: policy/hardline tests failed on missing `PolicyFacts` and hardline module.
- RED: tool integration failed until policy receipts, deny/allow execution, and scoped rules were
  wired at the chokepoint.
- RED: wait tests failed until bounded heartbeat/timeout/cancellation/channel semantics existed.
- GREEN: targeted policy/broker/tool suite passed 79 tests.
- GREEN: atlas-core passed **97** tests.
- GREEN: full agent-runtime passed **708**, with **2 skipped**.
- GREEN: Ruff check and format-check passed for all changed runtime/test modules.

No new production dependency, message bus, ORM, queue, or policy store was added.

