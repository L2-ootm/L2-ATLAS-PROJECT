---
phase: 10.7-web-agent-permission-queue-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/atlas-core/atlas_core/schemas/control_plane.py
  - packages/atlas-core/tests/test_permission_policy_schema.py
  - services/agent-runtime/atlas_runtime/control_plane_service.py
  - services/agent-runtime/tests/test_permission_policy_config.py
  - services/agent-runtime/tests/fixtures/surface_event_parity.json
  - services/agent-runtime/tests/fixtures/permission_policy_matrix.json
autonomous: true
requirements: [WEB-05]
must_haves:
  truths:
    - "Decision coverage: D-02, D-03, D-04, D-05, D-06, D-07, D-10, D-21, D-22, D-24, D-25"
    - "Permission policy is versioned JSON-stable data with backward-compatible defaults"
    - "A surface/workspace/project/agent/channel profile can narrow but never widen the master ceiling"
    - "The event and policy fixtures are frozen before gateway or frontend work begins"
  artifacts:
    - path: "packages/atlas-core/atlas_core/schemas/control_plane.py"
      provides: "Permission preset, selector, rule, profile, and explain-receipt schemas"
    - path: "services/agent-runtime/tests/fixtures/permission_policy_matrix.json"
      provides: "Cross-language policy contract fixture"
    - path: "services/agent-runtime/tests/fixtures/surface_event_parity.json"
      provides: "Every canonical normalized event kind in stable sequence"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/control_plane_service.py"
      to: "packages/atlas-core/atlas_core/schemas/control_plane.py"
      via: "validated revisioned config mutation"
      pattern: "Permission"
---

<objective>
Freeze the shared policy/configuration and event fixtures that every later runtime, gateway,
Go, and React implementation consumes. Extend the existing permission config instead of
creating a second policy file or persistence layer.
</objective>

<execution_context>
@C:/Users/Davi/.codex/skills/gsd-execute-phase/SKILL.md
@C:/Users/Davi/.codex/skills/superpowers/test-driven-development/SKILL.md
</execution_context>

<context>
@.planning/phases/10.7-web-agent-permission-queue-ux/10.7-SPEC.md
@.planning/phases/10.7-web-agent-permission-queue-ux/10.7-CONTEXT.md
@.planning/phases/10.7-web-agent-permission-queue-ux/10.7-RESEARCH.md
@packages/atlas-core/atlas_core/schemas/control_plane.py
@services/agent-runtime/atlas_runtime/control_plane_service.py
</context>

<decision_coverage>
D-02, D-03, D-04, D-05, D-06, D-07, D-10, D-21, D-22, D-24, D-25
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze policy schemas and compatibility defaults</name>
  <files>packages/atlas-core/atlas_core/schemas/control_plane.py,
  packages/atlas-core/tests/test_permission_policy_schema.py</files>
  <behavior>
    - Legacy config containing only permission.mode validates with the previous safe behavior.
    - Presets are manual, smart, and full_autonomy.
    - Rules contain stable ids, effect, typed selectors, scope, and optional explanation.
    - Explain receipts serialize effective decision, reason code, matched rule/source,
      effective preset/profile, evaluated facts, and maintenance use.
    - Unknown fields and executable/callable rule content are rejected.
  </behavior>
  <action>
  Write failing schema tests first. Add frozen Pydantic v2 models and Literal types with
  deterministic `model_dump(mode="json")`. Keep config schema version compatibility and map the
  existing `ask` behavior to the manual preset. Do not add a dependency.
  </action>
  <verify>
    <automated>python -m pytest packages/atlas-core/tests/test_permission_policy_schema.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate narrowing profiles through the revisioned control plane</name>
  <files>services/agent-runtime/atlas_runtime/control_plane_service.py,
  services/agent-runtime/tests/test_permission_policy_config.py</files>
  <behavior>
    - A profile equal to or stricter than the master policy is accepted.
    - Every attempted widening returns a typed revision/config conflict.
    - Permission TTL, disconnect, preset, rules, profiles, and workspace controls appear in
      setting metadata with masking/restart semantics preserved.
    - Rejected widening emits an audit-safe reason without rule secrets or raw prompts.
  </behavior>
  <action>
  Add the failing configuration tests, then extend setting metadata and validation. Compare
  profile capability/risk ceilings structurally; never infer widening from UI labels.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -m pytest tests/test_permission_policy_config.py tests/test_config_control_plane.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit frozen cross-language fixtures</name>
  <files>services/agent-runtime/tests/fixtures/surface_event_parity.json,
  services/agent-runtime/tests/fixtures/permission_policy_matrix.json</files>
  <action>
  Build one canonical event fixture covering every registered normalized event kind and terminal
  state, plus a policy matrix covering all presets, explicit deny, allow-all-except, narrowing,
  workspace escape, hardline, maintenance, and scoped allow. Validate both through the core
  schemas in tests. Never include a secret or raw chain-of-thought field.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -m pytest tests/test_permission_policy_config.py tests/test_surface_events.py -q</automated>
  </verify>
</task>

</tasks>

<verification>
Run the atlas-core and runtime targeted tests. Inspect JSON fixtures for stable keys and absence
of secrets. No gateway or frontend code may begin before this plan is green.
</verification>

<success_criteria>
- [ ] Legacy permission configuration remains safe and valid
- [ ] Master/profile widening is rejected deterministically
- [ ] Policy receipts and event fixtures are JSON-stable across languages
- [ ] No production dependency or persistence layer is added
</success_criteria>

<output>
Create `10.7-01-SUMMARY.md` with RED/GREEN evidence and frozen contract hashes.
</output>
