---
phase: 10.7-web-agent-permission-queue-ux
plan: 03
type: execute
wave: 2
depends_on: ["10.7-02"]
files_modified:
  - services/agent-runtime/atlas_runtime/cli/main.py
  - services/agent-runtime/atlas_runtime/cli/surface.py
  - services/agent-runtime/atlas_runtime/cli/tools.py
  - services/agent-runtime/tests/cli/test_surface_cli.py
  - services/agent-runtime/tests/cli/test_tools_cli.py
  - services/agent-runtime/tests/test_surface_permission_contract.py
autonomous: true
requirements: [WEB-01, WEB-04, WEB-05]
must_haves:
  truths:
    - "Decision coverage: D-08, D-09, D-10, D-20, D-21, D-22, D-25"
    - "Surface session and scoped approval mutations are exposed as stable JSON CLI contracts"
    - "Every actionable approval command requires owning session id and nonce"
    - "Lifecycle, policy explain, cancellation, reconnect, and resume use existing services"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/cli/surface.py"
      provides: "Thin JSON command bridge for session lifecycle and owned queues"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/cli/surface.py"
      to: "services/agent-runtime/atlas_runtime/surface_session_service.py"
      via: "direct service delegation"
      pattern: "surface_session_service"
    - from: "services/agent-runtime/atlas_runtime/cli/tools.py"
      to: "services/agent-runtime/atlas_runtime/permission_broker.py"
      via: "session-owned nonce-bound decisions"
      pattern: "permission_broker"
---

<objective>
Expose the shared session, policy receipt, and permission lifecycle through thin machine-readable
CLI commands so the Rust gateway and all nonvisual adapters use one service boundary.
</objective>

<context>
@.planning/phases/10.7-web-agent-permission-queue-ux/10.7-RESEARCH.md
@services/agent-runtime/atlas_runtime/surface_session_service.py
@services/agent-runtime/atlas_runtime/permission_broker.py
@services/agent-runtime/atlas_runtime/cli/main.py
</context>

<decision_coverage>
D-08, D-09, D-10, D-20, D-21, D-22, D-25
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stable surface-session JSON commands</name>
  <files>services/agent-runtime/atlas_runtime/cli/surface.py,
  services/agent-runtime/atlas_runtime/cli/main.py,
  services/agent-runtime/tests/cli/test_surface_cli.py</files>
  <behavior>
    - create supports global or registered-project workspace and returns stable identity.
    - get/list/heartbeat/resume/cancel/close delegate to existing lifecycle services.
    - invalid roots, revisions, transitions, or terminal resumes return typed JSON errors.
    - output contains no secret config values.
  </behavior>
  <action>
  Write CLI runner tests first. Add a `surface` command group that opens the existing DB/config
  services and serializes schema models. Do not put lifecycle logic in Typer handlers.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -m pytest tests/cli/test_surface_cli.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace actionable global approval commands with scoped contracts</name>
  <files>services/agent-runtime/atlas_runtime/cli/tools.py,
  services/agent-runtime/tests/cli/test_tools_cli.py,
  services/agent-runtime/tests/test_surface_permission_contract.py</files>
  <behavior>
    - list actionable requires a surface session and returns only owned pending rows.
    - approve/reject require approval id, owning session id, and nonce.
    - once/session/durable scope maps to the broker without widening policy.
    - foreign/stale/expired decisions return typed conflict/forbidden results.
    - read-only terminal outcomes remain globally auditable but non-actionable.
  </behavior>
  <action>
  Preserve legacy global list only as an explicitly read-only compatibility view. Route every
  mutation into broker claim/reject APIs and include policy receipt/provenance in JSON output.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -m pytest tests/cli/test_tools_cli.py tests/test_surface_permission_contract.py -q</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- [ ] CLI surface lifecycle round trips in global and project modes
- [ ] Actionable approvals are session-owned and nonce-bound
- [ ] Typed errors preserve fail-closed behavior
- [ ] CLI remains a thin adapter over existing services
</success_criteria>

<output>
Create `10.7-03-SUMMARY.md` with the frozen CLI JSON examples.
</output>
