---
phase: 10.7-web-agent-permission-queue-ux
plan: 02
type: execute
wave: 1
depends_on: ["10.7-01"]
files_modified:
  - services/agent-runtime/atlas_runtime/policy.py
  - services/agent-runtime/atlas_runtime/hardline_policy.py
  - services/agent-runtime/atlas_runtime/tool_service.py
  - services/agent-runtime/atlas_runtime/permission_broker.py
  - services/agent-runtime/tests/test_policy_matrix.py
  - services/agent-runtime/tests/test_hardline_policy.py
  - services/agent-runtime/tests/test_tool_policy_integration.py
  - services/agent-runtime/tests/test_permission_wait.py
autonomous: true
requirements: [WEB-04, WEB-05]
must_haves:
  truths:
    - "Decision coverage: D-01, D-02, D-04, D-05, D-07, D-08, D-09, D-10, D-21, D-22, D-24, D-25"
    - "Every guarded tool invocation reaches one deterministic policy evaluator and the existing broker"
    - "Hardline and explicit master denies cannot be overridden by profiles, autonomy, or approval scopes"
    - "Workspace-only and explicit ATLAS maintenance scopes use canonical paths and audited trusted intent"
    - "Silence, timeout, disconnect, stale nonce, and foreign ownership fail closed"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/policy.py"
      provides: "Pure deterministic policy evaluation and explain receipt"
    - path: "services/agent-runtime/atlas_runtime/hardline_policy.py"
      provides: "Versioned immutable catastrophic-action floor"
    - path: "services/agent-runtime/atlas_runtime/tool_service.py"
      provides: "Single evaluator-to-broker execution chokepoint"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/tool_service.py"
      to: "services/agent-runtime/atlas_runtime/policy.py"
      via: "evaluate before approval creation or execution"
      pattern: "policy"
    - from: "services/agent-runtime/atlas_runtime/tool_service.py"
      to: "services/agent-runtime/atlas_runtime/permission_broker.py"
      via: "owned pending approval and atomic claim"
      pattern: "permission_broker"
---

<objective>
Implement the backend policy authority: immutable hardline floor, deterministic master/profile
evaluation, workspace and maintenance scopes, and bounded approval waiting, integrated at the
existing tool-service chokepoint.
</objective>

<context>
@.planning/phases/10.7-web-agent-permission-queue-ux/10.7-CONTEXT.md
@.planning/phases/10.7-web-agent-permission-queue-ux/10.7-RESEARCH.md
@services/agent-runtime/atlas_runtime/policy.py
@services/agent-runtime/atlas_runtime/tool_service.py
@services/agent-runtime/atlas_runtime/permission_broker.py
@foundation/atlas-hermes/tools/approval.py
</context>

<decision_coverage>
D-01, D-02, D-04, D-05, D-07, D-08, D-09, D-10, D-21, D-22, D-24, D-25
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pure policy evaluation and hardline corpus</name>
  <files>services/agent-runtime/atlas_runtime/policy.py,
  services/agent-runtime/atlas_runtime/hardline_policy.py,
  services/agent-runtime/tests/test_policy_matrix.py,
  services/agent-runtime/tests/test_hardline_policy.py</files>
  <behavior>
    - Fixed precedence is hardline deny, explicit master deny, master ceiling, narrower profile,
      matching scoped allow, then ask/allow.
    - Manual, smart, and full autonomy produce the frozen matrix outcomes.
    - Smart advice cannot override deterministic deny and failure/timeout asks or denies.
    - Full autonomy allows only non-hardline, non-denied actions.
    - The explain receipt is deterministic for identical normalized facts.
  </behavior>
  <action>
  Write the corpus/matrix tests first. Implement pure functions over frozen facts and config.
  Normalize capability, risk, arguments, commands, and canonical paths once. Adapt donor pattern
  ideas with provenance comments but retain ATLAS types and storage.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -m pytest tests/test_policy_matrix.py tests/test_hardline_policy.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce workspace and protected maintenance scopes</name>
  <files>services/agent-runtime/atlas_runtime/policy.py,
  services/agent-runtime/tests/test_policy_matrix.py</files>
  <behavior>
    - Workspace-only denies canonical targets outside the active root.
    - Symlink, parent traversal, alternate separator, and case variants cannot escape.
    - Maintenance works only with trusted explicit-user intent, declared maintenance capability,
      and a target inside declared ATLAS roots.
    - Maintenance never bypasses hardline, config masking, update validation, or audit.
  </behavior>
  <action>
  Reuse existing path guards. Model explicit intent as trusted runtime context, never by matching
  prompt text. Keep secret-bearing auth roots outside non-secret maintenance roots.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -m pytest tests/test_policy_matrix.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate policy, broker lifecycle, bounded waits, and receipts</name>
  <files>services/agent-runtime/atlas_runtime/tool_service.py,
  services/agent-runtime/atlas_runtime/permission_broker.py,
  services/agent-runtime/tests/test_tool_policy_integration.py,
  services/agent-runtime/tests/test_permission_wait.py</files>
  <behavior>
    - Deny never creates an executable approval.
    - Ask creates exactly one owned nonce-bound approval with the policy receipt.
    - Allow executes through the existing audited adapter path.
    - Waiting adapters heartbeat in bounded intervals and cancellation interrupts promptly.
    - Timeout/disconnect deny and stale/foreign/concurrent claims never execute twice.
  </behavior>
  <action>
  Add integration and fake-clock concurrency tests first. Extend existing records only where
  provenance requires it. Add migration `0018_permission_policy_receipts.sql` with one nullable
  `tool_approvals.policy_receipt TEXT` column; existing rows load as `None`, fresh/updated
  installations apply migrations before runtime service startup, and no second receipt table or
  queue is introduced. Keep blocking behavior outside the Rust async runtime thread.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -m pytest tests/test_tool_policy_integration.py tests/test_permission_wait.py tests/test_permission_broker.py tests/test_permission_broker_concurrency.py -q</automated>
  </verify>
</task>

</tasks>

<verification>
Run the full policy/broker/tool-service suite, then statically verify that production guarded
tool execution has no alternate allow path.
</verification>

<success_criteria>
- [ ] All frozen policy matrix and hardline cases pass
- [ ] Workspace and maintenance scopes fail closed
- [ ] One tool-service/policy/broker authority owns guarded execution
- [ ] Timeout and concurrency behavior preserve exactly-once semantics
</success_criteria>

<output>
Create `10.7-02-SUMMARY.md` with hardline version, donor provenance, and test evidence.
</output>
