---
phase: 10.7-web-agent-permission-queue-ux
plan: 05
type: execute
wave: 3
depends_on: ["10.7-04"]
files_modified:
  - services/web-ui-react/package.json
  - services/web-ui-react/src/lib/api.ts
  - services/web-ui-react/src/lib/surfaceContracts.ts
  - services/web-ui-react/src/context/AgentSurfaceContext.tsx
  - services/web-ui-react/src/routes/Console.tsx
  - services/web-ui-react/src/routes/System.tsx
  - services/web-ui-react/src/test/surfaceContracts.test.ts
  - services/web-ui-react/src/test/agentSurfaceContext.test.tsx
  - services/web-ui-react/src/test/consoleSession.test.tsx
autonomous: true
requirements: [WEB-01, WEB-02, WEB-05]
must_haves:
  truths:
    - "Decision coverage: D-10, D-11, D-12, D-20, D-21, D-22, D-25"
    - "The React Console creates global/project sessions and consumes canonical normalized events"
    - "Reconnect, resume, cancel, model/config/project, and permission mode use shared contracts"
    - "No production React code calls /v1/console/stream"
    - "The System page cannot mutate approvals through the legacy global path"
  artifacts:
    - path: "services/web-ui-react/src/context/AgentSurfaceContext.tsx"
      provides: "Shell-surviving session coordinator using standard hooks"
    - path: "services/web-ui-react/src/lib/surfaceContracts.ts"
      provides: "Frozen TypeScript DTOs and validated fixture parser"
  key_links:
    - from: "services/web-ui-react/src/routes/Console.tsx"
      to: "services/web-ui-react/src/context/AgentSurfaceContext.tsx"
      via: "canonical session execution and event state"
      pattern: "AgentSurface"
    - from: "services/web-ui-react/src/lib/api.ts"
      to: "/v1/surface-sessions"
      via: "shared gateway API"
      pattern: "surface"
---

<objective>
Replace the Console's ad-hoc transport with the shared surface/session/event contracts and a
shell-level coordinator, preserving the existing workbench route and visual identity.
</objective>

<context>
@.planning/phases/10.7-web-agent-permission-queue-ux/10.7-UI-SPEC.md
@services/web-ui-react/src/routes/Console.tsx
@services/web-ui-react/src/lib/api.ts
@services/web-ui-react/src/layout/Layout.tsx
</context>

<decision_coverage>
D-10, D-11, D-12, D-20, D-21, D-22, D-25
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the minimum dev-only DOM test harness and frozen DTO parser</name>
  <files>services/web-ui-react/package.json,
  services/web-ui-react/src/lib/surfaceContracts.ts,
  services/web-ui-react/src/test/surfaceContracts.test.ts</files>
  <behavior>
    - Every event in the frozen fixture parses and preserves order/terminal outcome.
    - Unknown kinds fail visibly instead of disappearing.
    - Approval/config/session DTOs preserve ids, nonce, revision, ownership, and receipts.
  </behavior>
  <action>
  Add only the minimum dev dependencies for Vitest, jsdom, and React Testing Library if not
  already transitively available. The current package has no `test` script, so this task includes
  the explicit bootstrap cost: add `test: vitest`, one jsdom setup file, and lockfile changes as
  dev-only entries. Add no production framework. Consume a copied/generated frozen fixture with a
  hash check against the runtime source.
  </action>
  <verify>
    <automated>cd services/web-ui-react &amp;&amp; npm test -- --run src/test/surfaceContracts.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shell-level session coordinator</name>
  <files>services/web-ui-react/src/lib/api.ts,
  services/web-ui-react/src/context/AgentSurfaceContext.tsx,
  services/web-ui-react/src/test/agentSurfaceContext.test.tsx</files>
  <behavior>
    - Global/project create produces one stable Web surface session.
    - Reconnect/resume reuses identity and replay before live events.
    - Cancel transitions through cancelling to a terminal state.
    - Config revision conflicts and provider remediation remain explicit.
    - Owned approvals are fetched by matching session id only.
  </behavior>
  <action>
  Write mocked-fetch lifecycle tests first. Implement a context/reducer with standard React hooks.
  Store only reconnect identity locally if needed; server state remains authoritative.
  </action>
  <verify>
    <automated>cd services/web-ui-react &amp;&amp; npm test -- --run src/test/agentSurfaceContext.test.tsx</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate Console and retire actionable System approvals</name>
  <files>services/web-ui-react/src/routes/Console.tsx,
  services/web-ui-react/src/routes/System.tsx,
  services/web-ui-react/src/test/consoleSession.test.tsx</files>
  <behavior>
    - Console submits through the coordinator and renders normalized text/activity/tool/diff/task/
      retry/retrieval/approval/error/cancel/completion events.
    - No malformed/unknown event is silently ignored.
    - Production source has no invocation of consoleChatStream or /v1/console/stream.
    - System approval history is read-only and points active work to its owning session.
  </behavior>
  <action>
  Write component tests first. Reuse existing transcript components/styles while replacing the
  transport/event switch. Remove or quarantine the legacy API helper from production exports.
  </action>
  <verify>
    <automated>cd services/web-ui-react &amp;&amp; npm test -- --run src/test/consoleSession.test.tsx &amp;&amp; npm run build</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- [ ] React tests prove global/project lifecycle and every event kind
- [ ] Console has no ad-hoc stream dependency
- [ ] Shared controls preserve conflicts and remediation
- [ ] No production state framework/dependency is added
</success_criteria>

<output>
Create `10.7-05-SUMMARY.md` with transport removal evidence and fixture hash.
</output>
