---
phase: 10.7-web-agent-permission-queue-ux
plan: 04
type: execute
wave: 2
depends_on: ["10.7-03"]
files_modified:
  - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs
  - native/atlas-core-rs/crates/atlas-gateway/src/main.rs
  - native/atlas-core-rs/crates/atlas-gateway/tests/api.rs
  - native/atlas-core-rs/crates/atlas-gateway/tests/contract.rs
  - services/atlas-tui/internal/client/client.go
  - services/atlas-tui/internal/client/client_test.go
  - services/atlas-tui/internal/tui/update.go
  - services/atlas-tui/internal/tui/model.go
autonomous: true
requirements: [WEB-01, WEB-02, WEB-04, WEB-05]
must_haves:
  truths:
    - "Decision coverage: D-08, D-09, D-10, D-20, D-21, D-22, D-25"
    - "Rust exposes thin session and scoped-permission HTTP routes over the frozen CLI JSON"
    - "Go TUI carries one surface session id and nonce on every approval decision"
    - "Gateway and Go parse the same normalized event fixture without surface-specific policy"
  artifacts:
    - path: "native/atlas-core-rs/crates/atlas-gateway/src/lib.rs"
      provides: "Session lifecycle, event, policy receipt, and scoped queue endpoints"
    - path: "services/atlas-tui/internal/client/client.go"
      provides: "Frozen surface/scoped-permission Go client"
  key_links:
    - from: "native/atlas-core-rs/crates/atlas-gateway/src/lib.rs"
      to: "services/agent-runtime/atlas_runtime/cli/surface.py"
      via: "structured atlas CLI dispatch"
      pattern: "surface"
    - from: "services/atlas-tui/internal/client/client.go"
      to: "/v1/surface-sessions"
      via: "HTTP DTOs"
      pattern: "surface"
---

<objective>
Publish the frozen session/permission contracts through the Rust gateway and converge the Go TUI
and nonvisual client semantics on session ownership, normalized events, and nonce-bound decisions.
</objective>

<context>
@.planning/phases/10.7-web-agent-permission-queue-ux/10.7-RESEARCH.md
@native/atlas-core-rs/crates/atlas-gateway/src/lib.rs
@native/atlas-core-rs/crates/atlas-gateway/tests/api.rs
@services/atlas-tui/internal/client/client.go
</context>

<decision_coverage>
D-08, D-09, D-10, D-20, D-21, D-22, D-25
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Rust session and scoped-permission API contracts</name>
  <files>native/atlas-core-rs/crates/atlas-gateway/src/lib.rs,
  native/atlas-core-rs/crates/atlas-gateway/src/main.rs,
  native/atlas-core-rs/crates/atlas-gateway/tests/api.rs,
  native/atlas-core-rs/crates/atlas-gateway/tests/contract.rs</files>
  <behavior>
    - POST/GET session routes cover create, get, heartbeat, resume, cancel, and close.
    - Session event stream/replay emits normalized event DTOs in sequence.
    - Owned queue list and decision routes require session id and nonce.
    - CLI typed errors map deterministically to 400/403/404/409/410/500.
    - Legacy console/global approval endpoints are marked rollback/read-only and are not used by
      new clients.
  </behavior>
  <action>
  Add failing API and contract tests first. Deserialize explicit Rust DTOs and dispatch structured
  CLI arguments without shell interpolation. Keep all policy/lifecycle decisions in Python
  services.
  </action>
  <verify>
    <automated>cd native/atlas-core-rs &amp;&amp; cargo test -p atlas-gateway --test api --test contract</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Go TUI to the scoped contract</name>
  <files>services/atlas-tui/internal/client/client.go,
  services/atlas-tui/internal/client/client_test.go,
  services/atlas-tui/internal/tui/update.go,
  services/atlas-tui/internal/tui/model.go,
  services/atlas-tui/internal/tui/overlay.go,
  services/atlas-tui/internal/tui/overlay_test.go</files>
  <behavior>
    - The TUI creates/resumes one surface session for its workspace.
    - Queue polling requests only approvals owned by that id.
    - Approve/reject includes the server nonce and selected scope.
    - Foreign/stale outcomes are rendered, never retried as a looser global mutation.
    - Event fixture sequence and terminal outcome match the runtime fixture.
    - Approval overlay renders 4 options (once/session/always/deny) with arrow/number/enter
      navigation, command preview up to 10 lines, and Esc mapped to deny.
    - Clarify and confirm gateway events trigger overlay handlers that block normal input.
    - Any active overlay blocks key routing to the background panes.
  </behavior>
  <action>
  Write httptest cases first, then replace global approval URLs and add session lifecycle calls.
  Add an overlay model (overlay.go) that manages the approval/clarify/confirm state machine
  following the Hermes prompts.tsx pattern adapted to BubbleTea. The overlay renders as a
  double-bordered box with selectable options and a footer. Keep renderer behavior local and
  policy-free. Replace the hardcoded "once" scope in approveTool with the user-selected scope.
  </action>
  <verify>
    <automated>cd services/atlas-tui &amp;&amp; go test ./... &amp;&amp; go vet ./...</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- [ ] Rust route and contract suites cover lifecycle/ownership/error mapping
- [ ] Go TUI no longer calls actionable global approval routes
- [ ] Rust and Go accept the frozen normalized-event fixture
- [ ] No shell interpolation or duplicate policy logic is introduced
</success_criteria>

<output>
Create `10.7-04-SUMMARY.md` with route inventory and Go/Rust conformance evidence.
</output>
