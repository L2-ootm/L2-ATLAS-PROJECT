---
phase: 04-event-bus
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - services/agent-runtime/pyproject.toml
  - services/agent-runtime/atlas_runtime/__init__.py
  - services/agent-runtime/atlas_audit/__init__.py
  - services/agent-runtime/atlas_audit/plugin.yaml
  - services/agent-runtime/tests/__init__.py
  - services/agent-runtime/tests/conftest.py
  - services/agent-runtime/tests/test_audit_service.py
  - services/agent-runtime/tests/test_atlas_audit_plugin.py
autonomous: true
requirements:
  - RUNTIME-03
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "pytest services/agent-runtime/tests/ -x -q exits 0 (stubs collected, no import errors)"
    - "from atlas_runtime.audit_service import emit succeeds in an editable install"
    - "from atlas_audit import register succeeds (plugin package importable)"
    - "db fixture provides an in-memory SQLite with all 7 ATLAS tables"
  artifacts:
    - path: "services/agent-runtime/pyproject.toml"
      provides: "package config with atlas-core editable dep and pytest testpaths"
      contains: "atlas-core"
    - path: "services/agent-runtime/tests/conftest.py"
      provides: "db, run_id, lock fixtures"
      exports: ["db_fixture", "run_id_fixture", "lock_fixture"]
    - path: "services/agent-runtime/tests/test_audit_service.py"
      provides: "stub tests for RUNTIME-03, AUDIT-01, AUDIT-02 (xfail until Wave 1 impl)"
      contains: "test_emit_tool_call"
    - path: "services/agent-runtime/tests/test_atlas_audit_plugin.py"
      provides: "stub plugin hook tests (xfail until Wave 2 impl)"
      contains: "test_post_tool_call_emits_audit_row"
    - path: "services/agent-runtime/atlas_audit/plugin.yaml"
      provides: "Hermes plugin manifest"
      contains: "atlas_audit"
  key_links:
    - from: "services/agent-runtime/pyproject.toml"
      to: "packages/atlas-core"
      via: "path dependency"
      pattern: "atlas-core"
    - from: "services/agent-runtime/tests/conftest.py"
      to: "infra/migrations/0001_core.sql"
      via: "3 parent hops from tests/ to repo root"
      pattern: "parent.parent.parent"
---

<objective>
Create the services/agent-runtime/ package scaffold with editable atlas-core dependency, a pytest conftest mirroring the Phase 2 pattern, and stub test files for the audit service and plugin. All stubs must be collected by pytest without errors; implementations are xfail placeholders.

Purpose: All Wave 1 and Wave 2 plans depend on this infrastructure existing. Without the package scaffold and conftest, no subsequent plan can run tests.

Output: Importable atlas_runtime and atlas_audit packages; db/run_id/lock fixtures; stub test files; plugin.yaml manifest skeleton.
</objective>

<execution_context>
@C:/Users/Davi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Davi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-bus/CONTEXT.md
@.planning/phases/04-event-bus/04-RESEARCH.md
@.planning/phases/04-event-bus/04-PATTERNS.md

<interfaces>
<!-- Key types from packages/atlas-core/atlas_core/schemas/core.py -->

from atlas_core.schemas.core import AuditEvent, ToolCall, SECRET_PATTERNS

# AuditEvent.event_type valid literals:
# "llm_call" | "tool_call" | "subagent_run" | "approval" | "artifact" | "wiki_update" | "memory_change" | "failure"

# AuditEvent required fields: run_id: str, event_type: Literal[...]
# AuditEvent optional fields: task_id, session_id, tool_call_id, tool_name, data (str, default "{}"),
#                             duration_ms, policy_result, timestamp (auto)

# ToolCall required fields: audit_event_id: str, run_id: str, tool_name: str
# ToolCall optional: args (str, default "{}"), result, exit_code, stdout, stderr,
#                    duration_ms, policy_allowed, requires_approval, timestamp (auto)

# SECRET_PATTERNS: tuple of 3 compiled re.Pattern objects
# Pattern[0]: URL querystring  (?i)\b(token|api[_-]?key|secret|password)=([^\s&]+)
# Pattern[1]: JSON key-value   (?i)"(token|api[_-]?key|secret|password)"\s*:\s*"([^"]+)"
# Pattern[2]: Bearer token     (?i)\b(bearer)\s+([A-Za-z0-9._~+/=-]+)

<!-- conftest.py from packages/atlas-core/tests/conftest.py (Phase 2 exact pattern) -->
# MIGRATION_PATH depth for packages/atlas-core/tests/conftest.py:
#   4 parent hops → project root
# MIGRATION_PATH depth for services/agent-runtime/tests/conftest.py:
#   3 parent hops → project root (tests/ → agent-runtime/ → services/ → root)

MIGRATION_PATH = (
    pathlib.Path(__file__).parent.parent.parent
    / "infra" / "migrations" / "0001_core.sql"
)

<!-- packages/atlas-core/pyproject.toml (analog for pyproject.toml) -->
# [build-system] requires = ["hatchling"], build-backend = "hatchling.build"
# [project] name, version, description, requires-python = ">=3.11", dependencies = [...]
# [tool.hatch.build.targets.wheel] packages = ["atlas_runtime", "atlas_audit"]
# [tool.pytest.ini_options] testpaths = ["tests"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create agent-runtime package scaffold with pyproject.toml and package init files</name>
  <files>
    services/agent-runtime/pyproject.toml
    services/agent-runtime/atlas_runtime/__init__.py
    services/agent-runtime/atlas_audit/__init__.py
    services/agent-runtime/atlas_audit/plugin.yaml
  </files>
  <read_first>
    - packages/atlas-core/pyproject.toml — exact build-system block and project table pattern to replicate
    - .planning/phases/04-event-bus/04-PATTERNS.md — plugin.yaml analog from langfuse (lines 108-138)
    - .planning/phases/04-event-bus/04-RESEARCH.md — recommended project structure (lines 165-178)
  </read_first>
  <action>
    Create services/agent-runtime/ directory tree. All directories must exist before file creation.

    pyproject.toml: Use hatchling build backend. Project name = "atlas-runtime", version = "0.1.0", requires-python = ">=3.11". Dependencies must include atlas-core as a path dependency: "atlas-core @ file:///${PROJECT_ROOT}/packages/atlas-core" — use the editable path form: dependencies = ["atlas-core @ ../../packages/atlas-core"]. Include dev extras: pytest>=9.0. Hatch wheel target packages = ["atlas_runtime", "atlas_audit"]. pytest testpaths = ["tests"].

    atlas_runtime/__init__.py: Empty file with module docstring: "ATLAS agent-runtime service layer."

    atlas_audit/__init__.py: Minimal stub — module docstring "ATLAS Hermes audit plugin — stub (Wave 2 implements register())." Define one top-level name: __version__ = "0.1.0". No register() function yet (Wave 2 adds it).

    atlas_audit/plugin.yaml: Mirror the langfuse plugin.yaml structure exactly. Fields:
      name: atlas_audit
      version: "0.1.0"
      description: "ATLAS structured audit event bus — persists every Hermes action as an AuditEvent row in SQLite."
      author: L2-ootm
      hooks:
        - post_api_request
        - post_llm_call
        - post_tool_call
        - subagent_stop
        - post_approval_response
    No requires_env field (atlas_audit needs no credentials).

    After creating files, install the package in editable mode from the services/agent-runtime/ directory so atlas_runtime and atlas_audit are importable. Run: pip install -e services/agent-runtime/ --quiet
  </action>
  <verify>
    <automated>python -c "from atlas_runtime import __doc__; from atlas_audit import __version__; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - services/agent-runtime/pyproject.toml contains the string "atlas-core"
    - services/agent-runtime/atlas_audit/plugin.yaml contains "atlas_audit" and all 5 hook names: post_api_request, post_llm_call, post_tool_call, subagent_stop, post_approval_response
    - python -c "from atlas_runtime import __doc__; print(__doc__)" prints "ATLAS agent-runtime service layer." without ImportError
    - python -c "from atlas_audit import __version__; print(__version__)" prints "0.1.0" without ImportError
    - pip install -e services/agent-runtime/ exits 0
  </acceptance_criteria>
  <done>Package scaffold installed; atlas_runtime and atlas_audit importable; plugin.yaml manifest matches hooks list.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/conftest.py and failing stub test files</name>
  <files>
    services/agent-runtime/tests/__init__.py
    services/agent-runtime/tests/conftest.py
    services/agent-runtime/tests/test_audit_service.py
    services/agent-runtime/tests/test_atlas_audit_plugin.py
  </files>
  <read_first>
    - packages/atlas-core/tests/conftest.py — exact db_fixture pattern to replicate (3-parent vs 4-parent depth)
    - .planning/phases/04-event-bus/04-PATTERNS.md — test structure for test_audit_service.py (lines 314-353) and test_atlas_audit_plugin.py (lines 356-408)
    - .planning/phases/04-event-bus/04-VALIDATION.md — full list of test functions required (Per-Task Verification Map)
  </read_first>
  <action>
    tests/__init__.py: Empty file.

    tests/conftest.py: Mirror packages/atlas-core/tests/conftest.py with one change — MIGRATION_PATH uses 3 parent hops (not 4) because conftest.py is 3 levels from repo root: tests/ → agent-runtime/ → services/ → root. Add two new fixtures beyond the Phase 2 pattern:

      MIGRATION_PATH = pathlib.Path(__file__).parent.parent.parent / "infra" / "migrations" / "0001_core.sql"

      db_fixture: identical to Phase 2 — connect(":memory:"), PRAGMA journal_mode=WAL, PRAGMA foreign_keys=ON, executescript if file exists, yield conn, conn.close().

      run_id_fixture: name="run_id", returns str(uuid.uuid4()).

      lock_fixture: name="lock", returns threading.Lock().

    tests/test_audit_service.py: Import from atlas_runtime.audit_service (module does not exist yet — mark entire module with pytestmark = pytest.mark.xfail(reason="Wave 1 not implemented", strict=False)). Define these test function stubs — each body is just "pass":
      - test_emit_tool_call(db, run_id, lock)
      - test_emit_llm_call(db, run_id, lock)
      - test_emit_artifact(db, run_id, lock)
      - test_emit_redacts_secret_in_data(db, run_id, lock)
      - test_emit_invalid_event_type_raises_no_orphan(db, run_id, lock)
      - test_get_events_for_run_ordered(db, run_id, lock)
      - test_export_jsonl_valid(db, run_id, lock)

      For the import: wrap in try/except ImportError and set a module-level skip marker if import fails, so pytest collects the file even before atlas_runtime.audit_service exists:
        try:
            from atlas_runtime.audit_service import emit, get_events_for_run, export_jsonl
        except ImportError:
            pytest.skip("atlas_runtime.audit_service not implemented", allow_module_level=True)

    tests/test_atlas_audit_plugin.py: Same pattern — wrap import of on_post_tool_call, on_post_api_request, on_subagent_stop from atlas_audit in try/except ImportError with module-level skip. Define these stubs (body = pass):
      - test_post_tool_call_emits_audit_and_tool_call_rows(db, run_id)
      - test_post_api_request_emits_llm_call_row(db, run_id)
      - test_subagent_stop_emits_subagent_run_row(db, run_id)
      - test_hook_callback_does_not_reraise_on_error(db, run_id)

    After creating files, run: pytest services/agent-runtime/tests/ -x -q --tb=short
    All tests must either pass or be skipped (no collection errors, no import errors that cause failures).
  </action>
  <verify>
    <automated>pytest services/agent-runtime/tests/ -x -q --tb=short 2>&1 | Select-String -Pattern "error|Error|FAILED" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - pytest services/agent-runtime/tests/ -x -q exits 0 (all collected tests either pass, skip, or xfail — no ERROR or FAILED)
    - services/agent-runtime/tests/conftest.py contains "parent.parent.parent" (3-hop path, not 4)
    - services/agent-runtime/tests/conftest.py contains fixture names "run_id" and "lock"
    - services/agent-runtime/tests/test_audit_service.py contains "test_emit_tool_call" and "test_export_jsonl_valid"
    - services/agent-runtime/tests/test_atlas_audit_plugin.py contains "test_post_tool_call_emits_audit_and_tool_call_rows"
    - db fixture provides a connection with audit_events table: db.execute("SELECT name FROM sqlite_master WHERE name='audit_events'").fetchone() returns a non-None row when MIGRATION_PATH exists
  </acceptance_criteria>
  <done>Test infrastructure in place. pytest collects all stubs without collection errors. db/run_id/lock fixtures available to all subsequent test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → pyproject.toml | Developer-controlled package config; no external input |
| test fixture → SQLite :memory: | In-process in-memory DB; no network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SC | Tampering | pip install from pyproject.toml | accept | No new external packages — only atlas-core (local path dep) and pydantic (already installed and verified Phase 2); no slopcheck required |
| T-04-01-I | Information Disclosure | test fixture db | accept | :memory: SQLite — no data leaves process; no PII in scaffold phase |
</threat_model>

<verification>
1. python -c "from atlas_runtime import __doc__; from atlas_audit import __version__; print('imports OK')" exits 0.
2. pytest services/agent-runtime/tests/ -x -q exits 0 with 0 FAILED and 0 ERROR.
3. services/agent-runtime/atlas_audit/plugin.yaml contains all 5 hook names.
4. services/agent-runtime/tests/conftest.py uses 3 parent hops for MIGRATION_PATH.
</verification>

<success_criteria>
- Package scaffold installed and importable with no errors
- Test infrastructure (conftest + stubs) collected by pytest with 0 failures and 0 collection errors
- plugin.yaml manifest present and structurally valid (Wave 2 installs it to ~/.hermes/plugins/)
</success_criteria>

<output>
Create .planning/phases/04-event-bus/04-01-SUMMARY.md when done.
</output>
