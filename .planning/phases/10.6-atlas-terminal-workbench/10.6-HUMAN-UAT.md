---
status: partial
phase: 10.6-atlas-terminal-workbench
source: [10.6-VERIFICATION.md]
started: 2026-06-27
updated: 2026-06-27
---

# Phase 10.6 — Human Verification (deferred, environment-gated)

Code-level verification is fully passed (11/11 must-haves; all CR-01/02/03 resolved; 45 phase
tests green). The items below require a live environment that the autonomous session cannot drive
(real LLM provider, real terminals, interactive signals). They were anticipated by the locked
CONTEXT.md testing caveat — they are NOT newly discovered gaps.

## Outstanding Human Tests

1. **Live agent run** — With a real provider/auth configured, run `atlas` (bare) and `atlas tui`,
   submit a prompt, and confirm the transcript streams normalized agent/tool/task/subagent/retry/
   retrieval events end-to-end (session → mission → run → audit_events → transcript poll). [TUI-04, TUI-05]

2. **Cross-terminal rendering** — Launch the workbench in Windows Terminal, cmd, PowerShell, the
   VS Code integrated terminal, and WSL; verify header/transcript/composer render legibly at narrow
   (≤80c) and wide widths, with `NO_COLOR=1`, and with an ASCII-only font. [TUI-10]

3. **Interactive Ctrl-C / cancel unwind** — Start a run, press Ctrl-C mid-stream; confirm the run,
   tools, and child work unwind cleanly, no orphaned children, and the session is resumable (state
   not corrupted). [TUI-08]

4. **Live permission prompt** — Trigger a write-class tool to produce a real pending approval;
   verify the 4-option prompt and that approve-once / scoped-allow / reject / cancel each map to the
   correct `permission_broker` outcome, and that a headless surface fails closed. [TUI-06]

## Sign-off

- [ ] Item 1 verified
- [ ] Item 2 verified
- [ ] Item 3 verified
- [ ] Item 4 verified

When all are checked, rerun verification to flip 10.6 to `passed`.
</content>
