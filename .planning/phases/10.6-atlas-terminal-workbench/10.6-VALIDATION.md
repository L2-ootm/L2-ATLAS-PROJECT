---
phase: 10.6
slug: atlas-terminal-workbench
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-27
---

# Phase 10.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.x (services/agent-runtime) |
| **Config file** | services/agent-runtime/pyproject.toml ([tool.pytest.ini_options]) |
| **Quick run command** | `cd services/agent-runtime && python -m pytest tests/test_tui_*.py -q` |
| **Full suite command** | `cd services/agent-runtime && python -m pytest -q` |
| **Estimated runtime** | ~30-60 seconds |

---

## Sampling Rate

- **After every task commit:** Run the quick run command for the touched TUI test module.
- **After every plan wave:** Run the full suite command.
- **Before `/gsd-verify-work`:** Full suite must be green.
- **Max feedback latency:** 60 seconds.

---

## Per-Task Verification Map

> Concrete task IDs are assigned by the planner; this maps requirements to test type and the
> automatable-vs-manual split. The in-memory `db` fixture (conftest.py) is mandatory — never drive
> the live CLI against `~/.atlas/atlas.db`.

| Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | Notes |
|-------------|------------|-----------------|-----------|-------------------|-------|
| TUI-01 (atlas / atlas tui launch native) | — | Bare `atlas` and `atlas tui` resolve to the native workbench callback; no Hermes wrapper on default path | unit | `pytest tests/test_tui_cli.py -q` | CliRunner; assert launcher target |
| TUI-02 (global/Project selection + cwd) | — | Picker returns global workspace or registered Project; canonical cwd resolved via workspace_service | unit | `pytest tests/test_tui_startup.py -q` | fixture-injected DB |
| TUI-03 (compact identity/status header) | T-10.6-02 | Header shows masked model/auth only (no resolved secrets), permission mode, context budget, Focus, session state | unit | `pytest tests/test_tui_header.py -q` | assert masked snapshot used |
| TUI-04 (transcript + composer streaming) | — | Normalized agent events rendered append-only; composer multiline | unit | `pytest tests/test_tui_render.py -q` | render logic only |
| TUI-05 (tool/result/diff/task/subagent/retry/retrieval/cancel render) | — | Each EventKind maps to a distinct renderer; redacted payloads | unit | `pytest tests/test_tui_render.py -q` | drive normalized SurfaceEvents |
| TUI-06 (native permission prompt: approve once / scoped allow / reject / cancel) | T-10.6-01 | Maps to permission_broker.claim + record_allow_rule; headless → ApprovalChannelMissingError fail-closed; cancel leaves pending (no broker call) | unit | `pytest tests/test_tui_permissions.py -q` | assert claim/record_allow_rule calls + fail-closed |
| TUI-07 (command palette: project/workspace, mission/focus, config/model, permissions — core-first) | — | Commands dispatch to existing services; deferred groups have extension seams, not stubs that error opaquely | unit | `pytest tests/test_tui_commands.py -q` | wiki/Brain, subagents, context deferred |
| TUI-08 (Ctrl-C / cancel unwind) | T-10.6-03 | Ctrl-C cancels active run/tools/children via cancel token; session state not corrupted (no terminal-state mutation) | unit | `pytest tests/test_tui_cancel.py -q` | assert transition + no partial write |
| TUI-09 (resume/replay version pinning) | T-10.6-04 | resume_session + replay_contract; fail closed (ContractCompatibilityError) on prompt/context/tool-catalog version drift; workspace identity preserved | unit | `pytest tests/test_tui_resume.py -q` | assert raises on drift |
| TUI-10 (cross-terminal: narrow/wide, no-color, ASCII/Unicode) | — | Rendering logic honors width + no-color + ASCII box selection | unit | `pytest tests/test_tui_render.py -q` | real terminals are manual |
| TUI-11 (no imported product identity in source/bundles/output) | — | Source/help-text/output contain no Hermes/foundation product identity | unit | `pytest tests/test_tui_debrand.py -q` | grep-style assertion over rendered output + help |

---

## Wave 0 Requirements

- [ ] `tests/test_tui_cli.py` — extend/replace existing wrapper tests for the native launcher.
- [ ] `tests/test_tui_render.py` — RED stubs for transcript/event-kind/width/no-color/ASCII rendering.
- [ ] `tests/test_tui_permissions.py` — RED stubs for approve-once/scoped/reject/cancel + headless fail-closed.
- [ ] `tests/test_tui_resume.py` — RED stubs for resume/replay version-drift fail-closed.
- [ ] Reuse existing `tests/conftest.py` `db` fixture (in-memory, all migrations) — no new DB harness.

*Wave 0 must go RED before GREEN. Verify with pytest exit code, not token grep.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real-terminal rendering | TUI-10 | Actual Windows Terminal, cmd, PowerShell, VS Code integrated terminal, and WSL render differently than Rich's detection in CI | Launch `atlas tui` in each terminal; verify header/transcript/composer render legibly at narrow (≤80c) and wide widths, with NO_COLOR=1, and with an ASCII-only font |
| Interactive Ctrl-C unwind | TUI-08 | Real signal delivery + child-process unwind is environment-specific | Start a run, press Ctrl-C mid-stream; confirm clean cancel, no orphaned children, session resumable |
| Live permission prompt | TUI-06 | Requires a real pending approval and operator keypress | Trigger a write-class tool; verify the 4-option prompt and that each choice maps to the correct broker outcome |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
</content>
