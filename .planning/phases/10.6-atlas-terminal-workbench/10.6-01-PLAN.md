---
phase: 10.6-atlas-terminal-workbench
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/agent-runtime/pyproject.toml
  - services/agent-runtime/tests/tui/__init__.py
  - services/agent-runtime/tests/tui/conftest.py
  - services/agent-runtime/tests/tui/test_capabilities.py
  - services/agent-runtime/tests/tui/test_session_select.py
  - services/agent-runtime/tests/tui/test_header.py
  - services/agent-runtime/tests/tui/test_transcript_poll.py
  - services/agent-runtime/tests/tui/test_event_renderers.py
  - services/agent-runtime/tests/tui/test_permission_ui.py
  - services/agent-runtime/tests/tui/test_command_dispatch.py
  - services/agent-runtime/tests/tui/test_cancel_unwind.py
  - services/agent-runtime/tests/tui/test_resume_replay.py
  - services/agent-runtime/tests/test_tui_app_entry.py
  - services/agent-runtime/tests/test_tui_debrand_audit.py
autonomous: false
requirements: [TUI-01, TUI-02, TUI-03, TUI-04, TUI-05, TUI-06, TUI-07, TUI-08, TUI-09, TUI-10, TUI-11]
must_haves:
  truths:
    - "A pytest run against tests/tui/ and the new top-level TUI test files fails RED (ImportError/AttributeError) before any tui/ package code exists"
    - "rich and prompt_toolkit are declared as direct atlas-runtime dependencies with version constraints, not silently new installs"
  artifacts:
    - path: "services/agent-runtime/tests/tui/conftest.py"
      provides: "tui-scoped fixtures reusing the top-level db fixture; no new DB harness"
    - path: "services/agent-runtime/pyproject.toml"
      provides: "rich>=13.7,<15 and prompt_toolkit>=3.0,<4 as direct dependencies"
      contains: "prompt_toolkit"
  key_links:
    - from: "services/agent-runtime/tests/tui/conftest.py"
      to: "services/agent-runtime/tests/conftest.py"
      via: "pytest fixture inheritance (db fixture)"
      pattern: "db"
---

<objective>
Stand up the Wave 0 RED-first test scaffold for the entire ATLAS Terminal Workbench phase and
declare `rich`/`prompt_toolkit` as direct `atlas-runtime` dependencies. Every later wave's GREEN
implementation task points back at a test file created here. No `atlas_runtime/tui/` package code
exists yet — every new test must fail with `ImportError`/`ModuleNotFoundError`/`AttributeError`,
never silently pass or skip.

Purpose: Nyquist compliance (10.6-VALIDATION.md) requires RED-before-GREEN; this plan is the only
place all 10 new test modules are created so every downstream plan's `<verify>` is a real
pre-existing command, not something the executor must invent.
Output: `tests/tui/` package (8 RED test files + conftest), 2 new RED top-level test files
(`test_tui_app_entry.py`, `test_tui_debrand_audit.py`), and `pyproject.toml` with the two
declared dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-atlas-terminal-workbench/10.6-CONTEXT.md
@.planning/phases/10.6-atlas-terminal-workbench/10.6-RESEARCH.md
@.planning/phases/10.6-atlas-terminal-workbench/10.6-VALIDATION.md
@services/agent-runtime/tests/conftest.py
@services/agent-runtime/pyproject.toml
</context>

<interfaces>
<!-- The db fixture this plan's tests build on. Already exists, read-only reference. -->
From services/agent-runtime/tests/conftest.py:
```python
@pytest.fixture(name="db")
def db_fixture() -> sqlite3.Connection: ...  # in-memory, all migrations applied

@pytest.fixture(name="surface_session")
def surface_session_fixture(db) -> str: ...  # inserts a 'starting' surface_sessions row, returns id

@pytest.fixture(name="make_active_session")
def make_active_session_fixture(db, surface_session): ...  # callable: flips a session to 'active'

@pytest.fixture(name="seed_pending_approval")
def seed_pending_approval_fixture(db): ...  # callable: inserts a pending tool_approvals row

@pytest.fixture(name="register_test_channel")
def register_test_channel_fixture(db): ...  # callable: inserts an unrevoked approval_channels row
```

Future modules these tests import (DO NOT EXIST YET — that is the RED state):
`atlas_runtime.tui.capabilities`, `atlas_runtime.tui.theme`, `atlas_runtime.tui.session_select`,
`atlas_runtime.tui.header`, `atlas_runtime.tui.transcript`, `atlas_runtime.tui.permission_ui`,
`atlas_runtime.tui.command_dispatch`, `atlas_runtime.tui.resume`, `atlas_runtime.tui.app`.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify rich + prompt_toolkit package legitimacy before declaring as direct deps</name>
  <files>services/agent-runtime/pyproject.toml</files>
  <what-built>Nothing yet — this checkpoint gates the `pyproject.toml` dependency declaration
  BEFORE it is written, per the Package Legitimacy Gate (RESEARCH.md: both packages tagged
  `[ASSUMED]`, slopcheck unavailable offline).</what-built>
  <action>
  Present the Package Legitimacy Audit findings to the user for manual confirmation before Task 1
  writes the dependency declaration: both `rich` and `prompt_toolkit` are tagged `[ASSUMED]` in
  10.6-RESEARCH.md because slopcheck could not run offline during research. Neither is flagged
  `[SLOP]` or `[SUS]`. This checkpoint exists solely to get a human eyes-on confirmation before the
  pyproject.toml edit proceeds — it performs no automated action itself.
  </action>
  <how-to-verify>
    Both packages are already resolved transitively in the live `.venv` (rich via typer, 14.3.3;
    prompt_toolkit via hermes-agent, 3.0.52) — this task only makes them DIRECT dependencies, it
    installs nothing new. Verify manually:
    1. Open https://pypi.org/project/rich/ — confirm publisher is Textualize, ~6yr history, no
       suspicious recent ownership transfer.
    2. Open https://pypi.org/project/prompt-toolkit/ — confirm publisher is jonathanslenders /
       prompt-toolkit org, ~10yr history, IPython dependency.
    3. Confirm locally: `cd services/agent-runtime && python -c "import rich, prompt_toolkit;
       print(rich.__version__, prompt_toolkit.__version__)"` prints `14.3.3 3.0.52` (or close
       minor versions) with no error.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify checkpoint, no automated check; gate is the explicit user "approved" response</automated>
  </verify>
  <done>User has typed "approved" confirming both packages' registry/publisher/history, or raised a concern that blocks Task 1.</done>
  <resume-signal>Type "approved" to proceed with declaring both as direct dependencies, or report
  a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Declare rich + prompt_toolkit as direct dependencies</name>
  <files>services/agent-runtime/pyproject.toml</files>
  <read_first>services/agent-runtime/pyproject.toml</read_first>
  <action>
  In the `[project] dependencies` list (currently `atlas-core`, `typer>=0.25.0`,
  `pyyaml>=6,<7`), add two entries: `"rich>=13.7,<15"` and `"prompt_toolkit>=3.0,<4"`. Add a
  one-line comment above them noting they are already-resolved transitive dependencies (via
  typer→rich and hermes-agent→prompt_toolkit) being declared directly per supply-chain honesty,
  matching the existing `pyyaml` comment style immediately above it. Do not touch
  `[project.optional-dependencies]` or any other section. After editing, run `pip install -e
  services/agent-runtime` is NOT required (no new packages resolve) — confirm via the verify
  command only.
  </action>
  <acceptance_criteria>
  `pyproject.toml`'s `[project] dependencies` list contains both `rich>=13.7,<15` and
  `prompt_toolkit>=3.0,<4` as separate string entries; `python -c "import tomllib;
  d=tomllib.load(open('services/agent-runtime/pyproject.toml','rb'));
  print(d['project']['dependencies'])"` run from repo root shows both entries present.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=d['project']['dependencies']; assert any('rich' in x for x in deps); assert any('prompt_toolkit' in x for x in deps); print('OK')"</automated>
  </verify>
  <done>pyproject.toml lists rich and prompt_toolkit as direct dependencies with the specified version constraints; no new package installs (both already resolved transitively).</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/tui/ package + conftest + 8 RED test modules</name>
  <files>
    services/agent-runtime/tests/tui/__init__.py,
    services/agent-runtime/tests/tui/conftest.py,
    services/agent-runtime/tests/tui/test_capabilities.py,
    services/agent-runtime/tests/tui/test_session_select.py,
    services/agent-runtime/tests/tui/test_header.py,
    services/agent-runtime/tests/tui/test_transcript_poll.py,
    services/agent-runtime/tests/tui/test_event_renderers.py,
    services/agent-runtime/tests/tui/test_permission_ui.py,
    services/agent-runtime/tests/tui/test_command_dispatch.py,
    services/agent-runtime/tests/tui/test_cancel_unwind.py,
    services/agent-runtime/tests/tui/test_resume_replay.py
  </files>
  <read_first>
  services/agent-runtime/tests/conftest.py,
  packages/atlas-core/atlas_core/schemas/surface_session.py,
  services/agent-runtime/atlas_runtime/permission_broker.py,
  services/agent-runtime/atlas_runtime/agent_contract_service.py,
  .planning/phases/10.6-atlas-terminal-workbench/10.6-VALIDATION.md
  </read_first>
  <action>
  Create `tests/tui/__init__.py` (empty, makes the dir a package so pytest's existing recursive
  `testpaths = ["tests"]` discovers it — no pytest.ini change needed).

  Create `tests/tui/conftest.py` that imports nothing TUI-specific yet; it only re-exports/uses
  the top-level `db`, `surface_session`, `make_active_session`, `seed_pending_approval`,
  `register_test_channel` fixtures (pytest auto-discovers parent conftest fixtures — no explicit
  import needed, just document the dependency in a module docstring). Add one new fixture
  `forced_console` that returns a factory `make_console(*, no_color=False, width=None,
  legacy_windows=False, file=None)` constructing a `rich.console.Console(no_color=no_color,
  width=width, legacy_windows=legacy_windows, file=file or io.StringIO())` — this is shared by
  every rendering test in this package so width/no-color/ASCII assertions are consistent. `rich`
  is now a direct dependency (Task 1), so this import is safe even though `atlas_runtime.tui` does
  not exist yet.

  Write each test file as a genuine RED stub per the table in 10.6-VALIDATION.md and the Phase
  Requirements → Test Map in 10.6-RESEARCH.md. Each file imports the not-yet-existing module from
  `atlas_runtime.tui.*` at module level (so collection itself fails with ImportError — the
  loudest possible RED, not a runtime skip) and contains 2-4 test functions describing the target
  behavior in the test name and a one-line docstring/assert referencing the requirement ID. Do
  NOT write fake passing assertions or `pytest.skip` — RED means import failure or assertion
  failure, never a quiet pass.

  - `test_capabilities.py`: imports `from atlas_runtime.tui.capabilities import probe_capabilities`.
    Tests: `test_no_color_env_forces_no_color`, `test_legacy_windows_falls_back_to_ascii_box`,
    `test_narrow_width_under_80_cols_selected`. (TUI-10)
  - `test_session_select.py`: imports `from atlas_runtime.tui.session_select import
    select_workspace`. Tests: `test_explicit_project_flag_skips_picker`,
    `test_global_flag_resolves_via_workspace_service`,
    `test_no_flags_and_no_tty_raises_clean_error`. Use the `db` fixture + monkeypatch
    `workspace_service.resolve_workspace`. (TUI-02)
  - `test_header.py`: imports `from atlas_runtime.tui.header import render_status_header`.
    Tests: `test_header_shows_masked_model_no_secrets`,
    `test_header_includes_permission_mode_and_context_budget`,
    `test_header_never_contains_resolved_api_key`. Build a fake `ControlPlaneSnapshot`-shaped dict
    + a `surface_session` fixture row; assert via `forced_console` captured output. (TUI-03)
  - `test_transcript_poll.py`: imports `from atlas_runtime.tui.transcript import poll_and_render`.
    Tests: `test_replay_since_dedup_on_second_poll`, `test_append_only_no_full_redraw_call`.
    Seed two `SurfaceEvent`s via `surface_events.normalize_surface_events` fixtures; assert
    `poll_and_render` returns events with seq > last_seq and the second call returns nothing for
    already-seen seq. (TUI-04)
  - `test_event_renderers.py`: imports `from atlas_runtime.tui.transcript import render_event`.
    Parametrized test `test_every_event_kind_has_a_renderer` over
    `typing.get_args(atlas_core.schemas.surface_session.EventKind)` asserting `render_event`
    returns a non-empty Rich renderable for every kind with no exception. (TUI-05)
  - `test_permission_ui.py`: imports `from atlas_runtime.tui.permission_ui import
    resolve_approval_choice`. Tests: `test_approve_once_calls_claim_without_allow_rule`,
    `test_scoped_allow_calls_claim_then_record_allow_rule`, `test_reject_calls_claim_reject`,
    `test_cancel_does_not_call_claim`, `test_headless_missing_channel_fails_closed`. Use
    `seed_pending_approval` + `make_active_session` + monkeypatched `permission_broker.claim`/
    `record_allow_rule` call-spies. (TUI-06)
  - `test_command_dispatch.py`: imports `from atlas_runtime.tui.command_dispatch import
    dispatch_command`. Tests: `test_project_list_dispatches_to_project_service`,
    `test_focus_show_dispatches_to_focus_service`, `test_config_get_dispatches_to_config_service`,
    `test_unknown_command_returns_error_card_not_exception`,
    `test_deferred_group_wiki_returns_extension_seam_not_opaque_error`. (TUI-07)
  - `test_cancel_unwind.py`: imports `from atlas_runtime.tui.app import handle_cancel`. Tests:
    `test_ctrl_c_calls_transition_session_cancelling`,
    `test_cancel_does_not_leave_session_non_terminal`. Use `surface_session` + `make_active_session`
    fixtures; monkeypatch `surface_session_service.transition_session` call-spy. (TUI-08)
  - `test_resume_replay.py`: imports `from atlas_runtime.tui.resume import
    resume_or_fail_closed`. Tests: `test_resume_calls_replay_contract_before_transition`,
    `test_version_drift_raises_contract_compatibility_error_and_blocks_resume`. Seed a mismatched
    `agent_contract_snapshots` row directly via SQL against `db`; monkeypatch nothing — exercise
    the real `agent_contract_service.replay_contract` fail-closed path. (TUI-09)

  Every test file's module docstring states the requirement ID(s) it covers and confirms RED
  status: "RED until atlas_runtime.tui.<module> exists (Wave 1+)."
  </action>
  <acceptance_criteria>
  `cd services/agent-runtime && python -m pytest tests/tui/ -q` fails (non-zero exit) with
  collection errors (ImportError on `atlas_runtime.tui.*`) for all 9 new test files; no test is
  marked skip/xfail; `python -m pytest tests/tui/ --collect-only -q 2>&1 | grep -c "test_"`
  reports at least 20 collected (or failed-to-collect, since import fails at module level) test
  function names across the 9 files.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/tui/ -q; test $? -ne 0 && echo RED_CONFIRMED</automated>
  </verify>
  <done>tests/tui/ package exists with conftest.py + 9 test files; running pytest against it fails with ImportError (genuine RED, not skip) because atlas_runtime.tui does not exist yet.</done>
</task>

<task type="auto">
  <name>Task 3: Create top-level RED tests for app-entry wiring and de-brand audit</name>
  <files>
    services/agent-runtime/tests/test_tui_app_entry.py,
    services/agent-runtime/tests/test_tui_debrand_audit.py
  </files>
  <read_first>
  services/agent-runtime/atlas_runtime/cli/main.py,
  services/agent-runtime/atlas_runtime/cli/tui.py,
  services/agent-runtime/tests/test_tui_cli.py
  </read_first>
  <action>
  Create `tests/test_tui_app_entry.py` (TUI-01): import `typer.testing.CliRunner` and
  `from atlas_runtime.cli.main import app`. Tests: `test_bare_atlas_invokes_run_workbench`
  (monkeypatch `atlas_runtime.tui.app.run_workbench` BEFORE importing/invoking, assert called once
  when `CliRunner().invoke(app, [])` runs — this will currently fail because `cli/main.py`'s root
  has no `@app.callback(invoke_without_command=True)` yet, and `atlas_runtime.tui.app` does not
  exist); `test_atlas_tui_subcommand_invokes_same_run_workbench` (same assertion via `invoke(app,
  ["tui"])`); `test_no_hermes_wrapper_on_default_path` (assert `_resolve_launcher`/
  `_foundation_dir` from `cli/tui.py` are NOT called — monkeypatch them to raise if called, then
  invoke bare `atlas` and `atlas tui`, confirming neither path reaches the Hermes Ink launcher).

  Create `tests/test_tui_debrand_audit.py` (TUI-11): a grep-style smoke test, NOT a fixture-based
  unit test. Tests: `test_help_output_contains_no_hermes_identity` (CliRunner `invoke(app,
  ["--help"])` and `invoke(app, ["tui", "--help"])`; assert lowercase output contains none of
  `"hermes"`, `"ink"`, `"atlas-hermes"`); `test_tui_package_source_contains_no_hermes_identity`
  (once `atlas_runtime/tui/` exists, glob `*.py` under it and assert none of those tokens appear
  case-insensitively, excluding doc-comment references to "vendored Hermes Ink TUI" as a documented
  *source/reference*, which is permitted only in module docstrings under an explicit
  `# provenance:` comment marker — be lenient by skipping lines containing the substring
  `provenance:`). This test will currently fail because `atlas_runtime/tui/` does not exist (glob
  returns empty — assert the directory exists once Wave 1+ lands; for Task 3's RED state, mark the
  source-scan test to assert `pathlib.Path("atlas_runtime/tui").is_dir()` is False yet
  (`pytest.xfail` is forbidden per Nyquist — instead assert the CURRENT real condition: the
  directory does not exist, which will need updating to a positive assertion once Wave 1 lands;
  document this explicitly in a comment so the Wave 4 plan task knows to flip it).
  </action>
  <acceptance_criteria>
  `python -m pytest tests/test_tui_app_entry.py -q` fails (ImportError on `atlas_runtime.tui.app`,
  or AttributeError on the missing callback) — confirmed RED;
  `python -m pytest tests/test_tui_debrand_audit.py -q` collects and runs without ImportError
  (it does not depend on `atlas_runtime.tui` existing) and the help-output assertion currently
  passes trivially (no tui/ package yet means no leak possible) — explicitly note in the test
  docstring that this assertion becomes meaningful once Wave 4 lands the real CLI wiring.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_tui_app_entry.py -q; test $? -ne 0 && echo RED_CONFIRMED; python -m pytest tests/test_tui_debrand_audit.py -q</automated>
  </verify>
  <done>test_tui_app_entry.py exists and fails RED (ImportError/AttributeError) confirming bare atlas/atlas tui do not yet launch the native workbench; test_tui_debrand_audit.py exists, runs, and passes trivially pre-Wave-1 (documented as becoming meaningful post-wiring).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> in-memory db | Fixture-injected SQLite only; no boundary crossed into production `~/.atlas/atlas.db` |
| pyproject.toml -> package resolution | Declaring direct deps changes what `pip install -e .` resolves; supply-chain trust boundary |
| Test source -> CLI help output | `CliRunner.invoke` exercises the real Typer app object in-process, no subprocess boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-SC | Tampering | pip/PyPI install of rich/prompt_toolkit | mitigate | Blocking human-verify checkpoint (Task 0) confirms publisher/registry/history for both `[ASSUMED]` packages before declaring as direct deps |
| T-10.6-05 | Information Disclosure | tests/tui/conftest.py forced_console fixture | accept | Test-only StringIO sink, never writes to real terminal or log file; no secret material flows through fixtures in this plan |
| T-10.6-06 | Tampering | test_tui_debrand_audit.py false-negative (trivially passes pre-Wave-1) | mitigate | Explicit code comment + Wave 4 plan task instructed to flip the source-scan assertion to positive once atlas_runtime/tui/ exists, preventing the audit from silently staying toothless |
| T-10.6-07 | Denial of Service | Live test DB accidentally pointed at ~/.atlas/atlas.db | mitigate | All new tests exclusively consume the `db` fixture from conftest.py (in-memory); no test imports `db_module.connect()` directly or touches `ATLAS_HOME` |
</threat_model>

<verification>
Run `cd services/agent-runtime && python -m pytest tests/tui/ tests/test_tui_app_entry.py tests/test_tui_debrand_audit.py -q` — every new TUI test file must either fail with ImportError/AttributeError (genuine RED) or, for `test_tui_debrand_audit.py`, pass trivially with the pre-Wave-1 caveat documented. No test marked skip/xfail. `pyproject.toml` declares both new direct dependencies after the human-verify checkpoint is approved.
</verification>

<success_criteria>
- [ ] Human-verify checkpoint approved for rich + prompt_toolkit package legitimacy
- [ ] pyproject.toml declares rich>=13.7,<15 and prompt_toolkit>=3.0,<4 as direct dependencies
- [ ] tests/tui/ package exists with conftest.py + 9 RED test files covering TUI-02 through TUI-09
- [ ] tests/test_tui_app_entry.py exists and fails RED (TUI-01)
- [ ] tests/test_tui_debrand_audit.py exists and runs (TUI-11), documented as pre-Wave-1 trivial-pass
- [ ] No test silently skips or xfails; RED is collection/assertion failure only
</success_criteria>

<output>
Create `.planning/phases/10.6-atlas-terminal-workbench/10.6-01-SUMMARY.md` when done
</output>
