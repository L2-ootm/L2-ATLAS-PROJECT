---
phase: 10.6-atlas-terminal-workbench
plan: 03
type: execute
wave: 1
depends_on: ["10.6-01"]
files_modified:
  - services/agent-runtime/atlas_runtime/tui/transcript.py
autonomous: true
requirements: [TUI-04, TUI-05, TUI-10]
must_haves:
  truths:
    - "Polling the per-session event stream returns only events newer than the last seen seq, gap-free"
    - "Every EventKind (text, reasoning, tool_call, tool_result, task, retry, retrieval, approval, error, completion) has a distinct readable renderer"
    - "Rendering never reparses payload_json to re-derive event kind/ordering — it trusts the already-normalized SurfaceEvent.kind and seq"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/tui/transcript.py"
      provides: "poll_and_render() append-only poll/replay loop and render_event() per-kind renderer (TUI-04/05)"
      exports: ["poll_and_render", "render_event"]
  key_links:
    - from: "services/agent-runtime/atlas_runtime/tui/transcript.py"
      to: "services/agent-runtime/atlas_runtime/surface_events.py"
      via: "replay_since(events, last_seq)"
      pattern: "replay_since"
    - from: "services/agent-runtime/atlas_runtime/tui/transcript.py"
      to: "services/agent-runtime/atlas_runtime/audit_service.py"
      via: "get_events_for_session(conn, session_id)"
      pattern: "get_events_for_session"
---

<objective>
Implement the append-only transcript polling and per-EventKind rendering layer (TUI-04, TUI-05).
This is the module that turns Wave 0's RED `test_transcript_poll.py` and `test_event_renderers.py`
stubs GREEN. Pure render/projection logic — no asyncio loop ownership yet (Wave 3 wires this into
the app's background poll task).

Purpose: This is the single place SurfaceEvents are turned into Rich renderables. Every later
consumer (the Wave 3 app.py main loop) calls into this module rather than re-deriving event
ordering or kind-dispatch logic, per RESEARCH's explicit anti-pattern warning against re-parsing
payload_json.
Output: `atlas_runtime/tui/transcript.py` with `poll_and_render` and `render_event`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-atlas-terminal-workbench/10.6-RESEARCH.md
@services/agent-runtime/tests/tui/test_transcript_poll.py
@services/agent-runtime/tests/tui/test_event_renderers.py
@services/agent-runtime/atlas_runtime/surface_events.py
@services/agent-runtime/atlas_runtime/audit_service.py
</context>

<interfaces>
From services/agent-runtime/atlas_runtime/surface_events.py:
```python
def normalize_surface_events(
    audit_events: Sequence, run_outcome: Optional[RunOutcome] = None, *,
    session_id: str, start_seq: int = 0,
) -> tuple[SurfaceEvent, ...]: ...
def replay_since(events: Iterable[SurfaceEvent], last_seq: int) -> tuple[SurfaceEvent, ...]: ...
__all__ = ["normalize_surface_events", "replay_since"]
```

From services/agent-runtime/atlas_runtime/audit_service.py:
```python
def get_events_for_session(conn, session_id: str) -> Sequence[AuditEvent]: ...
def emit(conn, lock, *, event_type: str, payload: dict, ...) -> None: ...
```

From packages/atlas-core/atlas_core/schemas/surface_session.py:
```python
EventKind = Literal["text","reasoning","tool_call","tool_result","task","retry",
                     "retrieval","approval","error","completion"]
class SurfaceEvent(_FrozenContract):
    session_id: str
    seq: int
    kind: EventKind
    run_id: Optional[str] = None
    occurred_at: str
    payload_json: str = "{}"
```

Don't Hand-Roll (RESEARCH.md): event ordering/gap detection belongs to
`surface_events.replay_since(last_seq)` — never re-derive sequencing here.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap-free poll/replay primitive</name>
  <files>services/agent-runtime/atlas_runtime/tui/transcript.py</files>
  <read_first>
  services/agent-runtime/tests/tui/test_transcript_poll.py,
  services/agent-runtime/atlas_runtime/surface_events.py,
  services/agent-runtime/atlas_runtime/audit_service.py
  </read_first>
  <behavior>
    - poll_and_render(conn, session_id, *, last_seq=0) -> tuple[SurfaceEvent, ...] returns only
      events with seq > last_seq, in seq order.
    - Test 1 (test_replay_since_dedup_on_second_poll): seed two events via
      normalize_surface_events fixtures; first poll_and_render(last_seq=0) returns both; calling
      again with last_seq set to the highest returned seq returns an empty tuple (no duplicates).
    - Test 2 (test_append_only_no_full_redraw_call): poll_and_render itself performs no
      Console.print or Live.update call — it is a pure data function; printing/rendering is a
      separate call the caller makes per returned event. Assert by monkeypatching
      rich.console.Console.print as a call-spy and confirming zero calls during poll_and_render.
  </behavior>
  <action>
  Create `tui/transcript.py` (this task creates the file; Task 2 appends `render_event` to the
  same file). Define `poll_and_render(conn: sqlite3.Connection, session_id: str, *, last_seq: int
  = 0, run_outcome: Optional["RunOutcome"] = None) -> tuple[SurfaceEvent, ...]`. Implementation:
  call `audit_service.get_events_for_session(conn, session_id)`, pass the result to
  `surface_events.normalize_surface_events(audit_events, run_outcome, session_id=session_id,
  start_seq=0)` to get the full normalized event tuple, then return
  `surface_events.replay_since(normalized, last_seq)`. Do not implement any redraw/print logic in
  this function — it is pure data retrieval and gap-free slicing. The function name is
  intentionally "poll_and_render" to match the RESEARCH/VALIDATION naming, but its actual
  responsibility in this module is poll-and-slice; the caller (Wave 3's app.py background task)
  is responsible for the render/print side, calling `render_event` (Task 2) once per returned
  SurfaceEvent and `console.print`-ing the result itself.
  </action>
  <acceptance_criteria>
  `poll_and_render` returns a tuple of `SurfaceEvent` ordered by ascending `seq`, all with
  `seq > last_seq`; calling it twice with the second call's `last_seq` set to the first call's max
  seq returns an empty tuple; the function makes zero Rich Console/Live calls.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/tui/test_transcript_poll.py -q</automated>
  </verify>
  <done>tests/tui/test_transcript_poll.py passes; poll_and_render is a pure, gap-free polling function with zero rendering side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-EventKind renderer dispatch</name>
  <files>services/agent-runtime/atlas_runtime/tui/transcript.py</files>
  <read_first>
  services/agent-runtime/tests/tui/test_event_renderers.py,
  packages/atlas-core/atlas_core/schemas/surface_session.py
  </read_first>
  <behavior>
    - render_event(event: SurfaceEvent, *, caps) -> rich.console.RenderableType, with a distinct
      branch per EventKind value.
    - Test (test_every_event_kind_has_a_renderer, parametrized over
      typing.get_args(EventKind)): for every kind in
      ("text","reasoning","tool_call","tool_result","task","retry","retrieval","approval","error",
      "completion"), render_event with a minimally-populated SurfaceEvent of that kind returns a
      non-empty Rich renderable and raises no exception.
  </behavior>
  <action>
  Append to `tui/transcript.py`: `render_event(event: SurfaceEvent, *, caps: "Capabilities") ->
  rich.console.RenderableType`. Parse `event.payload_json` via `json.loads` defensively (wrap in
  try/except json.JSONDecodeError, fall back to `{}` on malformed payload rather than raising —
  per RESEARCH security note: malformed input must never crash the render loop). Implement one
  branch per `event.kind`: `"text"`/`"reasoning"` -> `rich.text.Text` of the payload's text/
  reasoning field, styled via `safe_style("muted", caps)` for reasoning vs default for text;
  `"tool_call"` -> a `rich.panel.Panel` titled with the tool name showing normalized args;
  `"tool_result"` -> a `Panel` showing the result/diff summary, styled `safe_style("success",
  caps)` on success; `"task"` -> a compact one-line `Text` noting task/subagent lifecycle (start/
  complete) from the payload; `"retry"` -> `Text` styled `safe_style("warning", caps)` noting
  attempt count; `"retrieval"` -> `Panel` showing provenance/source summary; `"approval"` -> `Text`
  summarizing the approval decision outcome (this is the terminal-outcome rendering, NOT the live
  interactive prompt — that is permission_ui.py in a later plan); `"error"` -> `Text` styled
  `safe_style("danger", caps)`; `"completion"` -> `Text` styled `safe_style("primary", caps)`
  noting run outcome. Import `Capabilities` from `tui.capabilities` (already implemented in plan
  10.6-02, same wave — file-disjoint, no conflict). Use `rich.box.ASCII` for all `Panel`
  instances when `caps.ascii_only` is True, default box otherwise. Never call
  `rich.console.Console.print` or `Live` from this function — it only returns a renderable; the
  caller prints it.
  </action>
  <acceptance_criteria>
  `render_event` has an explicit branch for every one of the 10 `EventKind` literals (no
  catch-all `else` masking an unhandled kind — an unrecognized kind value should raise a clear
  `ValueError` naming the unexpected kind, never silently render blank); malformed
  `payload_json` does not raise; every branch returns a non-empty renderable (panels/text with
  non-empty `.plain`/content).
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/tui/test_event_renderers.py -q</automated>
  </verify>
  <done>tests/tui/test_event_renderers.py passes; render_event handles all 10 EventKind values with distinct, non-empty, no-color/ASCII-safe renderers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit_service event payload_json -> render_event | Untrusted/legacy payload JSON crosses into the render layer; must not crash or leak unredacted content |
| poll_and_render last_seq caller input -> replay_since | Caller-supplied seq value crosses into the gap-detection primitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-08 | Denial of Service | tui/transcript.py render_event payload_json parsing | mitigate | json.loads wrapped in try/except JSONDecodeError, falls back to empty dict rather than raising/crashing the render loop, per RESEARCH Common Pitfall on malformed-command DoS applied analogously to malformed event payloads |
| T-10.6-09 | Information Disclosure | tui/transcript.py rendering raw payload fields | accept | payload_json already passed through audit_service.emit's SECRET_PATTERNS redaction boundary (D-013) before persistence; transcript.py renders as-is, never re-fetches or unmasks |
| T-10.6-10 | Tampering | render_event unrecognized EventKind | mitigate | explicit ValueError on any kind not in the 10 known literals, rather than a silent catch-all that could hide a future schema drift bug |
</threat_model>

<verification>
Run `cd services/agent-runtime && python -m pytest tests/tui/test_transcript_poll.py tests/tui/test_event_renderers.py -q` — both Wave 0 RED stubs now pass GREEN.
</verification>

<success_criteria>
- [ ] tui/transcript.py: poll_and_render() is gap-free and side-effect-free
- [ ] tui/transcript.py: render_event() has a distinct branch for all 10 EventKind values
- [ ] Malformed payload_json never raises out of render_event
- [ ] Unrecognized EventKind raises ValueError, never silently renders blank
</success_criteria>

<output>
Create `.planning/phases/10.6-atlas-terminal-workbench/10.6-03-SUMMARY.md` when done
</output>
