---
phase: 10.6-atlas-terminal-workbench
plan: 08
subsystem: cli
tags: [typer, cli-entrypoint, de-brand, tui]

# Dependency graph
requires:
  - phase: 10.6-atlas-terminal-workbench (06)
    provides: atlas_runtime.tui.app.run_workbench (native workbench entrypoint)
  - phase: 10.6-atlas-terminal-workbench (07)
    provides: atlas_runtime/tui/ command_dispatch and full tui package
provides:
  - Bare `atlas` and `atlas tui` both launch the native workbench by default (TUI-01)
  - Legacy Hermes Ink launcher renamed to legacy_foundation_tui, reachable only via hidden `dev-foundation-tui` command
  - De-branded user-visible strings in cli/tui.py (docstrings, help text, error message)
  - Real positive de-brand audit: glob scan of atlas_runtime/tui/*.py + --help output of root/tui/dev-foundation-tui (TUI-11)
affects: [10.6-atlas-terminal-workbench (phase closeout), future TUI/CLI work touching cli/main.py]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Typer root callback with invoke_without_command=True as the default-action entrypoint, paired with an explicit named subcommand calling the same function"
    - "Hidden Typer commands (hidden=True) for checkout-only/dev escape hatches that must never be the default path"
    - "Module-level import + attribute access (module.fn) instead of `from x import fn` when tests need to monkeypatch the referenced symbol post-import"

key-files:
  created: []
  modified:
    - services/agent-runtime/atlas_runtime/cli/main.py
    - services/agent-runtime/atlas_runtime/cli/tui.py
    - services/agent-runtime/tests/test_tui_app_entry.py (read-only consumption, no edits required)
    - services/agent-runtime/tests/test_tui_debrand_audit.py
    - services/agent-runtime/tests/test_tui_cli.py

key-decisions:
  - "main.py imports atlas_runtime.tui.app as a module (not `from ... import run_workbench`) so the test suite's monkeypatch.setattr(tui_app_mod, 'run_workbench', ...) takes effect at call time"
  - "Removed the now-stale test_tui_subcommand_help_leak_is_known_pre_existing_not_a_new_regression documentation test since the `tui` command's help text no longer carries the legacy phrase after rewiring"
  - "Narrowed _KNOWN_LEAK_PATTERNS to only the unrelated `foundation` command's pre-existing phrase, removing the `tui`-command pattern that is now obsolete"

requirements-completed: [TUI-01, TUI-11]

# Metrics
duration: 25min
completed: 2026-06-27
---

# Phase 10.6 Plan 08: Wire Native Workbench Entrypoint + De-brand Audit Flip Summary

**Bare `atlas` and `atlas tui` now launch the native terminal workbench by default; the legacy Hermes Ink launcher is demoted to a hidden `dev-foundation-tui` escape hatch, and the de-brand audit performs a real positive source/help-text scan instead of its Wave 0 placeholder.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 2
- **Files modified:** 4 (cli/main.py, cli/tui.py, test_tui_debrand_audit.py, test_tui_cli.py)

## Accomplishments
- `cli/main.py` gained a root `@app.callback(invoke_without_command=True)` and a `tui` subcommand, both calling `atlas_runtime.tui.app.run_workbench`
- `cli/tui.py`'s `tui` function renamed to `legacy_foundation_tui`, registered only under the hidden `dev-foundation-tui` command, with all user-visible strings de-branded (docstrings, help text, RuntimeError message)
- `test_tui_debrand_audit.py` flipped from Wave 0 placeholder assertions to real positive checks: glob scan of `atlas_runtime/tui/*.py` for forbidden identity tokens, and `--help` output scan across root/`tui`/`dev-foundation-tui`
- `test_tui_cli.py` retargeted from `tui` to `dev-foundation-tui`/`legacy_foundation_tui`, preserving its 4 regression tests without deletion

## Task Commits

1. **Task 1: Wire native workbench as default; hide+de-brand the legacy launcher** - `a7eb0bd` (feat)
2. **Task 2: Flip de-brand audit to positive assertion; retarget legacy CLI tests** - `6c95f3c` (test)

**Plan metadata:** (pending — final commit recorded by state_updates step)

## Files Created/Modified
- `services/agent-runtime/atlas_runtime/cli/main.py` - Root callback + `tui` + hidden `dev-foundation-tui` command registrations, replacing the old single `tui` registration
- `services/agent-runtime/atlas_runtime/cli/tui.py` - `tui` renamed to `legacy_foundation_tui`; de-branded docstrings/help/error text; `_foundation_dir`/`hermes_cli` internal literals kept unchanged with `# provenance:` comments (D-001)
- `services/agent-runtime/tests/test_tui_debrand_audit.py` - Real positive source-scan assertion; narrowed `_KNOWN_LEAK_PATTERNS` to only the out-of-scope `foundation` command; removed the now-obsolete documentation test for the old `tui` leak
- `services/agent-runtime/tests/test_tui_cli.py` - All 4 tests retargeted from `tui` to `dev-foundation-tui`, asserting against `legacy_foundation_tui` behavior

## Decisions Made
- Used `import atlas_runtime.tui.app as _tui_app_mod` plus `_tui_app_mod.run_workbench(...)` calls in `main.py`, rather than `from atlas_runtime.tui.app import run_workbench`, because `test_tui_app_entry.py` monkeypatches the attribute on the `tui_app` module object after import — a direct-binding import would not observe the patch.
- Removed `test_tui_subcommand_help_leak_is_known_pre_existing_not_a_new_regression` from the audit file since its only purpose was documenting a leak that no longer exists post-rewiring; keeping it would make it permanently fail (the plan's own WAVE-4-FLIP note anticipated this deletion path).

## Deviations from Plan

**1. [Rule 1 - Bug avoidance] Removed stale documentation test instead of leaving it failing**
- **Found during:** Task 2
- **Issue:** `test_tui_subcommand_help_leak_is_known_pre_existing_not_a_new_regression` asserted that `tui --help` contained the legacy `"foundation Ink TUI"` phrase. After Task 1's rewiring, that phrase no longer appears, so the test would fail permanently if left unchanged.
- **Fix:** Deleted the test (its own docstring explicitly says to delete it once the leak disappears) and narrowed `_KNOWN_LEAK_PATTERNS` to the one remaining out-of-scope pattern (`foundation` command).
- **Files modified:** services/agent-runtime/tests/test_tui_debrand_audit.py
- **Verification:** `pytest tests/test_tui_debrand_audit.py -q` passes (5 tests, was 6 minus the deleted one plus assertions retained)
- **Committed in:** 6c95f3c (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1, anticipated by plan's own commentary)
**Impact on plan:** No scope creep — the plan's WAVE-4-FLIP comment in the original test explicitly called for this deletion once the leak it documented disappeared.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 10.6 (atlas-terminal-workbench) is now fully wired end-to-end: bare `atlas` / `atlas tui` reach the native workbench; the legacy launcher is demoted behind `dev-foundation-tui`; the de-brand audit is a real, load-bearing regression guard.
- Full phase test set (`tests/tui/ tests/test_tui_app_entry.py tests/test_tui_debrand_audit.py tests/test_tui_cli.py`) passes 44/44.
- Broader CLI-related test sweep (`-k "cli or main or tui"`) passes 125/125 — no regressions from the `cli/main.py` entrypoint change.
- No blockers for phase closeout.

---
*Phase: 10.6-atlas-terminal-workbench*
*Completed: 2026-06-27*

## Self-Check: PASSED
