---
phase: 10.6-atlas-terminal-workbench
plan: 08
type: execute
wave: 4
depends_on: ["10.6-06", "10.6-07"]
files_modified:
  - services/agent-runtime/atlas_runtime/cli/main.py
  - services/agent-runtime/atlas_runtime/cli/tui.py
  - services/agent-runtime/tests/test_tui_app_entry.py
  - services/agent-runtime/tests/test_tui_debrand_audit.py
  - services/agent-runtime/tests/test_tui_cli.py
autonomous: true
requirements: [TUI-01, TUI-11]
must_haves:
  truths:
    - "Bare `atlas` (no subcommand) and `atlas tui` both launch the native workbench, never the Hermes Ink wrapper, by default"
    - "No Hermes/Ink/atlas-hermes identity string appears in --help output or anywhere under atlas_runtime/tui/ outside an explicit provenance: comment marker"
    - "The legacy Hermes Ink launcher remains reachable only behind an explicit hidden/dev flag, never as default behavior"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/cli/main.py"
      provides: "@app.callback(invoke_without_command=True) root + tui subcommand both calling atlas_runtime.tui.app.run_workbench (TUI-01)"
      contains: "invoke_without_command"
    - path: "services/agent-runtime/atlas_runtime/cli/tui.py"
      provides: "Hermes Ink launcher retained ONLY behind an explicit --dev-foundation-tui hidden flag, de-branded error/help text (TUI-11)"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/cli/main.py"
      to: "services/agent-runtime/atlas_runtime/tui/app.py"
      via: "run_workbench(project=..., global_=...)"
      pattern: "run_workbench"
---

<objective>
Wire the native workbench into `cli/main.py` as the default for both bare `atlas` and `atlas
tui` (TUI-01), retire the Hermes Ink wrapper from the default path while keeping it reachable
only behind an explicit hidden/dev flag, and flip the de-brand audit test
(`test_tui_debrand_audit.py`) from its Wave 0 pre-Wave-1 trivial-pass state to a real positive
assertion now that `atlas_runtime/tui/` exists (TUI-11). This is the final integration wave —
turns Wave 0's RED `test_tui_app_entry.py` GREEN and closes the loop on every other plan's
modules.

Purpose: Every prior wave built isolated, independently-tested modules. This plan is the only
place they are connected to the actual CLI entrypoint, and the only place the de-brand audit
becomes a meaningful (non-trivial) check, per the explicit Wave 0 comment instructing this flip.
Output: `cli/main.py` root callback wired to `tui.app.run_workbench`; `cli/tui.py` retained but
hidden and de-branded; `test_tui_app_entry.py`/`test_tui_debrand_audit.py` passing for real;
`test_tui_cli.py` retargeted to the hidden-flag path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-atlas-terminal-workbench/10.6-CONTEXT.md
@.planning/phases/10.6-atlas-terminal-workbench/10.6-RESEARCH.md
@services/agent-runtime/tests/test_tui_app_entry.py
@services/agent-runtime/tests/test_tui_debrand_audit.py
@services/agent-runtime/tests/test_tui_cli.py
@services/agent-runtime/atlas_runtime/cli/main.py
@services/agent-runtime/atlas_runtime/cli/tui.py
@services/agent-runtime/atlas_runtime/tui/app.py
</context>

<interfaces>
Current cli/main.py (lines ~98-99, to be replaced):
```python
from atlas_runtime.cli.tui import tui as _tui_cmd
app.command("tui", help="Launch the ATLAS terminal UI (foundation Ink TUI, ATLAS-skinned).")(_tui_cmd)
```
No existing `@app.callback`/`invoked_subcommand` usage anywhere in main.py (confirmed clean).

RESEARCH.md verified-clean code example for the replacement:
```python
import typer
from atlas_runtime.tui.app import run_workbench

app = typer.Typer()

@app.callback(invoke_without_command=True)
def _root(ctx: typer.Context) -> None:
    """ATLAS — bare invocation launches the terminal workbench."""
    if ctx.invoked_subcommand is None:
        run_workbench()

@app.command("tui", help="Launch the ATLAS terminal workbench.")
def _tui_cmd() -> None:
    run_workbench()
```

Current cli/tui.py (to be de-branded and hidden, not deleted — RESEARCH "State of the Art"
recommends retargeting rather than deleting the existing 4-test suite):
```python
def _foundation_dir() -> Optional[pathlib.Path]: ...  # walks up to foundation/atlas-hermes
def _resolve_launcher() -> Callable[..., object]: ...  # imports hermes_cli.main._launch_tui
def tui(model=None, provider=None, dev: bool = typer.Option(False, "--dev", ...)) -> None: ...
```

From this phase's plan 10.6-06 (already implemented, read-only consumption):
```python
def run_workbench(*, project=None, global_=False, conn=None, lock=None) -> None: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire native workbench as default; hide+de-brand the legacy launcher</name>
  <files>
    services/agent-runtime/atlas_runtime/cli/main.py,
    services/agent-runtime/atlas_runtime/cli/tui.py
  </files>
  <read_first>
  services/agent-runtime/tests/test_tui_app_entry.py,
  services/agent-runtime/atlas_runtime/cli/main.py,
  services/agent-runtime/atlas_runtime/cli/tui.py
  </read_first>
  <action>
  In `cli/main.py`: remove the existing lines registering `from atlas_runtime.cli.tui import tui
  as _tui_cmd` / `app.command("tui", ...)(_tui_cmd)`. Add `from atlas_runtime.tui.app import
  run_workbench` near the other top-level imports. Add `@app.callback(invoke_without_command=True)
  def _root(ctx: typer.Context, project: Optional[str] = typer.Option(None, "--project",
  help="Registered Project id to open (skips the interactive picker)."), global_: bool =
  typer.Option(False, "--global", help="Open the global workspace (skips the interactive
  picker).")) -> None:` with docstring `"""ATLAS — bare invocation launches the terminal
  workbench."""`; body: `if ctx.invoked_subcommand is None: run_workbench(project=project,
  global_=global_)`. Add `@app.command("tui", help="Launch the ATLAS terminal workbench.") def
  _tui_cmd(project: Optional[str] = typer.Option(None, "--project", ...), global_: bool =
  typer.Option(False, "--global", ...)) -> None: run_workbench(project=project,
  global_=global_)`. Place both at the same location the old registration occupied, preserving
  surrounding command registration order.

  In `cli/tui.py`: rename the existing `tui` function to `legacy_foundation_tui` (still callable,
  no longer registered as the `tui` Typer command in main.py) and register it instead under a
  HIDDEN flag-gated command: add `@app.command("dev-foundation-tui", hidden=True, help="Run the
  legacy vendored TUI from source (checkout-only, hidden).")` — but since `cli/tui.py` is a plain
  module (not its own Typer sub-app) and `app` lives in `main.py`, instead expose
  `legacy_foundation_tui` as a plain function from `cli/tui.py` and register it in `main.py`
  via `app.command("dev-foundation-tui", hidden=True, help="...")(legacy_foundation_tui)`
  alongside the new `_root`/`_tui_cmd` registrations, importing `from atlas_runtime.cli.tui import
  legacy_foundation_tui`. De-brand every visible string in `cli/tui.py`: change the docstring
  from "Launch the ATLAS terminal UI (the foundation Ink TUI, ATLAS-skinned)." to something with
  no "foundation"/"Hermes"/"Ink" tokens outside a `# provenance:` comment, e.g. "Run the legacy
  vendored terminal client from source (checkout-only; superseded by the native workbench)." and
  change the `RuntimeError` message in `_resolve_launcher` from "foundation/atlas-hermes not
  found..." to a de-branded equivalent like "legacy vendored TUI source tree not found (run from
  within the ATLAS repo checkout)." — keep `_foundation_dir`'s internal path-walking logic
  (`foundation/atlas-hermes` as a literal directory name) unchanged since that is the actual
  vendored path on disk (D-001, no-edit), only the user-visible strings change.
  </action>
  <acceptance_criteria>
  `CliRunner().invoke(app, [])` with `atlas_runtime.tui.app.run_workbench` monkeypatched is called
  exactly once; `CliRunner().invoke(app, ["tui"])` calls the same `run_workbench` once;
  `CliRunner().invoke(app, ["dev-foundation-tui"])` (monkeypatched `legacy_foundation_tui`) is the
  ONLY path that reaches the legacy launcher; `dev-foundation-tui` does not appear in
  `CliRunner().invoke(app, ["--help"]).output` (hidden=True suppresses it from help listing,
  though it remains directly invocable).
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_tui_app_entry.py -q</automated>
  </verify>
  <done>tests/test_tui_app_entry.py passes; bare atlas and atlas tui both launch run_workbench; the legacy launcher is reachable only via the hidden dev-foundation-tui command with de-branded user-visible strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip de-brand audit to positive assertion; retarget legacy CLI tests</name>
  <files>
    services/agent-runtime/tests/test_tui_debrand_audit.py,
    services/agent-runtime/tests/test_tui_cli.py
  </files>
  <read_first>
  services/agent-runtime/tests/test_tui_debrand_audit.py,
  services/agent-runtime/tests/test_tui_cli.py,
  services/agent-runtime/atlas_runtime/cli/tui.py
  </read_first>
  <action>
  In `tests/test_tui_debrand_audit.py`: flip the `test_tui_package_source_contains_no_hermes_
  identity` assertion from its Wave 0 placeholder (`pathlib.Path("atlas_runtime/tui").is_dir() is
  False`) to the real positive check now that `atlas_runtime/tui/` exists: glob `*.py` under
  `atlas_runtime/tui/` (relative to the package root, using the same path-resolution approach the
  rest of the test file already uses), skip any line containing `provenance:`, and assert none of
  `"hermes"`, `"ink"`, `"atlas-hermes"` (case-insensitive) appear in the remaining lines. Also
  extend `test_help_output_contains_no_hermes_identity` to additionally invoke
  `CliRunner().invoke(app, ["dev-foundation-tui", "--help"])` and assert ITS help output likewise
  contains none of those tokens (the de-branded help text from Task 1 must hold here too) — update
  the docstring noting this assertion is now load-bearing (no longer a pre-Wave-1 placeholder).

  In `tests/test_tui_cli.py`: this file currently tests the OLD `tui` command registration
  directly. Retarget every test that invoked `app, ["tui", ...]` expecting Hermes-launcher
  behavior to instead invoke `app, ["dev-foundation-tui", ...]` (matching Task 1's rename), and
  update monkeypatch targets from whatever `cli.tui.tui`-level patching they used to
  `cli.tui.legacy_foundation_tui`. Do not delete this file (RESEARCH explicitly recommends
  retargeting over deletion) — its 4 existing tests continue verifying the legacy launcher's
  behavior is intact, just reached through the new hidden command name.
  </action>
  <acceptance_criteria>
  `test_tui_package_source_contains_no_hermes_identity` performs a real glob over
  `atlas_runtime/tui/*.py` (not the Wave 0 directory-absence placeholder) and passes;
  `test_tui_cli.py`'s 4 tests all invoke `dev-foundation-tui` and pass against the renamed
  `legacy_foundation_tui` function.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_tui_debrand_audit.py tests/test_tui_cli.py -q</automated>
  </verify>
  <done>test_tui_debrand_audit.py performs a real (non-placeholder) source scan and passes; test_tui_cli.py is retargeted to dev-foundation-tui and passes without modification to its underlying legacy-launcher assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bare `atlas` invocation -> run_workbench | Default CLI entrypoint now reaches the native workbench, not the vendored Hermes Ink process |
| --help output -> end user | Public-facing surface that must contain zero imported-product identity per criterion 5 |
| dev-foundation-tui hidden command -> legacy launcher | Intentional escape hatch, gated to never be the default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-26 | Spoofing | Imported product identity surfacing as ATLAS's own branding | mitigate | test_tui_debrand_audit.py's flipped positive assertion scans atlas_runtime/tui/*.py and all --help output (including the hidden command's own help) for hermes/ink/atlas-hermes tokens outside provenance: comments; this is the final phase-wide enforcement point for TUI-11/criterion 5 |
| T-10.6-27 | Tampering | dev-foundation-tui accidentally becoming default-reachable | mitigate | hidden=True suppresses it from --help listing; test_tui_app_entry.py's test_no_hermes_wrapper_on_default_path (Wave 0) continues asserting neither bare atlas nor atlas tui reaches legacy_foundation_tui |
| T-10.6-28 | Repudiation | Deleting test_tui_cli.py's legacy-launcher coverage instead of retargeting | accept (avoided) | Per RESEARCH recommendation, the file is retargeted not deleted, preserving regression coverage for the legacy code path that remains in the repo (D-001 vendored foundation still present, no-edit) |
</threat_model>

<verification>
Run the full phase test suite: `cd services/agent-runtime && python -m pytest tests/tui/ tests/test_tui_app_entry.py tests/test_tui_debrand_audit.py tests/test_tui_cli.py -q` — every test across all 8 plans now passes GREEN. Run `python -m pytest -q` (full suite) to confirm no regression elsewhere in the codebase from the cli/main.py edit.
</verification>

<success_criteria>
- [ ] Bare atlas and atlas tui both launch the native workbench by default
- [ ] Legacy Hermes Ink launcher reachable only via hidden dev-foundation-tui command
- [ ] test_tui_debrand_audit.py performs a real positive source-scan assertion (no longer a placeholder)
- [ ] test_tui_cli.py retargeted and passing, not deleted
- [ ] Full agent-runtime pytest suite green
</success_criteria>

<output>
Create `.planning/phases/10.6-atlas-terminal-workbench/10.6-08-SUMMARY.md` when done
</output>
