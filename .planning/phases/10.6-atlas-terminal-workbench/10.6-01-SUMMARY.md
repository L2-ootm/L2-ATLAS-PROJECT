---
phase: 10.6-atlas-terminal-workbench
plan: 01
subsystem: testing
tags: [pytest, rich, prompt_toolkit, tdd, red-green, typer]

# Dependency graph
requires:
  - phase: 10.5-permission-broker
    provides: tool_approvals/approval_channels/session_allow_rules schema + permission_broker.claim/record_allow_rule
  - phase: 10.3-surface-session
    provides: surface_sessions/surface_events schema, surface_session_service, SurfaceEvent/EventKind contracts
provides:
  - tests/tui/ RED-first test package (9 modules covering TUI-02..TUI-10)
  - tests/test_tui_app_entry.py RED test for TUI-01 (native workbench launch wiring)
  - tests/test_tui_debrand_audit.py for TUI-11 (no imported product identity in CLI output)
  - rich and prompt_toolkit declared as direct atlas-runtime dependencies
affects: [10.6-02, 10.6-03, 10.6-04, 10.6-05]

# Tech tracking
tech-stack:
  added: ["rich>=13.7,<15 (direct dep)", "prompt_toolkit>=3.0,<4 (direct dep)"]
  patterns:
    - "RED-first Wave 0 scaffold: every future-wave test imports a not-yet-existing atlas_runtime.tui.* module so collection itself fails (ModuleNotFoundError), never a runtime skip"
    - "tests/tui/conftest.py reuses the top-level in-memory db fixture (no new DB harness) + adds a shared forced_console factory for deterministic Rich rendering assertions"
    - "Known-pre-existing-leak filtering pattern for grep-style audits: strip documented, out-of-scope phrases before asserting, so the audit stays a real regression guard instead of false-failing on unrelated legacy text"

key-files:
  created:
    - services/agent-runtime/tests/tui/__init__.py
    - services/agent-runtime/tests/tui/conftest.py
    - services/agent-runtime/tests/tui/test_capabilities.py
    - services/agent-runtime/tests/tui/test_session_select.py
    - services/agent-runtime/tests/tui/test_header.py
    - services/agent-runtime/tests/tui/test_transcript_poll.py
    - services/agent-runtime/tests/tui/test_event_renderers.py
    - services/agent-runtime/tests/tui/test_permission_ui.py
    - services/agent-runtime/tests/tui/test_command_dispatch.py
    - services/agent-runtime/tests/tui/test_cancel_unwind.py
    - services/agent-runtime/tests/tui/test_resume_replay.py
    - services/agent-runtime/tests/test_tui_app_entry.py
    - services/agent-runtime/tests/test_tui_debrand_audit.py
  modified:
    - services/agent-runtime/pyproject.toml

key-decisions:
  - "Package-legitimacy checkpoint pre-approved per orchestrator instruction (rich/prompt_toolkit already resolved transitively, mainstream, permissively licensed) — proceeded without pausing for human input"
  - "test_tui_debrand_audit.py: discovered two pre-existing, out-of-scope Hermes-identity mentions in --help output (the old `tui` wrapper's help string, which Wave 4 rewires, and the unrelated `foundation` command's help string). Filtered both via documented regex patterns rather than editing cli/main.py or cli/foundation.py, which are out of scope for this Wave-0 RED-scaffolding plan."

patterns-established:
  - "Pattern: per-module RED stub imports the not-yet-existing target at module level so pytest collection fails loudly (never pytest.skip/xfail)"
  - "Pattern: grep-style identity audits collapse non-alphanumeric characters (Rich box-drawing glyphs included) before substring matching, since --help table wrapping otherwise splits a phrase across rendered lines"

requirements-completed: [TUI-01, TUI-02, TUI-03, TUI-04, TUI-05, TUI-06, TUI-07, TUI-08, TUI-09, TUI-10, TUI-11]

# Metrics
duration: 35min
completed: 2026-06-27
---

# Phase 10.6 Plan 01: Wave 0 RED-First Test Scaffold Summary

**9-module `tests/tui/` RED package + 2 top-level RED/audit tests for the ATLAS Terminal Workbench, with `rich`/`prompt_toolkit` declared as direct dependencies.**

## Performance

- **Duration:** ~35 min
- **Tasks:** 3 (plus 1 pre-approved checkpoint)
- **Files modified:** 14 (13 created, 1 modified)

## Accomplishments
- Declared `rich>=13.7,<15` and `prompt_toolkit>=3.0,<4` as direct `atlas-runtime` dependencies (both already resolved transitively — no new installs).
- Created `tests/tui/` package: `conftest.py` (reuses the top-level `db` fixture, adds `forced_console` factory) + 9 RED test modules covering TUI-02 through TUI-10, each importing a not-yet-existing `atlas_runtime.tui.*` module so collection itself fails.
- Created `tests/test_tui_app_entry.py` (TUI-01, RED) and `tests/test_tui_debrand_audit.py` (TUI-11, passes today by design).
- Confirmed genuine RED state via pytest exit code (collection-level `ModuleNotFoundError`), not token grep, per Nyquist requirement.

## Task Commits

1. **Task 1: Declare rich + prompt_toolkit as direct dependencies** - `0bfd6b1` (feat)
2. **Task 2: Create tests/tui/ package + conftest + 9 RED test modules** - `cac6e68` (test)
3. **Task 3: Create top-level RED tests for app-entry wiring and de-brand audit** - `920c975` (test)

**Plan metadata:** pending (this commit)

## Files Created/Modified
- `services/agent-runtime/pyproject.toml` — added `rich`/`prompt_toolkit` to `[project] dependencies` with a supply-chain-honesty comment matching the existing `pyyaml` style
- `services/agent-runtime/tests/tui/__init__.py` — empty, makes the dir a discoverable pytest package
- `services/agent-runtime/tests/tui/conftest.py` — `forced_console` factory fixture; reuses top-level `db`/`surface_session`/`make_active_session`/`seed_pending_approval`/`register_test_channel` via pytest's automatic parent-conftest discovery
- `services/agent-runtime/tests/tui/test_capabilities.py` — TUI-10: NO_COLOR, legacy-Windows ASCII fallback, narrow-width layout
- `services/agent-runtime/tests/tui/test_session_select.py` — TUI-02: explicit `--project`/`--global` flags, no-TTY fail-closed
- `services/agent-runtime/tests/tui/test_header.py` — TUI-03: masked model identity, no resolved secrets, permission mode + context budget
- `services/agent-runtime/tests/tui/test_transcript_poll.py` — TUI-04: replay-since dedup, append-only (no full redraw)
- `services/agent-runtime/tests/tui/test_event_renderers.py` — TUI-05: parametrized over every `EventKind`, asserts a non-empty renderable
- `services/agent-runtime/tests/tui/test_permission_ui.py` — TUI-06: approve-once/scoped-allow/reject/cancel mapping to `permission_broker.claim`/`record_allow_rule`, headless fail-closed
- `services/agent-runtime/tests/tui/test_command_dispatch.py` — TUI-07: core command dispatch + deferred-group extension-seam (not opaque error)
- `services/agent-runtime/tests/tui/test_cancel_unwind.py` — TUI-08: Ctrl-C drives `transition_session` to `cancelling`, exactly once
- `services/agent-runtime/tests/tui/test_resume_replay.py` — TUI-09: `replay_contract` called before `resume_session`; real version-drift raises `ContractCompatibilityError`
- `services/agent-runtime/tests/test_tui_app_entry.py` — TUI-01: bare `atlas`/`atlas tui` should invoke `atlas_runtime.tui.app.run_workbench`; confirms no Hermes wrapper reached
- `services/agent-runtime/tests/test_tui_debrand_audit.py` — TUI-11: `--help` output and (once it exists) `atlas_runtime/tui/` source scanned for Hermes/Ink identity tokens

## Decisions Made
- Package-legitimacy checkpoint was pre-approved by explicit runtime instruction (both packages already transitively resolved, mainstream, MIT/BSD-licensed, no postinstall scripts) — proceeded directly to Task 1 without pausing.
- `test_tui_debrand_audit.py` discovered the plan's assumption ("no leak possible pre-Wave-1") was incorrect: the *existing* `tui` command's own `help=` string (`cli/main.py`) and the unrelated `foundation` command's `help=` string (`cli/foundation.py`) both legitimately name "foundation/atlas-hermes" today, since that is literally the vendored directory they reference. Rather than editing either pre-existing CLI registration (out of scope for this Wave-0 plan — Wave 4 owns the `tui` rewiring; `foundation` is untouched by this phase), the test filters both known, documented phrases before asserting, preserving its value as a regression guard for any genuinely NEW leak.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `test_tui_debrand_audit.py`'s help-output assertion was written per-plan-spec to "pass trivially" but initially failed against the real CLI**
- **Found during:** Task 3 verification
- **Issue:** The plan's acceptance criteria assumed `--help` output contains no Hermes/Ink tokens pre-Wave-1 ("no tui/ package yet means no leak possible"). In reality, two unrelated pre-existing commands (`tui`, `foundation`) already reference "foundation/atlas-hermes" in their own `help=` strings, causing the literal assertion to fail.
- **Fix:** Added `_KNOWN_LEAK_PATTERNS` (two documented regexes) and `_strip_known_pre_existing_leaks`/`_collapse_whitespace` helpers that normalize Rich's box-drawing-wrapped `--help` text and strip exactly the two known, out-of-scope phrases before asserting no OTHER identity token leaks. Added a companion test (`test_tui_subcommand_help_leak_is_known_pre_existing_not_a_new_regression`) that pins down the known leak so its eventual removal (Wave 4) is visible rather than silently passing.
- **Files modified:** services/agent-runtime/tests/test_tui_debrand_audit.py
- **Verification:** `python -m pytest tests/test_tui_debrand_audit.py -q` → 3 passed
- **Committed in:** 920c975 (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix to a test's filtering logic against real CLI output)
**Impact on plan:** No scope creep — no production CLI files (`cli/main.py`, `cli/foundation.py`, `cli/tui.py`) were modified. The fix is confined to the new test file itself and documents exactly which future wave (4) owns removing the now-pinned known leak.

## Issues Encountered
- RTK's pytest output wrapper truncates verbose tracebacks ("Pytest: No tests collected" / log-file redirect); worked around by invoking `rtk proxy python -m pytest ...` directly to see full collection-error tracebacks and confirm genuine RED (ModuleNotFoundError), not a silently-skipped test.
- Windows console code page (cp1252) raised `UnicodeEncodeError` when piping pytest output containing Rich box-drawing characters through some shell redirections; worked around with `iconv -f utf-8 -t utf-8//IGNORE` and explicit `sys.stdout.reconfigure(encoding='utf-8')` in ad-hoc inspection scripts. No production code affected.

## User Setup Required
None — no external service configuration required. `rich`/`prompt_toolkit` were already present in the environment.

## Next Phase Readiness
- Wave 1+ can now implement `atlas_runtime/tui/capabilities.py`, `theme.py`, `session_select.py`, `header.py`, `transcript.py`, `permission_ui.py`, `command_dispatch.py`, `resume.py`, and `app.py` against these RED tests as the GREEN target.
- `tests/test_tui_app_entry.py` and `tests/test_tui_debrand_audit.py::test_tui_subcommand_help_leak_is_known_pre_existing_not_a_new_regression` are explicitly called out as Wave 4's responsibility to flip once the `tui` CLI command is rewired to the native workbench.
- No blockers. Full pre-existing suite (`tests/test_tui_cli.py`, etc.) verified unaffected.

---
*Phase: 10.6-atlas-terminal-workbench*
*Completed: 2026-06-27*

## Self-Check: PASSED

All 15 created/modified files confirmed present on disk; all 3 task commit hashes (`0bfd6b1`, `cac6e68`, `920c975`) confirmed present in git log.
