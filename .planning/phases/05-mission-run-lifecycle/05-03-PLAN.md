---
phase: 05-mission-run-lifecycle
plan: 03
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - services/agent-runtime/atlas_runtime/policy.py
  - services/agent-runtime/atlas_runtime/subagent_service.py
  - services/agent-runtime/tests/test_policy.py
  - services/agent-runtime/tests/test_run_service.py
autonomous: true
requirements:
  - RUNTIME-06
  - RUNTIME-07

must_haves:
  truths:
    - "check_workspace_boundary() returns PolicyDecision(allowed=True) for paths inside the workspace root"
    - "check_workspace_boundary() returns PolicyDecision(allowed=False) for path traversal strings like ../outside"
    - "check_workspace_boundary() handles both POSIX-style and Windows-style path strings on the current OS via pathlib.Path.resolve()"
    - "check_workspace_boundary_and_emit() emits an AuditEvent with event_type=failure when path is outside workspace"
    - "check_tool_allowed() returns allowed=True when tool_name is in the allowed_tools list and False otherwise"
    - "dispatch_subagent() emits a subagent_run AuditEvent with role, model_tier, allowed_tools, autonomy_level, and token_budget in the data payload"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/policy.py"
      provides: "PolicyDecision, check_workspace_boundary, check_workspace_boundary_and_emit, check_tool_allowed"
      min_lines: 70
    - path: "services/agent-runtime/atlas_runtime/subagent_service.py"
      provides: "dispatch_subagent stub — emits subagent_run AuditEvent"
      min_lines: 30
    - path: "services/agent-runtime/tests/test_policy.py"
      provides: "Parametrized RUNTIME-07 tests for Linux + Windows path strings"
    - path: "services/agent-runtime/tests/test_run_service.py"
      provides: "test_dispatch_subagent_emits_subagent_run now passes (xfail removed)"
  key_links:
    - from: "policy.check_workspace_boundary_and_emit"
      to: "audit_service.emit"
      via: "emit(conn, lock, run_id=run_id, event_type='failure', ...)"
      pattern: "event_type=\"failure\""
    - from: "subagent_service.dispatch_subagent"
      to: "audit_service.emit"
      via: "emit(conn, lock, run_id=run_id, event_type='subagent_run', data=payload)"
      pattern: "event_type=\"subagent_run\""
---

<objective>
Implement policy.py (workspace boundary enforcement + tool allowlist) and subagent_service.py (governance stub). Both modules are parallel to run_service/mission_service with no file overlap, so this plan runs in Wave 1 alongside Plan 02.

Purpose: Satisfies RUNTIME-06 (subagent governance AuditEvent) and RUNTIME-07 (cross-platform policy engine).
Output: Two service modules implemented; test_policy.py and test_run_service.py subagent test green.
</objective>

<execution_context>
@C:/Users/Davi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Davi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-mission-run-lifecycle/CONTEXT.md
@.planning/phases/05-mission-run-lifecycle/05-RESEARCH.md
@.planning/phases/05-mission-run-lifecycle/05-PATTERNS.md
@.planning/phases/05-mission-run-lifecycle/05-01-SUMMARY.md

<interfaces>
<!-- Exact contracts extracted from codebase during planning. -->

audit_service.emit (audit_service.py lines 63-77):
  emit(conn, lock, *, run_id, event_type, ..., data=None, policy_result=None) -> AuditEvent
  emit() acquires `with lock:` internally — NEVER call while holding the lock.

AuditEvent.event_type Literal (core.py lines 95-104):
  "llm_call" | "tool_call" | "subagent_run" | "approval" | "artifact" |
  "wiki_update" | "memory_change" | "failure"
  "subagent_run" is already in the Literal — no schema change needed.
  "failure" is used for policy rejection events per success criterion 6.

AuditEvent.policy_result field (core.py line 111):
  Optional[str] — for policy rejection, pass the PolicyDecision.reason string here.

pathlib.Path.resolve() behavior:
  On Windows: Path("C:/Users/...").resolve() returns the Windows path correctly.
  On Linux: Path("/home/...").resolve() returns the POSIX path correctly.
  Path(workspace_root / target_path).resolve() resolves relative paths relative to workspace_root, preventing CWD-escape attacks (Pitfall 3 fix).

conftest.py fixtures:
  db — in-memory SQLite with all tables; pass to check_workspace_boundary_and_emit tests
  lock — threading.Lock()
  run_id — pre-seeded running run row (use for policy emit tests and subagent tests)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement policy.py (RUNTIME-07)</name>
  <files>
    services/agent-runtime/atlas_runtime/policy.py
    services/agent-runtime/tests/test_policy.py
  </files>
  <read_first>
    services/agent-runtime/atlas_runtime/policy.py
    services/agent-runtime/tests/test_policy.py
    services/agent-runtime/atlas_runtime/audit_service.py
    .planning/phases/05-mission-run-lifecycle/05-PATTERNS.md
    .planning/phases/05-mission-run-lifecycle/05-RESEARCH.md
  </read_first>
  <behavior>
    - check_workspace_boundary(target_path: str, workspace_root: str) -> PolicyDecision: resolves workspace_root via pathlib.Path(workspace_root).resolve(); resolves target as (resolved_root / target_path).resolve() (relative path pinned to workspace_root, prevents CWD escape); calls resolved_target.relative_to(resolved_root) — if it raises ValueError, path is outside; returns PolicyDecision(allowed=True, reason="within_workspace") or PolicyDecision(allowed=False, reason=f"path_outside_workspace: {target_path!r} not under {workspace_root!r}"). Both Windows-style absolute strings ("C:\\Users\\other\\file.txt") and relative traversal strings ("../outside/file.txt") must return allowed=False. A relative string inside the workspace ("subdir/file.txt") must return allowed=True.
    - check_workspace_boundary_and_emit(conn, lock, run_id, target_path, workspace_root) -> PolicyDecision: calls check_workspace_boundary, then if not decision.allowed calls emit(conn, lock, run_id=run_id, event_type="failure", data={"reason": decision.reason, "target_path": target_path}, policy_result=decision.reason). Returns decision.
    - check_tool_allowed(tool_name: str, allowed_tools: list[str]) -> PolicyDecision: returns PolicyDecision(allowed=True, reason="tool_in_allowlist") if tool_name in allowed_tools, else PolicyDecision(allowed=False, reason=f"tool_not_allowed: {tool_name!r} not in allowlist").
  </behavior>
  <action>
    Replace NotImplementedError bodies in policy.py with full implementations per the RESEARCH.md Pattern 3 specification and the Pitfall 3 (CWD-escape) fix.

    PolicyDecision is already a dataclass — keep it. Do not convert to Pydantic model.

    check_workspace_boundary: wrap the entire body in try/except ValueError. Inside try: resolved_root = pathlib.Path(workspace_root).resolve(); resolved_target = (resolved_root / target_path).resolve(); resolved_target.relative_to(resolved_root); return PolicyDecision(allowed=True, reason="within_workspace"). In except ValueError: return PolicyDecision(allowed=False, reason=f"path_outside_workspace: {target_path!r} not under {workspace_root!r}"). Note: passing an absolute path like "C:\\Users\\other\\file.txt" to `(resolved_root / target_path)` — on Python 3.12+ pathlib, joining an absolute path to another path returns the absolute path as-is (the workspace_root prefix is discarded). This is the intended behavior — an absolute path outside the workspace will fail the relative_to() check.

    check_workspace_boundary_and_emit: call check_workspace_boundary first (no lock needed for pure path logic). Then if not decision.allowed: call emit(conn, lock, run_id=run_id, event_type="failure", data={"reason": decision.reason, "target_path": target_path}, policy_result=decision.reason). Return decision.

    check_tool_allowed: single if-else, no DB or emit needed here.

    Update test_policy.py: remove all @pytest.mark.xfail decorators. All tests must pass. For the parametrized test test_workspace_boundary, the three cases must all produce the expected allowed values. Note: the Windows-style absolute path "C:\\Users\\other\\file.txt" test expects allowed=False because that path is not under tmp_path regardless of OS.
  </action>
  <verify>
    <automated>cd /c/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT/services/agent-runtime && python -m pytest tests/test_policy.py -x -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All tests in test_policy.py pass (no xfail, no FAILED)
    - test_workspace_boundary["subdir/file.txt-True"] passes — relative in-workspace path allowed
    - test_workspace_boundary["../outside/file.txt-False"] passes — traversal rejected
    - test_workspace_boundary["C:\\Users\\other\\file.txt-False"] passes — absolute out-of-workspace rejected
    - test_check_tool_allowed_in_list passes: check_tool_allowed("Read", ["Read","Write"]).allowed is True
    - test_check_tool_allowed_not_in_list passes: check_tool_allowed("Shell", ["Read"]).allowed is False
    - test_boundary_violation_emits_failure_event passes: audit_events table has row with event_type="failure" after check_workspace_boundary_and_emit called with traversal path
    - policy.py has no shell=True subprocess usage (grep -n "shell=True" policy.py == 0)
    - policy.py has no os.sep or string split on "\" or "/" for path logic (grep -n "os.sep\|split.*\\\\\|split.*/" policy.py == 0)
    - policy_result field on emitted AuditEvent equals the PolicyDecision.reason string
  </acceptance_criteria>
  <done>policy.py fully implemented with pathlib.Path.resolve() boundary check and tool allowlist; all test_policy.py tests green; failure AuditEvent emitted on boundary violation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement subagent_service.py and complete test_run_service.py subagent test (RUNTIME-06)</name>
  <files>
    services/agent-runtime/atlas_runtime/subagent_service.py
    services/agent-runtime/tests/test_run_service.py
  </files>
  <read_first>
    services/agent-runtime/atlas_runtime/subagent_service.py
    services/agent-runtime/tests/test_run_service.py
    services/agent-runtime/atlas_runtime/audit_service.py
    packages/atlas-core/atlas_core/schemas/core.py
    .planning/phases/05-mission-run-lifecycle/05-PATTERNS.md
  </read_first>
  <behavior>
    - dispatch_subagent(conn, lock, *, run_id, role, model_tier="sonnet", allowed_tools=None, autonomy_level="supervised", token_budget=4096) -> None: constructs governance payload dict with exactly these five keys (role, model_tier, allowed_tools, autonomy_level, token_budget); calls emit(conn, lock, run_id=run_id, event_type="subagent_run", data=payload). That is the entire function — no real spawning in Phase 5.
    - The function wraps the emit() call in try/except Exception to implement the fail-open error guard from 05-PATTERNS.md (log warning on failure, do not crash caller).
    - allowed_tools defaults to [] (not None in the stored payload) — normalize: payload["allowed_tools"] = allowed_tools if allowed_tools is not None else [].
    - event_type="subagent_run" is already a valid Literal in AuditEvent.event_type (verified in core.py line 98) — no schema change required.
  </behavior>
  <action>
    Replace the NotImplementedError body in subagent_service.py with the full stub implementation per RESEARCH.md Pattern 4.

    Import logging at the top. Add `logger = logging.getLogger(__name__)`.

    dispatch_subagent body: (1) build payload = {"role": role, "model_tier": model_tier, "allowed_tools": allowed_tools if allowed_tools is not None else [], "autonomy_level": autonomy_level, "token_budget": token_budget}. (2) try: emit(conn, lock, run_id=run_id, event_type="subagent_run", data=payload). (3) except Exception as exc: logger.warning("subagent_service.dispatch_subagent failed: %s", exc).

    Update test_run_service.py: locate test_dispatch_subagent_emits_subagent_run and remove its @pytest.mark.xfail decorator. The test calls start_run (plan 02 implements it) then dispatch_subagent(role="researcher"), then asserts: SELECT COUNT(*) FROM audit_events WHERE run_id=? AND event_type='subagent_run' >= 1. Also assert the stored data JSON contains "role" key: json.loads(row["data"])["role"] == "researcher".
  </action>
  <verify>
    <automated>cd /c/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT/services/agent-runtime && python -m pytest tests/test_run_service.py::test_dispatch_subagent_emits_subagent_run -xvs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test_dispatch_subagent_emits_subagent_run passes (no xfail)
    - SELECT event_type FROM audit_events WHERE run_id=? returns a row with event_type="subagent_run"
    - The data JSON stored in that row contains keys: role, model_tier, allowed_tools, autonomy_level, token_budget
    - json.loads(stored_data)["role"] == "researcher"
    - json.loads(stored_data)["allowed_tools"] is a list (empty list [] when not specified, not None)
    - subagent_service.dispatch_subagent has a try/except Exception wrapping the emit() call
    - subagent_service.py does not import or reference any actual subprocess spawning (grep -n "subprocess\|Popen\|spawn" subagent_service.py == 0)
  </acceptance_criteria>
  <done>subagent_service.py stub emits subagent_run AuditEvent with full governance payload; test_dispatch_subagent_emits_subagent_run green; no real subagent spawning in Phase 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy.py target_path input | User-supplied path string crosses into pathlib.resolve() — must not allow CWD-escape |
| subagent payload -> audit_events | governance payload passes through emit() _redact() before storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | policy.check_workspace_boundary path traversal | mitigate | (resolved_root / target_path).resolve() + relative_to() prevents ../../ CWD-escape; absolute paths outside workspace fail relative_to() check |
| T-05-06 | Elevation of Privilege | policy.check_tool_allowed unclassified tool bypass | mitigate | Reject any tool_name not in allowed_tools list (D-008); return PolicyDecision(allowed=False) — no default-allow |
| T-05-03 | Information Disclosure | subagent payload in audit_events | mitigate | dispatch_subagent data passes through emit() which calls _redact() via SECRET_PATTERNS before storage |
| T-05-SC | Tampering | uv pip install | mitigate | No new packages in this plan; audit from plan 01 applies |
</threat_model>

<verification>
After both tasks complete:

```bash
cd services/agent-runtime
python -m pytest tests/test_policy.py tests/test_run_service.py -v --tb=short 2>&1 | tail -20
```

Expected: test_policy.py all passing (no xfail), test_run_service.py 9/9 passing (subagent test now green, no remaining xfail).
</verification>

<success_criteria>
1. test_policy.py: all 6 tests passing (3 parametrized boundary cases + tool allowed + tool rejected + failure emit).
2. test_run_service.py: 9/9 passing after Plan 02 + Plan 03 both complete (the subagent test was the only remaining xfail).
3. policy.py uses only pathlib.Path.resolve() for path normalization — no shell logic, no os.sep, no subprocess.
4. subagent_service.py emits event_type="subagent_run" with governance payload containing all 5 required fields.
5. dispatch_subagent wraps emit() in try/except to prevent crashes from propagating to callers.
</success_criteria>

<output>
Create `.planning/phases/05-mission-run-lifecycle/05-03-SUMMARY.md` when done.
</output>
