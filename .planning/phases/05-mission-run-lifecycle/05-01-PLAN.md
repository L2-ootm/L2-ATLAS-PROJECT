---
phase: 05-mission-run-lifecycle
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/agent-runtime/pyproject.toml
  - services/agent-runtime/atlas_runtime/mission_service.py
  - services/agent-runtime/atlas_runtime/run_service.py
  - services/agent-runtime/atlas_runtime/policy.py
  - services/agent-runtime/atlas_runtime/subagent_service.py
  - services/agent-runtime/atlas_runtime/cli/__init__.py
  - services/agent-runtime/atlas_runtime/cli/main.py
  - services/agent-runtime/tests/test_mission_service.py
  - services/agent-runtime/tests/test_run_service.py
  - services/agent-runtime/tests/test_policy.py
  - services/agent-runtime/tests/test_cli.py
autonomous: true
requirements:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-04
  - RUNTIME-05
  - RUNTIME-06
  - RUNTIME-07

must_haves:
  truths:
    - "All stub modules import without error"
    - "All four test files collect (pytest --collect-only) without ImportError"
    - "pyproject.toml registers atlas console script and typer/pytest-cov deps"
    - "Every service function stub raises NotImplementedError with a docstring"
    - "Test files contain at least one xfail or skip marker per service function"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/mission_service.py"
      provides: "create_mission, get_mission, list_missions stubs"
    - path: "services/agent-runtime/atlas_runtime/run_service.py"
      provides: "start_run, complete_run, fail_run, cancel_run stubs"
    - path: "services/agent-runtime/atlas_runtime/policy.py"
      provides: "PolicyDecision dataclass, check_workspace_boundary, check_workspace_boundary_and_emit, check_tool_allowed stubs"
    - path: "services/agent-runtime/atlas_runtime/subagent_service.py"
      provides: "dispatch_subagent stub"
    - path: "services/agent-runtime/atlas_runtime/cli/main.py"
      provides: "Typer app with mission create/run/cancel/status subcommands"
    - path: "services/agent-runtime/pyproject.toml"
      provides: "[project.scripts] atlas entry, typer dep, pytest-cov dev dep, coverage config"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/mission_service.py"
      to: "atlas_core.schemas.core.Mission"
      via: "import at module top"
      pattern: "from atlas_core.schemas.core import Mission"
    - from: "services/agent-runtime/atlas_runtime/run_service.py"
      to: "atlas_runtime.audit_service.emit"
      via: "import at module top"
      pattern: "from atlas_runtime.audit_service import emit"
    - from: "services/agent-runtime/atlas_runtime/cli/main.py"
      to: "atlas_runtime.mission_service"
      via: "import at module top"
      pattern: "from atlas_runtime import mission_service"
---

<objective>
Scaffold all Phase 5 modules and test files so Wave 1 plans have concrete stub contracts to implement against. Every file created here defines the exact function signatures, import structure, and test shape that Wave 1 executors will fill in.

Purpose: Establish the compilation baseline. Wave 1 executors must not discover contracts — they are given them here.
Output: 11 files (7 source stubs + 4 test skeletons) + updated pyproject.toml.
</objective>

<execution_context>
@C:/Users/Davi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Davi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mission-run-lifecycle/CONTEXT.md
@.planning/phases/05-mission-run-lifecycle/05-RESEARCH.md
@.planning/phases/05-mission-run-lifecycle/05-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Read from codebase during planning. -->

From services/agent-runtime/atlas_runtime/audit_service.py (emit signature):
  emit(conn, lock, *, run_id, event_type, task_id=None, session_id=None,
       tool_call_id=None, tool_name=None, data=None, duration_ms=None,
       policy_result=None, tool_call_kwargs=None) -> AuditEvent

From packages/atlas-core/atlas_core/schemas/core.py:
  Mission: id, title, intent, status (Literal["pending","running","succeeded","failed","cancelled"]),
           project, created_at, updated_at — frozen=True
  Run: id, mission_id, session_id, status (Literal["running","succeeded","failed","cancelled"]),
       started_at, finished_at, summary — frozen=True
  AuditEvent.event_type Literal includes: "llm_call","tool_call","subagent_run",
           "approval","artifact","wiki_update","memory_change","failure"

From services/agent-runtime/tests/conftest.py (fixtures to reuse by name):
  db   — in-memory SQLite, WAL + FK ON + 0001_core.sql applied
  run_id — pre-seeded mission + run rows for FK-safe tests
  lock — threading.Lock()

From services/agent-runtime/pyproject.toml (current state):
  dependencies = ["atlas-core"]
  dev = ["pytest>=9.0"]
  packages = ["atlas_runtime", "atlas_audit"]
  NO [project.scripts] yet, NO typer dep, NO pytest-cov
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update pyproject.toml and create service module stubs</name>
  <files>
    services/agent-runtime/pyproject.toml
    services/agent-runtime/atlas_runtime/mission_service.py
    services/agent-runtime/atlas_runtime/run_service.py
    services/agent-runtime/atlas_runtime/policy.py
    services/agent-runtime/atlas_runtime/subagent_service.py
    services/agent-runtime/atlas_runtime/cli/__init__.py
    services/agent-runtime/atlas_runtime/cli/main.py
  </files>
  <read_first>
    services/agent-runtime/pyproject.toml
    services/agent-runtime/atlas_runtime/audit_service.py
    packages/atlas-core/atlas_core/schemas/core.py
    .planning/phases/05-mission-run-lifecycle/05-PATTERNS.md
  </read_first>
  <action>
    Update pyproject.toml: add typer>=0.25.0 to [project].dependencies list alongside atlas-core; add pytest-cov>=7.0 to [project.optional-dependencies].dev; add [project.scripts] section with atlas = "atlas_runtime.cli.main:app"; add [tool.coverage.run] with branch=true and source=["atlas_runtime"]; add [tool.coverage.report] with fail_under=80. Do not remove or change existing fields.

    Create services/agent-runtime/atlas_runtime/mission_service.py with module docstring referencing D-002 and D-003. Import from __future__ import annotations, then datetime, sqlite3, threading, uuid, typing.Optional/Literal. Import Mission from atlas_core.schemas.core and emit from atlas_runtime.audit_service. Define three stub functions — each raises NotImplementedError("not implemented"):
      create_mission(conn, lock, *, title: str, intent: str = "", project: str = "") -> Mission
      get_mission(conn: sqlite3.Connection, mission_id: str) -> Optional[Mission]
      list_missions(conn: sqlite3.Connection) -> list[Mission]

    Create services/agent-runtime/atlas_runtime/run_service.py with module docstring referencing D-001 and D-002. Same stdlib imports plus Run from atlas_core.schemas.core and emit from atlas_runtime.audit_service. Define four stub functions — each raises NotImplementedError("not implemented"):
      start_run(conn, lock, *, mission_id: str, session_id: Optional[str] = None) -> Run
      complete_run(conn, lock, *, run_id: str, mission_id: str, status: Literal["succeeded","failed"], summary: str = "") -> None
      fail_run(conn, lock, *, run_id: str, mission_id: str, summary: str = "") -> None
      cancel_run(conn, lock, *, run_id: str, mission_id: str) -> None

    Create services/agent-runtime/atlas_runtime/policy.py with module docstring referencing D-006 and RUNTIME-07. Import from __future__ import annotations, pathlib, sqlite3, threading, dataclasses.dataclass, typing.Optional. Import emit from atlas_runtime.audit_service. Define PolicyDecision dataclass with fields allowed: bool and reason: str. Define three stub functions — each raises NotImplementedError("not implemented"):
      check_workspace_boundary(target_path: str, workspace_root: str) -> PolicyDecision
      check_workspace_boundary_and_emit(conn, lock, run_id: str, target_path: str, workspace_root: str) -> PolicyDecision
      check_tool_allowed(tool_name: str, allowed_tools: list[str]) -> PolicyDecision

    Create services/agent-runtime/atlas_runtime/subagent_service.py with module docstring referencing RUNTIME-06 and noting Phase 5 is stub-only. Import from __future__ import annotations, sqlite3, threading. Import emit from atlas_runtime.audit_service. Define one stub function — raises NotImplementedError("not implemented"):
      dispatch_subagent(conn, lock, *, run_id: str, role: str, model_tier: str = "sonnet", allowed_tools: list[str] | None = None, autonomy_level: str = "supervised", token_budget: int = 4096) -> None

    Create services/agent-runtime/atlas_runtime/cli/__init__.py with content: module docstring "ATLAS runtime CLI entry point package." and __version__ = "0.1.0".

    Create services/agent-runtime/atlas_runtime/cli/main.py. Import from __future__ import annotations, sqlite3, pathlib, threading. Import typer. Import mission_service, run_service from atlas_runtime. Define app = typer.Typer() and mission_app = typer.Typer(name="mission"). Call app.add_typer(mission_app, name="mission"). Define _get_connection() -> sqlite3.Connection that connects to pathlib.Path.home() / ".atlas" / "atlas.db", applies PRAGMA journal_mode=WAL and PRAGMA foreign_keys=ON, returns conn. Define _get_lock() -> threading.Lock returns a module-level threading.Lock() (use a module-level singleton _LOCK = threading.Lock()). Define four subcommands on mission_app:
      - @mission_app.command("create"): args title: str = typer.Option(..., "--title"), intent: str = typer.Option("", "--intent") — body calls mission_service.create_mission with _get_connection()/_get_lock(), prints mission.id via typer.echo.
      - @mission_app.command("run"): arg mission_id: str (positional) — body calls run_service.start_run with _get_connection()/_get_lock(), on success prints run.id; on ValueError prints error and raises typer.Exit(1).
      - @mission_app.command("cancel"): arg mission_id: str (positional) — body queries all runs for mission_id (SELECT id,status FROM runs WHERE mission_id=? AND status='running'), calls run_service.cancel_run for any running run, prints "cancelled" or "no active run"; on ValueError raises typer.Exit(1).
      - @mission_app.command("status"): arg mission_id: str (positional) — body queries SELECT status FROM missions WHERE id=? and prints the status string; if no row prints "not found" and raises typer.Exit(1).
      End with if __name__ == "__main__": app().
  </action>
  <verify>
    <automated>cd /c/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT && python -c "from atlas_runtime import mission_service, run_service, policy, subagent_service; from atlas_runtime.cli import main; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains the line atlas = "atlas_runtime.cli.main:app" under [project.scripts]
    - pyproject.toml [project].dependencies list contains typer>=0.25.0
    - pyproject.toml [project.optional-dependencies].dev contains pytest-cov>=7.0
    - pyproject.toml contains [tool.coverage.run] section with branch = true
    - pyproject.toml contains [tool.coverage.report] section with fail_under = 80
    - python -c "from atlas_runtime import mission_service" exits 0 with no ImportError
    - python -c "from atlas_runtime import run_service" exits 0 with no ImportError
    - python -c "from atlas_runtime import policy; from atlas_runtime.policy import PolicyDecision" exits 0
    - python -c "from atlas_runtime import subagent_service" exits 0 with no ImportError
    - python -c "from atlas_runtime.cli import main" exits 0 with no ImportError
    - All seven stub functions raise NotImplementedError when called (not AttributeError, not pass)
    - atlas_runtime/cli/__init__.py contains __version__ = "0.1.0"
  </acceptance_criteria>
  <done>All service modules and CLI import cleanly; pyproject.toml has all three additions (scripts, typer dep, pytest-cov); every service function stub raises NotImplementedError.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test file skeletons with xfail stubs</name>
  <files>
    services/agent-runtime/tests/test_mission_service.py
    services/agent-runtime/tests/test_run_service.py
    services/agent-runtime/tests/test_policy.py
    services/agent-runtime/tests/test_cli.py
  </files>
  <read_first>
    services/agent-runtime/tests/conftest.py
    .planning/phases/05-mission-run-lifecycle/05-PATTERNS.md
    .planning/phases/05-mission-run-lifecycle/05-VALIDATION.md
  </read_first>
  <action>
    Create services/agent-runtime/tests/test_mission_service.py. Import pytest and from atlas_runtime import mission_service. Define the following test functions, each decorated with @pytest.mark.xfail(reason="stub — implement in Wave 1", strict=True):
      test_create_mission_persists_row(db, lock) — imports create_mission, calls it with title="Test", asserts row count == 1 in missions table
      test_create_mission_returns_mission_model(db, lock) — asserts returned object is a Mission instance with correct title
      test_get_mission_returns_none_for_missing(db) — calls get_mission with a random UUID, asserts None
      test_list_missions_empty(db) — calls list_missions, asserts result == []
      test_create_mission_status_is_pending(db, lock) — asserts returned mission.status == "pending"

    Create services/agent-runtime/tests/test_run_service.py. Import pytest, uuid, datetime. Import from atlas_runtime import run_service. Add a module-level fixture mission_id_fixture (name="mission_id") that inserts a minimal missions row with status="pending" and returns the id (follow the conftest.py run_id_fixture pattern exactly). Define the following test functions, each decorated with @pytest.mark.xfail(reason="stub — implement in Wave 1", strict=True):
      test_start_run_creates_run_row(db, lock, mission_id) — calls start_run, asserts run row exists
      test_start_run_emits_audit_event(db, lock, mission_id) — calls start_run, asserts audit_events count >= 1
      test_start_run_updates_mission_status_to_running(db, lock, mission_id) — asserts missions.status == "running" after start_run
      test_complete_run_succeeded(db, lock, mission_id) — calls start_run then complete_run(status="succeeded"), asserts runs.status == "succeeded" and runs.finished_at IS NOT NULL and missions.status == "succeeded"
      test_complete_run_failed(db, lock, mission_id) — same but status="failed"
      test_cancel_run_sets_cancelled(db, lock, mission_id) — calls start_run then cancel_run, asserts runs.status == "cancelled" and audit_events count >= 2
      test_cancel_preserves_existing_audit_events(db, lock, mission_id) — calls start_run, emits a manual tool_call event, then cancel_run, asserts count >= 2 (row not deleted)
      test_cancel_already_terminal_raises(db, lock, mission_id) — calls start_run, complete_run(status="succeeded"), then asserts pytest.raises(ValueError) on cancel_run
      test_dispatch_subagent_emits_subagent_run(db, lock, mission_id) — from atlas_runtime import subagent_service; calls start_run then dispatch_subagent(role="researcher"), asserts audit_events row with event_type="subagent_run" exists

    Create services/agent-runtime/tests/test_policy.py. Import pytest, pathlib. Import from atlas_runtime.policy import check_workspace_boundary, check_workspace_boundary_and_emit, check_tool_allowed, PolicyDecision. Define parametrized test:
      @pytest.mark.parametrize("target,expected_allowed", [("subdir/file.txt", True), ("../outside/file.txt", False), ("C:\\Users\\other\\file.txt", False)]) with @pytest.mark.xfail(reason="stub — implement in Wave 1", strict=True):
        test_workspace_boundary(tmp_path, target, expected_allowed) — calls check_workspace_boundary(target, str(tmp_path)), asserts decision.allowed is expected_allowed, asserts isinstance(decision, PolicyDecision)
      Define (xfail) test_check_tool_allowed_in_list(db, lock) — calls check_tool_allowed("Read", ["Read","Write"]), asserts decision.allowed is True
      Define (xfail) test_check_tool_allowed_not_in_list(db, lock) — calls check_tool_allowed("Shell", ["Read"]), asserts decision.allowed is False
      Define (xfail) test_boundary_violation_emits_failure_event(db, lock, run_id) — calls check_workspace_boundary_and_emit with a traversal path, asserts audit_events row with event_type="failure" exists

    Create services/agent-runtime/tests/test_cli.py. Import pytest. Import from typer.testing import CliRunner. Import from atlas_runtime.cli.main import app. Define runner = CliRunner() at module level. Define the following test functions, each decorated with @pytest.mark.xfail(reason="stub — implement in Wave 1", strict=True):
      test_create_command_exits_zero(db, monkeypatch) — monkeypatches atlas_runtime.cli.main._get_connection to return db; invokes app ["mission","create","--title","Test","--intent","do X"]; asserts exit_code == 0 and output.strip() has len == 36
      test_run_command_exits_zero(db, lock, monkeypatch) — inserts a mission via mission_service.create_mission first, monkeypatches both _get_connection and _get_lock, invokes ["mission","run", mission.id], asserts exit_code == 0
      test_cancel_command_exits_zero(db, lock, monkeypatch) — inserts mission, starts run, monkeypatches, invokes ["mission","cancel", mission_id], asserts exit_code == 0
      test_status_command_exits_zero(db, monkeypatch) — inserts mission, monkeypatches, invokes ["mission","status", mission.id], asserts exit_code == 0 and "pending" in output
      test_status_unknown_id_exits_one(db, monkeypatch) — monkeypatches, invokes ["mission","status","nonexistent-uuid"], asserts exit_code == 1
  </action>
  <verify>
    <automated>cd /c/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT/services/agent-runtime && python -m pytest tests/test_mission_service.py tests/test_run_service.py tests/test_policy.py tests/test_cli.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pytest --collect-only finds all four test files with no ImportError
    - total collected tests across the four files is >= 18
    - All collected tests are marked xfail (running the suite shows all xfailed, no errors, no plain FAILED)
    - test_run_service.py defines a mission_id fixture (not relying on conftest.py run_id which inserts a running run)
    - test_policy.py has at least one @pytest.mark.parametrize with ("subdir/file.txt", True) and ("../outside/file.txt", False) as cases
    - test_cli.py imports CliRunner from typer.testing
  </acceptance_criteria>
  <done>All four test files collect cleanly; all tests are xfail; no ImportError anywhere; test shapes match the Wave 1 implementation contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> service layer | User-supplied title/intent strings cross into SQL INSERT |
| target_path arg -> policy.py | User-supplied file path crosses into pathlib.resolve() workspace check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | mission_service.create_mission SQL INSERT | mitigate | Use parameterized queries (:id, :title style) via model_dump() dict — never f-string SQL; Pydantic validates before INSERT |
| T-05-02 | Tampering | policy.check_workspace_boundary path traversal | mitigate | pathlib.Path(workspace_root / target).resolve() + relative_to() check prevents ../../ CWD-escape; implemented in Wave 1 |
| T-05-03 | Information Disclosure | subagent_service.dispatch_subagent payload | mitigate | All data payloads pass through audit_service.emit() which calls _redact() via SECRET_PATTERNS before storage |
| T-05-SC | Tampering | uv pip install typer pytest-cov | mitigate | Package Legitimacy Audit in 05-RESEARCH.md: typer [OK], click [OK], pytest-cov [OK/ASSUMED]; no [SLOP] packages |
</threat_model>

<verification>
Run after Wave 0 completes:

```bash
cd services/agent-runtime
python -m pytest tests/test_mission_service.py tests/test_run_service.py tests/test_policy.py tests/test_cli.py -v 2>&1 | grep -E "xfail|error|FAILED"
python -c "from atlas_runtime import mission_service, run_service, policy, subagent_service; from atlas_runtime.cli.main import app; print('all imports OK')"
python -c "import tomllib; t = tomllib.load(open('pyproject.toml','rb')); assert 'atlas' in t['project']['scripts']; print('scripts OK')"
```

Expected: all tests xfailed (not failed/error), all imports clean, pyproject.toml has scripts section.
</verification>

<success_criteria>
1. All 11 files exist at the paths in files_modified.
2. `python -c "from atlas_runtime import mission_service, run_service, policy, subagent_service; from atlas_runtime.cli import main"` exits 0.
3. `pytest tests/ --collect-only -q` shows >= 18 tests collected, 0 errors.
4. Running the collected tests shows all xfailed (strict=True means a passing stub would flip to FAILED — correct behavior).
5. pyproject.toml has [project.scripts] atlas entry, typer dep, pytest-cov in dev, and [tool.coverage.run]/[tool.coverage.report] sections.
</success_criteria>

<output>
Create `.planning/phases/05-mission-run-lifecycle/05-01-SUMMARY.md` when done.
</output>
