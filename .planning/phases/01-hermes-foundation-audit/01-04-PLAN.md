---
phase: 01-hermes-foundation-audit
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
files_modified:
  - docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md
autonomous: false
requirements:
  - FOUND-04

must_haves:
  truths:
    - "Extraction plan document exists classifying all 6 atlas_core donor modules as port/rewrite/reference/discard"
    - "Each classification is supported by concrete evidence (imports, Hermes overlap, Pydantic v1 usage, Windows-coupling)"
    - "Data-carrying modules are linked to their target Phase 2 Pydantic schema (SCHEMA-01)"
    - "L2-Atlas source repo working tree is unmodified after the audit"
  artifacts:
    - path: "docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md"
      provides: "Per-module classification table for 6 atlas_core donor modules with Phase 2 schema links"
      contains: "port/rewrite/reference/discard"
  key_links:
    - from: "docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md"
      to: "C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/"
      via: "Module paths cited as evidence sources"
      pattern: "atlas_core"
    - from: "docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 2 schema links (SCHEMA-01)"
      pattern: "SCHEMA-01"

user_setup:
  - service: L2-Atlas local repo
    why: "Donor modules are in a local-only repo at C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/. This path must be readable. If the executor cannot access it, a human checkpoint will confirm the path."
    env_vars: []
    dashboard_config:
      - task: "Confirm L2-Atlas repo is accessible at C:/Users/Davi/Desktop/Projects/L2-Atlas/"
        location: "Local filesystem — no external action needed if path exists"
---

<objective>
Read the L2-Atlas atlas_core donor modules and produce the authoritative module extraction plan.

Purpose: FOUND-04 — Phase 2 needs to know which legacy atlas_core modules to port, rewrite, use as reference, or discard when building the Pydantic v2 domain schemas. This plan produces that classification with evidence.

Output:
- `docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md` — classification table for all 6 modules with evidence and Phase 2 schema links
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hermes-foundation-audit/CONTEXT.md
@.planning/phases/01-hermes-foundation-audit/01-RESEARCH.md
@.planning/phases/01-hermes-foundation-audit/01-01-SUMMARY.md

<interfaces>
<!-- L2-Atlas donor module paths (confirmed accessible). -->
<!-- These are READ-ONLY. The audit MUST NOT modify any of these files. -->

C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/mission_control/parser.py   (5.0KB)
C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/mission_control/task_model.py  (3.0KB)
C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/execution/policy.py          (3.0KB)
C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/execution/powershell.py      (4.3KB)
C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/logging/jsonl_logger.py      (1.9KB)
C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/runtime/orchestrator.py      (7.4KB)
C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/runtime/models.py            (2.6KB)
C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/skills/registry.py           (1.6KB)

Preliminary classifications from RESEARCH.md (to be confirmed at execution):
  mission_control/parser.py    → port (pure stdlib, Pydantic v2 rewrite needed, data contract)
  execution/policy.py          → reference → rewrite (PowerShell bias, cross-platform needed)
  execution/powershell.py      → rewrite (Windows-only hard dep on powershell.exe)
  logging/jsonl_logger.py      → reference (superseded by ATLAS AuditEvent)
  runtime/orchestrator.py      → reference (PowerShell dep inherited; concept maps to Phase 5)
  skills/registry.py           → reference (superseded by Hermes skill system)

Hermes equivalents to check for overlap (read from _EXTERNAL_REPOS/hermes-agent/):
  skills/registry.py vs: _EXTERNAL_REPOS/hermes-agent/agent/skill_utils.py,
                         _EXTERNAL_REPOS/hermes-agent/agent/skill_bundles.py
  logging/jsonl_logger.py vs: _EXTERNAL_REPOS/hermes-agent/hermes_logging.py (if exists)
  execution/policy.py vs: _EXTERNAL_REPOS/hermes-agent/tools/tool_guardrails.py (if exists)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Nothing yet — this is a path-confirmation gate before reading L2-Atlas source.</what-built>
  <how-to-verify>
    Confirm the following path is readable on your machine:
      C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/

    Run in PowerShell:
      Test-Path "C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/mission_control/parser.py"
      Test-Path "C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/execution/policy.py"
      Test-Path "C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/runtime/orchestrator.py"

    Expected: all three return True.

    Also confirm the L2-Atlas repo git status is currently clean (no uncommitted changes) before the audit reads it:
      git -C "C:/Users/Davi/Desktop/Projects/L2-Atlas" status --short

    Expected: empty output (clean working tree).
  </how-to-verify>
  <resume-signal>
    Type "confirmed — paths accessible and repo clean" to continue.
    If a path is wrong, provide the correct base path and the executor will update the read targets.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Read Donor Modules + Write L2_ATLAS_MODULE_EXTRACTION_PLAN.md</name>
  <files>docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md</files>

  <read_first>
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Module Extraction Inputs (preliminary classifications, evidence checklist per module)
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Data-Carrying Modules → Phase 2 Schema Linkage table
    - .planning/phases/01-hermes-foundation-audit/CONTEXT.md § Success Criteria item 4
    - .planning/phases/01-hermes-foundation-audit/01-VALIDATION.md (task 01-04-01 and 01-04-02 acceptance criteria)

    Then read all 8 donor module files (confirmed accessible in Task 1):
    - C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/mission_control/parser.py
    - C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/mission_control/task_model.py
    - C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/execution/policy.py
    - C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/execution/powershell.py
    - C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/logging/jsonl_logger.py
    - C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/runtime/orchestrator.py
    - C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/runtime/models.py
    - C:/Users/Davi/Desktop/Projects/L2-Atlas/src/atlas_core/skills/registry.py

    Also check Hermes equivalents for overlap:
    - _EXTERNAL_REPOS/hermes-agent/agent/skill_utils.py (vs skills/registry.py)
    - _EXTERNAL_REPOS/hermes-agent/hermes_logging.py (vs jsonl_logger.py — if file exists)
    - grep for "tool_guardrails" in _EXTERNAL_REPOS/hermes-agent/ (vs execution/policy.py)

    DO NOT modify any of these files. Read-only audit.
  </read_first>

  <action>
    Step 1 — For each of the 6 primary donor modules (parser.py, policy.py, powershell.py, jsonl_logger.py, orchestrator.py, skills/registry.py), collect the following evidence per the RESEARCH.md checklist:
      E1: External imports beyond stdlib + atlas_core (list them — signals porting cost)
      E2: Does Hermes already have this functionality? (check against Hermes overlap files listed above)
      E3: Are there Pydantic v1 models or v1-style validators? (D-012 mandates Pydantic v2)
      E4: Any Windows/PowerShell-only APIs (subprocess with powershell.exe, winreg, etc.)?
      E5: Does it carry data contracts (dataclasses, TypedDicts, schemas)? If yes, what fields?

    Step 2 — Finalize the classification for each module using:
      - port: logic is reusable as-is (minor Pydantic v2 adaptation)
      - rewrite: concept valid but implementation must be replaced (platform-specific, v1 models, wrong abstraction)
      - reference: read for patterns/concepts only; Hermes supersedes or Phase 2 supersedes with a cleaner model
      - discard: no useful content for ATLAS; Hermes equivalent is complete

    Step 3 — Write docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md with the following required sections:

    Section 1 — Header: date, purpose (feed Phase 2 SCHEMA-01 implementation), source path.

    Section 2 — Classification Table: one row per module with columns:
      Module path (relative to atlas_core/), Size, Classification, Evidence summary (E1–E5), Hermes overlap, Phase 2 action.

    The table MUST include all 6 modules:
      mission_control/parser.py
      execution/policy.py
      execution/powershell.py
      logging/jsonl_logger.py
      runtime/orchestrator.py
      skills/registry.py

    Section 3 — Data-Carrying Modules → Phase 2 Schema Links:
    For each module classified port or reference with data contracts, map its data structures to the Phase 2 SCHEMA-01 Pydantic v2 target:
      - mission_control/parser.py → Mission model fields (status enum, project, description, tags, steps)
      - mission_control/task_model.py → Run status enum
      - runtime/orchestrator.py → Run, ToolCall, AuditEvent outcome/status fields
      - execution/policy.py → AuditEvent.policy_decision field
      - logging/jsonl_logger.py → AuditEvent base shape + redaction patterns

    Section 4 — Redaction Patterns (jsonl_logger.py):
    Extract the secret-redaction regex patterns from jsonl_logger.py verbatim. These carry forward to Phase 2's AuditEvent sanitizer regardless of the module's classification.

    Section 5 — Post-Audit L2-Atlas Repo State Verification:
    Record the output of:
      git -C "C:/Users/Davi/Desktop/Projects/L2-Atlas" status --short
    Must be empty (clean). This confirms the read-only audit constraint was honored.

    Do NOT copy or paste actual source code files. Extract only field names, type annotations, import statements, and method signatures as evidence. The document is an audit report, not a code dump.
  </action>

  <verify>
    <automated>
      Test-Path docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md
    </automated>
    Also verify content markers:
    - Select-String "parser.py" docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md — must match
    - Select-String "orchestrator.py" docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md — must match
    - Select-String "SCHEMA-01" docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md — must match
    - git -C "C:/Users/Davi/Desktop/Projects/L2-Atlas" status --short — must return empty output
  </verify>

  <acceptance_criteria>
    - docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md exists and is non-empty
    - File classifies all 6 modules: mission_control/parser.py, execution/policy.py, execution/powershell.py, logging/jsonl_logger.py, runtime/orchestrator.py, skills/registry.py
    - Each module row uses exactly one of: port | rewrite | reference | discard
    - Each row cites concrete evidence (imports listed, Hermes overlap stated, Windows-coupling noted if applicable)
    - Data-carrying modules are linked to Phase 2 SCHEMA-01 target fields
    - jsonl_logger.py redaction patterns are extracted and recorded
    - git -C "C:/Users/Davi/Desktop/Projects/L2-Atlas" status --short returns empty (L2-Atlas repo unmodified)
    - git status --short from L2-ATLAS-PROJECT root shows only L2_ATLAS_MODULE_EXTRACTION_PLAN.md as new tracked file after commit
    - No full source-code file contents copied into the document (evidence is field names, imports, signatures only)
  </acceptance_criteria>

  <done>docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md committed. All 6 donor modules classified with evidence. Data-carrying modules linked to Phase 2 SCHEMA-01. L2-Atlas repo unmodified. Phase 2 executor has a clear classification table and schema mapping to work from.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| L2-Atlas local repo (read-only) → audit output | Donor modules are read but never executed or modified |
| docs/imports/ (audit output) → git commit | Only the extraction plan markdown is committed; no source code copied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Clone files or L2-Atlas source entering ATLAS git history | mitigate | Only docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md committed; acceptance criterion asserts git status clean; no source code copied verbatim |
| T-1-09 | Tampering | L2-Atlas source files modified during the read audit | mitigate | Executor uses Read tool only on donor modules; Task 2 acceptance criterion requires git -C L2-Atlas status --short to be empty after audit |
| T-1-10 | Information Disclosure | L2-Atlas secrets (if any in donor modules) copied into extraction plan | accept | Donor modules are stdlib-only Python files (confirmed in RESEARCH.md); no .env or credentials; redaction patterns are regex strings, not actual secrets |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After Wave 4 completes (phase complete):
1. Test-Path docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md returns True
2. File classifies all 6 donor modules with port/rewrite/reference/discard labels
3. git -C "C:/Users/Davi/Desktop/Projects/L2-Atlas" status --short returns empty
4. git status --short from L2-ATLAS-PROJECT root is clean after commit

Full phase-gate verification:
1. git -C _EXTERNAL_REPOS/hermes-agent rev-parse HEAD == e8b9369a9d2df36139a5055cae3ed3c15691e03e
2. Test-Path docs/research/HERMES_FOUNDATION_AUDIT.md and contains "VERDICT"
3. Get-ChildItem docs/decisions/DIV-*.md | Measure-Object returns 4 or more
4. Test-Path docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md and contains "SCHEMA-01"
5. git status --short (ATLAS project) is clean
</verification>

<success_criteria>
- docs/imports/L2_ATLAS_MODULE_EXTRACTION_PLAN.md exists with all 6 modules classified
- Data-carrying modules are explicitly linked to Phase 2 SCHEMA-01 Pydantic targets
- L2-Atlas source repo working tree is unmodified (git status clean)
- Phase 2 executor has a clear, evidence-backed classification for each module
</success_criteria>

<output>
Create .planning/phases/01-hermes-foundation-audit/01-04-SUMMARY.md when done
</output>
