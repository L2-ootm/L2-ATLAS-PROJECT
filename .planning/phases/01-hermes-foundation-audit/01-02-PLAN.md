---
phase: 01-hermes-foundation-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docs/research/HERMES_FOUNDATION_AUDIT.md
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "Extension-surface audit document exists with one row per surface, all sourced from cloned files"
    - "All 10 required surfaces are covered: hook, tool registry, session store, delegation, cron, profiles, gateway, MCP, plugin surface, CLI/TUI boundary"
    - "Audit states explicit YES/NO verdict on whether audit-event bus can attach via plugin/hook without editing cli.py or run_agent.py"
    - "Verdict is confirmed against cloned source files (not only the prior research notes)"
    - "Four VERIFY-AT-EXECUTION open questions from RESEARCH.md are resolved with concrete cloned-source evidence"
  artifacts:
    - path: "docs/research/HERMES_FOUNDATION_AUDIT.md"
      provides: "Authoritative extension-surface table + event-bus attach verdict confirmed from cloned source"
      contains: "YES — the ATLAS audit-event bus can attach"
  key_links:
    - from: "docs/research/HERMES_FOUNDATION_AUDIT.md"
      to: "_EXTERNAL_REPOS/hermes-agent/hermes_cli/plugins.py"
      via: "VALID_HOOKS list cited with file reference"
      pattern: "hermes_cli/plugins.py"
    - from: "docs/research/HERMES_FOUNDATION_AUDIT.md"
      to: "_EXTERNAL_REPOS/hermes-agent/plugins/observability/langfuse/"
      via: "Langfuse plugin cited as event-bus template"
      pattern: "langfuse"
---

<objective>
Read the cloned Hermes source and produce the authoritative extension-surface audit document.

Purpose: FOUND-02 — developers and Phase 4 planners need an accurate, source-confirmed table of every Hermes extension surface and a definitive answer on whether the ATLAS audit-event bus requires in-core edits.

Output:
- `docs/research/HERMES_FOUNDATION_AUDIT.md` — extension-surface table (10+ rows), full VALID_HOOKS list confirmed, event-bus YES/NO verdict with cited evidence, four open-question resolutions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hermes-foundation-audit/CONTEXT.md
@.planning/phases/01-hermes-foundation-audit/01-RESEARCH.md
@.planning/phases/01-hermes-foundation-audit/01-01-SUMMARY.md

<interfaces>
<!-- Key Hermes source files the executor must read from the clone. -->
<!-- These are confirmed present in the upstream repo at the pinned SHA. -->

_EXTERNAL_REPOS/hermes-agent/hermes_cli/plugins.py
  - VALID_HOOKS set (22 named hooks) — confirm exact list
  - PluginContext class — confirm register_hook(), register_tool(), inject_message(), register_auxiliary_task(), register_cli_command() signatures
  - invoke_hook() implementation — confirm fire order and return-value semantics
  - Plugin discovery sources (4): bundled plugins/, user ~/.hermes/plugins/, project ./.hermes/plugins/, pip entry-point group hermes_agent.plugins

_EXTERNAL_REPOS/hermes-agent/plugins/observability/langfuse/plugin.yaml
  - hooks: declaration listing pre/post_api_request, pre/post_llm_call, pre/post_tool_call

_EXTERNAL_REPOS/hermes-agent/plugins/observability/langfuse/__init__.py
  - register(ctx) pattern — confirm it calls ctx.register_hook() for each hook with no core edits

_EXTERNAL_REPOS/hermes-agent/tools/registry.py
  - ToolRegistry singleton — confirm register() signature: (name, toolset, schema, handler, override=False)
  - dispatch() behavior

_EXTERNAL_REPOS/hermes-agent/agent/shell_hooks.py
  - JSON stdin/stdout wire protocol
  - Event names and block/allow response shapes

_EXTERNAL_REPOS/hermes-agent/model_tools.py
  - _emit_post_tool_call_hook() — confirm fires for all code paths
  - Confirm turn_id and api_request_id are populated non-empty in production paths (A1 assumption)

_EXTERNAL_REPOS/hermes-agent/agent/tool_executor.py
  - Confirm both sequential and concurrent dispatch call _emit_terminal_post_tool_call

_EXTERNAL_REPOS/hermes-agent/agent/system_prompt.py
  - Determine whether ATLAS can augment system prompt via config/plugin (A2 assumption)
  - If in-core edit required → classify friction point HIGH for divergence stub

_EXTERNAL_REPOS/hermes-agent/hermes_state.py
  - Session ID shape, whether session_id is stable across a run (A4 assumption)
  - Whether a plugin can read session_id from hook context without write API

_EXTERNAL_REPOS/hermes-agent/cron/scheduler.py
_EXTERNAL_REPOS/hermes-agent/cron/jobs.py
  - Job schema, how jobs are added, toolset restrictions

_EXTERNAL_REPOS/hermes-agent/hermes_cli/profiles.py
  - ATLAS-named profile as isolation strategy

_EXTERNAL_REPOS/hermes-agent/hermes_cli/mcp_config.py
  - mcp_servers config key structure for adding ATLAS MCP servers without code edit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Cloned Source — Confirm Extension Surfaces</name>
  <files>(read-only, no files modified in this task)</files>

  <read_first>
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Extension Surface Table (preliminary table to confirm/correct)
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Audit-Event Bus Verdict (preliminary YES to confirm)
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Open Questions (A1–A4 to resolve)
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Wave 2 Audit Checklist (items 2.1–2.12)
    - _EXTERNAL_REPOS/hermes-agent/hermes_cli/plugins.py (VALID_HOOKS, PluginContext, invoke_hook)
    - _EXTERNAL_REPOS/hermes-agent/plugins/observability/langfuse/plugin.yaml
    - _EXTERNAL_REPOS/hermes-agent/plugins/observability/langfuse/__init__.py
    - _EXTERNAL_REPOS/hermes-agent/tools/registry.py
    - _EXTERNAL_REPOS/hermes-agent/agent/shell_hooks.py
    - _EXTERNAL_REPOS/hermes-agent/model_tools.py (targeted read: _emit_post_tool_call_hook and its call sites)
    - _EXTERNAL_REPOS/hermes-agent/agent/tool_executor.py (targeted: dispatch paths, _emit_terminal_post_tool_call)
    - _EXTERNAL_REPOS/hermes-agent/agent/system_prompt.py (targeted: is there a plugin hook or config for system prompt augmentation?)
    - _EXTERNAL_REPOS/hermes-agent/hermes_state.py (targeted: session_id shape, read API from plugin context)
    - _EXTERNAL_REPOS/hermes-agent/cron/scheduler.py
    - _EXTERNAL_REPOS/hermes-agent/hermes_cli/profiles.py
    - _EXTERNAL_REPOS/hermes-agent/hermes_cli/mcp_config.py

    IMPORTANT: These are large files. Use targeted Grep + Read with offset/limit. Never read cli.py (~685KB) or run_agent.py (~202KB) wholesale — use grep to find specific call sites.

    For artifact coverage (A3 assumption): run
      grep -r "artifact" _EXTERNAL_REPOS/hermes-agent --include="*.py" -l
    to find artifact creation sites; determine if post_tool_call is sufficient or a separate hook exists.
  </read_first>

  <action>
    Perform targeted reads of the cloned source files listed above. For each of the four open questions (A1–A4 from RESEARCH.md), collect a concrete finding with file reference and line numbers:

    A1: turn_id + api_request_id populated in all paths — check model_tools.py _emit_post_tool_call_hook; note if any code path passes empty values.

    A2: System prompt augmentation — read agent/system_prompt.py; check whether any plugin hook or config key can append to the system prompt without editing that file directly.

    A3: Artifact coverage — grep for artifact creation; determine if post_tool_call fires for artifact tools or if a separate hook/override is needed.

    A4: hermes_state.py plugin read access — check whether session_id is available in hook context (e.g., via ctx or hook kwargs) without requiring direct hermes_state imports.

    Do not write any files in this task. This is a read-only information-gathering task. The findings feed directly into Task 2.
  </action>

  <verify>
    <automated>
      Test-Path _EXTERNAL_REPOS/hermes-agent/hermes_cli/plugins.py
    </automated>
    Must return True. This task is read-only; verification is that the clone is accessible.
  </verify>

  <acceptance_criteria>
    - All 12 target files from the read_first list were accessed in the clone (not from the AppData runtime install)
    - A1 through A4 open questions each have a concrete finding with a file reference
    - The VALID_HOOKS set is confirmed (count and names match or corrections noted)
    - The Langfuse plugin register(ctx) pattern is confirmed as a viable ATLAS audit plugin template
    - No files in _EXTERNAL_REPOS/hermes-agent were modified (read-only audit)
  </acceptance_criteria>

  <done>All extension-surface source reads complete. Four open questions have concrete findings with file/line citations. Ready to write the audit document.</done>
</task>

<task type="auto">
  <name>Task 2: Write HERMES_FOUNDATION_AUDIT.md</name>
  <files>docs/research/HERMES_FOUNDATION_AUDIT.md</files>

  <read_first>
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Extension Surface Table (use as structure template)
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Audit-Event Bus Verdict (incorporate and confirm)
    - .planning/phases/01-hermes-foundation-audit/CONTEXT.md § Success Criteria (items 2 and 3)
    - .planning/phases/01-hermes-foundation-audit/01-VALIDATION.md (task 01-02-01 and 01-02-02 acceptance criteria)
    - findings collected in Task 1
  </read_first>

  <action>
    Write docs/research/HERMES_FOUNDATION_AUDIT.md with the following required sections:

    Section 1 — Header: phase reference, audit date, Hermes version + SHA, auditor note ("source confirmed from _EXTERNAL_REPOS/hermes-agent at pinned SHA").

    Section 2 — Extension Surface Table: one row per surface. The table MUST include ALL of the following rows (in any order):
      - Hook system (Python lifecycle hooks)
      - Tool registry
      - Session / state store
      - Delegation / subagents
      - Cron / scheduling
      - Profiles
      - Gateway / server interface
      - MCP integration
      - Plugin surface (directory plugins)
      - CLI / TUI boundary

    Each row must contain: Surface name, Exists (YES/NO), Registration mechanism (brief), Key file(s) in clone (relative path), ATLAS relevance note.

    Section 3 — VALID_HOOKS confirmed list: the complete set of hook names confirmed from hermes_cli/plugins.py in the clone, with return-value semantics for each hook that can veto or modify behavior (pre_tool_call, pre_llm_call, transform_* hooks).

    Section 4 — Audit-Event Bus Verdict:
    MUST start with one of: "VERDICT: YES" or "VERDICT: NO" or "VERDICT: PARTIAL"
    Then: evidence (cite specific cloned files and function names), and any caveats.
    The preliminary research verdict is YES — confirm or correct it based on Task 1 findings.
    Explicitly answer: "Can the ATLAS audit-event bus attach via plugin/hook without editing cli.py or run_agent.py?"

    Section 5 — Open Question Resolutions (A1–A4):
    For each assumption from RESEARCH.md, state: [CONFIRMED] / [REFUTED] / [PARTIAL] with evidence and file reference.

    Section 6 — Friction Points Summary:
    List the identified friction points (system prompt augmentation, artifact capture, hermes_state write path, turn_id gaps) with their current status based on the cloned source read. Flag any that require an in-core edit — these feed directly into Plan 03 divergence stubs.

    Do NOT implement anything. Do NOT write ATLAS code stubs. This is an audit document only.
  </action>

  <verify>
    <automated>
      Test-Path docs/research/HERMES_FOUNDATION_AUDIT.md
    </automated>
    Also verify content markers:
    - Select-String "VERDICT" docs/research/HERMES_FOUNDATION_AUDIT.md — must return a match
    - Select-String "tool registry" docs/research/HERMES_FOUNDATION_AUDIT.md -CaseSensitive:$false — must return a match
    - Select-String "CLI" docs/research/HERMES_FOUNDATION_AUDIT.md — must return a match
    - Select-String "MCP" docs/research/HERMES_FOUNDATION_AUDIT.md — must return a match
  </verify>

  <acceptance_criteria>
    - docs/research/HERMES_FOUNDATION_AUDIT.md exists and is non-empty
    - File contains a row or section for each of the 10 required surfaces: hook, tool registry, session store, delegation, cron, profiles, gateway, MCP, plugin surface, CLI/TUI boundary
    - File contains a VERDICT line explicitly stating YES, NO, or PARTIAL for the audit-event bus question
    - File cites specific files from _EXTERNAL_REPOS/hermes-agent/ (e.g., hermes_cli/plugins.py, plugins/observability/langfuse/)
    - File contains resolutions for A1–A4 open questions from RESEARCH.md
    - File contains a Friction Points section listing system prompt augmentation and artifact capture status
    - No ATLAS product code or implementation stubs appear in the document
    - git status --short shows HERMES_FOUNDATION_AUDIT.md as the only new/modified tracked file after commit
  </acceptance_criteria>

  <done>docs/research/HERMES_FOUNDATION_AUDIT.md committed. Contains all 10 extension-surface rows, confirmed VALID_HOOKS list, explicit YES/NO/PARTIAL verdict, and A1-A4 resolutions sourced from cloned files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _EXTERNAL_REPOS/hermes-agent (untrusted code) → executor reads | Code is read but never executed; read-only audit |
| docs/research/ (audit output) → git commit | Only the audit document is committed; no clone files enter the index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Secret-bearing clone file entering git history | mitigate | Wave 1 already gitignored _EXTERNAL_REPOS/; this plan commits only docs/research/HERMES_FOUNDATION_AUDIT.md; acceptance criterion asserts git status is clean except that file |
| T-1-06 | Tampering | Hermes code executed during audit (e.g., accidental import) | accept | Executor reads source files with Read tool only; does not run pip install or python inside the clone; risk is negligible |
| T-1-07 | Information Disclosure | Audit document inadvertently records actual secret values found in clone | accept | Fresh upstream clone is known-clean (verified in Plan 01); audit document records only structural observations, not file contents; low risk |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; read-only tasks only |
</threat_model>

<verification>
After Wave 2 completes:
1. Test-Path docs/research/HERMES_FOUNDATION_AUDIT.md returns True
2. Select-String "VERDICT" docs/research/HERMES_FOUNDATION_AUDIT.md returns a match
3. File contains rows for all 10 required extension surfaces
4. File cites hermes_cli/plugins.py and langfuse plugin as source evidence
5. git status --short is clean after commit
</verification>

<success_criteria>
- docs/research/HERMES_FOUNDATION_AUDIT.md exists with all 10 surface rows filled
- Audit-event bus verdict stated explicitly (YES/NO/PARTIAL) with source-cited evidence
- Four RESEARCH.md open questions (A1–A4) resolved with cloned-source findings
- No clone files committed to ATLAS repo
</success_criteria>

<output>
Create .planning/phases/01-hermes-foundation-audit/01-02-SUMMARY.md when done
</output>
