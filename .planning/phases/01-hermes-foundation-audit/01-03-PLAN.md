---
phase: 01-hermes-foundation-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - docs/decisions/DIV-001-system-prompt-augmentation.md
  - docs/decisions/DIV-002-artifact-capture.md
  - docs/decisions/DIV-003-hermes-state-write-path.md
  - docs/decisions/DIV-004-turn-id-propagation.md
autonomous: true
requirements:
  - FOUND-03

must_haves:
  truths:
    - "A divergence decision stub exists for every friction point identified in HERMES_FOUNDATION_AUDIT.md"
    - "Each stub is classified per the divergence policy order: plugin > tool > hook > skill > ATLAS-only > in-core edit"
    - "Stubs that resolve to in-core edit are classified upstreamable | plugin-tool | ATLAS-only | experimental"
    - "Stubs that resolve to plugin/hook remain PENDING until Phase 4 implementation confirms the attachment"
    - "The Hermes clone working tree is unmodified (no ATLAS files added inside _EXTERNAL_REPOS/)"
  artifacts:
    - path: "docs/decisions/DIV-001-system-prompt-augmentation.md"
      provides: "Divergence stub for system prompt augmentation friction point"
      contains: "system prompt"
    - path: "docs/decisions/DIV-002-artifact-capture.md"
      provides: "Divergence stub for artifact capture friction point"
      contains: "artifact"
    - path: "docs/decisions/DIV-003-hermes-state-write-path.md"
      provides: "Divergence stub for hermes_state session-linking friction point"
      contains: "hermes_state"
    - path: "docs/decisions/DIV-004-turn-id-propagation.md"
      provides: "Divergence stub for turn_id / api_request_id propagation gap"
      contains: "turn_id"
  key_links:
    - from: "docs/decisions/DIV-001-system-prompt-augmentation.md"
      to: "docs/research/HERMES_FOUNDATION_AUDIT.md"
      via: "Friction Point section reference"
      pattern: "HERMES_FOUNDATION_AUDIT"
    - from: "docs/decisions/DIV-002-artifact-capture.md"
      to: "docs/research/HERMES_FOUNDATION_AUDIT.md"
      via: "A3 resolution reference"
      pattern: "HERMES_FOUNDATION_AUDIT"
---

<objective>
Produce divergence decision stubs for each friction point identified in HERMES_FOUNDATION_AUDIT.md.

Purpose: FOUND-03 — every identified Hermes divergence must be classified before Phase 4 begins wiring the event bus. Stubs record the classification (upstreamable / plugin-tool / ATLAS-only / experimental) and disposition (mitigate via plugin OR requires in-core edit) so Phase 4 has an authoritative divergence register to work from.

Output:
- `docs/decisions/DIV-001-system-prompt-augmentation.md`
- `docs/decisions/DIV-002-artifact-capture.md`
- `docs/decisions/DIV-003-hermes-state-write-path.md`
- `docs/decisions/DIV-004-turn-id-propagation.md`

Note: The four stubs above cover the friction points pre-identified in RESEARCH.md. If HERMES_FOUNDATION_AUDIT.md (produced in Plan 02) reveals additional friction points not in this list, create additional DIV-NNN stubs with the next available numbers. Do not omit any friction point found during the audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hermes-foundation-audit/CONTEXT.md
@.planning/phases/01-hermes-foundation-audit/01-RESEARCH.md
@.planning/phases/01-hermes-foundation-audit/01-02-SUMMARY.md
@docs/research/HERMES_FOUNDATION_AUDIT.md
@docs/decisions/2026-06-04_DECISION_REGISTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Divergence Decision Stubs</name>
  <files>
    docs/decisions/DIV-001-system-prompt-augmentation.md,
    docs/decisions/DIV-002-artifact-capture.md,
    docs/decisions/DIV-003-hermes-state-write-path.md,
    docs/decisions/DIV-004-turn-id-propagation.md
  </files>

  <read_first>
    - docs/research/HERMES_FOUNDATION_AUDIT.md (Friction Points section — use actual A1–A4 resolutions from Task 2 of Plan 02)
    - .planning/phases/01-hermes-foundation-audit/01-RESEARCH.md § Divergence Policy Inputs (friction point details, preference-order table)
    - .planning/phases/01-hermes-foundation-audit/CONTEXT.md § Key Decisions (divergence policy order + classification taxonomy)
    - docs/decisions/2026-06-04_DECISION_REGISTER.md (existing decision format for consistency)
    - docs/foundation/HERMES_FOUNDATION_PIN.md (divergence policy definition)
  </read_first>

  <action>
    For each of the four friction points, create a decision stub using the following structure.
    Use the actual findings from HERMES_FOUNDATION_AUDIT.md (Plan 02 output) — do not use the preliminary RESEARCH.md findings if they differ from confirmed cloned-source readings.

    REQUIRED STUB STRUCTURE for each file:
      # DIV-NNN — [Friction Point Name]
      **Date:** [today]
      **Phase:** 1 — Hermes Foundation Clone & Extension Audit
      **Status:** STUB (pending Phase 4 implementation confirmation)
      **Classification:** [upstreamable | plugin-tool | ATLAS-only | experimental]
      **Disposition:** [mitigate via plugin/hook — no in-core edit required | requires in-core edit]

      ## Problem Statement
      [What ATLAS needs that Hermes does not expose via its plugin/hook surface]

      ## Evidence from Audit
      [Cite specific file(s) from _EXTERNAL_REPOS/hermes-agent/ and the A1/A2/A3/A4 finding]

      ## Preferred Resolution (Divergence Policy Order)
      [Plugin | Tool | Hook | Skill | ATLAS-only override | In-core edit]
      [Why this tier — one sentence]

      ## Phase 4 Action Required
      [What the Phase 4 executor must do to confirm or change this stub]

      ## ATLAS Impact
      [Which REQUIREMENTS.md REQ-IDs are affected if the resolution is in-core]

    CONTENT GUIDANCE per stub:

    DIV-001 (system-prompt-augmentation.md):
      Source: HERMES_FOUNDATION_AUDIT.md A2 resolution re agent/system_prompt.py.
      If A2 confirmed in-core edit required → Classification: ATLAS-only; Disposition: in-core edit required; Phase 4 action: evaluate whether system-prompt injection can be achieved via a forked system_prompt.py (ATLAS-only override) or must upstream a plugin hook.
      If A2 confirmed plugin hook exists → Classification: plugin-tool; Disposition: plugin hook; Phase 4 action: wire ctx.register_hook or config key found in audit.

    DIV-002 (artifact-capture.md):
      Source: HERMES_FOUNDATION_AUDIT.md A3 resolution re artifact tools and post_tool_call coverage.
      If A3 confirmed post_tool_call is sufficient → Classification: plugin-tool; Disposition: no in-core edit; Phase 4 action: register post_tool_call callback filtering for artifact tool names.
      If A3 found a gap → Classification: ATLAS-only or in-core; document the gap.

    DIV-003 (hermes-state-write-path.md):
      Source: HERMES_FOUNDATION_AUDIT.md A4 resolution re hermes_state.py session linking.
      ATLAS needs to link a Hermes session_id to an ATLAS Run record. If session_id is available in hook context → plugin-tool; no in-core edit. If not → ATLAS-only parallel DB with join keys.

    DIV-004 (turn-id-propagation.md):
      Source: HERMES_FOUNDATION_AUDIT.md A1 resolution re turn_id / api_request_id in _emit_post_tool_call_hook.
      If confirmed populated in all paths → no friction, but document as VERIFIED; Classification: plugin-tool.
      If empty in any path → ATLAS-only mitigation needed; document which code path gaps exist.

    Additional stubs: If HERMES_FOUNDATION_AUDIT.md reveals friction points beyond these four, create DIV-005, DIV-006, etc. using the same structure. Do not omit any friction point.

    Per CONTEXT.md § What NOT to Build: Do NOT implement the event bus or write any ATLAS service code. These are classification stubs only. The action item for each stub is "Phase 4 executor will confirm and implement."
  </action>

  <verify>
    <automated>
      Get-ChildItem docs/decisions/DIV-*.md | Measure-Object | Select-Object -ExpandProperty Count
    </automated>
    Must return 4 or greater (4 pre-identified stubs + any additional found in audit).
    Also: each file must contain "Classification:" and "Disposition:" headers.
  </verify>

  <acceptance_criteria>
    - docs/decisions/DIV-001-system-prompt-augmentation.md exists and contains "system prompt" and "Classification:"
    - docs/decisions/DIV-002-artifact-capture.md exists and contains "artifact" and "Classification:"
    - docs/decisions/DIV-003-hermes-state-write-path.md exists and contains "hermes_state" and "Classification:"
    - docs/decisions/DIV-004-turn-id-propagation.md exists and contains "turn_id" and "Classification:"
    - Each stub's Classification field uses exactly one of: upstreamable | plugin-tool | ATLAS-only | experimental
    - Each stub references a specific file from _EXTERNAL_REPOS/hermes-agent/ as evidence
    - Each stub has a "Phase 4 Action Required" section
    - git -C _EXTERNAL_REPOS/hermes-agent status --short is clean (no files added to the clone by this task)
    - No ATLAS service code appears in any stub (stubs are decision records, not implementations)
    - git status --short from ATLAS project root shows only the new DIV-*.md files as new tracked additions
  </acceptance_criteria>

  <done>4 or more divergence decision stubs committed to docs/decisions/. Each classified per divergence policy. Hermes clone tree unmodified. Phase 4 executor has a clear action item per stub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/research/HERMES_FOUNDATION_AUDIT.md (Plan 02 output) → stub content | Stubs reference audit findings; no new clone reads in this plan |
| docs/decisions/ (stub output) → git commit | Only stub markdown files committed; no code or clone content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Clone content entering git history | mitigate | This plan commits only docs/decisions/DIV-*.md; acceptance criterion asserts git -C _EXTERNAL_REPOS/hermes-agent status is clean; no clone files staged |
| T-1-08 | Tampering | Stubs misclassify a divergence causing Phase 4 to make wrong in-core edit decision | accept | Stubs are marked STUB status pending Phase 4 confirmation; Phase 4 executor is required to verify each stub before acting; low risk |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After Wave 3 completes:
1. Get-ChildItem docs/decisions/DIV-*.md | Measure-Object returns 4 or more
2. Each DIV-*.md contains "Classification:" field with a valid taxonomy value
3. git -C _EXTERNAL_REPOS/hermes-agent status --short is clean
4. git status --short from project root is clean after commit
</verification>

<success_criteria>
- 4+ divergence decision stubs exist in docs/decisions/
- Every friction point from HERMES_FOUNDATION_AUDIT.md has a stub
- Each stub is classified per divergence policy (plugin > tool > hook > skill > ATLAS-only > in-core)
- Hermes clone tree is unmodified
- Phase 4 has a clear action item per stub
</success_criteria>

<output>
Create .planning/phases/01-hermes-foundation-audit/01-03-SUMMARY.md when done
</output>
