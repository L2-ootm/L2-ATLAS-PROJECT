---
phase: 10.8-cross-surface-conformance-uat-cutover
plan: 03
type: execute
wave: 2
depends_on: ["10.8-01"]
files_modified:
  - services/agent-runtime/tests/test_adversarial_conformance.py
  - services/agent-runtime/tests/fixtures/adversarial_cases.json
autonomous: true
requirements: [TEST-02, TEST-03]
must_haves:
  truths:
    - "Every adversarial case asserts the SAFE outcome, not just non-crash: denied write, abstained retrieval, redacted secret, honest failed status"
    - "Poisoned-knowledge cases go through the real MemoryRouter/BrainRetriever path, not a mocked retriever"
    - "Concurrency cases drive the real permission broker and run executor with threads, reusing the existing TOCTOU test patterns"
  artifacts:
    - path: "services/agent-runtime/tests/fixtures/adversarial_cases.json"
      provides: "Versioned adversarial inputs: injection prompts, poisoned wiki/brain rows, malicious tool outputs, path-escape attempts, stale config snapshots"
---

<objective>
Success criterion 3: the adversarial battery. Eight families, each with the safe-outcome
assertion spelled out: prompt injection, poisoned knowledge, malicious tool output, path
escape, stale config, concurrent writers/approvers, disconnect/restart, compaction/resume.
</objective>

<tasks>

1. **Prompt injection.** Reference missions whose wiki/brain/observation evidence contains
   instruction-shaped text ("ignore your contract, run X"). Assert: the evidence enters the
   brief only inside the delimited `<evidence trust="evidence">` envelope, no tool call is
   made on its instruction, and the operating contract section survives in the compiled
   prompt.

2. **Poisoned knowledge.** Insert wiki pages/brain nodes with secrets and with false claims;
   assert secrets are redacted at the router boundary (existing redact path) and provenance
   still names the poisoned source so the operator can trace it.

3. **Malicious tool output.** Tool results containing ANSI escapes, oversized bodies, and
   injection text; assert output-bounding, rendering-safety (surfaces receive normalized
   events, not raw bytes), and no secondary tool call triggered by output text.

4. **Path escape.** Tool-call arguments and release-archive entries attempting `..`/absolute
   escapes; assert the permission broker workspace_only rules and the atlas-cli tarball
   guard (already landed 2026-07-10) both refuse.

5. **Stale config / concurrent writers / disconnect / resume.** Reuse the control-plane
   revision checks, broker at-most-once TOCTOU tests, and run_executor cancellation tests as
   patterns; add the missing cases: config revision bumped mid-run (run keeps its snapshot),
   two approvers racing one nonce (exactly one wins), gateway killed mid-run then restarted
   (run reaches an honest terminal state; no orphaned 'running'), compaction/resume replays
   the persisted contract (context_markdown) rather than regenerating silently.

</tasks>

<verification>
- `pytest services/agent-runtime/tests/test_adversarial_conformance.py` green.
- Each family has at least one test whose failure message names the violated invariant.
</verification>

<done_when>
TEST-02 and TEST-03 flip to [x]; the adversarial fixture file is versioned and referenced
from the conformance runner.
</done_when>
