---
phase: 10.8-cross-surface-conformance-uat-cutover
plan: 02
type: execute
wave: 2
depends_on: ["10.8-01"]
files_modified:
  - scripts/conformance/run_representative_battery.py
  - .planning/phases/10.8-cross-surface-conformance-uat-cutover/10.8-RUN-EVIDENCE.md
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "At least 20 end-to-end runs execute against a live provider route (oauth_import or freellmapi), not mock"
    - "Every budget check is measured, not asserted: reliability, no-secret, no-unapproved-write, exactly-once approval, startup, memory, latency, recovery"
    - "A failed budget blocks the phase; there is no waiver path in the report"
  artifacts:
    - path: ".planning/phases/10.8-cross-surface-conformance-uat-cutover/10.8-RUN-EVIDENCE.md"
      provides: "Dated table of the 20+ runs with per-budget measurements and pass/fail"
---

<objective>
Success criterion 2: at least 20 representative end-to-end runs meeting the TEST-05 budgets,
with measurements recorded as evidence, executed across the three surfaces (minimum 6 per
surface, mixed mission types from the 10.8-01 fixture set plus free-form operator-style
missions).
</objective>

<tasks>

1. **Budget definitions.** Fix numeric budgets before running: run success rate (≥ 90% over
   the battery, provider-fault retries allowed once), startup-to-composer time, gateway
   memory ceiling, first-token and terminal-status latency ceilings, zero secret leaks
   (scan run contracts + audit rows with SECRET_PATTERNS), zero unapproved writes (audit
   diff against approval nonces), exactly-once approval per ask (broker dedupe evidence),
   recovery (kill gateway mid-run → resume or honest-fail, never a stuck 'running' run).

2. **Battery runner.** `run_representative_battery.py` executes the mix, records wall times,
   memory (via psutil-free /proc-less approach: `GetProcessMemoryInfo` through `tasklist`
   parse or gateway self-report), and writes the evidence table. Runs use the live provider
   configured on this machine (openai-codex oauth_import verified live on 2026-07-04) with
   freellmapi as the fallback route.

3. **Evidence report.** `10.8-RUN-EVIDENCE.md` — one row per run (surface, mission type,
   status, duration, budgets touched), one summary table per budget, and the raw command
   used, so the battery is re-runnable at cutover time.

</tasks>

<verification>
- The report shows ≥ 20 runs, every budget measured, and every budget passing.
- Re-running the battery is one command and reproduces a fresh dated report.
</verification>

<done_when>
TEST-05 flips to [x] with the evidence file as the citation.
</done_when>
