---
phase: 10.0.2-one-command-install-path
plan: 03
subsystem: cli
tags: [typer, health-check, cli, doctor, install-troubleshooting]

# Dependency graph
requires:
  - phase: 10.0.2-01
    provides: cockpit_control.health_ok() (cockpit lifecycle primitive)
  - phase: 10.0.2-02
    provides: config_service.resolve_provider() empty-api_key mock-mode signal
provides:
  - "atlas doctor CLI command aggregating db/config/gateway/cockpit/provider health"
  - "Secret-safe health reporting pattern (configured/mock, never raw api_key)"
affects: [10.0.2-04 (install troubleshooting docs reference doctor output format)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Health aggregator wraps existing primitives (db.migration_status, gateway_control.health_ok, cockpit_control.health_ok, config_service.load_config/resolve_provider) rather than re-implementing checks"
    - "Independent try/except per check so one failing subsystem never hides the others' status"

key-files:
  created:
    - services/agent-runtime/atlas_runtime/cli/doctor.py
    - services/agent-runtime/tests/test_cli_doctor.py
  modified:
    - services/agent-runtime/atlas_runtime/cli/main.py

key-decisions:
  - "Provider/mock-mode status is informational only and never fails the overall doctor exit code — only db/config/gateway/cockpit failures set exit 1"
  - "Provider check is skipped (not failed) when config failed to load, since resolve_provider needs a valid cfg"

patterns-established:
  - "Secret-safe CLI output: only ever print configured/mock/missing tokens, never the dereferenced credential value, even when test doubles inject key-shaped strings into the resolved dict"

requirements-completed: [SC3]

# Metrics
duration: 12min
completed: 2026-06-22
---

# Phase 10.0.2 Plan 03: atlas doctor Health Aggregator Summary

**`atlas doctor` CLI command aggregating db migration status, config validity, gateway/cockpit health_ok(), and provider mock-mode detection into one pass/fail report with secret-safe output.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-06-22T01:01:00Z
- **Completed:** 2026-06-22T01:13:20Z
- **Tasks:** 1 completed (TDD: RED -> GREEN)
- **Files modified:** 3 (1 created service module, 1 created test file, 1 modified for registration)

## Accomplishments
- `atlas doctor` exists and is registered as a flat Typer command alongside `atlas up`/`atlas setup`
- Wraps `db.migration_status`, `config_service.load_config`/`resolve_provider`, `gateway_control.health_ok`, `cockpit_control.health_ok` — no check logic duplicated
- Exits 0 on a fully healthy install (mock mode counted as healthy); exits 1 identifying the specific failing check (gateway down, cockpit down, pending migration version, invalid config) otherwise
- Never echoes a resolved `api_key` value under any code path, verified by a test that injects a fake key-shaped string into the patched return dict and asserts it never appears in captured output

## Task Commits

TDD task, two commits (RED then GREEN):

1. **Task 1 RED: failing tests for atlas doctor** - `test(10.0.2-03): add failing tests for atlas doctor aggregator` (test)
2. **Task 1 GREEN: implement atlas doctor** - `feat(10.0.2-03): implement atlas doctor health aggregator` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `services/agent-runtime/atlas_runtime/cli/doctor.py` - `_doctor_cmd()` aggregator; 5 independently try/except-wrapped checks (db, config, gateway, cockpit, provider)
- `services/agent-runtime/tests/test_cli_doctor.py` - 5 behavior tests via `typer.testing.CliRunner`, mirroring `tests/test_cli_up.py`'s monkeypatch pattern
- `services/agent-runtime/atlas_runtime/cli/main.py` - registered `atlas doctor` as a flat command next to `atlas up`

## Decisions Made
- Provider/mock status never fails the overall `doctor` exit code — mock mode is a valid healthy state per the plan's must_haves truths.
- When `config_service.load_config()` raises, the provider check is skipped entirely (echoes `"provider: skipped (config invalid)"`) rather than attempting `resolve_provider(None)`, since the function signature expects a loaded `AtlasConfig`.

## Deviations from Plan

None - plan executed exactly as written. All 5 behavior tests specified in the plan's `<behavior>` block pass without modification to the planned check logic.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `atlas doctor`'s output format (`"db: ok"`, `"config: invalid - {msg}"`, `"gateway: down"`, `"provider: mock"`, etc.) is stable and ready for 10.0.2-04 to reference in install troubleshooting docs.
- Full agent-runtime suite (289 tests, up from 284) green, confirming no regression to `atlas up` (plan 01) or `atlas db init --demo` (plan 02).

---
*Phase: 10.0.2-one-command-install-path*
*Completed: 2026-06-22*
