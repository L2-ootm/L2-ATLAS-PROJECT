---
phase: 10.0.2-one-command-install-path
plan: 01
subsystem: infra
tags: [cli, typer, subprocess, lifecycle, windows, npm, vite]

# Dependency graph
requires:
  - phase: 10.0 (Harness Architecture & Threat-Model Design)
    provides: gateway_control.py lifecycle primitive (the exact analog mirrored here)
provides:
  - cockpit_control.py lifecycle primitive (health_ok/start/stop, mirrors gateway_control.py)
  - atlas up CLI command (boots gateway + cockpit together, idempotent)
  - Windows console-flash regression guard extended to npm-shim subprocess spawns
affects: [10.0.2-02, 10.0.2-03, 10.0.2-04 (install docs surface the env-var port-authority decision)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Process lifecycle primitive (spawn/health/stop) — gateway_control.py / cockpit_control.py shared shape"
    - "Windows DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP | CREATE_NO_WINDOW for .cmd shell-shim child processes (npm), vs two-flag set for native .exe spawns"
    - "CLI thin-wrapper + lazy import convention (no SQL/no emit() in cli/main.py handlers)"

key-files:
  created:
    - services/agent-runtime/atlas_runtime/cockpit_control.py
    - services/agent-runtime/tests/test_cockpit_control.py
    - services/agent-runtime/tests/test_cli_up.py
  modified:
    - services/agent-runtime/atlas_runtime/cli/main.py

key-decisions:
  - "Port authority: ATLAS_COCKPIT_URL env var is authoritative for cockpit spawn/health-check URL (mirrors gateway_control.py's ATLAS_GATEWAY_URL precedent); config.yaml's CockpitConfig.port stays informational-only until something wires it into a spawn command"
  - "atlas up registered as a flat command (app.command), not a sub-app — matches the existing atlas setup precedent since up takes no subcommands"
  - "atlas up runs gateway_control.start() then cockpit_control.start() sequentially (not parallel) for simpler failure semantics; exits 1 if either reports ok=False"

patterns-established:
  - "cockpit_control.py mirrors gateway_control.py's exact module shape (URL constant, PID_FILE, health_ok, start, stop) for any future per-process lifecycle primitive"

requirements-completed: [SC1]

# Metrics
duration: 18min
completed: 2026-06-22
---

# Phase 10.0.2 Plan 01: Cockpit Lifecycle Primitive + atlas up Summary

**Built cockpit_control.py (gateway_control.py's twin) and registered `atlas up` to boot gateway + cockpit together idempotently, with the npm-shim Windows console-flash flag (CREATE_NO_WINDOW) that the native gateway .exe spawn didn't need.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-06-22T00:53:00Z (approx, per STATE.md last_activity)
- **Completed:** 2026-06-22
- **Tasks:** 2
- **Files modified:** 4 (2 created, 1 created test, 1 modified)

## Accomplishments
- `cockpit_control.py`: health_ok/start/stop lifecycle primitive mirroring gateway_control.py exactly, with the npm `.cmd`-shim-specific `CREATE_NO_WINDOW` flag added on top of the existing `DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP` pair
- `atlas up`: flat Typer command that boots gateway then cockpit, echoes both lifecycle messages, exits 1 on partial failure (never reports false success)
- Port-authority decision (env-var authoritative) documented inline in cockpit_control.py's module docstring for the 10.0.2-04 docs plan to surface

## Task Commits

Each task was committed atomically:

1. **Task 1: Write cockpit_control.py (cockpit lifecycle primitive)** - `46ff072` (feat, tdd)
2. **Task 2: Wire `atlas up` into cli/main.py** - `e9a0775` (feat, tdd)

**Plan metadata:** (this commit, pending)

## Files Created/Modified
- `services/agent-runtime/atlas_runtime/cockpit_control.py` - cockpit lifecycle primitive (health_ok/start/stop/_parse_port), COCKPIT_URL + PID_FILE constants
- `services/agent-runtime/tests/test_cockpit_control.py` - 7 tests (health probe x2, idempotent start, Windows flag guard, npm.cmd/npm resolution x2, stop-no-pidfile)
- `services/agent-runtime/atlas_runtime/cli/main.py` - added `_up_cmd` handler + `app.command("up", ...)` registration, placed after the `gateway_app` command block
- `services/agent-runtime/tests/test_cli_up.py` - 3 tests (full success echo, partial-failure exit 1, call-order assertion)

## Decisions Made
- Kept env-var (`ATLAS_COCKPIT_URL`) as the authoritative spawn/health-check URL source rather than reconciling `config.yaml`'s `CockpitConfig.port` — matches the existing de facto gateway precedent (nothing currently wires `config.yaml` port fields into an actual spawn command), and the plan's `<interfaces>` section explicitly called this the resolved decision for this plan.
- Split the plan's Test 5 ("resolves npm.cmd on Windows / npm otherwise") into two separate test functions (`test_start_resolves_npm_cmd_on_windows`, `test_start_resolves_npm_on_posix`) for clearer failure isolation — functionally a superset of the single combined test the plan described, not a reduction in coverage (7 tests total vs the 6 specified).

## Deviations from Plan

None - plan executed exactly as written. The only adjustment was the test-count split noted above, which is additive (more granular assertions, same behavior coverage) and not a Rule 1-4 deviation.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Operators only need `npm run build` already run in `services/web-ui-react` (so `npm run preview` has a `dist/` to serve) before `atlas up` produces a usable cockpit; this is the existing build-step prerequisite already covered by `scripts/install-atlas-cli.ps1` per 10.0.2-PATTERNS.md.

## Next Phase Readiness

- `cockpit_control.py` and `atlas up` are ready for the install-script wiring (10.0.2-02/03) and `docs/INSTALL.md` (10.0.2-04), which can now document the env-var port-authority precedent and the one-command boot path as a fact, not a plan.
- No blockers. Full agent-runtime test suite (272 tests) green after both commits.

## Self-Check: PASSED

All created files and commit hashes verified present on disk / in git log:
- FOUND: services/agent-runtime/atlas_runtime/cockpit_control.py
- FOUND: services/agent-runtime/tests/test_cockpit_control.py
- FOUND: services/agent-runtime/tests/test_cli_up.py
- FOUND: .planning/phases/10.0.2-one-command-install-path/10.0.2-01-SUMMARY.md
- FOUND: commit 46ff072 (Task 1)
- FOUND: commit e9a0775 (Task 2)
