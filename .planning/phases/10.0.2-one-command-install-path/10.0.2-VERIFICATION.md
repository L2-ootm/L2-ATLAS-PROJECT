---
phase: 10.0.2-one-command-install-path
verified: 2026-06-21T00:00:00Z
status: human_needed
score: 4/4 must-haves verified (automated); 2 items require human/manual confirmation
overrides_applied: 0
human_verification:
  - test: "Boot ATLAS with no provider credentials configured (`atlas up` against a fresh ~/.atlas), open the cockpit at http://127.0.0.1:5173 in a real browser, and visually confirm the 'MOCK MODE — no live model' banner renders above the routed content."
    expected: "Banner is visible, styled per MockModeBanner.tsx (warning-colored strip with dot indicator), and disappears once a real provider credential is configured."
    why_human: "Requires a running gateway + cockpit + real browser rendering — cannot be confirmed by static analysis alone. Code-level wiring (Layout.tsx -> MockModeBanner, config_service.masked_dict -> mock_mode -> /v1/config -> getConfig()) is verified in source, but visual confirmation in a live browser was not performed in this verification pass."
  - test: "Run `docker compose up --build` from repo root on a machine with Docker installed, confirm all three services (gateway, agent-runtime, cockpit) build and the gateway /health + cockpit :5173 respond."
    expected: "All three containers build from source and serve; gateway /health returns ok; cockpit loads in browser."
    why_human: "No container engine is installed on this verification machine (confirmed, documented constraint inherited from 10.0.2-PATTERNS.md / VALIDATION.md). docker-compose.yml exists and is structurally sound (dockerfile_inline blocks for 3 services, env_file wiring, port mappings) but was never executed end-to-end. This is the explicitly accepted secondary-path constraint per CONTEXT.md ('No container engine on the dev machine — Docker path cannot be verified here')."
---

# Phase 10.0.2: One-Command Install Path Verification Report

**Phase Goal:** A developer can clone, configure, and run ATLAS without the operator's help.
**Verified:** 2026-06-21T00:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Success Criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| SC1 | One-command native setup boots gateway + cockpit (Docker Compose optional) | VERIFIED (native) / docs-only (Docker) | `atlas up` registered in `cli/main.py:881` calling `gateway_control.start()` then `cockpit_control.start()` (`cli/main.py:868-879`); `cockpit_control.py` is a full lifecycle primitive (health_ok/start/stop, Windows console-flash guard `CREATE_NO_WINDOW`); `scripts/install-atlas-cli.ps1` and `scripts/setup.sh` build both cargo gateway and npm cockpit bundles with exit-code checks (`install-atlas-cli.ps1:54,56,70,84,86`); `docker-compose.yml` exists with 3 services (gateway/agent-runtime/cockpit) using `dockerfile_inline` — structurally correct but unexecuted (no Docker engine on this machine, an accepted constraint per CONTEXT.md) |
| SC2 | `.env.example` + quickstart doc + seed/demo data exist; deterministic MOCK MODE covers missing keys | VERIFIED | `.env.example` (repo root) documents provider/gateway/cockpit/db env vars, zero real secrets, explicit port-authority disclaimer; `docs/INSTALL.md` is a full 131-line quickstart+troubleshooting doc; `demo_seed.py::seed_demo_data` creates mission+run+2 audit events+wiki entry, idempotent via lazily-resolved ATLAS_HOME-aware sentinel (`demo_seed.py:58-68`), wired into `atlas db init --demo` (`cli/main.py:796-822`); `agents/mock.py::MockAtlasAgent`/`mock_factory` is a fully deterministic canned response, gated at `native.py`'s `if not api_key:` factory-selection branch (confirmed at the line cited in SUMMARY, verified via grep) — a configured-but-wrong key still routes to `_default_factory` (honest-failure preserved, tested by `test_native_execute_routes_to_real_provider_when_key_present_but_wrong`) |
| SC3 | `atlas doctor` (+ existing `/health`) exists with install troubleshooting docs | VERIFIED | `cli/doctor.py::_doctor_cmd` aggregates db migration status, config validity, `gateway_control.health_ok()` (which probes Rust gateway's `/health` route at `lib.rs:150,1743`), `cockpit_control.health_ok()`, and provider/mock state — secret-safe (never echoes raw api_key, only "configured"/"mock"); registered at `cli/main.py:883-887`; `docs/INSTALL.md` "Troubleshooting" section covers pip-less-PATH-python, Windows console-flash, port-authority confusion, gateway/cockpit-down diagnosis |
| SC4 | Verified on a clean environment (native fresh-checkout smoke; Docker documented) | VERIFIED (native) / docs-only (Docker) | `scripts/fresh_install_smoke.py` exercises db.apply_migrations -> demo_seed.seed_demo_data -> gateway/cockpit health_ok -> a real mock-mode mission run via `run_executor.execute_run`, against an isolated temp DB + temp ATLAS_HOME (never touches the operator's real `~/.atlas`); `tests/test_fresh_install_smoke.py` (2 tests) RAN and PASSED in this verification session; Docker path is explicitly documented as unverifiable on this machine in CONTEXT.md/VALIDATION.md ("No container engine on the dev machine") — an accepted, pre-declared constraint, not a silent gap |

**Score:** 4/4 success criteria have automated-verifiable evidence; 2 sub-items within SC1/SC4 (Docker live execution) and SC2 (cockpit visual banner rendering) require human/manual confirmation per the planner's own VALIDATION.md "Manual-Only Verifications" table.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `atlas up` boots gateway + cockpit together, idempotently | VERIFIED | `cli/main.py:868-879`; full test suite includes `test_cli_up.py` (3 tests, part of 301 passing) |
| 2 | Mock provider auto-engages on empty creds, honest-failure preserved on wrong-but-present key | VERIFIED | `native.py` `if not api_key:` gate; `test_provider_routing.py` regression-fixed and passing; `agents/mock.py` fully deterministic (no `datetime.now()`, no interpolation) |
| 3 | Demo seed creates mission/run/audit/wiki, idempotent | VERIFIED | `demo_seed.py:80-133`; `test_demo_seed.py` (7 tests) passing |
| 4 | `atlas doctor` aggregates 5 independent checks, secret-safe | VERIFIED | `cli/doctor.py:23-93`; `test_cli_doctor.py` (5 tests) passing; never echoes raw api_key |
| 5 | Cockpit displays a MOCK MODE banner when running without credentials | VERIFIED (code-level wiring) / UNCERTAIN (visual) | `config_service.masked_dict` derives `mock_mode` via `resolve_provider` (`config_service.py:146-160`); Rust `/v1/config` thin-passes `masked_dict` (`lib.rs:548-553`); cockpit `api.ts` types `mock_mode?: boolean` (`api.ts:888`); `Layout.tsx` fetches config on mount and renders `<MockModeBanner mockMode={mockMode} />` unconditionally in the app shell (`Layout.tsx:27-30,59`) — this was the CR-01 critical finding from 10.0.2-REVIEW.md, fixed in commits `2942dbb`/`c45745c`, and the fix is genuinely present in source, not just claimed. Browser-rendered visual confirmation was not performed in this pass. |
| 6 | `.env.example` ships with zero real secrets and machine-independent values | VERIFIED | Read in full; only `env:VAR` reference syntax and placeholder ports, no literal keys/paths; `.env` itself is gitignored (`.gitignore:14`) |
| 7 | Clean-environment smoke proves setup->boot->doctor->mock-run end-to-end | VERIFIED | `scripts/fresh_install_smoke.py` (167 lines, calls real service-layer functions against isolated temp DB/ATLAS_HOME); `tests/test_fresh_install_smoke.py` (2 tests) executed live in this verification session — both passed |
| 8 | All review-identified defects (1 critical, 5 warnings, 2 info) were actually fixed, not just marked fixed | VERIFIED | Cross-checked every fix claim in 10.0.2-REVIEW-FIX.md against live source: CR-01 (mock_mode wiring) confirmed in config_service.py/api.ts/Layout.tsx; WR-01 (`_pid_alive` liveness check) confirmed in cockpit_control.py:99-149; WR-02 (`_get_lock()` singleton) confirmed in cli/main.py:813,821; WR-03 (invalid-port ValueError) confirmed in cockpit_control.py:44-48; WR-04 (exit-code checks) confirmed in install-atlas-cli.ps1:54,56,70,84,86; WR-05 (case statement, unknown-arg rejection) confirmed in setup.sh:19; IN-01/IN-02 comments confirmed present. Commit hashes (`2942dbb`,`c45745c`,`e541a62`,`aeb3f32`,`320140f`,`96f97cf`,`7c0c11f`,`ba5446b`) all match `git log` exactly. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `services/agent-runtime/atlas_runtime/cockpit_control.py` | Cockpit lifecycle primitive | VERIFIED | health_ok/start/stop/_pid_alive/_parse_port all present and substantive |
| `services/agent-runtime/atlas_runtime/agents/mock.py` | Deterministic mock agent | VERIFIED | `MockAtlasAgent`/`mock_factory`, fully canned, no randomization |
| `services/agent-runtime/atlas_runtime/agents/native.py` | Mock-mode routing gate | VERIFIED | `if not api_key:` factory branch confirmed via grep |
| `services/agent-runtime/atlas_runtime/demo_seed.py` | Idempotent demo seed | VERIFIED | mission+run+2 audit+wiki, ATLAS_HOME-aware sentinel |
| `services/agent-runtime/atlas_runtime/cli/main.py` | `atlas up`/`atlas doctor`/`db init --demo` registration | VERIFIED | All three confirmed registered and wired |
| `services/agent-runtime/atlas_runtime/cli/doctor.py` | Health aggregator | VERIFIED | 5 independent try/except checks, secret-safe |
| `services/agent-runtime/atlas_runtime/config_service.py` | `mock_mode` derived field | VERIFIED | `masked_dict()` adds `mock_mode` via `resolve_provider` |
| `services/web-ui-react/src/lib/api.ts` | `mock_mode` type + `getConfig()` | VERIFIED | `AtlasConfigView.mock_mode?: boolean` at line 888 |
| `services/web-ui-react/src/shell/Layout.tsx` | App-shell wiring of banner | VERIFIED | Renders `<MockModeBanner mockMode={mockMode}/>` unconditionally in layout |
| `services/web-ui-react/src/components/MockModeBanner.tsx` | Banner component | VERIFIED | Conditional render, "MOCK MODE — no live model" copy matches docs |
| `.env.example` | Env var template | VERIFIED | No secrets, port-authority disclaimer |
| `docker-compose.yml` | Optional 3-service compose | VERIFIED (structure) / UNEXECUTED | dockerfile_inline x3, env_file wiring, ports mapped — never run on this machine |
| `docs/INSTALL.md` | Quickstart + troubleshooting | VERIFIED | 131 lines, covers all documented pitfalls |
| `README.md` | Quickstart section | VERIFIED | Links to docs/INSTALL.md |
| `scripts/install-atlas-cli.ps1` | Extended installer (gateway+cockpit build) | VERIFIED | cargo+npm build steps with exit-code checks |
| `scripts/setup.sh` | POSIX twin | VERIFIED | Same build steps, case-statement arg validation |
| `scripts/fresh_install_smoke.py` | Clean-env e2e smoke | VERIFIED + RAN GREEN | Executed live in this verification session, 2/2 passed |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `cli/main.py::_up_cmd` | `gateway_control.start()` / `cockpit_control.start()` | direct call | WIRED | Sequential start, exits 1 on partial failure |
| `cli/doctor.py` | `gateway_control.health_ok()` (-> Rust `/health`) | direct call | WIRED | `gateway_control.py` GETs `{GATEWAY_URL}/health`; Rust route registered at `lib.rs:1743` |
| `cli/main.py::db_init` | `demo_seed.seed_demo_data()` | `--demo` flag gate | WIRED | Only invoked when `demo=True`; shares `_get_lock()` singleton with model_registry seed |
| `native.py::execute()` | `agents/mock.py::mock_factory` | `if not api_key:` branch | WIRED | Confirmed honest-failure regression test passes |
| `config_service.masked_dict` | Rust `GET /v1/config` | `atlas config json` subprocess -> JSON passthrough | WIRED | `lib.rs:548-553` is a thin pass-through, confirmed no Rust-side filtering strips `mock_mode` |
| `lib/api.ts::getConfig()` | `shell/Layout.tsx` `useEffect` | fetch + `setMockMode` | WIRED | `Layout.tsx:24-30`, fails closed (`.catch(() => setMockMode(false))`) |
| `shell/Layout.tsx` | `components/MockModeBanner.tsx` | JSX render with `mockMode` prop | WIRED | `Layout.tsx:59`, unconditional render call (banner internally returns null when false) |

### Behavioral Spot-Checks / Probe Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full agent-runtime test suite | `../../.venv/Scripts/python.exe -m pytest -q` (from `services/agent-runtime`) | `301 passed in 10.01s` | PASS |
| Cockpit typecheck | `npm run check` (from `services/web-ui-react`) | `tsc -b --noEmit` clean, no errors | PASS |
| Fresh-install clean-environment smoke | `pytest tests/test_fresh_install_smoke.py -v` | `2 passed in 0.58s` (both `test_fresh_install_smoke_passes_end_to_end` and `test_fresh_install_smoke_fails_when_atlas_up_fails`) | PASS |
| Debt-marker scan on phase-touched files | grep TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER across 17 key files | No matches | PASS |
| Commit hash cross-check | `git log --oneline -20` vs hashes cited in SUMMARY/REVIEW-FIX | All 8 fix commits + earlier feature commits match exactly | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| SC1 | 10.0.2-01 | atlas up boots gateway+cockpit | SATISFIED | cockpit_control.py + cli wiring |
| SC2 | 10.0.2-02, 10.0.2-04 | Mock mode + demo seed + .env.example + quickstart | SATISFIED | mock.py, demo_seed.py, .env.example, INSTALL.md |
| SC3 | 10.0.2-03 | atlas doctor + troubleshooting docs | SATISFIED | doctor.py + INSTALL.md Troubleshooting |
| SC4 | 10.0.2-04 | Clean-env verification | SATISFIED (native); docs-only (Docker, pre-declared constraint) | fresh_install_smoke.py ran green |

No orphaned requirements found — REQUIREMENTS.md cross-reference for Phase 10.0.2 maps cleanly to all 4 plans' declared `requirements-completed` frontmatter.

### Anti-Patterns Found

None. Scanned all 17 key files modified/created in this phase for TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers and stub-shaped patterns (`return null`, empty handlers, hardcoded `[]`/`{}` flowing to render) — zero matches. The one component that could plausibly be a stub (`MockModeBanner.tsx`) was the subject of a critical review finding (CR-01) precisely because it WAS dead code at one point in the phase's history — but the fix commits (`2942dbb`, `c45745c`) are confirmed live in source, and the component is now genuinely wired end-to-end from `config_service.py` through the Rust gateway to the cockpit's app shell.

### Human Verification Required

#### 1. Cockpit Mock Mode banner visual confirmation

**Test:** Run `atlas up` with no provider credentials configured (fresh `~/.atlas` or unset `ATLAS_PROVIDER_API_KEY`), open `http://127.0.0.1:5173` in a browser, and look for the "MOCK MODE — no live model" banner above the routed content.
**Expected:** A warning-colored horizontal strip with a small dot indicator and the exact copy "MOCK MODE — no live model" appears at the top of every page; configuring a real provider and reloading makes the banner disappear.
**Why human:** All code-level wiring (config_service -> gateway /v1/config -> cockpit api.ts -> Layout.tsx -> MockModeBanner.tsx) is verified statically and the cockpit typechecks clean, but no browser was launched in this verification pass to confirm the visual render, CSS styling, or that the `getConfig()` fetch actually resolves against a live gateway in practice (as opposed to in isolated unit tests).

#### 2. Docker Compose clean-environment boot

**Test:** On a machine with Docker installed, run `docker compose up --build` from the repo root and confirm all three services (gateway, agent-runtime, cockpit) build successfully and the gateway `/health` + cockpit `:5173` respond.
**Expected:** All three containers build from source inside their `dockerfile_inline` blocks; gateway responds healthy; cockpit serves at port 5173.
**Why human:** No container engine is installed on this verification machine — this is an explicitly pre-declared, accepted constraint from the phase's own CONTEXT.md and VALIDATION.md ("No container engine on the dev machine — Docker path cannot be verified here; use a native fresh-checkout smoke... for criterion 4"). The native path was substituted and verified instead (and passed). This Docker gap is not a phase failure — it is a known, scoped-out limitation that SC4's wording explicitly anticipates ("Docker path documented").

### Gaps Summary

No blocking gaps. All 4 success criteria have substantive, wired, tested implementations confirmed directly in source (not merely claimed in SUMMARY.md). The one critical defect found during code review (CR-01: MockModeBanner dead code) was independently re-verified in this pass and is confirmed genuinely fixed — the fix is real, not just documented as fixed. All review warnings (WR-01 through WR-05) and info notes (IN-01, IN-02) were also independently re-verified present in source. The two items requiring human verification (cockpit visual banner rendering in a live browser, and a true Docker Compose clean-machine run) are pre-declared, scoped limitations from the phase's own VALIDATION.md "Manual-Only Verifications" table, not undiscovered gaps — they were anticipated by the planner and substituted with equivalent automated coverage (native fresh-install smoke test, which ran and passed live in this session) wherever automation was possible.

---

_Verified: 2026-06-21T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
