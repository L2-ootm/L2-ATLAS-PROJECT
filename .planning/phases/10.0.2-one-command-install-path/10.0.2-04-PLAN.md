---
phase: 10.0.2
plan: 04
type: execute
wave: 3
depends_on: ["10.0.2-01", "10.0.2-02"]
files_modified:
  - .env.example
  - docker-compose.yml
  - docs/INSTALL.md
  - README.md
  - scripts/install-atlas-cli.ps1
  - scripts/setup.sh
  - scripts/fresh_install_smoke.py
  - services/web-ui-react/src/components/MockModeBanner.tsx
autonomous: true
requirements: [SC2, SC4]
must_haves:
  truths:
    - "A developer can clone the repo, copy .env.example to .env, run one setup script, then `atlas up`, and reach a working gateway+cockpit with zero prior knowledge of the codebase"
    - "A fresh-checkout smoke (no Docker required) proves setup -> boot -> atlas doctor -> mock-mode mission run succeeds end-to-end on a throwaway environment"
    - ".env.example and docs/INSTALL.md contain zero real credentials or machine-specific absolute paths"
  artifacts:
    - path: ".env.example"
      provides: "Documented provider key refs (env:VAR placeholders only), gateway/cockpit port vars, db path var"
      contains: "ATLAS_GATEWAY_PORT"
    - path: "docs/INSTALL.md"
      provides: "Quickstart + troubleshooting doc covering native path (primary) and Docker Compose (optional)"
      min_lines: 40
    - path: "scripts/fresh_install_smoke.py"
      provides: "Clean-environment e2e smoke: setup -> atlas up -> atlas doctor -> mock-mode mission run"
      contains: "def main"
  key_links:
    - from: "docs/INSTALL.md"
      to: ".env.example"
      via: "documented `cp .env.example .env` step"
      pattern: "\\.env\\.example"
    - from: "scripts/fresh_install_smoke.py"
      to: "services/agent-runtime/atlas_runtime/cli/main.py"
      via: "subprocess invocation of the atlas CLI (up, doctor)"
      pattern: "atlas (up|doctor)"
---

<objective>
Ship the install/docs layer that makes the one-command boot path discoverable and
verifiable on a clean environment: `.env.example`, optional root `docker-compose.yml`,
`docs/INSTALL.md`, a README quickstart section, an extended `install-atlas-cli.ps1`
(adds gateway+cockpit build steps), a new bash twin `scripts/setup.sh`, a fresh-checkout
e2e smoke script, and a cockpit `MockModeBanner` component.

Purpose: SC2 requires `.env.example`/quickstart docs to exist; SC4 requires verification
on a clean environment. This plan is the only one that touches docs/scripts/cockpit-UI —
it depends on Plan 01 (`atlas up`/`cockpit_control`) and Plan 02 (mock provider/demo seed)
because the docs and smoke script reference both.
Output: `.env.example`, `docker-compose.yml`, `docs/INSTALL.md`, README quickstart
section, extended `install-atlas-cli.ps1`, new `scripts/setup.sh`, new
`scripts/fresh_install_smoke.py`, new `MockModeBanner.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.0.2-one-command-install-path/10.0.2-CONTEXT.md
@.planning/phases/10.0.2-one-command-install-path/10.0.2-RESEARCH.md
@.planning/phases/10.0.2-one-command-install-path/10.0.2-PATTERNS.md
@scripts/install-atlas-cli.ps1
@scripts/fresh_db_smoke.py
@services/agent-runtime/atlas_runtime/config_service.py
</context>

<interfaces>
<!-- Exact patterns and field names gathered by direct reads. Reuse verbatim. -->

From scripts/install-atlas-cli.ps1 (full file, 64 lines — the existing TUI-build step,
lines 45-55, is the exact pattern to mirror for new cargo/vite build steps):
```powershell
$tui = Join-Path $root 'foundation/atlas-hermes/ui-tui'
if ((Get-Command npm -ErrorAction SilentlyContinue) -and (Test-Path $tui)) {
    Write-Host "Building the terminal UI ($tui)"
    Push-Location $tui
    try { npm install --silent; npm run build } finally { Pop-Location }
} else {
    Write-Host "Skipping TUI build (npm not found); 'atlas tui' will build on first run."
}
```
The script currently builds the TUI bundle, installs the Python venv/CLI, and runs
`atlas db init` — it does NOT build the Rust gateway binary or the cockpit. Both new
build steps must follow this exact `Get-Command ... -ErrorAction SilentlyContinue` +
graceful-skip shape.

From config_service.py (field names `.env.example` must mirror so the two configuration
surfaces stay in sync — provider/gateway/cockpit field names):
```python
class ProviderConfig: ...  # .api_key field is "env:VAR_NAME" reference only (D-002)
class GatewayConfig: rust_port: int = 8484
class CockpitConfig: port: int = 3000  # NOTE: unused today — Vite's real default is 5173
```
Per Plan 01's documented port-authority decision: env-var (`ATLAS_GATEWAY_URL`/
`ATLAS_GATEWAY_PORT`, `ATLAS_COCKPIT_URL`) is authoritative for the spawn/health-check
URLs that `gateway_control.py`/`cockpit_control.py` actually use — `config.yaml`'s
`gateway.rust_port`/`cockpit.port` fields are informational only today (nothing wires
them into a spawn command). `.env.example` and `docs/INSTALL.md` MUST state this
explicitly and use `ATLAS_GATEWAY_PORT=8484` / `ATLAS_COCKPIT_URL=http://127.0.0.1:5173`
as the real, working knobs — do not present `config.yaml`'s port fields as effective.

From scripts/fresh_db_smoke.py (full file, 109 lines — the structural model for
`fresh_install_smoke.py`'s temp-dir + numbered-step + exit-0/1 shape):
```python
REPO_ROOT = pathlib.Path(__file__).resolve().parents[1]
with tempfile.TemporaryDirectory(ignore_cleanup_errors=True) as tmp:
    db_path = pathlib.Path(tmp) / "atlas-smoke.db"
    ...
    try:
        return _run_loop(conn, lock)
    finally:
        conn.close()
```
`fresh_install_smoke.py` extends this shape with subprocess calls instead of direct
service-layer imports (it spans process/build boundaries, unlike the DB-only smoke):
set `ATLAS_HOME` env var to a temp dir before invoking subprocess steps (per
`config_service.py`'s env-aware `atlas_home()` resolution), then run, in order:
the appropriate setup script, `atlas db init --demo` (Plan 02 output),
`atlas up` (Plan 01 output, poll for health), `atlas doctor` (Plan 03 output, assert
exit 0), then a mock-mode mission run (no real provider keys set in the temp
`ATLAS_HOME`), printing numbered steps and returning 0/1 like `fresh_db_smoke.py`.

From services/web-ui-react (Vite SPA, React + TypeScript; no existing banner component —
author fresh, consistent with the project's existing component file-naming/structure
convention under `src/components/`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write `.env.example`, `docker-compose.yml`, and extend the install scripts</name>
  <files>.env.example, docker-compose.yml, scripts/install-atlas-cli.ps1, scripts/setup.sh</files>
  <read_first>
    scripts/install-atlas-cli.ps1 (full file — extend in place, do not rewrite unrelated sections)
    services/agent-runtime/atlas_runtime/config_service.py (field names for `.env.example` parity)
  </read_first>
  <action>
    Create root `.env.example` with commented sections: Provider (e.g.
    `ATLAS_PROVIDER_API_KEY_REF=env:ATLAS_PROVIDER_API_KEY` documenting that the actual
    secret value goes in a real, gitignored `.env`/shell env — `.env.example` itself
    contains ONLY the variable names and a `# set your key here, never commit it` comment,
    zero placeholder strings that look like a real key prefix such as `sk-`), Gateway
    (`ATLAS_GATEWAY_PORT=8484`, `ATLAS_GATEWAY_URL=http://127.0.0.1:8484`), Cockpit
    (`ATLAS_COCKPIT_URL=http://127.0.0.1:5173`), DB (`ATLAS_HOME=` left blank with a
    comment noting the default is `~/.atlas`). Add a header comment explaining the
    port-authority note from `<interfaces>` (env-var is authoritative, config.yaml's
    port fields are informational only).
    Create root `docker-compose.yml` with three services (`gateway`, `agent-runtime`,
    `cockpit`) each building from the relevant subdirectory Dockerfile if one exists, or
    add a minimal inline `build.context`/`command` referencing the same build-from-source
    steps as the native scripts (cargo build / pip install -e / npm run build) — mark
    this file as the documented OPTIONAL secondary path per CONTEXT.md's Install
    Mechanism decision; it does not need its own Dockerfiles authored from scratch if
    none exist — check for `foundation/atlas-hermes/docker-compose.yml` as a sibling
    pattern first; if absent, write a straightforward 3-service compose file using
    standard `python:3.x-slim`/`rust:1.x`/`node:20-slim` base images with volume-mounted
    source and the same commands the native scripts run, environment variables sourced
    from `.env` via `env_file: .env`.
    Extend `scripts/install-atlas-cli.ps1`: after the existing TUI-build block (lines
    45-55) and before the `atlas db init` line, add a cargo-build step
    (`cargo build --release -p atlas-gateway` inside `native/atlas-core-rs`, gated on
    `Get-Command cargo -ErrorAction SilentlyContinue`, graceful skip message otherwise)
    and a cockpit-build step (`npm install --silent; npm run build` inside
    `services/web-ui-react`, gated on `Get-Command npm` + path exists, graceful skip
    otherwise) — both following the exact `Push-Location`/`try`/`finally`/`Pop-Location`
    shape already used for the TUI step.
    Create `scripts/setup.sh` as the POSIX bash twin of the now-extended
    `install-atlas-cli.ps1`: `Join-Path` becomes string concatenation
    (`"$root/.venv"`), `Scripts\python.exe` becomes `bin/python` (POSIX venv layout),
    `Get-Command X -ErrorAction SilentlyContinue` becomes `command -v X >/dev/null 2>&1`,
    `Push-Location`/`Pop-Location`/`finally` become `pushd ... && ... ; popd` or a
    subshell `( cd ... && ... )`, and the `atlas.cmd` shim becomes a `./atlas` bash
    script (`#!/usr/bin/env bash` + `exec "$VENV_PY" -m atlas_runtime.cli.main "$@"`,
    `chmod +x` after writing).
  </action>
  <acceptance_criteria>
    - `.env.example` exists; `grep -ci "sk-\|Bearer " .env.example` == 0 (no real-looking secret prefixes).
    - `grep -c "ATLAS_GATEWAY_PORT" .env.example` >= 1; `grep -c "ATLAS_COCKPIT_URL" .env.example` >= 1.
    - `.env.example` contains an explicit port-authority disclaimer comment stating that `config.yaml`'s `gateway.rust_port`/`cockpit.port` are NOT read by `atlas up` and these env vars are authoritative (assert a comment line matching `grep -i "config.yaml.*not\|authoritative" .env.example` >= 1) — prevents a future maintainer editing the config.yaml port expecting it to take effect.
    - `docker-compose.yml` exists at repo root with three service blocks (`gateway:`, `agent-runtime:` or equivalent, `cockpit:`).
    - `grep -c "cargo build" scripts/install-atlas-cli.ps1` >= 1; `grep -c "npm run build" scripts/install-atlas-cli.ps1` >= 2 (TUI step + new cockpit step).
    - `scripts/setup.sh` exists and is syntactically valid bash: `bash -n scripts/setup.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/setup.sh</automated>
  </verify>
  <done>.env.example documents provider/gateway/cockpit/db vars with zero real secrets; docker-compose.yml provides the optional 3-service clean-env path; install-atlas-cli.ps1 now builds the gateway binary and cockpit bundle; scripts/setup.sh is a working bash twin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write docs/INSTALL.md, README quickstart, MockModeBanner, and the fresh-install smoke</name>
  <files>docs/INSTALL.md, README.md, services/web-ui-react/src/components/MockModeBanner.tsx, scripts/fresh_install_smoke.py</files>
  <read_first>
    scripts/fresh_db_smoke.py (full file — structural model)
    services/agent-runtime/atlas_runtime/config_service.py (env var / port-authority note, for parity with Task 1's `.env.example`)
  </read_first>
  <behavior>
    - Test 1: `scripts/fresh_install_smoke.py` run against a throwaway `ATLAS_HOME` temp dir with no provider credentials set completes its full step sequence (setup invocation skipped or stubbed for unit-test speed if a real setup-script subprocess run is too slow for CI — assert at minimum that `atlas db init --demo`, `atlas up`, `atlas doctor`, and a mock-mode mission run each report success) and exits 0.
    - Test 2: the same script, with `gateway_control`/`atlas up` simulated to fail (monkeypatch or a forced-bad `ATLAS_GATEWAY_PORT` causing health_ok to time out), exits non-zero and prints which step failed.
  </behavior>
  <action>
    Write `docs/INSTALL.md` with sections: Quickstart (native path — `git clone`, `cp
    .env.example .env`, run `scripts/setup.ps1`/`scripts/setup.sh` per OS, `atlas up`,
    confirm via `atlas doctor`), Docker Compose (optional path — `docker compose up`,
    same `.env` file consumed via `env_file`), Mock Mode (explain the "MOCK MODE — no
    live model" banner appears whenever no provider credentials are configured, and that
    `atlas db init --demo` seeds a sample mission/run/wiki entry to populate the cockpit
    immediately), Troubleshooting (one entry per RESEARCH.md pitfall: ambient PATH
    python being the pip-less Hermes venv — use the dedicated `.venv`; Windows console-
    flash — note this is handled automatically, not an operator action; port-authority —
    explicitly state env-var (`ATLAS_GATEWAY_PORT`/`ATLAS_COCKPIT_URL`) is authoritative,
    not `config.yaml`'s port fields). Use only relative paths and placeholder usernames —
    zero machine-specific absolute paths (no `C:\Users\<realname>\...`).
    Add a "Quickstart" section near the top of `README.md` (3-5 lines, links to
    `docs/INSTALL.md` for the full walkthrough) — do not duplicate the full doc inline.
    Create `MockModeBanner.tsx` in `services/web-ui-react/src/components/`: a small React
    functional component accepting a `mockMode: boolean` prop, rendering a visible banner
    with the exact text "MOCK MODE — no live model" when `mockMode` is true and rendering
    nothing (`null`) otherwise — follow this project's existing component file conventions
    (check a sibling component under `src/components/` for the project's prop-typing/
    export style before authoring; do not invent a new export convention).
    Create `scripts/fresh_install_smoke.py` modeled on `fresh_db_smoke.py`'s structure:
    set `ATLAS_HOME` to a `tempfile.TemporaryDirectory()` path before any subprocess
    call, then run numbered steps via `subprocess.run([...], check=True, env=env)`:
    [1] `atlas db init --demo` (Plan 02), [2] `atlas up` (Plan 01, poll `cockpit_control.health_ok`/`gateway_control.health_ok` or just rely on the command's own poll-and-report),
    [3] `atlas doctor` (Plan 03, assert returncode 0), [4] a mock-mode mission run
    (invoke whatever existing CLI surface runs a mission, e.g. via the agent-runtime's
    CLI or a direct service call against the temp DB — confirm the simplest existing
    entry point during implementation rather than inventing a new one). Print numbered
    steps and `SMOKE PASSED`/`FAIL: ...` like `fresh_db_smoke.py`; return 0/1 from `main()`.
  </action>
  <acceptance_criteria>
    - `docs/INSTALL.md` exists, >= 40 lines, contains sections "Quickstart", "Docker Compose", "Mock Mode", "Troubleshooting" (grep each section heading).
    - `grep -c "C:\\\\Users" docs/INSTALL.md README.md .env.example` == 0 (no machine-specific absolute paths leaked).
    - `README.md` contains a "Quickstart" heading linking to `docs/INSTALL.md`.
    - `MockModeBanner.tsx` exists, exports a component, renders the literal string "MOCK MODE — no live model" conditionally on a `mockMode` prop.
    - `scripts/fresh_install_smoke.py` exists with a `def main() -> int:` entry point; both behavior tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && .venv\Scripts\python.exe -m pytest ../../tests/test_fresh_install_smoke.py -x -v</automated>
  </verify>
  <done>docs/INSTALL.md and README quickstart exist with no leaked secrets/machine-specific paths; MockModeBanner.tsx renders the exact mock-mode copy; fresh_install_smoke.py proves setup->boot->doctor->mock-mode-run end-to-end on a throwaway environment and exits 0/1 correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.example` / `docs/INSTALL.md` (tracked files) -> git history | Anything committed here is permanently public in repo history; this is the highest-risk boundary in the whole phase for secret leakage |
| `scripts/fresh_install_smoke.py` -> temp `ATLAS_HOME` | The smoke must never touch the real `~/.atlas/atlas.db` or the operator's real config.yaml |
| `docker-compose.yml` -> `.env` file | Compose's `env_file: .env` reads the operator's real `.env` (gitignored), never `.env.example` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.0.2-11 | Information Disclosure | `.env.example` accidentally containing a real-looking secret or a copy-pasted real key | mitigate | Task 1 acceptance criteria includes a grep gate (`sk-`/`Bearer ` substring count == 0) before the task is considered done; `.env.example` only ever holds variable names and `env:VAR` reference syntax, never a value |
| T-10.0.2-12 | Information Disclosure | `docs/INSTALL.md`/`README.md` leaking the author's real machine username/absolute path | mitigate | Task 2 acceptance criteria includes a grep gate for `C:\Users\` patterns across all three doc/env files == 0 |
| T-10.0.2-13 | Tampering | `scripts/fresh_install_smoke.py` accidentally targeting the real `~/.atlas/atlas.db` instead of a temp dir | mitigate | `ATLAS_HOME` env var is explicitly set to a `tempfile.TemporaryDirectory()` path before any subprocess call, mirroring `fresh_db_smoke.py`'s never-touch-the-real-db guarantee |
| T-10.0.2-SC | Tampering | `docker-compose.yml` base images (`python:3.x-slim`, `rust:1.x`, `node:20-slim`) | accept | Official Docker Hub language images, not third-party/community images; no npm/pip/cargo package install is introduced by this plan beyond what Plans 01-03 already audited (no new dependency, only existing services packaged into containers) |
</threat_model>

<verification>
- `bash -n scripts/setup.sh` — syntax valid.
- `grep -ci "sk-\|Bearer " .env.example` == 0; `grep -c "C:\\\\Users" docs/INSTALL.md README.md .env.example` == 0.
- `python scripts/fresh_install_smoke.py` (or its pytest wrapper) exits 0 against a clean temp `ATLAS_HOME`.
</verification>

<success_criteria>
- A developer can go from `git clone` to a running gateway+cockpit using only `.env.example` + one setup script + `atlas up`, per `docs/INSTALL.md`.
- `scripts/fresh_install_smoke.py` proves the full path end-to-end on a throwaway environment without Docker.
- Zero real credentials or machine-specific paths in any tracked install/doc file.
</success_criteria>

<output>
Create `.planning/phases/10.0.2-one-command-install-path/10.0.2-04-SUMMARY.md` when done
</output>
