---
phase: 10.0.2
slug: one-command-install-path
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-22
---

# Phase 10.0.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (agent-runtime + atlas-core), cargo test (atlas-gateway), vitest/tsc+vite (cockpit) |
| **Config file** | services/agent-runtime/pyproject.toml / pytest.ini; native/atlas-core-rs/Cargo.toml; services/web-ui-react/package.json |
| **Quick run command** | `python -m pytest services/agent-runtime/tests -q` (targeted module subset per task) |
| **Full suite command** | `python -m pytest services/agent-runtime/tests packages/atlas-core/tests -q && cargo test -p atlas-gateway && (cd services/web-ui-react && npm run check)` |
| **Estimated runtime** | ~120s (python) + ~60s (rust) + ~30s (cockpit check) |

---

## Sampling Rate

- **After every task commit:** Run the targeted quick command for the touched module.
- **After every plan wave:** Run the full suite command.
- **Before `/gsd-verify-work`:** Full suite green + a clean-checkout boot smoke (native; Docker optional).
- **Max feedback latency:** 120 seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {N}-01-01 | 01 | 1 | REQ-{XX} | T-{N}-01 / — | {expected secure behavior or "N/A"} | unit | `{command}` | ✅ / ❌ W0 | ⬜ pending |

*Planner fills this map: one row per task, mapped to the four success criteria. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Confirm existing pytest/cargo/vitest infra runs green pre-phase (baseline).
- [ ] Add a fresh-checkout boot smoke harness (model: `scripts/fresh_db_smoke.py`) for criterion 4.

*Existing infrastructure covers most phase requirements; the boot smoke is the net-new harness.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Docker Compose clean-env boot | Criterion 4 (optional path) | No container engine on the dev machine | Run `docker compose up` on a machine with Docker; confirm gateway `/health` + cockpit load |
| Cockpit visual MOCK-MODE banner | Criterion 2 | Visual confirmation in browser | `atlas up` with no keys → open cockpit → banner "MOCK MODE — no live model" visible |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
