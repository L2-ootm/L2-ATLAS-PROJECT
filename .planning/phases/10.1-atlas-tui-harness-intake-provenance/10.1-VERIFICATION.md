---
phase: 10.1-atlas-tui-harness-intake-provenance
status: passed_with_exception
verified: 2026-06-24
requirements: [INTAKE-01, INTAKE-02, INTAKE-03, INTAKE-04, INTAKE-05, SEC-01]
---

# Phase 10.1 Verification

## Verdict

**PASSED WITH ONE EXPLICIT PERFORMANCE EXCEPTION.**

The intake, provenance, package isolation, deterministic shell, build, offline,
identity, and artifact requirements are satisfied. The initial 150 MiB
idle-working-set budget is not satisfied; the measured intake prototype uses
230.24 MiB. This exception is bounded at 240 MiB and becomes a mandatory
optimization/architecture gate in Phase 10.6 before default cutover.

## Evidence

- Inventory: 180 unique files, 2,630,082 bytes, zero hash/size mismatches.
- Classification: 4 adopt-pattern, 80 rewrite, 96 reject.
- Runtime dependencies: 3; development dependencies: 2.
- Source: 8 files, 6.96 KiB.
- Standalone artifact: 114.54 MiB.
- Cold snapshot: 225.98 ms.
- Warm snapshot p95: 282.47 ms.
- Idle working set: 230.24 MiB.
- Unexpected network connections: 0.
- Bun tests: 8 passed, 0 failed.
- TypeScript: clean.
- Source and compiled snapshots: identical.
- Source, metadata, dependencies, and compiled artifact boundary scans: clean.

## Commands

- `bun install --frozen-lockfile`
- `bun test`
- `bun run check`
- `bun run snapshot -- --ascii --no-color`
- `bun run build`
- `dist/atlas-tui.exe --snapshot --ascii --no-color`
- `pwsh -NoProfile -File scripts/test-tui-boundary-check.ps1`
- `pwsh -NoProfile -File scripts/test-tui-source-inventory.ps1`
- `pwsh -NoProfile -File scripts/test-tui-baseline.ps1`
- `pwsh -NoProfile -File scripts/tui-boundary-check.ps1 ...`
- `pwsh -NoProfile -File scripts/tui-baseline.ps1 ...`

## Requirement assessment

| Requirement | Result |
|---|---|
| INTAKE-01 | Passed — complete reviewed intake inventory and decisions. |
| INTAKE-02 | Passed — no second runtime/provider/config/memory authority imported. |
| INTAKE-03 | Passed — source and compiled artifact identity scans clean. |
| INTAKE-04 | Passed — notices, attribution, provenance, and legal gate retained. |
| INTAKE-05 | Passed with exception — all metrics recorded; idle target miss carried forward. |
| SEC-01 | Passed — executable local gates cover source, metadata, dependencies, and artifacts. |
