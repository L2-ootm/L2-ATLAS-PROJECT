---
phase: 10.1-atlas-tui-harness-intake-provenance
plan: 03
type: tdd
wave: 3
depends_on: ["10.1-02"]
files_modified:
  - apps/atlas-tui/bun.lock
  - apps/atlas-tui/src/main.tsx
  - apps/atlas-tui/src/app.tsx
  - apps/atlas-tui/src/brand/atlas-mark.ts
  - apps/atlas-tui/src/terminal/profile.ts
  - apps/atlas-tui/src/view-model.ts
  - apps/atlas-tui/test/view-model.test.ts
  - apps/atlas-tui/test/lifecycle.test.ts
  - scripts/tui-baseline.ps1
  - docs/imports/ATLAS_TUI_BASELINE.md
autonomous: true
requirements: [INTAKE-02, INTAKE-03, INTAKE-05, SEC-01]
user_setup: []
must_haves:
  truths:
    - "A local operator can start a compact offline ATLAS terminal shell and exit cleanly without invoking any agent, provider, config, memory, or network path"
    - "Snapshot mode renders deterministic ATLAS-only text without requiring a TTY or native renderer"
    - "Source, dependencies, built artifact, startup, memory, and file-count measurements are recorded against explicit budgets"
    - "Boundary scans pass over both source and compiled output"
  artifacts:
    - path: "apps/atlas-tui/src/main.tsx"
      provides: "ATLAS-only TUI entrypoint with snapshot mode and deterministic cleanup"
      contains: "--snapshot"
    - path: "apps/atlas-tui/src/app.tsx"
      provides: "Minimal OpenTUI/Solid shell"
      contains: "ATLAS"
    - path: "docs/imports/ATLAS_TUI_BASELINE.md"
      provides: "Reproducible dependency, size, startup, memory, and file-count evidence"
      contains: "Cold start"
    - path: "scripts/tui-baseline.ps1"
      provides: "Repeatable Windows baseline measurement"
      contains: "WarmSamples"
  key_links:
    - from: "apps/atlas-tui/src/main.tsx"
      to: "@opentui/core"
      via: "Normal interactive mode creates the native renderer"
      pattern: "createCliRenderer"
    - from: "apps/atlas-tui/src/main.tsx"
      to: "apps/atlas-tui/src/view-model.ts"
      via: "Snapshot mode prints the same pure shell model used by the UI"
      pattern: "snapshot"
    - from: "scripts/tui-baseline.ps1"
      to: "docs/imports/ATLAS_TUI_BASELINE.md"
      via: "Measured results and machine metadata"
      pattern: "p95"
---

<objective>
Prove the selected OpenTUI/Solid substrate with a minimal, offline,
measurable ATLAS terminal shell.

Purpose: validate the rendering path and engineering budgets before Phase 10.6
adds real workbench behavior or Phase 10.3 introduces backend contracts.
Output: compact ATLAS shell, deterministic snapshot/lifecycle tests, standalone
artifact, boundary scan, and recorded Windows performance baseline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-CONTEXT.md
@.planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-RESEARCH.md
@.planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-02-SUMMARY.md
@docs/plans/2026-06-23-atlas-tui-agent-contract-design.md
@services/web-ui-react/src/lib/tokens.css
@services/web-ui-react/src/brand/AtlasMark.tsx
@docs/decisions/D-023-atlas-multi-surface-agent-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN deterministic ATLAS shell model and lifecycle</name>
  <files>apps/atlas-tui/src/view-model.ts, apps/atlas-tui/src/brand/atlas-mark.ts, apps/atlas-tui/src/terminal/profile.ts, apps/atlas-tui/test/view-model.test.ts, apps/atlas-tui/test/lifecycle.test.ts</files>
  <read_first>
    apps/atlas-tui/package.json;
    docs/plans/2026-06-23-atlas-tui-agent-contract-design.md;
    services/web-ui-react/src/lib/tokens.css;
    services/web-ui-react/src/brand/AtlasMark.tsx;
    .planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-CONTEXT.md
  </read_first>
  <action>
    Write failing tests first for a pure `buildShellViewModel()` result containing:
    compact `ATLAS` text mark, optional Unicode border with ASCII fallback,
    `Workspace: not connected`, `Agent: not connected`, `Mode: intake baseline`,
    empty transcript text, composer placeholder, and `Ctrl-C to exit`. The model
    must be deterministic and contain no dates, random IDs, environment-derived
    paths, URLs, provider names, or backend state. Add terminal capability
    selection for Unicode/color/no-color without reading or writing config.
    Add lifecycle tests around an injected renderer/cleanup interface proving
    cleanup executes exactly once on normal exit, Ctrl-C, and render failure.
  </action>
  <verify>`cd apps/atlas-tui; bun test` exits 0.</verify>
  <acceptance_criteria>
    - Snapshot is byte-stable across repeated calls.
    - ASCII fallback contains only printable ASCII plus newline.
    - No test or source imports an ATLAS backend, SDK, provider, auth, config, storage, or network module.
    - Cleanup count is exactly one for each terminal outcome.
  </acceptance_criteria>
  <done>The shell's content and lifecycle contract are fully testable without a TTY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the minimal native shell and build a deterministic snapshot path</name>
  <files>apps/atlas-tui/src/main.tsx, apps/atlas-tui/src/app.tsx, apps/atlas-tui/bun.lock</files>
  <read_first>
    apps/atlas-tui/src/view-model.ts;
    apps/atlas-tui/src/terminal/profile.ts;
    official OpenTUI getting-started and Solid documentation;
    the pinned upstream app renderer configuration only as a behavior reference, not as a copy target
  </read_first>
  <action>
    Implement `main.tsx` so `bun run src/main.tsx --snapshot [--ascii]
    [--no-color]` prints the pure view model and exits without creating a renderer.
    Normal mode creates an OpenTUI renderer with `exitOnCtrlC: false`, renders a
    compact full-screen Solid layout, and routes Ctrl-C through the tested
    exactly-once cleanup path. The layout contains only the Phase 10.1 static
    regions; no fetch, WebSocket, subprocess, file mutation, provider, config,
    session, or agent call is allowed. Add `--help` with ATLAS-only copy. Generate
    and commit the package-local Bun lockfile. Build with Bun's standalone compile
    path; if OpenTUI requires documented asset flags, record the exact command in
    package scripts rather than adding a custom bundler.
  </action>
  <verify>
    `cd apps/atlas-tui; bun run check; bun test; bun run snapshot -- --ascii;
    bun run build` all exit 0, snapshot contains ATLAS, and the built artifact
    starts and exits in snapshot mode.
  </verify>
  <acceptance_criteria>
    - Interactive entrypoint uses only OpenTUI/Solid and local presentation modules.
    - `--snapshot --ascii` works without a TTY and emits no stderr.
    - Build succeeds on the reference Windows host.
    - Boundary scanner passes over source, lockfile, and built artifact.
    - Running in an offline environment produces identical snapshot output.
  </acceptance_criteria>
  <done>A minimal native ATLAS terminal shell builds, renders, snapshots, and exits without backend coupling.</done>
</task>

<task type="auto">
  <name>Task 3: Measure and enforce the intake budgets</name>
  <files>scripts/tui-baseline.ps1, docs/imports/ATLAS_TUI_BASELINE.md</files>
  <read_first>
    .planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-RESEARCH.md;
    apps/atlas-tui/package.json;
    scripts/tui-boundary-check.ps1
  </read_first>
  <action>
    Write `scripts/tui-baseline.ps1` with explicit `-AppRoot`,
    `-ColdSamples` (default 10), `-WarmSamples` (default 20), and
    `-IdleSamples` (default 5). Record UTC timestamp, OS, CPU, architecture,
    terminal/shell, Bun version, dependency counts, source file count/bytes,
    compiled artifact bytes, every sample, p50/p95 startup, and idle working set
    after five seconds. Use snapshot mode for repeatable startup timing. Add a
    production-module `--idle-probe 5000` mode that loads the same package and
    shell model, performs no I/O or network work, holds for five seconds, then
    exits; use that process for reproducible working-set measurement. Record this
    explicitly as the Phase 10.1 process-idle baseline, not a claim about a fully
    connected future workbench. Terminate only the exact PID started by the
    script. Emit JSON to a temporary or ignored output and
    write a human-readable baseline doc with pass/fail against RESEARCH.md budgets.
    A standalone artifact over 140 MB, p95 cold over 2.0 s, p95 warm over 1.0 s,
    idle memory over 150 MB, more than 30 source files, more than 250 KB source,
    or more than three runtime dependencies fails the script. Run the boundary
    scanner over both source and build output as the final gate.
  </action>
  <verify>
    `pwsh -NoProfile -File scripts/tui-baseline.ps1 -AppRoot apps/atlas-tui`
    exits 0 and `docs/imports/ATLAS_TUI_BASELINE.md` contains machine metadata,
    sample counts, p50/p95, memory, sizes, and explicit PASS rows.
  </verify>
  <acceptance_criteria>
    - Baseline is reproducible from one command.
    - All raw sample values and aggregation method are documented.
    - Budget failures return non-zero.
    - Boundary scan reports zero findings in source and build output.
    - No measurement command touches the existing Hermes TUI or real ATLAS state.
  </acceptance_criteria>
  <done>The substrate is accepted or rejected using measured evidence rather than aesthetics or intuition.</done>
</task>

</tasks>

<verification>
- [ ] `bun run check`, `bun test`, snapshot, and standalone build pass.
- [ ] Offline snapshot is deterministic and ATLAS-only.
- [ ] Interactive shell exits cleanly through exactly-once cleanup.
- [ ] Boundary scanner passes over source, metadata, lockfile, tests, and build output.
- [ ] Baseline command passes every Phase 10.1 budget and records machine metadata.
- [ ] No backend, agent, provider, config, storage, network, updater, share, or telemetry code exists in the package.
</verification>

<success_criteria>
- A small ATLAS-owned terminal rendering foundation is proven without importing a second runtime.
- INTAKE-02, INTAKE-03, INTAKE-05, and SEC-01 have executable evidence.
- Phase 10.2/10.3 can define the agent and surface contracts without being constrained by a transplanted backend.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-03-SUMMARY.md`.
</output>
