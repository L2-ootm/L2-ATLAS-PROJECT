---
phase: 10.1-atlas-tui-harness-intake-provenance
plan: 02
type: tdd
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - apps/atlas-tui/package.json
  - apps/atlas-tui/tsconfig.json
  - apps/atlas-tui/src/README.md
  - apps/atlas-tui/test/boundary-fixtures/allowed.txt
  - scripts/tui-source-inventory.ps1
  - scripts/tui-boundary-check.ps1
autonomous: true
requirements: [INTAKE-02, INTAKE-03, SEC-01]
user_setup: []
must_haves:
  truths:
    - "The ATLAS TUI package starts as a clean ATLAS-owned package with no copied runtime, SDK, provider, auth, storage, updater, share, telemetry, or hosted-service dependency"
    - "A product-neutral scanner fails on a seeded forbidden fixture and passes on an ATLAS-only fixture"
    - "The scanner checks source, package metadata, lockfiles, snapshots, source maps, and built artifacts when present"
  artifacts:
    - path: "scripts/tui-boundary-check.ps1"
      provides: "Generic fail-closed identity, dependency, path, environment, and URL scanner"
      contains: "ForbiddenTermsFile"
    - path: "scripts/tui-source-inventory.ps1"
      provides: "Generic external-source hashing and CSV generation utility"
      contains: "SourceRoot"
    - path: "apps/atlas-tui/package.json"
      provides: "Minimal ATLAS TUI package boundary"
      contains: "@atlas/tui"
  key_links:
    - from: "scripts/tui-boundary-check.ps1"
      to: "docs/third-party/atlas-tui-forbidden-terms.txt"
      via: "Terms are loaded from documentation-owned test data"
      pattern: "ForbiddenTermsFile"
    - from: "scripts/tui-source-inventory.ps1"
      to: "docs/imports/ATLAS_TUI_SOURCE_INVENTORY.csv"
      via: "Reproducible CSV schema from an explicit external source root"
      pattern: "sha256"
---

<objective>
Create the clean ATLAS TUI package boundary and the fail-closed tooling that
prevents source contamination before UI implementation begins.

Purpose: turn the intake policy into executable checks while keeping all
upstream identity out of scanner implementation and shipped package code.
Output: minimal package metadata, generic inventory generator, generic boundary
scanner, and red/green fixtures.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-CONTEXT.md
@.planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-RESEARCH.md
@.planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-01-SUMMARY.md
@docs/imports/ATLAS_TUI_SOURCE_INVENTORY.csv
@docs/third-party/atlas-tui-forbidden-terms.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN generic boundary scanner</name>
  <files>scripts/tui-boundary-check.ps1, apps/atlas-tui/test/boundary-fixtures/allowed.txt</files>
  <read_first>
    docs/third-party/atlas-tui-forbidden-terms.txt;
    .planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-RESEARCH.md
  </read_first>
  <action>
    First create an allowed ATLAS-only fixture. Write
    `scripts/tui-boundary-check.ps1` with parameters `-Root`,
    `-ForbiddenTermsFile`, optional `-ApprovedDocumentationRoots`, and
    `-SelfTest`. It must recursively inspect text-bearing source, JSON, lock,
    snapshot, source-map, and generated/bundle files; use `rg -a` or equivalent
    byte-safe fixed-string scanning for compiled binaries; report file, line/offset,
    and matched rule; and return non-zero on any finding or unreadable terms file.
    It must also parse `package.json` and reject dependencies matching configured
    forbidden terms or any dependency outside an optional allowlist supplied by
    the caller. Executable source must contain no upstream product/package/domain
    literals: all such values come from the terms file. `-SelfTest` must create a
    forbidden fixture in a temporary directory from a term loaded at runtime,
    prove that it fails, remove it in `finally`, and prove the committed
    ATLAS-only fixture passes. No forbidden upstream term may be committed under
    `apps/atlas-tui/`, including test fixtures.
  </action>
  <verify>
    `pwsh -NoProfile -File scripts/tui-boundary-check.ps1 -SelfTest -Root apps/atlas-tui -ForbiddenTermsFile docs/third-party/atlas-tui-forbidden-terms.txt`
    exits 0, while direct scanning of the forbidden fixture exits non-zero.
  </verify>
  <acceptance_criteria>
    - Scanner source contains no term from the forbidden-terms file.
    - Missing/empty terms file fails closed.
    - Allowed fixture exits 0; forbidden fixture exits non-zero.
    - Findings include path and matched rule.
    - Compiled binaries are scanned in byte-safe/text-forced mode rather than skipped.
  </acceptance_criteria>
  <done>The boundary policy has a deterministic red/green executable gate.</done>
</task>

<task type="auto">
  <name>Task 2: Build the generic inventory reproducer and clean package skeleton</name>
  <files>scripts/tui-source-inventory.ps1, apps/atlas-tui/package.json, apps/atlas-tui/tsconfig.json, apps/atlas-tui/src/README.md</files>
  <read_first>
    docs/imports/ATLAS_TUI_SOURCE_INVENTORY.csv;
    docs/imports/ATLAS_TUI_INTAKE_DECISIONS.md;
    services/web-ui-react/package.json;
    foundation/atlas-hermes/ui-tui/package.json
  </read_first>
  <action>
    Write `scripts/tui-source-inventory.ps1` with explicit `-SourceRoot`,
    `-SourceCommit`, and `-OutputCsv` arguments. It emits deterministic,
    source-path-sorted rows with SHA-256 and size; classification columns remain
    preserved when regenerating an existing reviewed CSV, and new/unmatched rows
    default to an invalid `unclassified` value so review fails closed. Do not
    hardcode any upstream repository or product string. Create `apps/atlas-tui`
    as package `@atlas/tui`, private, ESM, with Bun scripts for check/test/build/
    snapshot/boundary. Add only the exact audited rendering dependencies:
    `@opentui/core@0.1.101`, `@opentui/solid@0.1.101`, `solid-js@1.9.10`, plus no
    more than three dev dependencies. Do not install or import any agent/backend
    package. `src/README.md` is an internal boundary note stating this directory
    owns presentation only and must consume future ATLAS contracts rather than
    inventing runtime authority.
  </action>
  <verify>
    Regenerate to a temporary CSV from the pinned external checkout and compare
    path/hash/size triples with the reviewed inventory; parse package.json and
    assert exactly three runtime dependencies, all on the approved allowlist.
  </verify>
  <acceptance_criteria>
    - Inventory regeneration reproduces all 180 path/hash/size triples.
    - New source files become `unclassified` and fail review.
    - Package name, scripts, and metadata are ATLAS-only.
    - Runtime dependency count is exactly 3; no workspace-wide donor dependency is present.
    - Boundary scanner passes over the new package.
  </acceptance_criteria>
  <done>The repo has a clean package boundary and reproducible intake evidence, but no UI or backend coupling yet.</done>
</task>

</tasks>

<verification>
- [ ] Scanner self-test proves red and green cases.
- [ ] Scanner implementation contains no forbidden source terms.
- [ ] Inventory regeneration matches the pinned reviewed source set.
- [ ] Package metadata has exactly three approved runtime dependencies.
- [ ] `pwsh -NoProfile -File scripts/tui-boundary-check.ps1 ...` passes on `apps/atlas-tui`.
</verification>

<success_criteria>
- INTAKE-02 and INTAKE-03 are enforced as executable package-boundary rules.
- SEC-01 has a reusable local/CI scanner before UI code arrives.
- The package is ready for a minimal renderer proof without importing any backend.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-atlas-tui-harness-intake-provenance/10.1-02-SUMMARY.md`.
</output>
