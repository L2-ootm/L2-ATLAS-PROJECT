---
phase: 10.2-agent-contract-tool-context-intelligence
plan: 03
type: tdd
wave: 2
depends_on: ["10.2-01"]
files_modified:
  - infra/migrations/0014_brain_graph.sql
  - packages/atlas-core/atlas_core/schemas/brain.py
  - services/agent-runtime/atlas_runtime/brain_service.py
  - services/agent-runtime/atlas_runtime/memory_router.py
  - services/agent-runtime/atlas_runtime/context_service.py
  - services/agent-runtime/tests/test_brain_service.py
  - services/agent-runtime/tests/test_context_retrieval_contract.py
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05]
user_setup: []
must_haves:
  truths:
    - "D-08: Brain entity neighborhoods and paths select evidence from wiki, runs, artifacts, observations, failures, and skills"
    - "D-09: retrieval records confidence, freshness, provenance, selected/rejected sources, budgets, and abstention"
    - "D-10: retrieved and tool-produced text is delimited as evidence and cannot become higher-priority instructions"
    - "Local retrieval adds <=250 ms p95 on the phase fixture corpus"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/brain_service.py"
      provides: "Bounded search/neighbors/path/explain graph queries"
      contains: "find_path"
    - path: "infra/migrations/0014_brain_graph.sql"
      provides: "Additive durable graph node/edge index"
      contains: "brain_nodes"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/memory_router.py"
      to: "services/agent-runtime/atlas_runtime/brain_service.py"
      via: "Brain candidate retrieval before evidence expansion"
      pattern: "Brain"
---

<objective>
Turn the existing cockpit graph concept into a bounded queryable Brain retrieval
spine and upgrade dynamic context to a typed provenance-bearing envelope.

Purpose: retrieve useful ATLAS evidence with explicit abstention and trust
boundaries instead of injecting broad markdown by availability.
Output: additive graph index, query service, retriever integration, and quality tests.
</objective>

<context>
@.planning/phases/10.2-agent-contract-tool-context-intelligence/10.2-CONTEXT.md
@services/agent-runtime/atlas_runtime/graph_service.py
@services/agent-runtime/atlas_runtime/memory_router.py
@services/agent-runtime/atlas_runtime/context_service.py
@services/wiki-runtime/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN additive Brain index and bounded query API</name>
  <files>infra/migrations/0014_brain_graph.sql, packages/atlas-core/atlas_core/schemas/brain.py, services/agent-runtime/atlas_runtime/brain_service.py, services/agent-runtime/tests/test_brain_service.py</files>
  <action>
    Write migration/service tests first. Add generic nodes and directed edges
    with stable IDs, entity type, project scope, source/version/freshness,
    confidence, and metadata JSON strings. Implement deterministic upsert/
    replacement and bounded `search`, `neighbors`, `path`, and `explain`
    operations with depth/result caps. Index fixtures for project/focus/goal/
    task/wiki/source/run/artifact/observation/skill relations. Reject traversal
    across project scope unless explicitly global.
  </action>
  <verify>Run migration and Brain service tests, including cycles, stale nodes, missing paths, and scope isolation.</verify>
  <done>The Brain is a queryable evidence graph rather than only a visualization payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN graph-routed retrieval envelope and abstention</name>
  <files>services/agent-runtime/atlas_runtime/memory_router.py, services/agent-runtime/atlas_runtime/context_service.py, services/agent-runtime/tests/test_context_retrieval_contract.py</files>
  <action>
    Test labeled relevant, irrelevant, stale, poisoned, duplicate, and
    over-budget corpora before implementation. Route context-dependent queries
    through Brain candidates, then blend graph distance, FTS5, semantic,
    recency, failure, skill, scope, and authority signals. Return a structured
    context envelope containing query, retrievers, selected/rejected sources,
    score, confidence, trust, timestamps, truncation, and token estimate.
    Preserve the existing markdown compatibility projection. Explicitly abstain
    below threshold and for context-free requests. Delimit all evidence and
    secret-redact before output.
  </action>
  <verify>Run context/router tests and a local p95 benchmark over the fixture corpus.</verify>
  <done>Context retrieval is selective, explainable, bounded, and safe against instruction injection.</done>
</task>

</tasks>

<verification>
- [ ] Search/neighbors/path/explain are deterministic and scope-safe.
- [ ] Required evidence precision >=0.80 and recall >=0.85 on labeled fixtures.
- [ ] Irrelevant-query abstention >=0.90.
- [ ] Every injected snippet has provenance/trust/freshness/confidence.
- [ ] Retrieval p95 <=250 ms and performs no external network call.
</verification>

<success_criteria>
- CTX-01..05 are implemented behind stable local contracts.
- Existing context callers retain a compatibility markdown view.
- No unrestricted graph walk or context dump can bypass budgets.
</success_criteria>

<output>Create `10.2-03-SUMMARY.md` after completion.</output>
