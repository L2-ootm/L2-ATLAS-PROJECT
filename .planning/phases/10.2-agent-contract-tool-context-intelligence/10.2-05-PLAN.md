---
phase: 10.2-agent-contract-tool-context-intelligence
plan: 05
type: tdd
wave: 4
depends_on: ["10.2-02", "10.2-03", "10.2-04"]
files_modified:
  - services/agent-runtime/atlas_runtime/evals/agent_contract.py
  - services/agent-runtime/tests/evals/fixtures/agent_contract_scenarios.json
  - services/agent-runtime/tests/evals/test_agent_contract_evals.py
  - scripts/agent-contract-eval.ps1
  - docs/verification/ATLAS_AGENT_CONTRACT_EVAL.md
autonomous: true
requirements: [EVAL-01, EVAL-02]
user_setup: []
must_haves:
  truths:
    - "D-12: deterministic critical safety checks outrank optional calibrated LLM judging"
    - "The reference set contains at least 30 curated prompt/tool/permission/retrieval/poisoning/subagent/resume scenarios"
    - "Promotion fails on any critical identity, schema, permission, provenance, secret, or resume regression"
    - "No external tracing or general AI evaluation framework becomes a runtime dependency"
  artifacts:
    - path: "services/agent-runtime/tests/evals/fixtures/agent_contract_scenarios.json"
      provides: "Versioned 30+ scenario reference dataset"
      contains: "scenario_id"
    - path: "scripts/agent-contract-eval.ps1"
      provides: "One-command deterministic promotion gate"
      contains: "pytest"
    - path: "docs/verification/ATLAS_AGENT_CONTRACT_EVAL.md"
      provides: "Recorded dimensions, thresholds, evidence, and promotion verdict"
      contains: "Promotion verdict"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/evals/agent_contract.py"
      to: "services/agent-runtime/tests/evals/fixtures/agent_contract_scenarios.json"
      via: "scenario runner"
      pattern: "scenario"
---

<objective>
Create the versioned reference dataset and promotion gate proving the Phase
10.2 prompt, tool, context, permission, and resume contracts.

Purpose: prevent subjective "looks intelligent" acceptance and turn agent
behavior changes into measurable releases.
Output: 30+ scenarios, deterministic evaluators, optional calibrated judge seam,
one-command gate, and recorded verdict.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the reference dataset and deterministic scenario runner</name>
  <files>services/agent-runtime/atlas_runtime/evals/agent_contract.py, services/agent-runtime/tests/evals/fixtures/agent_contract_scenarios.json, services/agent-runtime/tests/evals/test_agent_contract_evals.py</files>
  <action>
    Define failing evaluation tests and a JSON-stable scenario schema first.
    Add at least 30 curated cases matching the AI-SPEC distribution: identity/
    hierarchy conflicts, tool name/schema/workspace, permission ownership,
    Brain/wiki retrieval and abstention, stale/poisoned evidence, malicious tool
    output, child capability narrowing, compaction/resume, cancellation,
    uncertainty, and completion honesty. Implement deterministic evaluators for
    critical invariants, metrics aggregation, thresholds, and machine-readable
    failures. An LLM-judge adapter may exist only behind injection and cannot
    approve a deterministic failure.
  </action>
  <verify>Run the eval tests twice; scenario IDs, metrics, and verdict must be stable.</verify>
  <done>The agent contract has a reusable failure-oriented reference dataset.</done>
</task>

<task type="auto">
  <name>Task 2: Run promotion gate, performance checks, and record evidence</name>
  <files>scripts/agent-contract-eval.ps1, docs/verification/ATLAS_AGENT_CONTRACT_EVAL.md</files>
  <action>
    Add a PowerShell gate that runs targeted schema/prompt/catalog/Brain/context/
    replay/eval suites, secret scans, prompt-size checks, catalog-generation
    timing, retrieval p95, and deterministic threshold evaluation. Record exact
    commands, counts, metrics, known limitations, and promotion verdict. Keep
    optional Phoenix/OpenTelemetry absent and disabled; document only the seam.
  </action>
  <verify>Run the one-command gate from a clean process and require exit 0.</verify>
  <done>Phase 10.2 promotion is evidence-backed and reproducible without a model or network call.</done>
</task>

</tasks>

<verification>
- [ ] Dataset contains >=30 unique versioned scenarios in required categories.
- [ ] Critical deterministic dimensions are 100% and unapproved side effects/secrets are 0.
- [ ] Retrieval precision >=0.80, recall >=0.85, abstention >=0.90.
- [ ] Completion honesty >=95% or remains non-promotable.
- [ ] One-command gate records exact metrics and exits non-zero on injected regressions.
</verification>

<success_criteria>
- EVAL-01 and EVAL-02 are complete.
- Phase 10.2 contracts are ready for Phase 10.3 session/workspace binding.
- No network/model call is required for the blocking gate.
</success_criteria>

<output>Create `10.2-05-SUMMARY.md` after completion.</output>
