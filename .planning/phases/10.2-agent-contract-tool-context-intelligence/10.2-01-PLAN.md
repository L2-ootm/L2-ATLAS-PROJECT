---
phase: 10.2-agent-contract-tool-context-intelligence
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/atlas-core/atlas_core/schemas/agent_contract.py
  - packages/atlas-core/atlas_core/schemas/__init__.py
  - services/agent-runtime/atlas_runtime/prompt_compiler.py
  - services/agent-runtime/atlas_runtime/prompts/atlas_core.md
  - services/agent-runtime/tests/test_agent_contract_schema.py
  - services/agent-runtime/tests/test_prompt_compiler.py
  - services/agent-runtime/tests/golden/prompts/
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: prompt layer order and immutable ATLAS identity are deterministic and provider/surface independent"
    - "D-03: bootstrap and context are typed generated envelopes, not prose-authored authority"
    - "D-04: provider adapters cannot leak donor or foundation product identity into the ATLAS core"
    - "Stable prompt bytes remain unchanged when only dynamic context changes"
  artifacts:
    - path: "packages/atlas-core/atlas_core/schemas/agent_contract.py"
      provides: "Frozen JSON-stable prompt/bootstrap/context version contracts"
      contains: "SessionBootstrap"
    - path: "services/agent-runtime/atlas_runtime/prompt_compiler.py"
      provides: "Deterministic layered prompt compilation"
      contains: "compile_prompt"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/prompt_compiler.py"
      to: "services/agent-runtime/atlas_runtime/prompts/atlas_core.md"
      via: "versioned immutable identity source"
      pattern: "atlas_core"
---

<objective>
Freeze the typed ATLAS prompt contract and deterministic compiler before any
surface/session wiring.

Purpose: establish one cache-stable identity/policy/tool/workflow prefix plus
separate generated bootstrap and context envelopes.
Output: frozen schemas, compiler, provider adapters, and golden snapshots.
</objective>

<context>
@.planning/phases/10.2-agent-contract-tool-context-intelligence/10.2-CONTEXT.md
@.planning/phases/10.2-agent-contract-tool-context-intelligence/10.2-RESEARCH.md
@.planning/phases/10.2-agent-contract-tool-context-intelligence/10.2-AI-SPEC.md
@foundation/atlas-hermes/agent/prompt_builder.py
@foundation/atlas-hermes/agent/conversation_loop.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN frozen prompt and bootstrap schemas</name>
  <files>packages/atlas-core/atlas_core/schemas/agent_contract.py, packages/atlas-core/atlas_core/schemas/__init__.py, services/agent-runtime/tests/test_agent_contract_schema.py</files>
  <action>
    Write failing schema tests first. Add frozen Pydantic v2 models for prompt,
    tool-catalog and context-policy version references, trusted instruction
    sources, workspace/surface bootstrap identity, model identity, permission
    mode, capability names, and structured context source metadata. Public
    fields must remain JSON-stable strings/tuples/primitives with no Path,
    datetime object, circular import, or dict[str, Any] escape hatch. Validate
    semantic versions, SHA-256 values, non-empty IDs, and bounded budgets.
  </action>
  <verify>Run the new schema tests and the atlas-core suite.</verify>
  <done>Every prompt/session/context input has one frozen serializable contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN deterministic layered prompt compiler</name>
  <files>services/agent-runtime/atlas_runtime/prompt_compiler.py, services/agent-runtime/atlas_runtime/prompts/atlas_core.md, services/agent-runtime/tests/test_prompt_compiler.py, services/agent-runtime/tests/golden/prompts/</files>
  <action>
    Write golden tests before implementation for layer precedence, immutable
    ATLAS identity, provider-family adapter isolation, hashed workspace
    instructions, stable-prefix byte identity, structured bootstrap/context
    separation, secret rejection/redaction, and <=4,000 estimated stable tokens
    before tool schemas. Implement a pure compiler with no network/model call.
    The result must expose stable prompt bytes/hash/version and separately
    serialized bootstrap/context messages. Add golden fixtures for at least
    generic, OpenAI-compatible, Anthropic-compatible, TUI, and WebUI inputs.
  </action>
  <verify>Run prompt tests twice and prove golden output is byte-identical.</verify>
  <done>The same authoritative ATLAS prompt compiles deterministically across supported adapters.</done>
</task>

</tasks>

<verification>
- [ ] Frozen models pass JSON round-trip tests.
- [ ] Layer precedence and identity-conflict tests pass.
- [ ] Dynamic context changes do not alter stable prompt bytes/hash.
- [ ] Golden snapshots contain no donor/foundation operator identity.
- [ ] Stable prompt remains within the 4,000-token estimate budget.
</verification>

<success_criteria>
- PROMPT-01..04 are executable contracts, not prose only.
- Later plans can consume typed versions/bootstrap/context without inventing fields.
- No new dependency or framework is added.
</success_criteria>

<output>Create `10.2-01-SUMMARY.md` after completion.</output>
