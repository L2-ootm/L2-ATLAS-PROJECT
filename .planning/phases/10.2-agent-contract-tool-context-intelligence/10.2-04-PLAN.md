---
phase: 10.2-agent-contract-tool-context-intelligence
plan: 04
type: tdd
wave: 3
depends_on: ["10.2-01", "10.2-02", "10.2-03"]
files_modified:
  - infra/migrations/0015_agent_contract_snapshots.sql
  - services/agent-runtime/atlas_runtime/agent_contract_service.py
  - services/agent-runtime/atlas_runtime/context_service.py
  - services/agent-runtime/atlas_runtime/agents/native.py
  - services/agent-runtime/tests/test_agent_contract_service.py
  - services/agent-runtime/tests/test_compaction_resume_contract.py
autonomous: true
requirements: [PROMPT-05, CTX-03, CTX-04]
user_setup: []
must_haves:
  truths:
    - "D-11: prompt/catalog/context-policy/instruction/source versions are replayable without secrets or hidden reasoning"
    - "Compaction and resume preserve workspace, directives, task, changed files, tool/permission state, errors, uncertainty, verification, and next action"
    - "Existing mission execution can compile and persist a run contract without requiring a Phase 10.3 surface session"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/agent_contract_service.py"
      provides: "Compile, persist, load, compare, and replay run contracts"
      contains: "replay_contract"
    - path: "infra/migrations/0015_agent_contract_snapshots.sql"
      provides: "Additive immutable prompt/context/tool snapshot records"
      contains: "agent_contract_snapshots"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/agents/native.py"
      to: "services/agent-runtime/atlas_runtime/agent_contract_service.py"
      via: "run-bound contract preparation"
      pattern: "contract"
---

<objective>
Persist and replay the exact prompt/tool/context contract shaping a run, and
prove compaction/resume cannot silently lose safety-critical state.

Purpose: make agent behavior explainable and reproducible before persistent
multi-surface sessions arrive.
Output: additive snapshots, service API, native-run seam, and resume tests.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN immutable run contract persistence and replay</name>
  <files>infra/migrations/0015_agent_contract_snapshots.sql, services/agent-runtime/atlas_runtime/agent_contract_service.py, services/agent-runtime/tests/test_agent_contract_service.py</files>
  <action>
    Test first for compile/persist/load/replay, canonical hashes, duplicate
    idempotency, source IDs, and secret canaries. Persist prompt version/hash,
    tool catalog version/hash, context policy, instruction sources, bootstrap,
    selected/rejected context metadata, and redacted rendered messages. Do not
    persist unrestricted chain-of-thought. Make snapshot rows immutable and
    run-linked; mismatched replay versions return an explicit compatibility error.
  </action>
  <verify>Run fresh-DB, migration, round-trip, redaction, and replay tests.</verify>
  <done>A run can explain and reconstruct the contract that shaped it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN native preparation seam and compaction/resume invariants</name>
  <files>services/agent-runtime/atlas_runtime/context_service.py, services/agent-runtime/atlas_runtime/agents/native.py, services/agent-runtime/tests/test_compaction_resume_contract.py</files>
  <action>
    Add the smallest backwards-compatible seam allowing native mission runs to
    prepare/persist the Phase 10.2 contract before foundation execution. Do not
    introduce surface-session fields early; use a generated CLI/mission
    bootstrap compatible with Phase 10.3 extension. Add deterministic resume
    snapshot tests covering operator directives, workspace/project, current
    task, modified files, tool/approval state, unresolved errors, child work,
    verification, uncertainty, prompt/catalog/context versions, and next action.
    Denied permissions and failed verification must never be summarized away.
  </action>
  <verify>Run native agent, run executor, context, snapshot, and compaction/resume tests.</verify>
  <done>Current execution can adopt the contract without pre-implementing the surface protocol.</done>
</task>

</tasks>

<verification>
- [ ] Snapshot rows are immutable, redacted, run-linked, and JSON-stable.
- [ ] Replay either reconstructs exact hashes or reports explicit incompatibility.
- [ ] Secret canaries never persist.
- [ ] Resume preserves every critical field in the AI-SPEC.
- [ ] Existing runtime signatures and non-surface callers remain compatible.
</verification>

<success_criteria>
- PROMPT-05 is implemented and CTX provenance is replayable.
- Phase 10.3 receives an extensible contract rather than a speculative session rewrite.
</success_criteria>

<output>Create `10.2-04-SUMMARY.md` after completion.</output>
