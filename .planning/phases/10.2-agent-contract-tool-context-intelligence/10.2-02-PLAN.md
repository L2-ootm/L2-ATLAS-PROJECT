---
phase: 10.2-agent-contract-tool-context-intelligence
plan: 02
type: tdd
wave: 2
depends_on: ["10.2-01"]
files_modified:
  - packages/atlas-core/atlas_core/schemas/agent_contract.py
  - services/agent-runtime/atlas_runtime/tool_catalog.py
  - services/agent-runtime/atlas_runtime/tools/registry.py
  - services/agent-runtime/tests/test_tool_catalog.py
  - services/agent-runtime/tests/test_tool_capability_conformance.py
  - docs/contracts/ATLAS_TOOL_CAPABILITIES.json
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04, TOOL-05]
user_setup: []
must_haves:
  truths:
    - "D-05: every available tool normalizes to one frozen ToolCapability record"
    - "D-06: catalog output is generated from registries/manifests and rejects duplicate names or incomplete risk/side-effect data"
    - "D-07: child capabilities only narrow and tool errors normalize fail-closed"
    - "Catalog generation performs no paid/network probe and is cached by source generation/version"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/tool_catalog.py"
      provides: "ATLAS/Hermes/MCP capability normalization and catalog versioning"
      contains: "build_tool_catalog"
    - path: "docs/contracts/ATLAS_TOOL_CAPABILITIES.json"
      provides: "Generated review artifact for shipped capabilities"
      contains: "catalog_version"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/tool_catalog.py"
      to: "foundation/atlas-hermes/tools/registry.py"
      via: "read-only registry adapter"
      pattern: "ToolRegistry"
---

<objective>
Generate one complete, versioned tool capability catalog from the ATLAS and
Hermes registry sources and prove its policy/audit/error semantics.

Purpose: eliminate split tool authority before surfaces render or invoke tools.
Output: normalized catalog, generated review artifact, and conformance suite.
</objective>

<context>
@.planning/phases/10.2-agent-contract-tool-context-intelligence/10.2-CONTEXT.md
@services/agent-runtime/atlas_runtime/tools/registry.py
@foundation/atlas-hermes/tools/registry.py
@foundation/atlas-hermes/model_tools.py
@packages/atlas-core/atlas_core/schemas/tool.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN capability normalization and catalog generation</name>
  <files>packages/atlas-core/atlas_core/schemas/agent_contract.py, services/agent-runtime/atlas_runtime/tool_catalog.py, services/agent-runtime/tests/test_tool_catalog.py</files>
  <action>
    Test first for schema, canonical name, aliases, category, input/output
    schema, permission/risk, workspace/network scope, side effects, timeout,
    cancellation, idempotency, result cap, approval policy, audit events, and
    renderer. Implement adapters for injected ATLAS manifests, injected Hermes
    registry snapshots, and MCP descriptors. Production loading may read the
    live registries but tests must not import/probe optional external tools.
    Reject duplicate canonical names, missing risk/side effects, incompatible
    schemas, or non-deterministic ordering. Hash canonical JSON for versioning.
  </action>
  <verify>Run catalog tests twice; hashes and ordering must match.</verify>
  <done>Every capability source compiles into one deterministic catalog.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tool conformance, narrowing, normalized errors, and generated contract</name>
  <files>services/agent-runtime/tests/test_tool_capability_conformance.py, services/agent-runtime/atlas_runtime/tools/registry.py, docs/contracts/ATLAS_TOOL_CAPABILITIES.json</files>
  <action>
    Add table-driven conformance tests for malformed/unavailable/unknown/
    disallowed/wrong-workspace/stale calls, bounded results, cancellation,
    timeout metadata, audit mapping, and parent-child capability narrowing.
    Do not rewrite tool execution in this phase; add the smallest catalog/error
    normalization seam required for existing `tool_service` and future Phase
    10.3 event wiring. Generate the JSON review artifact from the tested
    catalog, never by hand.
  </action>
  <verify>Run registry, tool-service, policy, catalog, and conformance tests.</verify>
  <done>Tool semantics are complete enough for every surface to render and enforce consistently.</done>
</task>

</tasks>

<verification>
- [ ] No duplicate or unclassified tool capability exists.
- [ ] Catalog hash is deterministic and changes when source metadata changes.
- [ ] Parent/child narrowing cannot widen workspace, network, risk, or approval scope.
- [ ] Critical error cases fail closed with normalized machine-readable reasons.
- [ ] Generated JSON matches the runtime catalog.
</verification>

<success_criteria>
- TOOL-01..05 are covered without a parallel handler registry.
- Catalog generation adds no startup network probe or framework dependency.
</success_criteria>

<output>Create `10.2-02-SUMMARY.md` after completion.</output>
