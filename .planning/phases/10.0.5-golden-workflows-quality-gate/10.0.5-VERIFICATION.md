---
phase: 10.0.5-golden-workflows-quality-gate
status: passed
date: 2026-06-23
score: 3/3 success criteria
human_verification: 1 (live cockpit screenshots — deferred to operator UAT, documented)
---

# Phase 10.0.5 — Golden Workflows & Quality Gate — VERIFICATION

Goal-backward verification against the ROADMAP success criteria. Phase goal:
**"ATLAS survives repeated demos on the three wedge use cases."**

## SC1 — Three workflows run 3× with consistent output ✅

> Repo Triage, Research Brief, Self-Review each run 3× with consistent output
> (artifact + audit trail + wiki entries; Self-Review never writes without approval).

**Verified by `tests/test_golden_workflows_smoke.py` (3 tests green):**
- `test_each_golden_workflow_runs_three_times_with_consistent_structure` — repo_triage +
  research_brief each: artifact row accrues per call, wiki page exists,
  `golden_workflow_started`/`completed` (+ `tool_requested`/`tool_completed` for repo_triage)
  in the audit trail; cross-rep structural consistency (same artifact_type, non-null sha256).
- `test_self_review_three_runs_never_auto_writes` — self_review yields **exactly 3** pending
  approvals across 3 calls and the proposed file never lands on disk (`==`, not `>=`).
- `test_full_3x3_sequence_raises_no_exceptions` — full 9-dispatch sequence runs clean.

Deterministic-orchestrator design (not LLM templates) is what makes this reproducible in
mock mode (the mock provider emits no structural output — see CONTEXT.md grounding).

## SC2 — Repeated failures fixed; smoke test + demo-reset exist ✅

- **Smoke test:** `pytest tests/test_golden_workflows_smoke.py` — the SC1/SC2 gate, green.
- **Demo-reset:** `atlas golden reset` (in `cli/golden.py`) — dry-run by default, `--confirm`
  to delete; scoped to golden-tagged rows only (`golden/` artifacts, `repo-triage-*`/
  `research-brief-*`/`self-review-*` wiki slugs, `golden_workflow:*` approval reasons).
  `tests/test_cli_golden.py::test_cli_golden_reset_confirm_is_scoped` proves co-existing
  non-golden artifact/wiki/approval rows survive a `--confirm` reset.

## SC3 — Sample data, screenshots, known-failures list ✅ (1 item deferred, documented)

- **Sample data:** 3 real artifacts under `sample-data/` (generated by running the shipped
  workflows against this repo; secret-scanned clean).
- **Known-failures:** `docs/known-failures.md` — documents all 3 deferred items
  (live screenshots → UAT, mock-mode determinism by design, no web_fetch variant) + edge cases.
- **Runbook:** `docs/golden-workflows.md` — complete operator runbook.
- **human_verification (deferred):** live cockpit/Playwright screenshots of each workflow
  require a full-stack boot (`atlas-local-run-recipe`) and are deferred to operator UAT —
  documented honestly in known-failures.md rather than silently omitted. Sample-data
  artifacts satisfy the "see expected shape without running ATLAS" intent.

## Test evidence

- Golden-workflow suite: **34 passed** (`test_golden_workflow_service`, `_repo_triage`,
  `_research_brief`, `_self_review`, `_smoke`, `test_cli_golden`).
- Full agent-runtime suite: **369 passed, 1 skipped, 1 failed** — the single failure is the
  known pre-existing `claude_agent_sdk`-not-installed env issue (unrelated; documented).

## Verdict: PASSED (3/3 SC). Plan-check was PASS-WITH-CONCERNS (no blockers); concerns were
cosmetic and did not materialize in execution.
