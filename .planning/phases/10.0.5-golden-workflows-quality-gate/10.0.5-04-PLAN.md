---
phase: 10.0.5-golden-workflows-quality-gate
plan: 04
type: tdd
wave: 4
depends_on: ["10.0.5-03"]
files_modified:
  - services/agent-runtime/tests/test_golden_workflows_smoke.py
  - services/agent-runtime/atlas_runtime/cli/golden.py
autonomous: true
requirements: [GW-SMOKE-01, GW-RESET-01]
must_haves:
  truths:
    - "A single pytest command runs each of the 3 golden workflows 3x in mock-mode-equivalent conditions (deterministic, no network, no LLM) and asserts structure (artifact exists, audit trail has the expected event set, wiki entry exists; self_review never auto-executes)"
    - "A demo-reset command clears only golden-workflow-tagged rows (artifacts under golden/, wiki pages with golden-workflow slugs, pending/rejected/executed tool_approvals tagged reason='golden_workflow:*') and leaves all other operator data untouched"
  artifacts:
    - path: "services/agent-runtime/tests/test_golden_workflows_smoke.py"
      provides: "the SC1/SC2 quality-gate smoke test"
      contains: "def test_each_golden_workflow_runs_three_times_with_consistent_structure"
    - path: "services/agent-runtime/atlas_runtime/cli/golden.py"
      provides: "atlas golden reset --confirm command (extends the wave-3 file)"
      contains: "def golden_reset"
  key_links:
    - from: "test_golden_workflows_smoke.py"
      to: "golden_workflow_registry.dispatch"
      via: "calls dispatch 3x per workflow id, asserts structure not byte-equality"
      pattern: "dispatch\\("
    - from: "cli/golden.py golden_reset"
      to: "DELETE FROM artifacts WHERE path LIKE 'golden/%'"
      via: "scoped demo-reset, never a blanket DELETE"
      pattern: "LIKE 'golden/%'"
---

<objective>
Close SC2 (repeated failures fixed; a smoke test and demo-reset path exist) by
adding the single pytest entry point that runs all three golden workflows 3x each
under deterministic mock-equivalent conditions and asserts D-2's structural
contract, plus a demo-reset CLI command scoped ONLY to golden-workflow-tagged rows
so repeated demos never accumulate cruft and never touch unrelated operator data.

Purpose: this is the actual quality gate — the mechanism that proves "ATLAS
survives repeated demos" rather than asserting it by claim.
Output: `test_golden_workflows_smoke.py` (the gate), `golden_reset` CLI command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.0.5-golden-workflows-quality-gate/10.0.5-CONTEXT.md
@services/agent-runtime/atlas_runtime/golden_workflow_registry.py
@services/agent-runtime/atlas_runtime/golden_workflow_service.py
@services/agent-runtime/atlas_runtime/tool_service.py
@services/agent-runtime/atlas_runtime/cli/golden.py
@services/agent-runtime/atlas_runtime/cli/tools.py
@services/agent-runtime/atlas_runtime/mission_service.py
@services/agent-runtime/tests/conftest.py
</context>

<interfaces>
<!-- From wave-3 golden_workflow_registry.py: -->
```python
def list_workflows() -> list[GoldenWorkflow]   # 3 entries: repo_triage, research_brief, self_review
def dispatch(workflow_id: str, *, conn, lock, workspace_root: str, wiki_dir: pathlib.Path, topic: str="atlas") -> dict
```

<!-- mission_service.purge_expired_archives (the closest existing precedent for a
     scoped multi-table DELETE — mirror its "collect ids, then delete dependents,
     then delete the parent, inside a single lock" shape, NOT its actual predicate): -->
```python
def purge_expired_archives(conn, lock, *, now=None) -> int
```

<!-- tool_service.list_approvals (for the reset's tool_approvals cleanup target): -->
```python
def list_approvals(conn, *, status=None) -> list[ToolApproval]
```
ToolApproval rows created by self_review carry `reason="golden_workflow:self_review"`
(set in wave-3 Task 1's `tool_service.invoke(..., reason="golden_workflow:self_review")`
call) — this is the scoping tag for demo-reset, not a new column.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — the 3x3 structural-assert smoke test (the quality gate)</name>
  <files>services/agent-runtime/tests/test_golden_workflows_smoke.py</files>
  <read_first>
    services/agent-runtime/atlas_runtime/golden_workflow_registry.py (dispatch signature, the 3 ids);
    services/agent-runtime/atlas_runtime/audit_service.py get_events_for_run (assert event_type sets);
    services/agent-runtime/atlas_runtime/tool_service.py list_approvals (self_review's pending-approval assertion);
    .planning/phases/10.0.5-golden-workflows-quality-gate/10.0.5-CONTEXT.md D-2 (re-read the exact structural contract: artifact produced, audit trail emitted, wiki entry written; self_review yields PENDING, no write executed)
  </read_first>
  <behavior>
    - Test `test_each_golden_workflow_runs_three_times_with_consistent_structure`:
      for each of `repo_triage`, `research_brief` (workflows that should leave a
      committed artifact+wiki entry on every call), loop 3 times calling
      `golden_workflow_registry.dispatch(wf_id, conn=db, lock=lock,
      workspace_root=str(tmp_path), wiki_dir=tmp_path/"wiki", topic="atlas")` and
      after EACH call assert: (a) `len(db.execute("SELECT 1 FROM artifacts WHERE
      run_id=?", (run_id,)).fetchall()) >= call_number` (artifacts accumulate, one
      new row per call — not deduplicated away); (b) the audit trail for that run
      contains `golden_workflow_started` and `golden_workflow_completed` AND (for
      repo_triage specifically) `tool_requested`/`tool_completed`; (c) at least one
      row exists in `wiki_pages` matching the expected slug prefix.
    - Test (same function or a sibling): for `self_review`, loop 3 times calling
      `dispatch("self_review", ...)` and after EACH call assert the returned dict's
      `status == "pending"` AND no NEW file exists on disk for that call's proposed
      path AND `tool_service.list_approvals(db, status="pending")` grows by exactly
      1 per call (3 total after the loop) — this is the single most important
      assertion in the entire phase per D-1/D-2; do not weaken it to "at least 1".
    - Test: run the full 3x3 sequence (9 dispatch calls total: 3 workflows x 3
      reps) and assert NO exception propagates from any call — "repeated failures
      fixed" means the gate itself must be green, not merely present.
    - Test: assert structural consistency ACROSS the 3 reps of the SAME workflow —
      e.g. every repo_triage artifact's content (read back via
      `db.execute("SELECT path FROM artifacts ...")` then reading the file, or by
      comparing recorded `artifact_type` and a non-empty `sha256` across all 3 rows)
      has the same `artifact_type` and a non-null `sha256` each time — this is the
      "structure not byte-identical" assertion from D-2 (do NOT compare
      sha256 values to each other for equality — different timestamps in the
      generated content make them differ; assert presence/shape only).
  </behavior>
  <action>
    Implement the smoke test using only the `db`/`lock`/`tmp_path` pytest fixtures
    (db/lock from conftest.py, tmp_path is pytest-builtin). Use
    `golden_workflow_registry` and `tool_service` directly — no subprocess/CLI
    invocation in this test (that is covered separately by `test_cli_golden.py`
    from wave 3). Keep the test deterministic: no `datetime.now()` comparisons
    beyond existence checks, no sleeps, no reliance on wall-clock ordering beyond
    what SQLite's own row order naturally provides.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -m pytest tests/test_golden_workflows_smoke.py -v 2>&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    `pytest tests/test_golden_workflows_smoke.py -x` exits 0. The self_review
    pending-count assertion is exact (==3 after 3 calls), not a loose ">=1" check.
  </acceptance_criteria>
  <done>The smoke test exists, passes, and is the single command that proves SC1's "each run 3x with consistent output" claim for all three workflows including self_review's never-auto-write guarantee.</done>
</task>

<task type="auto">
  <name>Task 2: Demo-reset — scoped cleanup of golden-workflow-tagged rows</name>
  <files>services/agent-runtime/atlas_runtime/cli/golden.py</files>
  <read_first>
    services/agent-runtime/atlas_runtime/mission_service.py purge_expired_archives (the multi-table scoped-DELETE shape: collect ids first, delete dependents, delete parent, all inside one lock);
    services/agent-runtime/atlas_runtime/cli/tools.py (the _get_connection/_get_lock + --json + typer.Exit(1) CLI idiom already used in this same file from wave 3);
    infra/migrations/0013_tool_approvals.sql (tool_approvals.reason column — the scoping tag for self_review rows)
  </read_first>
  <action>
    Add `golden_reset` to `cli/golden.py` as `@golden_app.command("reset")`: a
    `--confirm` flag (default False; without it, print what WOULD be deleted and
    exit 0 without deleting — dry-run-by-default is the safety rail for a
    destructive command) and a `--json` flag. The actual delete (only when
    `--confirm` is passed) runs inside `with lock: with conn:` and executes, in
    order: `DELETE FROM artifacts WHERE path LIKE 'golden/%'`; `DELETE FROM
    wiki_pages WHERE slug LIKE 'repo-triage-%' OR slug LIKE 'research-brief-%' OR
    slug LIKE 'self-review-%'`; `DELETE FROM tool_approvals WHERE reason LIKE
    'golden_workflow:%'`. Each DELETE's rowcount is collected and returned/printed
    as a summary dict `{"artifacts_deleted": n, "wiki_pages_deleted": n,
    "tool_approvals_deleted": n}`. Do NOT touch `audit_events`, `missions`, `runs`,
    or any row outside these three scoped predicates — the operator's run/audit
    history for the demo stays intact (it IS the audit trail SC1 requires to exist
    "consistently"; resetting it would defeat the point). Add a short docstring
    explaining the dry-run default and the exact scoping predicates, referencing
    the threat (accidental deletion of real operator data) this guards against.
  </action>
  <verify>
    <automated>cd services/agent-runtime &amp;&amp; python -c "
import pathlib, sqlite3, threading
import sys
sys.path.insert(0, '.')
" &amp;&amp; python -m pytest tests/test_cli_golden.py -k reset -v 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    Calling `golden_reset` without `--confirm` deletes nothing (verify by counting
    rows before/after — unchanged) and reports a would-delete summary. Calling it
    WITH `--confirm` after seeding golden-tagged rows via a `dispatch()` call
    removes exactly those rows and leaves any non-golden artifact/wiki_page/
    tool_approval row (seeded separately in the test with a non-golden path/slug/
    reason) untouched — add this differential test to
    `services/agent-runtime/tests/test_cli_golden.py` if it does not already exist
    from wave 3 (extend, do not duplicate the file).
  </acceptance_criteria>
  <done>`atlas golden reset` exists, defaults to a safe dry-run, and on --confirm deletes only golden-workflow-tagged rows, proven by a differential test against co-existing non-golden rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| golden_reset --confirm -> real ~/.atlas/atlas.db | a destructive command; must never delete non-golden-tagged rows (real operator missions/runs/audit/approvals) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-1005-10 | Repudiation / Data Loss | golden_reset scope | mitigate | every DELETE predicate is an explicit LIKE-prefix tied to the golden-workflow naming convention (golden/ paths, repo-triage-/research-brief-/self-review- slugs, golden_workflow: reasons) — never a bare DELETE FROM with no WHERE |
| T-1005-11 | Denial of Service | golden_reset accidental invocation | mitigate | dry-run is the default; deletion requires explicit --confirm |
| T-1005-12 | Tampering | smoke test producing false-green | mitigate | self_review pending-count assertion is exact equality (==3), not a loose lower bound, so a regression that silently auto-executes the write would fail the count check |
</threat_model>

<verification>
`cd services/agent-runtime && python -m pytest tests/test_golden_workflows_smoke.py tests/test_cli_golden.py -v` exits 0.
</verification>

<success_criteria>
One pytest command proves all 3 golden workflows survive 3 repeated runs with
correct structure; one CLI command resets demo state without risking real operator
data.
</success_criteria>

<output>
Create `.planning/phases/10.0.5-golden-workflows-quality-gate/10.0.5-04-SUMMARY.md` when done
</output>
