---
phase: 10.0.5-golden-workflows-quality-gate
plan: 05
type: execute
wave: 5
depends_on: ["10.0.5-04"]
files_modified:
  - docs/golden-workflows.md
  - docs/known-failures.md
  - .planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/repo-triage-sample.md
  - .planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/research-brief-sample.md
  - .planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/self-review-sample.md
autonomous: true
requirements: [GW-DOCS-01]
must_haves:
  truths:
    - "An operator unfamiliar with the codebase can read docs/golden-workflows.md and run all 3 workflows plus the smoke test and demo-reset without reading source code"
    - "Sample artifact output for each of the 3 workflows exists on disk so a reader can see expected shape without running ATLAS"
    - "Known limitations (mock-mode-only determinism, no live screenshots, no web_fetch Research Brief variant) are documented honestly, not glossed over"
  artifacts:
    - path: "docs/golden-workflows.md"
      provides: "runbook: what each workflow does, how to trigger it (CLI), how the smoke test and demo-reset work"
      contains: "## Repo Triage"
    - path: "docs/known-failures.md"
      provides: "the documented known-failures list required by SC3"
      contains: "## Known Limitations"
  key_links:
    - from: "docs/golden-workflows.md"
      to: "services/agent-runtime/atlas_runtime/cli/golden.py"
      via: "documents the exact `atlas golden ...` command surface shipped in wave 3/4"
      pattern: "atlas golden"
---

<objective>
Close SC3 (sample data, screenshots, and a documented known-failures list exist) by
writing the operator-facing runbook for the three golden workflows, capturing
representative sample artifact output for each, and writing an honest
known-failures/limitations document — mirroring the existing `docs/tools.md` style
established in Phase 10.0.4.

Purpose: a developer evaluating ATLAS for the public launch wedge must be able to
read this doc and trust the golden-workflow claim without re-deriving it from code.
Output: `docs/golden-workflows.md`, `docs/known-failures.md`, 3 sample artifact
files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.0.5-golden-workflows-quality-gate/10.0.5-CONTEXT.md
@docs/tools.md
@services/agent-runtime/atlas_runtime/cli/golden.py
@services/agent-runtime/atlas_runtime/golden_workflow_registry.py
@services/agent-runtime/atlas_runtime/golden_workflows/repo_triage.py
@services/agent-runtime/atlas_runtime/golden_workflows/research_brief.py
@services/agent-runtime/atlas_runtime/golden_workflows/self_review.py
@services/agent-runtime/tests/test_golden_workflows_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate real sample artifacts by running the workflows once</name>
  <files>
    .planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/repo-triage-sample.md,
    .planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/research-brief-sample.md,
    .planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/self-review-sample.md
  </files>
  <read_first>
    services/agent-runtime/atlas_runtime/golden_workflow_registry.py (dispatch signature);
    services/agent-runtime/atlas_runtime/golden_workflows/repo_triage.py,research_brief.py,self_review.py (the actual artifact content format each produces — read the real implementation, do not invent placeholder content)
  </read_first>
  <action>
    Write a small throwaway Python snippet (run via `python -c` or a temp script in
    the scratchpad, NOT committed) that: opens an in-memory or temp-file SQLite DB
    with all `infra/migrations/*.sql` applied (same pattern as
    `tests/conftest.py`'s `db` fixture), seeds 2-3 wiki pages so Research Brief has
    real matches, then calls `golden_workflow_registry.dispatch("repo_triage", ...)`
    against this repository's own root as `workspace_root` (real README, real repo
    structure — this IS the actual demo content), `dispatch("research_brief",
    topic="atlas", ...)`, and `dispatch("self_review", ...)`. Copy the resulting
    artifact content (read back from the `artifacts` table path, or from the dict
    returned by dispatch if it embeds the body) into the three sample-data files
    verbatim — these are REAL generated output, not hand-written mockups. For
    self_review, since the write is pending (never executed), the sample file is
    the review-note CONTENT that was proposed (available from the `args` stored on
    the `ToolApproval`, JSON-decoded), annotated with a header noting "Proposed —
    pending approval, not yet written" so a reader does not mistake it for an
    executed write.
  </action>
  <verify>
    <automated>ls "C:/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT/.planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/" | grep -c sample</automated>
  </verify>
  <acceptance_criteria>
    `ls .planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/` shows
    exactly 3 files, each non-empty (`wc -l` &gt; 5 lines), each containing content
    traceable to the real workflow implementation (e.g. repo-triage-sample.md
    contains an actual top-level file listing of this repository, not a generic
    placeholder).
  </acceptance_criteria>
  <done>3 real (not fabricated) sample artifact files exist, generated by actually running the shipped workflows once against this repo.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/golden-workflows.md and docs/known-failures.md</name>
  <files>docs/golden-workflows.md, docs/known-failures.md</files>
  <read_first>
    docs/tools.md (the exact doc style/structure to mirror: safety model up top, a table of the 3 workflows, a CLI command block, an audit-events naming note at the end);
    services/agent-runtime/atlas_runtime/cli/golden.py (the real, final command surface — list/run/reset with their actual flags);
    .planning/phases/10.0.5-golden-workflows-quality-gate/10.0.5-CONTEXT.md deferred section (the 3 deferred ideas become the seed content for known-failures.md, not omitted)
  </read_first>
  <action>
    Write `docs/golden-workflows.md` mirroring `docs/tools.md`'s structure: an
    intro paragraph stating the goal ("ATLAS survives repeated demos on 3 wedge use
    cases"); a table of the 3 workflows (id/risk/what it produces); a `## Repo
    Triage`, `## Research Brief`, `## Self-Review` section each describing what it
    reads, what it writes, and linking to the matching sample-data file from Task
    1; a `## Running the workflows` section with the real `atlas golden list`,
    `atlas golden run <id> --workspace <path>`, `atlas golden run self_review
    --workspace <path>` (then `atlas tools approve <id>` to execute the proposed
    write, `atlas tools reject <id>` to discard it — cross-reference docs/tools.md
    here since approval lives in that surface) command blocks; a `## Quality gate`
    section documenting `pytest services/agent-runtime/tests/test_golden_workflows_smoke.py`
    as the SC1/SC2 proof and `atlas golden reset --confirm` as the demo-reset path
    (state the dry-run-by-default safety rail explicitly). Write
    `docs/known-failures.md` with a `## Known Limitations` section listing, taken
    directly from CONTEXT.md's `<deferred>` block: (1) live Playwright/cockpit
    screenshots are deferred to operator UAT (needs full-stack boot per the
    `atlas-local-run-recipe` memory) — state this explicitly rather than silently
    omitting screenshots from SC3; (2) real (non-mock) LLM consistency runs are
    inherently non-deterministic — mock mode is the demo-stable path, and this is
    by design, not a bug; (3) Research Brief has no `web_fetch`-backed variant yet
    — offline codex search only. Add any ADDITIONAL failure discovered while
    actually running the workflows in Task 1 (e.g. if Research Brief's "no matches"
    path or Repo Triage's "no README" path triggered unexpectedly during sample
    generation, document it here as a known edge case, not silently).
  </action>
  <verify>
    <automated>test -f "C:/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT/docs/golden-workflows.md" &amp;&amp; test -f "C:/Users/Davi/Desktop/Projects/L2-ATLAS-PROJECT/docs/known-failures.md" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    Both files exist and are non-empty. `docs/known-failures.md` explicitly
    mentions all 3 deferred items from CONTEXT.md (screenshots, non-mock LLM
    non-determinism, no web_fetch Research Brief variant) — grep each phrase to
    confirm presence rather than asserting by inspection alone.
  </acceptance_criteria>
  <done>docs/golden-workflows.md is a complete operator runbook; docs/known-failures.md honestly documents all known limitations including the 3 explicitly deferred items from CONTEXT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| sample-data files -> public repo | sample content is real repo-triage output of THIS repository — confirm no secrets/credentials leak into the committed sample (the repo's own README/structure is already public, but re-check before commit per the repo-hygiene precedent from Phase 10.0.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-1005-13 | Information Disclosure | sample-data artifact content | mitigate | manually review the 3 generated sample files before commit for any accidental path/secret leakage beyond what is already public in the repo (re-uses the Phase 10.0.1 secret-scan discipline) |
</threat_model>

<verification>
`test -f docs/golden-workflows.md && test -f docs/known-failures.md` and
`ls .planning/phases/10.0.5-golden-workflows-quality-gate/sample-data/` shows 3 files.
</verification>

<success_criteria>
SC3 is closed: sample data exists (real, not fabricated), and a known-failures list
honestly documents every deferred/limited item from CONTEXT.md plus anything
discovered while generating the samples.
</success_criteria>

<output>
Create `.planning/phases/10.0.5-golden-workflows-quality-gate/10.0.5-05-SUMMARY.md` when done
</output>
