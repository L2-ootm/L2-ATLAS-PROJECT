---
phase: 10.4-global-config-auth-model-control-plane
plan: 03
type: tdd
wave: 2
depends_on: ["10.4-01"]
files_modified:
  - services/agent-runtime/atlas_runtime/config_service.py
  - services/agent-runtime/atlas_runtime/control_plane_service.py
  - services/agent-runtime/atlas_runtime/cli/config.py
  - services/agent-runtime/tests/test_config_control_plane.py
  - services/agent-runtime/tests/test_config_cli.py
autonomous: true
requirements: [CFG-03, CFG-04, CFG-05, CFG-06, MOD-02, UX-01]
user_setup: []

must_haves:
  truths:
    - "One masked snapshot preserves existing top-level config fields and adds schema/revision, per-setting configured/effective/source/validation/restart/remediation metadata"
    - "All CLI writes, including set/import/setup, flow through the same expected-revision patch/replace transaction"
    - "Every successful config mutation emits one redacted config_change event identifying source surface/session and reload semantics"
    - "Hot fields are visible to the next read/new execution without restart; active SurfaceSession and RunContractSnapshot model identity never changes"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/control_plane_service.py"
      provides: "Masked config snapshot + audit-backed patch facade"
      contains: "def get_config_snapshot"
    - path: "services/agent-runtime/atlas_runtime/cli/config.py"
      provides: "Stable JSON GET/PATCH CLI contract used by gateway"
      contains: "def patch"
  key_links:
    - from: "cli/config.py"
      to: "control_plane_service"
      via: "thin JSON adapter"
      pattern: "control_plane_service"
    - from: "control_plane_service.patch"
      to: "audit_service.emit"
      via: "config_change event after successful file commit"
      pattern: "config_change"
---

<objective>
Expose the versioned config transaction as one backward-compatible masked GET/PATCH contract with
field metadata, audit events, and active-session stability.
</objective>

<context>
@.planning/phases/10.4-global-config-auth-model-control-plane/10.4-CONTEXT.md
@.planning/phases/10.4-global-config-auth-model-control-plane/10.4-PATTERNS.md
@.planning/phases/10.3-shared-surface-session-workspace-protocol/10.3-05-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Per-setting source/effective/reload metadata</name>
  <files>services/agent-runtime/atlas_runtime/control_plane_service.py, services/agent-runtime/tests/test_config_control_plane.py</files>
  <behavior>
    - Snapshot keeps `provider`, `runtime`, `gateway`, `cockpit`, `context`, `permission`, `modules`, and `mock_mode` top-level.
    - Each supported dotted setting has configured_json, effective_json, source, validation_status, restart_required, and remediation.
    - provider.model reports focus/session override source when supplied; provider.api_key reports reference/presence only.
    - Gateway/cockpit ports are restart-required; provider/model/context/permission are new-session or hot as documented.
  </behavior>
  <action>
    Write failing snapshot tests. Implement a declarative `_SETTING_METADATA` table and
    `get_config_snapshot(cfg=None, *, focus_framework=None)` that derives effective values through
    the same `resolve_provider` logic. Preserve current top-level JSON compatibility for the cockpit.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_config_control_plane.py -k snapshot -q</automated>
  </verify>
  <done>The masked read contract is complete and self-explaining.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Audited patch facade and active-session invariants</name>
  <files>services/agent-runtime/atlas_runtime/control_plane_service.py, services/agent-runtime/tests/test_config_control_plane.py</files>
  <behavior>
    - patch accepts expected_revision + changes + optional source surface/session and returns the new snapshot.
    - Successful patch emits `config_change` with masked before/after, changed paths, revision, and reload metadata.
    - Conflict/validation failures emit no success event and do not mutate bytes.
    - A persisted SurfaceSession model and RunContractSnapshot remain byte/field identical after provider/model patch.
    - If audit emission fails after the file commit, the API raises `config_audit_failed` with the committed revision and remediation; it never claims the file was rolled back.
  </behavior>
  <action>
    Add failing audit/session-stability tests. Implement the facade using
    `config_service.patch_config`, the `config_change` Literal from Plan 01,
    `ensure_operator_run`, and `audit_service.emit` after the file lock releases. Build diffs from
    masked values only. Explicitly test a config model change against 10.3 session/snapshot records.
    If audit fails, surface the committed revision and an explicit reconciliation remediation.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_config_control_plane.py -k "audit or session or conflict" -q</automated>
  </verify>
  <done>Writes are audited and cannot retroactively alter active-session identity.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: CLI GET/PATCH and legacy command convergence</name>
  <files>services/agent-runtime/atlas_runtime/cli/config.py, services/agent-runtime/tests/test_config_cli.py</files>
  <behavior>
    - `atlas config json` emits the masked snapshot as one JSON object.
    - `atlas config patch --expected-revision N --changes-json JSON` emits success or structured error JSON.
    - `config set`, `import`, and setup use the revisioned service path; no direct save bypass remains.
    - Conflict exits with a distinct code and includes current_revision/remediation.
  </behavior>
  <action>
    Write failing CLI tests including an argv-safe multi-field patch and stale revision. Refactor
    `cli/config.py` so all mutations call the control-plane facade. Keep human `show/get/set` output
    concise; keep `json/patch` stable for Rust. Search for and remove direct `save_config` write
    bypasses in this CLI. When rebuilding config in setup/import, preserve context, permission,
    modules, and all untouched sections rather than reconstructing only the original five groups.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_config_cli.py services/agent-runtime/tests/test_config_control_plane.py -q</automated>
  </verify>
  <done>Every local config surface uses one GET/PATCH contract and one transaction path.</done>
</task>

</tasks>

<verification>
- Config control-plane and CLI suites green.
- Existing config/cockpit compatibility assertions remain green.
- Audit rows contain no resolved environment secret.
</verification>

<success_criteria>
- CFG-03/04/05/06 and MOD-02 are represented in the public contract and tests.
- UX-01 typed failures are stable for CLI and gateway consumption.
</success_criteria>

<output>Create `10.4-03-SUMMARY.md` when complete.</output>
