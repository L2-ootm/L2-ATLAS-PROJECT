---
phase: 10.4-global-config-auth-model-control-plane
verified: 2026-06-26T00:00:00Z
status: passed
score: 4/4 success criteria verified, 11/11 requirements verified
overrides_applied: 0
---

# Phase 10.4: Global Configuration, Auth & Model Control Plane Verification Report

**Phase Goal:** Make ATLAS configuration, provider/model selection, and secret-safe auth status
consistent and writable from every authorized surface without independent config implementations.

**Verified:** 2026-06-26
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria from ROADMAP.md)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `~/.atlas/config.yaml` has versioned frozen schema, atomic cross-process updates, optimistic conflict detection, current-user permissions, masked reads, and audit/change events | VERIFIED | `AtlasConfig.schema_version: Literal[1]`, `revision: int` (control_plane.py:87-88), `frozen=True, extra="forbid"` (control_plane.py:16-20); `config_service.patch_config` rejects stale revision with `config_revision_conflict` (config_service.py:198-207) and commits via `secure_store.durable_replace_text` (O_EXCL temp file, fsync, os.replace, parent fsync, chmod 0600/icacls) under `file_lock` (OS-handle flock/msvcrt); `control_plane_service.patch` emits `config_change` audit event with masked before/after (control_plane_service.py:183-213). Tests: `test_config_control_plane.py` 27 passed including `test_provider_patch_leaves_active_session_and_run_contract_identical`, revision-conflict rejection test (line ~260-269), and audit-emission test. |
| 2 | CLI, gateway, TUI, and WebUI use the same GET/PATCH contract; a change from one surface becomes visible to others without restart or silent last-writer-wins loss (TUI/WebUI deferred to 10.6/10.7 by design) | VERIFIED (contract layer) | CLI (`cli/config.py` show/json/patch) and Rust gateway (`lib.rs` `config_view`/`config_patch`) both call the identical canonical service surface: CLI calls `control_plane_service`/`config_service` directly; gateway dispatches `atlas config json` / `atlas config patch --expected-revision N --changes-json <json>` as a pure adapter with zero independent config logic (D-022, confirmed no schema/validation code in `config_patch` handler, lib.rs:604-660). Stale-revision second writer returns 409 (`config_revision_conflict`), not silent overwrite — proven by `config_patch_stale_second_writer_rejected_not_silently_applied` (api.rs:1751+) and Python-side `test_audited_patch_emits_masked_diff_after_commit`/conflict test. Cross-surface visibility: `config_patch_then_get_reflects_same_revision_without_restart` (api.rs:1726-1749) proves PATCH→GET reflects the new revision with no gateway-side caching. `cargo test -p atlas-gateway config_` → 13/13 passed; full gateway suite 87/87 passed. TUI/WebUI surfaces correctly do not exist yet (Phases 10.6/10.7) — this is deferred scope, not a gap, per phase boundary explicitly stated in 10.4-CONTEXT.md. |
| 3 | Secrets remain references/ATLAS-owned auth records and never cross masked config APIs; external credential stores are detected read-only unless separately authorized | VERIFIED | `ProviderConfig.api_key` validator enforces empty-or-`env:VAR` only (control_plane.py:29-40) — raw secrets cannot even be stored in config.yaml. `auth_service.py` stores real secrets only in `~/.atlas/auth.json` (owner-permissioned, locked); `list_auth_status`/`doctor`/`_record_status` return `AuthStatus` (provider/auth_type/status/source/health/redacted_hint) and never the raw key; `resolve_secret` is explicitly the only function that returns a real secret and is documented "never call from status APIs". `detect_external_auth` (auth_service.py:363-410) uses only `shutil.which()` and `path.stat()` — never opens/parses/copies Codex/Claude credential files; 10.4-02 SUMMARY verification claims (before/after byte/SHA-256/mtime identical) are consistent with this read-only implementation. Gateway secret-sentinel test `config_get_and_patch_never_contain_secret_sentinel` (api.rs:1678-1703) proves a known sentinel string never appears in GET or PATCH response bodies. Tests: `test_auth_service.py`/`test_auth_cli.py` 13 passed, gateway secret test passed. |
| 4 | Provider/model/runtime/permission/context settings expose source, effective value, validation, restart requirement, health, and actionable remediation | VERIFIED | `control_plane_service._setting_status` produces `SettingStatus{path, configured_json, effective_json, source, validation_status, restart_required, remediation}` for every tracked setting path including provider/runtime/gateway/cockpit/context/permission/modules (control_plane_service.py:25-92); restart-required is explicit per-field (gateway/cockpit ports = True, others False). `model_control_service.get_provider_model_status` produces `ProviderModelStatus{configured_provider, effective_provider, configured_model, effective_model, source, auth_status, provider_health, model_health, fallback_status, remediation}` with explicit source precedence (session > focus > config) and concrete remediation strings (model_control_service.py:47-130). `atlas models status --json` is proven byte-equivalent to `config` snapshot's `effective` field by `test_models_status_json_matches_shared_snapshot`. Tests: `test_model_control_plane.py` 6 passed. |

**Score:** 4/4 success criteria verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/atlas-core/atlas_core/schemas/control_plane.py` | Frozen versioned config/status contracts | VERIFIED | `AtlasConfig`, `SettingStatus`, `AuthStatus`, `ProviderModelStatus`, `ControlPlaneSnapshot`, `ControlPlaneError` all present, frozen, extra-forbid |
| `services/agent-runtime/atlas_runtime/secure_store.py` | Cross-platform lock + durable replace | VERIFIED | `file_lock` (flock/msvcrt), `durable_replace_text` (O_EXCL temp, fsync, os.replace, parent fsync, chmod/icacls), `preserve_corrupt` |
| `services/agent-runtime/atlas_runtime/config_service.py` | Versioned, locked config with PATCH/conflict | VERIFIED | `patch_config` with revision check, `load_config` under lock, migration defaults for unversioned files |
| `services/agent-runtime/atlas_runtime/auth_service.py` | ATLAS-owned auth store + external detection | VERIFIED | `set_api_key`/`remove_provider`/`resolve_secret` (private) / `list_auth_status` (masked) / `detect_external_auth` (stat-only) |
| `services/agent-runtime/atlas_runtime/control_plane_service.py` | Canonical GET/PATCH + audit | VERIFIED | `get_config_snapshot`, `patch` with `config_change` audit emission and honest audit-failure error |
| `services/agent-runtime/atlas_runtime/model_control_service.py` | Provider/model effective status projection | VERIFIED | `get_provider_model_status` — pure, no mutation, source precedence implemented |
| `native/atlas-core-rs/crates/atlas-gateway/src/lib.rs` | GET/PATCH /v1/config thin adapter | VERIFIED | `config_view`, `config_patch`, `dispatch_atlas_raw`, `ApiError::Structured` — zero independent config logic, lines 586-660 |
| `services/agent-runtime/atlas_runtime/cli/config.py`, `cli/auth.py`, `cli/models.py` | Thin CLI adapters over shared service | VERIFIED | All call `control_plane_service`/`auth_service`/`model_control_service`; no parallel logic found |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| Rust gateway `config_patch` | Python `atlas config patch` CLI | `dispatch_atlas_raw` + argv-safe serde_json serialization | WIRED | Single argv element for `--changes-json`, no shell; verified by argv-injection test in api.rs |
| Rust gateway error mapping | `ControlPlaneError.as_dict()` JSON shape | structured stderr JSON parse (`error.code`) | WIRED | `cli/config.py:_error_payload` emits `{"error": {"code", "message", "remediation"}, "current_revision"}`; gateway parses `error_body.get("error").get("code")` and maps `config_revision_conflict`→409, `config_invalid`/`config_schema_unsupported`→400 |
| `control_plane_service.get_config_snapshot` | `model_control_service.get_provider_model_status` | direct call, in-memory SQLite fallback when no conn supplied | WIRED | Joined into `effective` field of `ControlPlaneSnapshot`; `atlas models status --json` proven byte-equivalent |
| `control_plane_service.patch` | `audit_service.emit` | direct call inside same lock-protected transaction boundary, post-commit | WIRED | `config_change` event written with masked before/after; audit failure surfaces as honest reconciliation error rather than false success |
| `auth_service.set_api_key`/`remove_provider` | `audit_service.emit` (`auth_change`) | `_emit_auth_change`, redacted via `SECRET_PATTERNS` | WIRED | Metadata-only audit confirmed by source read |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| CFG-01 | 10.4-01 | Versioned frozen schema + migration | SATISFIED | `AtlasConfig.schema_version`, in-memory migration for unversioned files in `_load_unlocked` |
| CFG-02 | 10.4-01 | Atomic, locked, conflict-checked, validated, permission-hardened, audited updates | SATISFIED | `patch_config` + `secure_store` + `control_plane_service.patch` audit |
| CFG-03 | 10.4-03, 10.4-05 | One masked GET/PATCH contract across surfaces | SATISFIED | CLI + Rust gateway both consume `control_plane_service`/`config_service`; REQUIREMENTS.md checkbox updated |
| CFG-04 | 10.4-01/03 | Change events + cross-surface visibility/restart semantics | SATISFIED | `config_change` audit event with `reload` metadata (`restart_required`/`visibility`); REQUIREMENTS.md checkbox stale (see Gaps note below) but code evidence is conclusive |
| CFG-05 | 10.4-03/04 | Per-setting configured/effective/source/validation/restart/remediation | SATISFIED | `SettingStatus` + `ProviderModelStatus`; REQUIREMENTS.md checkbox stale but code evidence conclusive |
| CFG-06 | 10.4-01/03/05 | Version conflict instead of silent overwrite | SATISFIED | `config_revision_conflict` at Python and Rust (409) layers; checkbox updated |
| AUTH-01 | 10.4-02/04 | Secrets stay in ATLAS auth store/env refs, never cross masked APIs | SATISFIED | `ProviderConfig.api_key` validator + `AuthStatus` masking + secret-sentinel gateway test; REQUIREMENTS.md checkbox stale but code evidence conclusive |
| AUTH-02 | 10.4-02 | External stores read-only detected | SATISFIED | `detect_external_auth` stat-only, no parse/mutate; REQUIREMENTS.md checkbox stale but code evidence conclusive |
| MOD-01 | 10.4-04 | Same effective provider/model/auth/health/fallback across surfaces | SATISFIED | `ProviderModelStatus` shared by `config` snapshot and `models status`; checkbox updated |
| MOD-02 | 10.4-03/04 | Provider/model changes preserve active-session prompt/version semantics | SATISFIED | `test_provider_patch_leaves_active_session_and_run_contract_identical` directly proves this; REQUIREMENTS.md checkbox stale but code+test evidence conclusive |
| UX-01 | all plans | Errors state what failed, which contract decided it, next safe remediation | SATISFIED | `ControlPlaneError`/`AuthServiceError`/`SecureStoreError` all carry `code`/`message`/`remediation`; checkbox updated |

**Note on REQUIREMENTS.md staleness:** The 10.4-05 documentation commit (`0c09c42`) only checked off CFG-03, CFG-06, MOD-01, UX-01 in `.planning/REQUIREMENTS.md`. It did not also check off CFG-01, CFG-02, CFG-04, CFG-05, AUTH-01, AUTH-02, MOD-02, which plans 10.4-01 through 10.4-04 already claimed as `requirements-completed` in their own SUMMARY frontmatter. This verification independently confirmed all of those requirements against source code and passing tests (see table above) — the implementation is real; only the cumulative checkbox bookkeeping in REQUIREMENTS.md is incomplete. This is a documentation-process gap, not an implementation gap, and does not block the phase goal. Recommend a follow-up doc-only commit to check off CFG-01, CFG-02, CFG-04, CFG-05, AUTH-01, AUTH-02, MOD-02 in REQUIREMENTS.md.

### Anti-Patterns Found

None. Scanned all phase-touched files (`secure_store.py`, `auth_service.py`, `config_service.py`, `control_plane_service.py`, `model_control_service.py`, `cli/config.py`, `cli/auth.py`, `control_plane.py` schema, gateway `lib.rs`) for `TODO|FIXME|XXX|TBD|HACK|PLACEHOLDER|not implemented|coming soon` — zero matches.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Python control-plane/auth/model test suite passes | `pytest tests/test_config_control_plane.py tests/test_config_cli.py tests/test_auth_service.py tests/test_auth_cli.py tests/test_model_control_plane.py -q` | 50 passed | PASS |
| Rust gateway config-specific tests pass | `cargo test -p atlas-gateway config_` | 13 passed | PASS |
| Full Rust gateway suite passes (no regression) | `cargo test -p atlas-gateway` | 87 passed | PASS |
| No debt markers in phase-touched files | grep TODO/FIXME/XXX/TBD/HACK/PLACEHOLDER | 0 matches | PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` style probes declared for this phase; SUMMARY.md verification claims are backed by direct `pytest`/`cargo test` invocations re-run independently above (not merely trusted from SUMMARY narration).

### Human Verification Required

None. All success criteria are verifiable via code inspection and automated test execution; no UI, real-time, or external-service behavior is in scope for this phase (TUI/WebUI rendering is explicitly out of scope, deferred to 10.6/10.7).

### Gaps Summary

No implementation gaps found. The phase goal — a single shared config/auth/model control-plane contract consumed identically by CLI and gateway, with no independent config logic — is achieved and supported by passing tests at both the Python service layer and the Rust gateway transport layer. The only discrepancy found is a documentation bookkeeping gap in `.planning/REQUIREMENTS.md` (several already-implemented requirements were not checked off in the final phase-completion commit); this does not affect the actual code and is noted above for cleanup, not as a blocking gap.

Deferred-by-design items (not gaps): TUI/WebUI consumption of the GET/PATCH contract (Phases 10.6/10.7); full live-process end-to-end integration test with the real `atlas` binary spawned by the real gateway binary in one test run (explicitly deferred to Phase 10.8 per 10.4-05 SUMMARY); OAuth/device-code flows and OS keychain integration (explicitly out of phase scope per 10.4-CONTEXT.md).

---

*Verified: 2026-06-26*
*Verifier: Claude (gsd-verifier)*
