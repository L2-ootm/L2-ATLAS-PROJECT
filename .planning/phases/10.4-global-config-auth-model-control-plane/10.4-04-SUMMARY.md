---
phase: 10.4-global-config-auth-model-control-plane
plan: 04
subsystem: model-control-plane
tags: [registry-v2, provider-status, model-health, fallback-audit, auth-status]
requires:
  - phase: 10.4-02
    provides: masked owned/env/external auth status
  - phase: 10.4-03
    provides: shared config snapshot and canonical PATCH
provides:
  - idempotent v2 provider/model dual-write with legacy compatibility
  - configured/effective provider-model status projection
  - shared snapshot and CLI model-status parity
affects: [10.4-05, 10.6, 10.7, 10.8]
tech-stack:
  added: []
  patterns:
    - registry availability is descriptive and never overrides config
    - fallback status is derived from recorded audit only
key-files:
  created:
    - services/agent-runtime/atlas_runtime/model_control_service.py
    - services/agent-runtime/tests/test_model_control_plane.py
  modified:
    - services/agent-runtime/atlas_runtime/model_registry.py
    - services/agent-runtime/atlas_runtime/control_plane_service.py
    - services/agent-runtime/atlas_runtime/cli/models.py
key-decisions:
  - "The v2 registry is preferred only for status reads; legacy rows remain the compatibility fallback when v2 is empty."
  - "Session model identity outranks focus, which outranks config, but the projection never mutates any selection."
patterns-established:
  - "ProviderModelStatus joins configured/effective selection, masked auth, registry health, and recorded fallback state."
requirements-completed: [MOD-01, MOD-02, AUTH-01, CFG-05]
duration: 4 min
completed: 2026-06-25
---

# Phase 10.4 Plan 04: Provider and Model Status Plane Summary

**V2 registry compatibility and one secret-free configured/effective provider-model truth object shared by config and model diagnostics**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-25T20:18:41-03:00
- **Completed:** 2026-06-25T20:22:48-03:00
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added idempotent provider/model v2 UPSERTs, source-scoped deactivation, composite identities, and legacy fallback reads.
- Added a pure status projection joining config precedence, masked auth, registry availability, and recorded fallback audit.
- Added `atlas models status --json` with byte-equivalent object shape to `config` snapshot `effective`.

## Task Commits

1. **Task 1 RED: v2 registry compatibility tests** — `7160d40`
2. **Task 1 GREEN: v2 dual-write registry** — `5729e5b`
3. **Task 2 RED: model status matrix** — `528e625`
4. **Task 2 GREEN: effective model status projection** — `3bc23a5`
5. **Task 3 RED: status parity tests** — `9fe014b`
6. **Task 3 GREEN: shared model status exposure** — `2f502a7`

## Verification

- Model registry/control plane: 19 passed
- Integrated config/auth snapshot plane: 42 passed
- Registry secret scan: no env reference or raw secret present
- No model-selection mutation command exists in `cli/models.py`
- Ruff on model/config control-plane files: passed

## Decisions Made

- V2 rows preserve first-seen and separate model status from derived auth status; arbitrary discovery payload metadata is not persisted.
- A recorded `provider_fallback` event means `used`; no event means `not_used`; malformed/unavailable audit state means `unknown`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Injected the registry DB into config JSON/show**
- **Found during:** Task 3 (shared snapshot parity)
- **Issue:** The existing config CLI called the snapshot without a database, which could only report an isolated unknown registry and would diverge from `models status`.
- **Fix:** `config show/json` now inject the canonical CLI connection; isolated service callers retain an in-memory unknown-registry fallback.
- **Files modified:** `atlas_runtime/cli/config.py`, `tests/test_config_cli.py`
- **Verification:** parity and integrated config/model suites pass.
- **Committed in:** `2f502a7`

**Total deviations:** 1 auto-fixed (blocking integration). **Impact:** Required for one cross-surface truth object; no new authority or dependency.

## Issues Encountered

None.

## User Setup Required

None - no credentials or provider endpoints were changed.

## Next Phase Readiness

The complete Python control plane is ready for thin Rust gateway GET/PATCH delivery and typed HTTP
error mapping in Plan 10.4-05.

---
*Phase: 10.4-global-config-auth-model-control-plane*
*Completed: 2026-06-25*

