---
phase: 10.4-global-config-auth-model-control-plane
plan: 02
type: tdd
wave: 2
depends_on: ["10.4-01"]
files_modified:
  - services/agent-runtime/atlas_runtime/auth_service.py
  - services/agent-runtime/atlas_runtime/cli/auth.py
  - services/agent-runtime/atlas_runtime/cli/main.py
  - services/agent-runtime/tests/test_auth_service.py
  - services/agent-runtime/tests/test_auth_cli.py
autonomous: true
requirements: [AUTH-01, AUTH-02, UX-01]
user_setup: []

must_haves:
  truths:
    - "ATLAS-owned secret material is stored only in owner-only auth.json and is never returned by list/status/json commands"
    - "External Codex/Claude credential locations are detected by presence only and remain byte/hash/mtime identical after every ATLAS auth command"
    - "Auth writes use the shared secure-store lock and record only redacted metadata in audit"
    - "Operators can add, remove, list, inspect, and diagnose ATLAS-owned credentials without putting a secret on the command line"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/auth_service.py"
      provides: "ATLAS-owned auth store + masked status + external detection"
      contains: "def set_api_key"
    - path: "services/agent-runtime/atlas_runtime/cli/auth.py"
      provides: "Hidden-input auth add/list/remove/status/doctor/json commands"
      contains: "auth_app"
  key_links:
    - from: "auth_service.py"
      to: "secure_store.py"
      via: "same lock/durable replacement primitive as config"
      pattern: "secure_store"
    - from: "auth_service mutation"
      to: "audit_service.emit"
      via: "metadata-only auth_change event after lock release"
      pattern: "auth_change"
---

<objective>
Implement the committed flat `~/.atlas/auth.json` store, masked status projection, read-only
external auth detection, and local safe CLI.
</objective>

<context>
@.planning/phases/10.4-global-config-auth-model-control-plane/10.4-CONTEXT.md
@.planning/phases/10.4-global-config-auth-model-control-plane/10.4-RESEARCH.md
@docs/architecture/AUTH_STORE_DESIGN.md
@.planning/prep/v1.1-owned-auth-architecture.md

<interfaces>
- `auth_store_path()` honors `ATLAS_HOME`.
- Store shape: `{version: 1, providers: {provider_id: record}, updated_at: ...}`.
- Raw record may contain `api_key`/tokens privately; public `AuthStatus` contains no raw secret.
- Use `mission_service.ensure_operator_run()` before audit emission.
- Add `auth_change` to AuditEvent.event_type; no SQL migration is required.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Auth store roundtrip, masking, and corruption handling</name>
  <files>services/agent-runtime/atlas_runtime/auth_service.py, services/agent-runtime/tests/test_auth_service.py</files>
  <behavior>
    - set_api_key writes version 1 auth.json through secure_store and stores a computed redacted hint.
    - list_status/get_secret never confuse public status with private secret resolution.
    - malformed auth.json is preserved as auth.json.corrupt and status fails closed as corrupt.
    - remove_provider is idempotent and leaves no secret in returned/audited values.
    - env references can be reported by presence without copying environment values into auth.json.
  </behavior>
  <action>
    Write failing tests first. Implement `AuthServiceError`, `auth_store_path`,
    `auth_lock_path`, `_load_store_unlocked`, `set_api_key`, `remove_provider`,
    `resolve_secret`, `list_auth_status`, and `doctor`. Keep raw records private dictionaries or
    private frozen models; return only public `AuthStatus`. Use SHA-independent last-four masking and
    reuse `SECRET_PATTERNS` for defensive redaction.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_auth_service.py -q</automated>
  </verify>
  <done>ATLAS-owned credentials roundtrip securely and public status is secret-free.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Read-only external credential detection</name>
  <files>services/agent-runtime/atlas_runtime/auth_service.py, services/agent-runtime/tests/test_auth_service.py</files>
  <behavior>
    - Codex and Claude-style external stores report not_installed/installed_no_auth/auth_present/unknown_error using binary/file presence only.
    - Detector never opens/parses external auth payloads.
    - Before/after bytes, hash, size, and mtime are identical after detection and every auth command.
    - Detection errors produce safe remediation and no secret path contents.
  </behavior>
  <action>
    Add failing invariant tests with fake external homes and monkeypatched `shutil.which`. Implement
    a small descriptor table for Codex and Claude external sources, stat/presence-only detection,
    and public `AuthStatus(source="external_read_only")`. Do not call provider CLIs or parse JSON.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_auth_service.py -k external -q</automated>
  </verify>
  <done>External auth is observable but unowned and unmodified.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Auth CLI and metadata-only audit</name>
  <files>services/agent-runtime/atlas_runtime/cli/auth.py, services/agent-runtime/atlas_runtime/cli/main.py, services/agent-runtime/tests/test_auth_cli.py</files>
  <behavior>
    - `atlas auth add PROVIDER` uses hidden prompt input; no secret argv flag exists.
    - list/status/json/doctor show masked metadata only; remove is explicit and idempotent.
    - successful add/remove emits one `auth_change` event with provider/action/source and no secret.
    - validation/lock/corruption errors render code + remediation and exit nonzero.
  </behavior>
  <action>
    Write Typer tests first. Create `cli/auth.py`, register `auth_app` in `cli/main.py`, and inject
    DB/lock only for mutation audit using the `auth_change` Literal already added by Plan 01. JSON
    output serializes public status models. Ensure CLI tests search captured output and audit rows
    for the submitted secret and fail if it appears.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_auth_cli.py services/agent-runtime/tests/test_auth_service.py -q</automated>
  </verify>
  <done>Local auth management is usable, audited, actionable, and never transports raw secrets through masked APIs.</done>
</task>

</tasks>

<verification>
- Auth service/CLI suites green.
- `rg -n "access_token|refresh_token|api_key" services/agent-runtime/atlas_runtime/cli/auth.py` shows no raw output path.
- External byte-identity tests pass.
</verification>

<success_criteria>
- AUTH-01 and AUTH-02 are automated.
- UX-01 errors identify the failed boundary and remediation.
</success_criteria>

<output>Create `10.4-02-SUMMARY.md` when complete.</output>
