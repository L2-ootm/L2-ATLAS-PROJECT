---
phase: 10.4-global-config-auth-model-control-plane
plan: 04
type: tdd
wave: 3
depends_on: ["10.4-02", "10.4-03"]
files_modified:
  - services/agent-runtime/atlas_runtime/model_registry.py
  - services/agent-runtime/atlas_runtime/model_control_service.py
  - services/agent-runtime/atlas_runtime/control_plane_service.py
  - services/agent-runtime/atlas_runtime/cli/models.py
  - services/agent-runtime/tests/test_model_registry.py
  - services/agent-runtime/tests/test_model_control_plane.py
autonomous: true
requirements: [MOD-01, MOD-02, AUTH-01, CFG-05]
user_setup: []

must_haves:
  truths:
    - "Every surface receives the same configured/effective provider and model, source, auth state, registry health, fallback status, and remediation"
    - "Registry availability never silently overrides operator selection and never contains secret material"
    - "Model discovery populates provider_registry/model_registry_v2 idempotently while preserving the legacy reader during compatibility"
    - "Fallback status is an honest projection of recorded events/configuration; this phase does not invent or execute fallback behavior"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/model_control_service.py"
      provides: "ProviderModelStatus projection"
      contains: "def get_provider_model_status"
    - path: "services/agent-runtime/atlas_runtime/model_registry.py"
      provides: "v2 dual-write + compatibility reads"
      contains: "model_registry_v2"
  key_links:
    - from: "model_control_service.py"
      to: "config_service + auth_service + model_registry"
      via: "single joined status projection"
      pattern: "get_provider_model_status"
    - from: "control_plane_service snapshot"
      to: "ProviderModelStatus"
      via: "effective field included in GET /v1/config response"
      pattern: "effective="
---

<objective>
Complete the provider/model status plane by joining operator selection, auth presence, registry
availability, and recorded fallback state into the shared config snapshot.
</objective>

<context>
@.planning/phases/10.4-global-config-auth-model-control-plane/10.4-RESEARCH.md
@packages/atlas-core/atlas_core/schemas/registry_v2.py
@infra/migrations/0004_registry_v2.sql
@docs/architecture/FALLBACK_CASCADE.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Idempotent v2 provider/model registry writes</name>
  <files>services/agent-runtime/atlas_runtime/model_registry.py, services/agent-runtime/tests/test_model_registry.py</files>
  <behavior>
    - refresh upserts provider_registry and model_registry_v2 with first_seen preserved and source-scoped deactivation.
    - status and auth_status remain distinct; no secret/ref value is persisted.
    - Existing legacy model_registry behavior remains available for old readers.
    - Multi-source same model ids do not collide in v2.
  </behavior>
  <action>
    Add failing v2 tests. Extend refresh with an optional `auth_status_resolver(provider_id)` and
    dual-write v2 rows using the documented UPSERT. Add `list_models_v2` and a compatibility
    `list_models_control_plane` that prefers v2 and falls back to legacy only when v2 is empty.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_model_registry.py -q</automated>
  </verify>
  <done>Discovery maintains the v2 truth without breaking legacy reads.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Effective provider/model/auth/health/fallback projection</name>
  <files>services/agent-runtime/atlas_runtime/model_control_service.py, services/agent-runtime/tests/test_model_control_plane.py</files>
  <behavior>
    - Config provider/model are reported as configured/effective with source config or focus/session override.
    - Auth state joins ATLAS auth, env reference presence, and external read-only detection without returning values.
    - Registry state reports available/offline/unknown and remediation for an unavailable configured model.
    - Fallback status is `used` only when a recorded provider_fallback event exists, otherwise `not_used`/`unknown`; no fallback execution is added.
  </behavior>
  <action>
    Write failing matrix tests. Reuse the `model_call_start`, `model_call_end`, and
    `provider_fallback` audit Literals added by Plan 01. Create `model_control_service.py` with a
    pure `get_provider_model_status(conn, cfg, *,
    focus_framework=None, session_model=None)` projection. Query only masked auth and registry rows;
    parse recent fallback audit metadata defensively.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_model_control_plane.py -q</automated>
  </verify>
  <done>The status plane honestly explains the selected and effective provider/model state.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Include model plane in shared snapshot and CLI diagnostics</name>
  <files>services/agent-runtime/atlas_runtime/control_plane_service.py, services/agent-runtime/atlas_runtime/cli/models.py, services/agent-runtime/tests/test_model_control_plane.py</files>
  <behavior>
    - config snapshot includes auth tuple and effective ProviderModelStatus.
    - `atlas models status --json` emits the identical effective object.
    - provider/model changes still occur only through config PATCH.
  </behavior>
  <action>
    Add failing parity tests, then compose auth and model projections into
    `ControlPlaneSnapshot`. Add `models status --json` as a read-only adapter and keep refresh/list
    backward compatible.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_model_control_plane.py services/agent-runtime/tests/test_model_registry.py -q</automated>
  </verify>
  <done>CLI and future surfaces share one provider/model truth object.</done>
</task>

</tasks>

<verification>
- Model registry/control-plane suites green.
- SQLite scans prove no configured env ref or raw secret appears in provider/model tables.
- Config patch remains the only provider/model mutation path.
</verification>

<success_criteria>
- MOD-01/02 and CFG-05 status fields are complete and consistent.
- AUTH-01 credential boundary remains intact.
</success_criteria>

<output>Create `10.4-04-SUMMARY.md` when complete.</output>
