---
phase: 10.4-global-config-auth-model-control-plane
plan: 05
type: tdd
wave: 4
depends_on: ["10.4-03", "10.4-04"]
files_modified:
  - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs
  - native/atlas-core-rs/crates/atlas-gateway/tests/api.rs
  - services/agent-runtime/tests/test_config_cli.py
autonomous: true
requirements: [CFG-03, CFG-06, MOD-01, UX-01]
user_setup: []

must_haves:
  truths:
    - "GET /v1/config returns the same masked control-plane snapshot as atlas config json"
    - "PATCH /v1/config forwards expected_revision and non-secret changes to the canonical CLI contract and returns the new snapshot"
    - "A config revision conflict maps to HTTP 409 with code/current_revision/remediation, not generic 500"
    - "Gateway source contains no config schema, auth parser, secret resolver, or independent model selection logic"
  artifacts:
    - path: "native/atlas-core-rs/crates/atlas-gateway/src/lib.rs"
      provides: "GET/PATCH /v1/config dispatch adapter and structured error mapping"
      contains: "patch(config_patch)"
    - path: "native/atlas-core-rs/crates/atlas-gateway/tests/api.rs"
      provides: "HTTP parity, 409, argv, and no-secret tests"
      contains: "config_patch"
  key_links:
    - from: "PATCH /v1/config"
      to: "atlas config patch"
      via: "serialized non-secret changes JSON + expected revision"
      pattern: "config.*patch"
---

<objective>
Finish the shared surface contract by adding Rust gateway PATCH delivery and precise error mapping,
while keeping all config/auth/model authority in Python.
</objective>

<context>
@.planning/phases/10.4-global-config-auth-model-control-plane/10.4-CONTEXT.md
@native/atlas-core-rs/crates/atlas-gateway/src/lib.rs
@native/atlas-core-rs/crates/atlas-gateway/tests/api.rs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Gateway config PATCH happy path and argv contract</name>
  <files>native/atlas-core-rs/crates/atlas-gateway/tests/api.rs, native/atlas-core-rs/crates/atlas-gateway/src/lib.rs</files>
  <behavior>
    - PATCH body `{expected_revision, changes}` dispatches `atlas config patch --expected-revision N --changes-json <json>`.
    - Response is parsed and returned without reshaping.
    - Empty/non-object changes are 400 before dispatch.
    - Changes values are serialized as one argv element; no shell interpolation.
  </behavior>
  <action>
    Add Rust tests first using the existing arg-capture stub. Implement `ConfigPatchBody`, a
    dispatch helper that preserves child exit status/stdout/stderr, `config_patch`, and
    `.route("/v1/config", get(config_view).patch(config_patch))`. Serialize the changes object with
    serde_json and pass explicit argv.
  </action>
  <verify>
    <automated>cargo test -p atlas-gateway config_patch -- --nocapture</automated>
  </verify>
  <done>Gateway PATCH reaches the canonical CLI contract safely.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Structured 409/400 error mapping and secret guard</name>
  <files>native/atlas-core-rs/crates/atlas-gateway/tests/api.rs, native/atlas-core-rs/crates/atlas-gateway/src/lib.rs</files>
  <behavior>
    - CLI `config_revision_conflict` becomes HTTP 409 and preserves current_revision/remediation.
    - Validation/unknown-key errors become HTTP 400; unexpected failures remain 500.
    - GET and PATCH response bodies never contain resolved secret values.
    - Existing GET /v1/config top-level provider/runtime compatibility stays green.
  </behavior>
  <action>
    Add failing stub tests for each error class and a known secret sentinel. Extend ApiError with a
    structured conflict/bad-request JSON path or return a prebuilt response from config_patch.
    Parse only the CLI's structured JSON error; never inspect config fields in Rust.
  </action>
  <verify>
    <automated>cargo test -p atlas-gateway config_ -- --nocapture</automated>
  </verify>
  <done>HTTP clients receive actionable, machine-readable config errors with no secret leakage.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Cross-surface conformance smoke</name>
  <files>services/agent-runtime/tests/test_config_cli.py, native/atlas-core-rs/crates/atlas-gateway/tests/api.rs</files>
  <behavior>
    - The same fixture snapshot is accepted by CLI JSON and gateway GET tests.
    - A patch from gateway is visible to a subsequent CLI/service read without restart.
    - A stale second writer is rejected rather than silently winning.
  </behavior>
  <action>
    Add fixture-level conformance assertions to the Python and Rust suites. Use a throwaway
    `ATLAS_HOME`; do not touch the real user config. Keep full end-to-end live-process testing for
    Phase 10.8, but prove the transport contracts now.
  </action>
  <verify>
    <automated>python -m pytest services/agent-runtime/tests/test_config_cli.py -q; cargo test -p atlas-gateway config_</automated>
  </verify>
  <done>CLI and gateway demonstrably consume one control-plane contract.</done>
</task>

</tasks>

<verification>
- `cargo test -p atlas-gateway`
- Focused Python config CLI suite
- `git diff --check`
</verification>

<success_criteria>
- CFG-03 and CFG-06 have HTTP-level proof.
- MOD-01 snapshot reaches gateway unchanged.
- UX-01 maps conflicts and validation errors to safe actionable responses.
</success_criteria>

<output>Create `10.4-05-SUMMARY.md` when complete.</output>
