---
phase: 10.5-surface-scoped-permission-broker
plan: 02
subsystem: permissions
tags: [permissions, approval, broker, migration, schema, config, sqlite, pydantic]

requires:
  - phase: 10.5-01
    provides: "Wave 0 conftest helpers (seed_pending_approval / register_test_channel / make_active_session) referencing the 0017 columns + approval_channels"
  - phase: 10.4
    provides: "revisioned PermissionConfig in control_plane.py (extended here with TTL + fail-closed)"
  - phase: 10.3
    provides: "0016 soft-FK + immutable-terminal-trigger precedent reused by 0017"
provides:
  - "Migration 0017: additive surface columns + scoped index on tool_approvals, approval_channels (PERM-05 gate), session_allow_rules (PERM-07 scope store)"
  - "Extended ToolApproval (7 Optional surface fields, legacy NULL-safe) + frozen ApprovalChannel / SessionAllowRule models"
  - "PermissionConfig.approval_ttl_seconds (default 300, ge=1) + fail_closed_on_disconnect (default True)"
  - "tool_service lockstep _COLS/INSERT/_row_to_approval (18 cols) + shared _normalize_args policy match key"
affects: [10.5-03, 10.5-04, 10.5-05, permission_broker, tool_service]

tech-stack:
  added: []
  patterns:
    - "Additive ALTER + nullable surface columns (legacy NULL back-compat under PRAGMA foreign_keys=ON)"
    - "Immutable-terminal BEFORE UPDATE trigger on tool_approvals (un-split BEGIN..END, executescript-safe)"
    - "_COLS == INSERT placeholders == ToolApproval model fields, asserted by exact set-equality (Pitfall 2 guard)"
    - "_normalize_args = single-redaction sorted-key canonical form, reused verbatim as Plan 05 policy match key"

key-files:
  created:
    - infra/migrations/0017_permission_broker.sql
    - .planning/phases/10.5-surface-scoped-permission-broker/deferred-items.md
  modified:
    - packages/atlas-core/atlas_core/schemas/tool.py
    - packages/atlas-core/atlas_core/schemas/control_plane.py
    - services/agent-runtime/atlas_runtime/tool_service.py

key-decisions:
  - "surface_session_id stored + indexed, NOT REFERENCES-enforced (soft FK, 0013/0016 precedent) so legacy operator/NULL rows survive"
  - "Reused the existing 5-value ToolApprovalStatus; expiry/deny cause carried in reason/decision (no new status, no extra migration)"
  - "Added immutable-terminal trigger on tool_approvals (verified it does not block pending->executing->executed/rejected transitions)"
  - "Added _normalize_args helper now (Rule 2) so args_normalized population and Plan 05 policy matching share one canonical key"

patterns-established:
  - "Exact INSERT-placeholder/model-key set-equality assertion as the Pitfall-2 lockstep guard"
  - "Optional-everywhere surface fields keep frozen-model back-compat for pre-0017 persisted rows"

requirements-completed: [PERM-01]

duration: 5min
completed: 2026-06-26
---

# Phase 10.5 Plan 02: Permission-Broker Schema & Config Foundation Summary

**Additive migration 0017 (surface/expiry/nonce columns + scoped index on tool_approvals, plus approval_channels and session_allow_rules), the matching frozen ToolApproval/ApprovalChannel/SessionAllowRule model extensions, PermissionConfig TTL + fail-closed defaults, and a lockstep 18-column persistence layer with a shared _normalize_args policy key.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-26T16:50:57Z
- **Completed:** 2026-06-26T16:56:18Z
- **Tasks:** 3
- **Files modified:** 4 (1 migration + 2 schemas + 1 service) + 1 deferred log

## Accomplishments

- Migration 0017 applies cleanly through the runner; legacy tool_approvals rows load with NULL surface columns (proven by a direct insert+load smoke). The seven new columns are nullable; surface_session_id is a soft FK (stored + indexed, never REFERENCES) so operator/NULL rows survive under `PRAGMA foreign_keys=ON`.
- `approval_channels` (PERM-05 fail-closed gate) and `session_allow_rules` (PERM-07 scope-bound store, non-unique session index, no global policy row) created `IF NOT EXISTS`. Optional immutable-terminal BEFORE UPDATE trigger added on tool_approvals (un-split BEGIN..END), verified not to block legitimate pending->executing->executed/rejected transitions.
- `ToolApproval` extended with 7 Optional surface fields (legacy NULL-safe), `expiry_at` added to the `@field_serializer` list; new frozen `ApprovalChannel` and `SessionAllowRule` models exported via `__all__`. `PermissionConfig` gains `approval_ttl_seconds` (ge=1, default 300) and `fail_closed_on_disconnect` (True).
- `tool_service._COLS`, the `invoke()` INSERT column list + named placeholders, and `_row_to_approval` updated in lockstep to 18 columns; the INSERT placeholder set is now EXACTLY equal to `set(ToolApproval(...).model_dump().keys())` (Warning-3 / Pitfall-2 guard). Added the shared `_normalize_args` helper (single redaction + sorted keys) that doubles as the Plan 05 policy match key.
- Confirmed the Wave 0 conftest helper column sets (seed_pending_approval / register_test_channel / session_allow_rules) all run against the migrated schema.

## Task Commits

Each task was committed atomically:

1. **Task 1: Migration 0017** - `a05b45a` (feat)
2. **Task 2: ToolApproval/ApprovalChannel/SessionAllowRule + PermissionConfig** - `59400fb` (feat)
3. **Task 3: tool_service lockstep _COLS/INSERT/_row_to_approval + _normalize_args** - `86efe1a` (feat)

**Plan metadata:** (final docs commit — this SUMMARY + STATE/ROADMAP/REQUIREMENTS)

## Files Created/Modified

- `infra/migrations/0017_permission_broker.sql` - additive surface columns + scoped index + approval_channels + session_allow_rules + immutable-terminal trigger
- `packages/atlas-core/atlas_core/schemas/tool.py` - extended ToolApproval; new ApprovalChannel, SessionAllowRule, SessionAllowRuleKind
- `packages/atlas-core/atlas_core/schemas/control_plane.py` - PermissionConfig TTL + fail-closed defaults
- `services/agent-runtime/atlas_runtime/tool_service.py` - lockstep persistence columns + _normalize_args helper
- `.planning/phases/10.5-surface-scoped-permission-broker/deferred-items.md` - out-of-scope pre-existing failure log

## Decisions Made

- Soft FK on surface_session_id (stored + indexed, not REFERENCES) — preserves legacy/operator rows under FK enforcement.
- Reused the existing 5-value ToolApprovalStatus; deny/expiry cause rides in reason/decision (no new status, no extra migration).
- Added the immutable-terminal trigger; verified against the tool_service approve/reject suite that no legitimate transition is blocked (those UPDATE rows whose OLD.status is still pending/executing, never terminal).
- Added `_normalize_args` in this plan (Rule 2 — missing critical functionality the brief flags as reused verbatim by Plan 05), so the args_normalized column and the future policy match key share one canonical, single-redacted form.

## Deviations from Plan

### Auto-added functionality

**1. [Rule 2 - Missing Critical] Added `tool_service._normalize_args` helper**
- **Found during:** Task 3
- **Issue:** The plan's `<important>` brief mandates a shared `_normalize_args(args) = _redact(json.dumps(args, sort_keys=True))` that doubles as the Plan 05 policy match key, but no task action explicitly created it. Omitting it would force Plan 05 to invent a parallel normalization path (divergence risk / double-redaction).
- **Fix:** Added `_normalize_args` in the Helpers section, built on the existing `_redact` (single redaction at the audit boundary, D-002). Not yet wired into `invoke()` (Wave 2 populates args_normalized); defined now as the single source of truth.
- **Files modified:** services/agent-runtime/atlas_runtime/tool_service.py
- **Verification:** Import + placeholder-parity check pass; tool_service suite green (7 passed).
- **Committed in:** `86efe1a` (Task 3 commit)

---

**Total deviations:** 1 auto-added (Rule 2 critical functionality)
**Impact on plan:** Necessary to satisfy the brief's reuse contract and avoid a parallel redaction path. No scope creep — the helper is defined, not yet invoked.

## Issues Encountered

- **Out-of-scope pre-existing test failure:** `tests/test_surface_events.py::test_kind_map_covers_every_audit_event_type` fails because `surface_events._KIND_MAP` does not cover five Phase 10.4 control-plane `AuditEvent.event_type` values (`auth_change`, `config_change`, `model_call_start`, `model_call_end`, `provider_fallback`). Confirmed failing on parent commit `561c717` (before any 10.5-02 change). Unrelated to permission-broker schema work; logged to `deferred-items.md` and NOT fixed (scope boundary).
- One known pre-existing env failure (`test_claude_code_missing_sdk_raises` — `claude_agent_sdk` not importable in this venv), documented in STATE; unaffected by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Schema, models, and config foundation in place; later 10.5 waves can implement `atlas_runtime/permission_broker.py` against the frozen API the Wave 0 contract locked.
- Wave 0 conftest helpers are now runnable against the migrated schema (migration-dependent inserts succeed); broker-logic tests in `test_permission_broker.py` remain RED until the broker module lands (Waves 2-5), which is the intended state.
- `_normalize_args` ready for `invoke()` wiring and Plan 05 policy matching.

## Self-Check: PASSED

- FOUND: infra/migrations/0017_permission_broker.sql
- FOUND: packages/atlas-core/atlas_core/schemas/tool.py
- FOUND: packages/atlas-core/atlas_core/schemas/control_plane.py
- FOUND: services/agent-runtime/atlas_runtime/tool_service.py
- FOUND: .planning/phases/10.5-surface-scoped-permission-broker/10.5-02-SUMMARY.md
- Commits verified present: a05b45a (Task 1), 59400fb (Task 2), 86efe1a (Task 3)

---
*Phase: 10.5-surface-scoped-permission-broker*
*Completed: 2026-06-26*
