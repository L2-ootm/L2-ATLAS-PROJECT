---
phase: 10.5-surface-scoped-permission-broker
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/agent-runtime/tests/test_permission_broker.py
  - services/agent-runtime/tests/conftest.py
autonomous: true
requirements: [PERM-01, PERM-02, PERM-05, PERM-06, PERM-07, SEC-02, AUD-02]
tags: [permissions, approval, broker, tests, security]

must_haves:
  truths:
    - "A failing test module exists asserting every broker behavior before any broker code is written"
    - "Tests reference the exact public API names the later waves must implement (claim, list_actionable, register_channel, reconcile, list_outcomes)"
    - "conftest exposes a helper to build an active surface session + a pending surface-anchored approval + a registered approval channel"
  artifacts:
    - path: "services/agent-runtime/tests/test_permission_broker.py"
      provides: "Failing-first coverage for PERM-01..07, SEC-02, AUD-02"
      contains: "def test_concurrent_claim_at_most_once"
      min_lines: 120
    - path: "services/agent-runtime/tests/conftest.py"
      provides: "active-session + pending-approval + approval-channel fixtures/helpers"
      contains: "approval_channels"
  key_links:
    - from: "services/agent-runtime/tests/test_permission_broker.py"
      to: "atlas_runtime.permission_broker"
      via: "import (RED: module does not exist yet)"
      pattern: "import permission_broker|from atlas_runtime import permission_broker"
---

<objective>
Author the Wave 0 failing-first test module `test_permission_broker.py` and extend `conftest.py`
with the fixtures the broker tests require. The module imports the not-yet-existing
`atlas_runtime.permission_broker` and asserts the full behavioral contract, so it MUST fail
(import error / missing attribute) until Waves 1–4 land. This is the test-first phase pattern
the repo used in 10.4.

Purpose: Lock the broker's public API surface and required behaviors as executable assertions
before implementation, per VALIDATION.md Wave 0 and the repo's test-first convention.
Output: One failing test module + conftest fixtures, no production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.5-surface-scoped-permission-broker/10.5-RESEARCH.md
@.planning/phases/10.5-surface-scoped-permission-broker/10.5-PATTERNS.md
@.planning/phases/10.5-surface-scoped-permission-broker/10.5-VALIDATION.md

<interfaces>
<!-- Names the test module asserts against — later waves implement these verbatim. -->

The broker module (`atlas_runtime/permission_broker.py`, built in Waves 2–4) exposes:
- `list_actionable(conn, *, surface_session_id) -> list[ToolApproval]`  (PERM-02/04)
- `claim(conn, lock, *, approval_id, surface_session_id, decision, nonce, ctx=None, reason=None)`
   returning either the terminal `ToolApproval` (winner) or a typed `AlreadyDecided` (loser).
- `register_channel(conn, lock, *, surface_session_id, surface_kind) -> ApprovalChannel`
- `revoke_channel(conn, lock, *, surface_session_id) -> None`
- `reconcile_executing_orphans(conn, lock) -> int`  (returns count flipped to failed)
- `list_outcomes(conn) -> list[...]`  (read-only terminal projection; AUD-02)
- typed errors: `AlreadyDecided`, `NotActiveSessionError`, `WrongSessionError`,
  `StaleApprovalError` (nonce mismatch / expired), `ApprovalChannelMissingError`

`tool_service.invoke(...)` (extended in Wave 2) gains keyword params:
`surface_session_id: Optional[str] = None`, `surface_kind: Optional[str] = None`.

Existing fixtures in conftest.py: `db` (in-memory, ALL migrations applied), `lock`,
`run_id`, `surface_session` (a persisted 'starting' session id).

SessionState values (surface_session.py): starting, active, suspended, resuming,
cancelling, completed, failed, reclaimed. Authority requires `state == 'active'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add active-session, pending-approval, and approval-channel test helpers to conftest</name>
  <read_first>
    - services/agent-runtime/tests/conftest.py (the file being modified; `db`, `lock`, `run_id`, `surface_session` fixtures)
    - services/agent-runtime/tests/test_surface_session_service.py (local-builder + DB-side-effect assertion pattern — PATTERNS analog)
    - .planning/phases/10.5-surface-scoped-permission-broker/10.5-PATTERNS.md (test section: fixtures + builders)
  </read_first>
  <action>
    Extend conftest.py with a helper (a fixture-returned callable, mirroring the existing
    `mock_gh`/`mock_urlopen` installer style) named `make_active_session` that transitions the
    existing `surface_session` row to `state='active'` via a direct UPDATE on `surface_sessions`
    and returns its id. Add a second helper `seed_pending_approval(db, *, surface_session_id,
    surface_kind='cli', nonce, expiry_at, tool_name='workspace')` that INSERTs a `pending`
    `tool_approvals` row carrying the new surface columns (surface_session_id, surface_kind,
    workspace_root, expiry_at, decision=None, nonce, args_normalized) and returns the approval id.
    Add `register_test_channel(db, *, surface_session_id, surface_kind='api')` that INSERTs an
    unrevoked `approval_channels` row. These helpers depend on the 0017 schema (Wave 1); guard the
    INSERT column lists so they reference the post-0017 columns. Do NOT probe PIDs anywhere; set
    session state purely by column UPDATE. Keep helpers stdlib-only (sqlite3, datetime, uuid).
  </action>
  <acceptance_criteria>
    - conftest.py defines fixtures/callables `make_active_session`, `seed_pending_approval`,
      `register_test_channel`.
    - `seed_pending_approval` INSERT references columns: surface_session_id, surface_kind,
      workspace_root, expiry_at, nonce, args_normalized (the 0017 additions).
    - No use of `os.kill` or PID probing for liveness anywhere in conftest.
    - `grep -v '^#' services/agent-runtime/tests/conftest.py | grep -c "approval_channels"` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -c "import ast; ast.parse(open('tests/conftest.py').read())" && grep -q "approval_channels" tests/conftest.py && echo CONFTEST_OK</automated>
  </verify>
  <done>conftest exposes the three broker helpers; file parses; references approval_channels and surface columns.</done>
</task>

<task type="auto">
  <name>Task 2: Write the failing broker test module covering PERM-01..07, SEC-02, AUD-02</name>
  <read_first>
    - services/agent-runtime/tests/test_tool_service.py (atomic-claim TOCTOU test pattern)
    - services/agent-runtime/tests/test_surface_session_service.py (audit-emission assertion pattern)
    - packages/atlas-core/atlas_core/schemas/tool.py (ToolApproval fields the row-field test asserts)
    - .planning/phases/10.5-surface-scoped-permission-broker/10.5-RESEARCH.md (Test Map; SEC-02 "Tests must cover" list)
  </read_first>
  <action>
    Create `services/agent-runtime/tests/test_permission_broker.py` importing
    `from atlas_runtime import permission_broker as broker` and `tool_service`. Author these tests
    (names are the contract; map 1:1 to RESEARCH Test Map):
    `test_approval_record_fields` (PERM-01: a pending approval persists surface_session_id,
    surface_kind, workspace_root, expiry_at, nonce, args_normalized, risk_level, reason);
    `test_claim_requires_matching_active_session` (PERM-02: claim from a different session_id
    raises WrongSessionError; claim when the owning session is terminal/suspended raises
    NotActiveSessionError; matching active session succeeds);
    `test_headless_fail_closed_without_channel` (PERM-05: invoke with surface_kind='api' and no
    approval_channels row resolves to deny — no pending row created; with register_channel a
    pending row IS created);
    `test_concurrent_claim_at_most_once` (PERM-06: two threads claim the same approval; exactly one
    returns a winning terminal ToolApproval, the other returns AlreadyDecided carrying the winner's
    status/decision; assert no double execution);
    `test_restart_reconcile_executing_orphan` (PERM-06: an `executing` row with NULL result is
    flipped to `failed` by reconcile_executing_orphans; a `pending` row is left untouched);
    `test_allow_rules_cannot_widen_scope` (PERM-07: an allow-always rule on session A does NOT
    auto-resolve an approval owned by session B nor one in a different workspace_root; assert the
    rule never appears in config/policy state);
    `test_replay_and_stale_rejected` (SEC-02: claim with a mismatched nonce raises StaleApprovalError;
    claim after expiry_at raises StaleApprovalError; a malformed claim raises a validation/typed error);
    `test_outcome_projection_read_only` (AUD-02: list_outcomes returns terminal outcomes and exposes
    no callable that mutates status — assert the module has no outcome-path status flip);
    `test_audit_provenance_emitted` (PERM-01/AUD: an approval lifecycle transition writes an
    `audit_events` row with event_type='approval' and session_id == surface_session_id, with
    surface provenance in the data payload).
    Use the Task-1 conftest helpers. Drive concurrency with `threading.Thread` (mirror
    test_tool_service TOCTOU style). Each test asserts concrete state via direct SELECTs.
    Do NOT implement the broker; tests MUST fail at import (module absent) until Wave 2+.
  </action>
  <acceptance_criteria>
    - File defines all nine named test functions above.
    - Module imports `from atlas_runtime import permission_broker` (RED: ImportError expected now).
    - `python -m pytest services/agent-runtime/tests/test_permission_broker.py -q` errors/fails
      (collection import error is acceptable RED) — it does NOT pass.
    - No production module under atlas_runtime/ is created by this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_permission_broker.py -q 2>&1 | grep -Eq "error|failed|ERRORS|no tests ran|ModuleNotFoundError|ImportError" && echo RED_AS_EXPECTED</automated>
  </verify>
  <done>The broker test module exists, references the exact public API, and fails (RED) because the broker is unimplemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → broker contract | Tests encode the security contract the implementation must satisfy; a weak/missing assertion lets an insecure implementation pass later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.5-01-COVERAGE | Repudiation | Wave 0 test set | mitigate | Tests assert every PERM-01..07/SEC-02/AUD-02 behavior by name; VALIDATION.md per-task map is the checklist |
| T-10.5-01-FALSEGREEN | Tampering | RED gate | mitigate | The module MUST import the absent broker so it cannot green-pass before implementation; verify command asserts RED |
| T-10.5-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (stdlib + existing pytest only); slopcheck N/A |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` passes (conftest valid).
- `python -m pytest services/agent-runtime/tests/test_permission_broker.py -q` is RED (import/assert failure), not green.
- The existing suite still collects: `python -m pytest services/agent-runtime/tests -q --collect-only` succeeds.
</verification>

<success_criteria>
- A failing-first broker test module exists with the nine named tests.
- conftest provides active-session / pending-approval / approval-channel helpers.
- Tests reference the exact public API names Waves 2–4 must implement.
</success_criteria>

<output>
Create `.planning/phases/10.5-surface-scoped-permission-broker/10.5-01-SUMMARY.md` when done.
</output>
