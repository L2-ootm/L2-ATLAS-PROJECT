---
phase: 10.5-surface-scoped-permission-broker
plan: 05
type: execute
wave: 4
depends_on: ["10.5-04"]
files_modified:
  - services/agent-runtime/atlas_runtime/permission_broker.py
autonomous: true
requirements: [PERM-07, AUD-02]
tags: [permissions, approval, broker, scope-guard, audit, security]

must_haves:
  truths:
    - "Session-scoped allow-once/session/always rules are stored on session-keyed state and never written to global policy or config"
    - "An allow-always rule is bounded to workspace_root + surface_kind + tool_name + arg_pattern and cannot match across that 4-tuple"
    - "A rule on session A never auto-resolves an approval owned by session B or a different workspace"
    - "list_outcomes returns terminal cross-surface outcomes as a pure read and exposes no status-mutating path (AUD-02)"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/permission_broker.py"
      provides: "session allow-rule store/consume + list_outcomes/get_outcome read projection"
      contains: "def list_outcomes"
  key_links:
    - from: "services/agent-runtime/atlas_runtime/permission_broker.py"
      to: "session_allow_rules"
      via: "rules keyed by surface_session_id, 4-tuple bound, never to policy.py/config.yaml"
      pattern: "session_allow_rules"
    - from: "services/agent-runtime/atlas_runtime/permission_broker.py"
      to: "audit_events / tool_approvals"
      via: "read-only terminal-outcome projection (no UPDATE)"
      pattern: "def list_outcomes|def get_outcome"
---

<objective>
Finish the broker: the PERM-07 scope guard (session-scoped allow-once/session/always rules held on
session-keyed state, bounded to a 4-tuple, never promoted to global policy/config) and the AUD-02
cross-surface read-only outcome projection (terminal outcomes are observable but expose no decision
authority). This closes the phase's two remaining requirements and the full broker contract.

Purpose: Guarantee session allow-rules cannot widen global scope (PERM-07) and that cross-surface
visibility never grants another surface decision authority (AUD-02).
Output: allow-rule store/consume logic + list_outcomes/get_outcome pure-read projection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.5-surface-scoped-permission-broker/10.5-RESEARCH.md
@.planning/phases/10.5-surface-scoped-permission-broker/10.5-PATTERNS.md
@services/agent-runtime/atlas_runtime/permission_broker.py
@services/agent-runtime/atlas_runtime/policy.py

<interfaces>
<!-- Built in Plans 03/04; extended here. -->

session_allow_rules table (Wave 1): keyed by surface_session_id; columns workspace_root,
surface_kind, tool_name, arg_pattern, rule_kind (allow_once|allow_session|allow_always),
created_at. NEVER write allow rules to policy.py or config.yaml (PERM-07; Pitfall 5).

SessionAllowRule model (Wave 1, tool.py): frozen; mirror the table.

policy.decide(manifest, mode) stays the GLOBAL authority and is read-only-posture (write/shell
always requires_approval). The broker may use a session rule only as a per-claim shortcut for the
initiating session — it cannot change what policy.decide returns for any other session or globally.

permission_broker.claim(...) (Plans 03/04): the place where an allow rule may auto-resolve a pending
approval for the SAME session. allow-once is consumed on first match; allow-session lives for the
session; allow-always persists bounded to the 4-tuple.

audit_service: terminal approval events ride event_type='approval' with status in data. list_outcomes
reads these / terminal tool_approvals rows — pure SELECT, no UPDATE path (AUD-02 line).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session-scoped allow-rule store + consume with hard scope bound (PERM-07)</name>
  <read_first>
    - services/agent-runtime/atlas_runtime/permission_broker.py (claim() resolution path from Plans 03/04)
    - services/agent-runtime/atlas_runtime/policy.py (the GLOBAL authority that must stay untouched)
    - packages/atlas-core/atlas_core/schemas/tool.py (SessionAllowRule model)
    - .planning/phases/10.5-surface-scoped-permission-broker/10.5-RESEARCH.md (Scope-Bounding allow-once/session/always section; Pitfall 5)
  </read_first>
  <behavior>
    - record_allow_rule stores a rule on session_allow_rules keyed by surface_session_id and bounded
      to (workspace_root, surface_kind, tool_name, arg_pattern, rule_kind); it NEVER writes to
      policy.py or config.yaml.
    - A rule recorded for session A does NOT match/auto-resolve an approval owned by session B.
    - An allow-always rule for (ws1, cli, writer, pattern) does NOT match a different workspace,
      surface, tool, or arg_pattern (no wildcard widening).
    - allow-once is consumed (deleted/marked) on first match; allow-session persists for the session;
      allow-always persists bounded to the 4-tuple.
    - policy.decide() output is unchanged for every other session and globally after a rule is recorded.
  </behavior>
  <action>
    In `permission_broker.py` add `record_allow_rule(conn, lock, *, surface_session_id,
    workspace_root, surface_kind, tool_name, arg_pattern, rule_kind)` constructing+validating a
    `SessionAllowRule` and INSERTing into `session_allow_rules` inside `with lock: with conn:`. Add
    `match_allow_rule(conn, *, surface_session_id, workspace_root, surface_kind, tool_name,
    args_normalized) -> Optional[rule]` that SELECTs rules for THAT session only and requires an
    exact 4-tuple match (workspace_root AND surface_kind AND tool_name AND arg_pattern == the
    approval's args_normalized) — no cross-session, no wildcard. CRITICAL — arg_pattern MUST be
    produced by the SAME normalization helper Plan 03 uses for args_normalized
    (`_redact(json.dumps(args, sort_keys=True))`); factor that into one shared helper
    (`tool_service._normalize_args(args)`, created in Plan 03 Task 1) and call it from BOTH the
    invoke() insert path and record_allow_rule, so a rule's arg_pattern is byte-identical to the
    approval's args_normalized and the `==` match is reliable. Do not hand-roll a second
    normalization. Wire the consume path into claim():
    when a decision is allow_once/allow_session/allow_always for the matching active session, record
    the rule per rule_kind (allow_once consumed immediately, allow_session/allow_always persisted)
    and resolve the approval via the existing approve() path; allow-always recording stays bounded to
    the 4-tuple. Add an explicit negative guard/assertion in code comments and a no-op for any
    attempt to touch policy/config (there must be NO import of config_service write APIs or policy
    mutation here). Confirm `policy.decide` is never called to write and is not modified.
  </action>
  <acceptance_criteria>
    - permission_broker defines record_allow_rule and match_allow_rule operating only on
      session_allow_rules keyed by surface_session_id.
    - No write to policy.py / config.yaml: the module does not import a config-write API and does not
      mutate policy state (grep shows no patch_config/policy write in this module).
    - match_allow_rule requires an exact 4-tuple match and is session-scoped (session A rule never
      matches session B / other workspace).
    - arg_pattern is produced by the SAME shared normalization helper as the approval's
      args_normalized (tool_service._normalize_args); assert a cross-path parity case: the same
      args run through the invoke() insert path and through record_allow_rule yield identical
      normalized strings, so a recorded rule actually matches a real approval.
    - test_allow_rules_cannot_widen_scope passes.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_permission_broker.py::test_allow_rules_cannot_widen_scope -x -q && grep -v '^#' atlas_runtime/permission_broker.py | grep -Eqv "patch_config|policy\.decide *=" && echo SCOPE_GUARD_OK</automated>
  </verify>
  <done>Session allow-rules are session-keyed and 4-tuple bound; never widen to global policy/config; cross-session/workspace negative test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-surface read-only outcome projection (AUD-02)</name>
  <read_first>
    - services/agent-runtime/atlas_runtime/permission_broker.py (module from Plans 03/04/Task 1)
    - services/agent-runtime/atlas_runtime/audit_service.py (get_events_for_session read-projection pattern; emit boundary)
    - .planning/phases/10.5-surface-scoped-permission-broker/10.5-RESEARCH.md (Audit Provenance & AUD-02 section)
  </read_first>
  <behavior>
    - list_outcomes returns terminal approval outcomes across surfaces (executed/rejected/failed)
      with surface provenance, as a pure read.
    - get_outcome(approval_id) returns a single terminal outcome view.
    - Neither function nor any other read-projection helper can flip a status — the projection
      exposes no conn-mutating path (AUD-02).
    - An observer using list_outcomes gains no claim/decision authority: there is no parameter or
      return value that resolves a pending approval.
  </behavior>
  <action>
    In `permission_broker.py` add `list_outcomes(conn, *, limit=None) -> list[...]` that SELECTs
    terminal `tool_approvals` rows (status IN ('executed','rejected','failed')) and/or terminal
    `approval` audit events, returning outcome-only views (approval_id, tool_name, status, decision,
    reason, surface_kind, workspace_root, decided_at) — never the live nonce or a claim handle. Add
    `get_outcome(conn, approval_id)`. Both are pure reads: no `lock` parameter, no INSERT/UPDATE,
    no call into claim/approve/reject. Keep them in the broker module so the AUD-02 line is one
    file. Update `__all__`. The projection is the cross-surface visibility mechanism (D-022: no
    pub/sub bus — audit IS the mechanism); it must expose terminal outcomes for observers while the
    actionable claim path stays strictly session-scoped (Plan 03 list_actionable).
  </action>
  <acceptance_criteria>
    - permission_broker defines list_outcomes and get_outcome; both take no `lock` and perform no
      INSERT/UPDATE/claim call.
    - list_outcomes returns terminal outcomes only (no pending) with surface provenance and no nonce.
    - test_outcome_projection_read_only passes; test_audit_provenance_emitted passes.
    - Full broker module is green: `pytest tests/test_permission_broker.py -q`.
  </acceptance_criteria>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_permission_broker.py -q && echo BROKER_FULL_GREEN</automated>
  </verify>
  <done>Cross-surface outcome projection is read-only and grants no decision authority; full broker test module green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session allow-rule → global policy | A session convenience rule must never escalate into global/cross-session authority |
| observer → outcome projection | A read-only observer must be able to see terminal outcomes without gaining any decision authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.5-05-WIDEN | Elevation of Privilege | session allow-rules | mitigate | Rules stored on session_allow_rules keyed by surface_session_id, exact 4-tuple match, never written to policy.py/config.yaml; negative cross-session test (PERM-07, Pitfall 5) |
| T-10.5-05-WILDCARD | Elevation of Privilege | allow-always bound | mitigate | match_allow_rule requires exact workspace_root+surface_kind+tool_name+arg_pattern; no wildcard tool/workspace match (PERM-07) |
| T-10.5-05-OBSERVER | Elevation of Privilege | list_outcomes | mitigate | Projection is pure read (no lock, no UPDATE, no claim call); exposes terminal outcomes only, no nonce/claim handle (AUD-02) |
| T-10.5-05-LEAK | Information disclosure | outcome views | mitigate | Outcome views carry redacted data (emit already redacts); the live nonce is never returned by the projection |
| T-10.5-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; stdlib + existing atlas_core/atlas_runtime only; slopcheck N/A |
</threat_model>

<verification>
- `python -m pytest services/agent-runtime/tests/test_permission_broker.py -q` FULLY green (all nine tests).
- `python -m pytest services/agent-runtime/tests -q` green (full agent-runtime suite, no regression).
- `python -m pytest packages/atlas-core/tests -q` green (schema additions).
- grep confirms no policy/config write path in permission_broker.py.
</verification>

<success_criteria>
- Session allow-rules are session-keyed and 4-tuple bound; never widen to global policy/config (PERM-07).
- Cross-surface outcome projection is read-only and grants no decision authority (AUD-02).
- The full broker test module is green; full agent-runtime + atlas-core suites pass.
</success_criteria>

<output>
Create `.planning/phases/10.5-surface-scoped-permission-broker/10.5-05-SUMMARY.md` when done.
</output>
