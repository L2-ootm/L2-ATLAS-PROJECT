# Plan 10.0.4-05 — Summary

**Wave:** 5 (cockpit + docs + live verify) · **Status:** complete · **Executed:** 2026-06-22 (inline)

## What shipped
The SC3 WebUI visibility surface + the Tool Manifest v0 docs, then end-to-end live verification.

- **`web-ui-react/src/lib/api.ts`** — `ToolManifest`/`ToolApproval`/`ToolResult` interfaces + the
  5-value `ToolApprovalStatus`; `getToolManifests`, `listToolApprovals` (offline-tolerant: []
  on 404/500/503), `proposeToolCall`, `approveToolCall`, `rejectToolCall`. Mirrors the discord client.
- **`web-ui-react/src/routes/System.tsx`** — three new panels (existing glassPanel + bronze-mono
  header convention, no new design system):
  - **TOOL POLICY** (`PolicyPanel`) — literal "READ-ONLY BY DEFAULT" badge, risk-tier legend
    (read=auto-allowed; write/shell=approval), and the explicit no-sensitive-data posture statement
    (env:VAR refs + redaction + SSRF/workspace guards). SC3 text is rendered, not derived.
  - **TOOLS** (`ToolsPanel`) — manifest-driven list (name / risk_level / permissions) from
    `getToolManifests`.
  - **TOOL APPROVALS** (`ToolApprovalsPanel`) — clones the Discord ApprovalsPanel; Approve/Reject
    `IconBtn`s wired to `approveToolCall`/`rejectToolCall`; proposals never execute inline.
- **`docs/tools.md`** — Tool Manifest v0 schema + safety model + the four shipped tools + the
  "adding a tool = manifest + adapter" workflow + the **SC4 naming note** (snake_case enum vs dotted
  external label). **`README.md`** — pointer added to the orientation index.

## Verification
- `npx tsc --noEmit` → clean; `npm run build` → built (pre-existing chunk-size warning only).
- docs gate: `docs/tools.md` contains `risk_level`, `tool_requested`, `tool.requested`; README points to it.
- **Live end-to-end** (release gateway rebuilt + `atlas up`, doctor all ok):
  - SC1 read: `atlas tools call --args '{"op":"read","path":"README.md"}' -- workspace` → ToolResult ok.
  - SC1/SC3 write-gate: `… -- webhook_notify` → **pending** approval, NOT executed.
  - approve path executes the deferred adapter (terminal status `failed` = honest 404 from the test URL — the gate ran it); reject path leaves it `rejected`, no execution.
  - SC4 audit verbs on the operator run: `tool_requested`×3, `tool_completed`×1, `tool_failed`×1.
  - Gateway dispatch-only: `GET /v1/tools/manifests` → 4 tools; `GET /v1/tools/approvals?status=all` → rows; `POST /v1/tools/calls {tool:""}` → **400** (require_arg guard).

## Human-verify checkpoint (SC3 visual)
Automated evidence covers 5 of the 6 checkpoint items (read auto-run, write-gate, approve-executes,
reject-no-exec, manifest list, audit verbs). The **one remaining item is the pure visual eyeball** of
the System-page panels in a browser — the stack is live at `http://127.0.0.1:5173` (System page); the
data endpoints feeding the panels are confirmed returning correct payloads, and the build is green.
Deferred to the operator (same pattern as the 10.0.2 banner check).

## Threats mitigated
- T-10.0.4-13 (hidden posture → PolicyPanel renders literal posture text),
  T-10.0.4-14 (UI auto-execute → Approve/Reject only; write-gate verified live).

## Self-Check: PASSED
