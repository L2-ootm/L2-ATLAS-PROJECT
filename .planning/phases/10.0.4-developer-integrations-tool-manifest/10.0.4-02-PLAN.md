---
phase: 10.0.4-developer-integrations-tool-manifest
plan: 02
type: execute
wave: 2
depends_on: [10.0.4-01]
files_modified:
  - services/agent-runtime/atlas_runtime/tools/__init__.py
  - services/agent-runtime/atlas_runtime/tools/adapters/__init__.py
  - services/agent-runtime/atlas_runtime/tools/adapters/base.py
  - services/agent-runtime/atlas_runtime/tools/adapters/workspace.py
  - services/agent-runtime/atlas_runtime/tools/adapters/github.py
  - services/agent-runtime/atlas_runtime/tools/adapters/web_fetch.py
  - services/agent-runtime/atlas_runtime/tools/adapters/webhook_notify.py
  - services/agent-runtime/atlas_runtime/tools/manifests/workspace.yaml
  - services/agent-runtime/atlas_runtime/tools/manifests/github.yaml
  - services/agent-runtime/atlas_runtime/tools/manifests/web_fetch.yaml
  - services/agent-runtime/atlas_runtime/tools/manifests/webhook_notify.yaml
autonomous: true
requirements: [SC1, SC2]
must_haves:
  truths:
    - "workspace adapter reads files / lists dirs / greps only within the workspace boundary"
    - "github adapter shells to the gh CLI for read subcommands and returns ok=False (not crash) when gh is absent/non-zero"
    - "web_fetch performs GET-only with an SSRF guard, size cap, and timeout"
    - "webhook_notify performs POST and reuses the same SSRF guard (write-class)"
    - "each adapter has a matching manifest declaring name/risk_level/permissions/inputs/outputs/audit_events"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/tools/adapters/web_fetch.py"
      provides: "GET adapter with _assert_safe SSRF guard + size cap"
      contains: "_assert_safe"
    - path: "services/agent-runtime/atlas_runtime/tools/adapters/workspace.py"
      provides: "read/list/grep bounded by check_workspace_boundary"
      contains: "check_workspace_boundary"
    - path: "services/agent-runtime/atlas_runtime/tools/manifests/web_fetch.yaml"
      provides: "web_fetch manifest (risk_level: read)"
      contains: "risk_level"
  key_links:
    - from: "tools/adapters/workspace.py"
      to: "policy.check_workspace_boundary"
      via: "boundary check before file I/O"
      pattern: "check_workspace_boundary"
    - from: "tools/adapters/github.py"
      to: "gh CLI"
      via: "subprocess argv vector (no shell=True)"
      pattern: "subprocess.run"
---

<objective>
Implement the four integration adapters (workspace, github, web_fetch, webhook_notify) and their YAML manifests, using only stdlib + the installed `gh` CLI (no new Python deps). Each adapter exposes `run(args, ctx) -> ToolResult` and assumes it has already been authorized — adapters perform NO policy checks (the chokepoint in Plan 03 owns policy).

Purpose: SC1 (four integrations work end-to-end) + SC2 (manifest-per-tool contract). Security-critical: SSRF guard on web_fetch/webhook_notify, workspace-boundary guard, gh argv safety (no shell=True), Windows CREATE_NO_WINDOW.
Output: tools/ package skeleton, base protocol, four adapters, four manifests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-PATTERNS.md

<interfaces>
<!-- Contracts from Plan 01 (atlas-core) and existing runtime. Use directly. -->

From packages/atlas-core/atlas_core/schemas/tool.py (Plan 01 output):
- ToolResult(tool_name: str, ok: bool, output: str = "", error: str|None = None, exit_code: int|None = None) — frozen; construct, never mutate.
- ToolManifest(name, description, risk_level: "read"|"write"|"shell", permissions: list[str], inputs: dict[str,str], outputs: dict[str,str], audit_events: list[str]).

From services/agent-runtime/atlas_runtime/policy.py:
- check_workspace_boundary(workspace_root, target_path) -> PolicyDecision (policy.py:38-67): resolved_root = Path(root).resolve(); (resolved_root/target).resolve().relative_to(resolved_root) → ValueError when outside. CWD-escape-safe, cross-platform (D-006). Reuse — do NOT hand-roll ".."/abs-path checks.

From services/agent-runtime/atlas_runtime/discord_api.py (urllib HTTP client analog, lines 29-56):
- stdlib urllib.request with HTTPError handling; no requests/httpx.

From services/agent-runtime/atlas_runtime/discord_control.py (lines 162-177):
- Windows subprocess creation flags. For capture-and-wait use ONLY CREATE_NO_WINDOW, NOT the detached triad.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tools package skeleton + base protocol + workspace adapter</name>
  <read_first>
    - services/agent-runtime/atlas_runtime/agents/base.py (the AgentRuntime protocol analog for base.py)
    - services/agent-runtime/atlas_runtime/policy.py (check_workspace_boundary 38-67 — the boundary guard to consume)
    - services/agent-runtime/tests/test_tool_adapters.py (Plan 00 RED test — workspace-escape case)
  </read_first>
  <behavior>
    - workspace read of an in-workspace file → ToolResult(ok=True, output=<file text or JSON>)
    - workspace read/list/grep of a path escaping the workspace root → ToolResult(ok=False, error=...) or raises before I/O (boundary rejected)
    - base.ToolAdapter protocol declares run(args: dict, ctx) -> ToolResult
  </behavior>
  <action>
    Create services/agent-runtime/atlas_runtime/tools/__init__.py and tools/adapters/__init__.py (package markers). Create tools/adapters/base.py defining a `ToolAdapter` protocol with `run(args: dict, ctx) -> ToolResult` (mirror agents/base.py). Create tools/adapters/workspace.py: `run(args, ctx)` dispatches on `args["op"]` in {"read_file","list_dir","grep"}; resolve the workspace root from `ctx` (or CWD); gate EVERY path through `policy.check_workspace_boundary(root, target)` BEFORE any filesystem read — on ValueError/boundary failure return ToolResult(ok=False, error="path outside workspace"). read_file returns file text; list_dir returns a JSON array of names; grep returns matching lines for a literal substring. Do NOT hand-roll path-escape checks (use the policy helper). The adapter performs NO policy/risk decision (that is the chokepoint's job).
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_tool_adapters.py -q -k "workspace"</automated>
  </verify>
  <acceptance_criteria>
    - tools/ and tools/adapters/ are importable packages
    - base.ToolAdapter protocol defined with run(args, ctx) -> ToolResult
    - workspace adapter gates all I/O through check_workspace_boundary; escape rejected
    - workspace test cases pass
  </acceptance_criteria>
  <done>Workspace read adapter is boundary-safe; package skeleton ready for the other adapters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: github (gh subprocess) + web_fetch (SSRF) + webhook_notify (POST) adapters</name>
  <read_first>
    - services/agent-runtime/atlas_runtime/discord_api.py (urllib client pattern 29-56 — for web_fetch/webhook_notify)
    - services/agent-runtime/atlas_runtime/discord_control.py (Windows spawn flags 162-177 — for the CREATE_NO_WINDOW helper)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md (Pattern 4 SSRF guard; Pattern 5 gh subcommands + field names; Pitfall 1 CREATE_NO_WINDOW; A1 verify gh --json fields)
    - services/agent-runtime/tests/test_tool_adapters.py (Plan 00 RED tests — SSRF cases, size cap, gh-failure honest case)
  </read_first>
  <behavior>
    - web_fetch _assert_safe raises for http://localhost, http://127.0.0.1, http://10.0.0.1, http://169.254.1.1, file://x (non-http(s)/loopback/RFC1918/link-local/reserved blocked)
    - web_fetch oversize response (> size cap) raises before returning
    - web_fetch valid public GET (urlopen mocked) → ToolResult(ok=True, output=<text>)
    - github with gh returncode!=0 or gh absent → ToolResult(ok=False, error=...), does NOT raise out
    - webhook_notify reuses the same _assert_safe SSRF guard before POST
  </behavior>
  <action>
    Create a shared `_no_window_flags()` helper (subprocess.CREATE_NO_WINDOW if os.name=="nt" else 0) — place in base.py or a small util importable by github/webhook adapters (Pitfall 1; NOT the detached triad).
    tools/adapters/github.py: `run(args, ctx)` dispatches on `args["op"]` in {"repo_view","issue_list","pr_view"}; build the argv VECTOR (never shell=True, never a shell string) per RESEARCH Pattern 5 (gh repo view <repo> --json name,description,url,visibility; gh issue list --repo <repo> --json number,title,state,author --limit N; gh pr view <n> --repo <repo> --json number,title,state,body,author); call subprocess.run(argv, capture_output=True, text=True, timeout=30, creationflags=_no_window_flags()). returncode!=0 OR FileNotFoundError (gh absent) → return ToolResult(ok=False, error=stderr-or-"gh CLI not found") — never raise out of the adapter (honest-failure case; must not crash registry load). VERIFY the --json field names against the installed gh 2.92.0 before locking the manifest (A1: `gh <cmd> --json` with no value prints valid fields).
    tools/adapters/web_fetch.py: per RESEARCH Pattern 4 — `_assert_safe(url)`: urlparse scheme must be http/https else raise; block host in {localhost,0.0.0.0,::1}; socket.getaddrinfo + ipaddress reject is_private/is_loopback/is_link_local/is_reserved/is_multicast. `run`: GET-only urllib.request.Request(method="GET"), timeout=10, read at most _MAX_BYTES+1 and raise if over the cap, decode utf-8 replace → ToolResult(ok=True, ...). Add a comment documenting the DNS-rebinding TOCTOU as a known v0 limitation (do not over-engineer).
    tools/adapters/webhook_notify.py: write-class POST via urllib; reuse the SAME `_assert_safe` (import from web_fetch or a shared module) before sending; pass the body/headers from args. webhook target URLs/headers may carry tokens → they are redacted at the audit boundary by tool_service (Plan 03), NOT here.
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_tool_adapters.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All SSRF cases (localhost/127.0.0.1/10.0.0.1/169.254.x/file://) raise in _assert_safe
    - web_fetch enforces GET-only, size cap, timeout
    - github returns ok=False (no raise) on gh non-zero/absent
    - webhook_notify reuses the SSRF guard
    - github --json field names verified against installed gh 2.92.0
    - No subprocess uses shell=True; argv vectors only
  </acceptance_criteria>
  <done>Three network/subprocess adapters honor SSRF, argv-safety, and honest-failure; test_tool_adapters.py GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Write the four tool manifests</name>
  <read_first>
    - packages/atlas-core/atlas_core/schemas/tool.py (Plan 01 ToolManifest field names + ToolRiskLevel — manifests must validate against it)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-CONTEXT.md (risk taxonomy read|write|shell; read-only default; permissions examples like fs:read, net:get)
  </read_first>
  <action>
    Create one YAML manifest per adapter under tools/manifests/, each validating against ToolManifest (yaml.safe_load → model_validate must succeed). Set: workspace.yaml risk_level: read, permissions e.g. ["fs:read"], inputs op/path/pattern, outputs; github.yaml risk_level: read, permissions ["net:gh"], inputs op/repo/number/limit; web_fetch.yaml risk_level: read, permissions ["net:get"], inputs url; webhook_notify.yaml risk_level: write, permissions ["net:post"], inputs url/body/headers. Each declares audit_events: [tool_requested, tool_completed, tool_failed] (snake_case; the dotted form is the external label per SC4). `name` MUST match the adapter binding key the registry (Plan 03) will use. Keep descriptions concrete and short. No fenced production code — these are data files.
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -c "import yaml,glob; from atlas_core.schemas.tool import ToolManifest; [ToolManifest.model_validate(yaml.safe_load(open(p,encoding='utf-8'))) for p in glob.glob('atlas_runtime/tools/manifests/*.yaml')]; print('4 manifests valid')"</automated>
  </verify>
  <acceptance_criteria>
    - Four manifests exist (workspace/github/web_fetch/webhook_notify)
    - Each validates against ToolManifest
    - webhook_notify is risk_level: write; the other three are read
    - Each declares the three audit_event verbs and a name matching its adapter
  </acceptance_criteria>
  <done>Manifest-per-tool contract satisfied (SC2); adding a tool = manifest + adapter is now demonstrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator args → filesystem | workspace adapter must not read outside the workspace root |
| operator args → outbound HTTP | web_fetch/webhook_notify must not reach loopback/RFC1918/non-http(s) targets (SSRF) |
| operator args → gh subprocess | github adapter must pass argv vectors, never a shell string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.0.4-04 | Information Disclosure / EoP | web_fetch / webhook_notify SSRF | mitigate | _assert_safe blocks non-http(s), localhost, RFC1918/loopback/link-local/reserved; size cap + timeout; GET-only on web_fetch (test_tool_adapters covers all five cases). DNS-rebind TOCTOU documented as v0 limitation |
| T-10.0.4-05 | Tampering / Information Disclosure | workspace path traversal | mitigate | every path gated through policy.check_workspace_boundary before I/O; escape → ToolResult(ok=False) (test covers escape) |
| T-10.0.4-07 | Tampering (command injection) | github gh subprocess | mitigate | argv vector only, never shell=True; user values are discrete argv elements; timeout=30; CREATE_NO_WINDOW on Windows |
| T-10.0.4-08 | Denial of Service | gh absent / network failure | mitigate | github/web_fetch/webhook return ToolResult(ok=False) on failure; registry load never crashes |
</threat_model>

<verification>
- test_tool_adapters.py green (SSRF, size cap, workspace escape, gh-failure)
- four manifests validate against ToolManifest
- no shell=True anywhere in adapters
</verification>

<success_criteria>
- [ ] Four adapters implemented, stdlib + gh only (SC1)
- [ ] SSRF guard + size cap + GET-only on web_fetch; same guard on webhook_notify
- [ ] workspace bounded by check_workspace_boundary
- [ ] github honest-failure on gh non-zero/absent
- [ ] Four valid manifests, name-matched to adapters (SC2)
</success_criteria>

<output>
Create `.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-02-SUMMARY.md` when done
</output>
