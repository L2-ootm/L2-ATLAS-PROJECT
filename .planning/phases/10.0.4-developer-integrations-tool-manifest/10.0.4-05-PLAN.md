---
phase: 10.0.4-developer-integrations-tool-manifest
plan: 05
type: execute
wave: 5
depends_on: [10.0.4-04]
files_modified:
  - services/web-ui-react/src/lib/api.ts
  - services/web-ui-react/src/routes/System.tsx
  - docs/tools.md
  - README.md
autonomous: false
requirements: [SC2, SC3, SC4]
must_haves:
  truths:
    - "System page shows a POLICY panel: read-only-by-default badge, risk-tier legend, explicit no-sensitive-data posture statement"
    - "System page shows a TOOLS list rendered from /v1/tools/manifests (name/risk_level/permissions)"
    - "System page shows a Tool Approvals queue (Approve/Reject) reusing the Discord ApprovalsPanel pattern; proposals never execute inline"
    - "docs explain Tool Manifest v0 authoring (manifest + adapter) and that dotted tool.requested maps to snake_case tool_requested"
  artifacts:
    - path: "services/web-ui-react/src/routes/System.tsx"
      provides: "PolicyPanel + ToolsPanel + ToolApprovalsPanel"
      contains: "PolicyPanel"
    - path: "services/web-ui-react/src/lib/api.ts"
      provides: "getToolManifests/listToolApprovals/proposeToolCall/approveToolCall/rejectToolCall"
      contains: "getToolManifests"
    - path: "docs/tools.md"
      provides: "Tool Manifest v0 schema + authoring guide + SC4 naming note"
      contains: "risk_level"
  key_links:
    - from: "System.tsx ToolsPanel"
      to: "/v1/tools/manifests"
      via: "api.getToolManifests"
      pattern: "getToolManifests"
    - from: "System.tsx ToolApprovalsPanel"
      to: "/v1/tools/approvals/{id}/approve"
      via: "api.approveToolCall"
      pattern: "approveToolCall"
---

<objective>
Surface policy/tools/approvals in the WebUI (SC3) and document the Tool Manifest v0 contract (SC2/SC4 naming note). The System page gains three panels derived from existing System panels + the Discord ApprovalsPanel. SC3 requires the read-only-by-default posture and the no-sensitive-data statement to be VISIBLE in the UI, not merely enforced in code — so this plan ends with a human-verify checkpoint.

Purpose: SC3 (policy/permissions visible in WebUI), SC2 (manifest-driven tools list + authoring doc), SC4 (document dotted vs snake_case naming so verification traces unambiguously).
Output: api.ts client functions + interfaces, System.tsx panels, docs/tools.md, README pointer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-PATTERNS.md

<interfaces>
<!-- Cockpit analogs: System.tsx panels + Discord.tsx ApprovalsPanel + api.ts discord client. -->

From services/web-ui-react/src/lib/api.ts (discord client block 952-1110):
- apiFetch<T>(path, init?) wrapper; ApiError with .status.
- listDiscordApprovals pattern (offline-tolerant): catch ApiError 404/500/503 → return []; else throw.
- approveDiscordWrite: apiFetch(`/v1/discord/approvals/${encodeURIComponent(id)}/approve`, {method:'POST'}).
- DiscordApproval interface (1051-1064) + the 5-value status Literal — mirror for ToolApproval.

From services/web-ui-react/src/routes/System.tsx:
- glassPanel({...}) helper; RuntimeConfigPanel/ModulesPanel header convention: bronze-mono header (var(--atlas-bronze), var(--l2-font-mono), letterSpacing 0.22em), Cpu icon, key/value grid rows, var(--l2-hairline) borders.

From services/web-ui-react/src/routes/Discord.tsx (ApprovalsPanel 211-240):
- Clock-icon header "PENDING APPROVALS" + count; per-row status dot + summary + action·status; Approve (var(--atlas-cyan) Check) / Reject (var(--l2-error) X) IconBtns only when status==='pending'; statusTone helper (204-209).

From the gateway (Plan 04): GET /v1/tools/manifests, GET /v1/tools/approvals, POST /v1/tools/calls, POST /v1/tools/approvals/{id}/approve, POST /v1/tools/approvals/{id}/reject.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts tool client functions + interfaces</name>
  <read_first>
    - services/web-ui-react/src/lib/api.ts (discord client block 952-1110: interfaces, apiFetch wrappers, offline-tolerant list pattern, encodeURIComponent usage)
  </read_first>
  <action>
    Extend api.ts mirroring the discord client block. Add interfaces: `ToolManifest` (name, description, risk_level: 'read'|'write'|'shell', permissions: string[], inputs, outputs, audit_events) and `ToolApproval` (mirror the DiscordApproval interface, reusing a 5-value `ToolApprovalStatus` Literal 'pending'|'executing'|'executed'|'rejected'|'failed'). Add functions: `getToolManifests(): Promise<ToolManifest[]>`; `listToolApprovals(): Promise<ToolApproval[]>` (offline-tolerant — catch ApiError 404/500/503 → return [] like listDiscordApprovals); `proposeToolCall(args:{tool, args?, mode?, reason?}): Promise<ToolResult | ToolApproval>` → POST /v1/tools/calls; `approveToolCall(id): Promise<ToolApproval>` and `rejectToolCall(id, reason?): Promise<ToolApproval>` → POST with encodeURIComponent(id). Match the existing typing/error conventions exactly.
  </action>
  <verify>
    <automated>cd services/web-ui-react && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - api.ts exports getToolManifests/listToolApprovals/proposeToolCall/approveToolCall/rejectToolCall + ToolManifest/ToolApproval interfaces
    - listToolApprovals is offline-tolerant (returns [] on 404/500/503)
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>Typed client for the tool endpoints; cockpit panels can consume it.</done>
</task>

<task type="auto">
  <name>Task 2: System.tsx PolicyPanel + ToolsPanel + ToolApprovalsPanel</name>
  <read_first>
    - services/web-ui-react/src/routes/System.tsx (glassPanel; RuntimeConfigPanel/ModulesPanel header + grid convention)
    - services/web-ui-react/src/routes/Discord.tsx (ApprovalsPanel 211-240; statusTone 204-209)
    - services/web-ui-react/src/lib/api.ts (Task 1 client functions)
  </read_first>
  <action>
    Add three panels to System.tsx using the existing glassPanel + bronze-mono header convention (reuse the design tokens; no new design system):
    - `PolicyPanel` (static): a "READ-ONLY BY DEFAULT" badge; a risk-tier legend (read = auto-allowed; write/shell = require operator approval); and an explicit no-sensitive-data posture statement (e.g. "ATLAS stores no secrets — credentials are env:VAR references and are redacted before any audit/approval row is persisted"). SC3 requires this text be SHOWN, not just enforced — render literal strings, not derived-only state.
    - `ToolsPanel`: call getToolManifests on mount; render a read-only key/value grid of name / risk_level / permissions per tool (RuntimeConfigPanel grid style).
    - `ToolApprovalsPanel`: clone Discord's ApprovalsPanel; poll listToolApprovals; render pending tool approvals with Approve (approveToolCall) / Reject (rejectToolCall) IconBtns; reuse statusTone; proposals NEVER execute inline (Approve triggers the gated execution server-side). Mount the three panels in the System route alongside the existing panels.
  </action>
  <verify>
    <automated>cd services/web-ui-react && npx tsc --noEmit 2>&1 | tail -3 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PolicyPanel renders the read-only badge + risk legend + literal no-sensitive-data statement
    - ToolsPanel lists manifests (name/risk_level/permissions) from the API
    - ToolApprovalsPanel renders pending approvals with working Approve/Reject wired to the API
    - tsc --noEmit + npm run build pass
  </acceptance_criteria>
  <done>SC3 surface implemented (pending the human-verify checkpoint below).</done>
</task>

<task type="auto">
  <name>Task 3: docs/tools.md authoring guide + README pointer</name>
  <read_first>
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-CONTEXT.md (manifest schema, risk taxonomy, deferred items)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md (Pitfall 6 dotted vs snake_case; the four shipped adapters)
    - README.md (where to add the docs pointer — find the existing docs/feature index)
  </read_first>
  <action>
    Create docs/tools.md documenting Tool Manifest v0: the YAML schema (name/description/risk_level/permissions/inputs/outputs/audit_events) with the read|write|shell taxonomy and the read-only-by-default + approval-gate policy; the four shipped tools (workspace/github/web_fetch/webhook_notify) and their risk levels; the "adding a tool = write a manifest + adapter" workflow (manifest under tools/manifests/, adapter run(args, ctx)->ToolResult under tools/adapters/, registry binds by name); and an explicit SC4 NAMING NOTE that the audit-bus enum values are snake_case `tool_requested`/`tool_completed`/`tool_failed` while the SC's dotted `tool.requested`/`.completed`/`.failed` is the conceptual/external label (so /gsd-verify-work traces SC4 unambiguously). State the no-sensitive-data posture (env:VAR refs, redaction at the audit boundary) and the SSRF/workspace-boundary guards as the safety model. Add a one-line pointer to docs/tools.md from README.md's docs index.
  </action>
  <verify>
    <automated>test -f docs/tools.md && grep -qi "tool_requested" docs/tools.md && grep -qi "tool.requested" docs/tools.md && grep -qi "risk_level" docs/tools.md && grep -q "docs/tools.md" README.md && echo "docs present + SC4 naming note + README pointer"</automated>
  </verify>
  <acceptance_criteria>
    - docs/tools.md documents the manifest v0 schema + authoring workflow (SC2)
    - SC4 naming note present (snake_case enum vs dotted external label)
    - no-sensitive-data posture + SSRF/boundary guards documented
    - README points to docs/tools.md
  </acceptance_criteria>
  <done>Tool Manifest v0 is documented (SC2); SC4 trace is unambiguous.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The System page POLICY panel (read-only-by-default badge + risk-tier legend + no-sensitive-data posture statement), the manifest-driven TOOLS list, and the Tool Approvals queue (Approve/Reject) — the SC3 visibility surface, plus the four working integrations end-to-end (SC1).</what-built>
  <how-to-verify>
    1. Start the stack: `atlas up` (rebuild the gateway binary first if lib.rs changed this phase — per project memory it is stale otherwise), then open the cockpit System page.
    2. Confirm the POLICY panel SHOWS: a "READ-ONLY BY DEFAULT" badge, the risk-tier legend (read=allowed, write/shell=approval), and the explicit no-sensitive-data posture statement. (SC3 — must be visible text.)
    3. Confirm the TOOLS list shows all four tools with their risk_level and permissions (workspace/github/web_fetch read; webhook_notify write).
    4. Read-class end-to-end (SC1): run `atlas tools call --json -- workspace` (or web_fetch against a public URL) and confirm it returns a ToolResult AND that the System page / audit shows tool_requested then tool_completed.
    5. GitHub adapter (SC1, depends on your live gh auth): `atlas tools call ... github repo_view` returns repo JSON; ATLAS handles no token.
    6. Write-gate (SC1/SC3): propose a webhook_notify call; confirm it does NOT fire — it appears as a PENDING approval in the Tool Approvals queue. Click Approve and confirm it then executes (tool_completed); separately propose another and Reject it and confirm it does NOT execute.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe what is wrong (e.g. posture text missing, write tool auto-fired, manifest missing from the list).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cockpit → gateway | the UI calls the dispatch-only endpoints; no policy decision happens client-side |
| operator eyes → posture text | SC3 requires the safety posture to be readable in the UI, not inferred |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.0.4-13 | Repudiation / Info (UX) | hidden policy posture | mitigate | PolicyPanel renders literal read-only badge + no-sensitive-data statement; human-verify checkpoint confirms it is visible (SC3) |
| T-10.0.4-14 | Elevation of Privilege | UI auto-executing a write | mitigate | Approve/Reject only; proposals never execute inline; checkpoint step 6 verifies the write-gate fires |
| T-10.0.4-SC | Tampering | npm dependency | accept | No new npm packages added; panels use existing components/tokens (RESEARCH: no install this phase) |
</threat_model>

<verification>
- tsc --noEmit + npm run build green
- docs/tools.md present with SC4 naming note + README pointer
- human-verify checkpoint passed (SC3 visibility + SC1 end-to-end + write-gate)
</verification>

<success_criteria>
- [ ] POLICY panel visible with read-only badge + risk legend + no-sensitive-data statement (SC3)
- [ ] TOOLS list from manifests (SC2/SC3)
- [ ] Tool Approvals queue Approve/Reject; no inline execution (SC1/SC3)
- [ ] docs/tools.md + README pointer (SC2); SC4 naming note documented
- [ ] checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-05-SUMMARY.md` when done
</output>
