---
phase: 10.0.4-developer-integrations-tool-manifest
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - services/agent-runtime/tests/tools/conftest.py
  - services/agent-runtime/tests/test_tool_registry.py
  - services/agent-runtime/tests/test_tool_service.py
  - services/agent-runtime/tests/test_tool_adapters.py
  - packages/atlas-core/tests/test_tool_manifest.py
  - packages/atlas-core/tests/test_schemas.py
autonomous: true
requirements: [SC1, SC2, SC4]
must_haves:
  truths:
    - "Every implementation wave has a failing test file to make pass (RED state)"
    - "Tests run against an isolated temp DB, never the live ~/.atlas/atlas.db"
    - "0001_core.sql audit_events.event_type confirmed as TEXT with no CHECK constraint"
  artifacts:
    - path: "services/agent-runtime/tests/tools/conftest.py"
      provides: "temp-DB + migrations fixture; subprocess/urllib mock helpers"
      contains: "apply_migrations"
    - path: "services/agent-runtime/tests/test_tool_service.py"
      provides: "invoke/propose/approve/reject + audit-verb test stubs"
    - path: "packages/atlas-core/tests/test_tool_manifest.py"
      provides: "ToolManifest validation stubs"
  key_links:
    - from: "tests/tools/conftest.py"
      to: "db.apply_migrations"
      via: "fixture builds temp DB"
      pattern: "apply_migrations"
---

<objective>
Create the Wave 0 test scaffolding for Phase 10.0.4 per VALIDATION.md, and confirm research assumption A2 (no CHECK constraint on `audit_events.event_type`). Every downstream wave gets a failing test file (RED) so implementation is verifiable.

Purpose: Establish the Nyquist test baseline before any implementation. Tests must use a temp-DB pytest fixture — NEVER the live `atlas` CLI (which hardcodes `~/.atlas/atlas.db` and ignores ATLAS_HOME; project-memory gotcha `cli-db-path-not-atlas-home`).
Output: Five test files + one tools/conftest.py fixture, all failing/skipped on missing-not-yet-implemented imports; confirmation that A2 holds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-VALIDATION.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm A2 — no CHECK constraint on audit_events.event_type</name>
  <read_first>
    - infra/migrations/0001_core.sql (the `CREATE TABLE ... audit_events` DDL — event_type column definition)
    - services/agent-runtime/atlas_runtime/audit_service.py (HB-04-02 docstring: validation is Pydantic-side)
    - packages/atlas-core/tests/test_schemas.py (the AuditEvent test analog being extended)
  </read_first>
  <action>
    Read the `audit_events` DDL in infra/migrations/0001_core.sql and confirm `event_type` is declared `TEXT NOT NULL` with NO CHECK constraint enumerating event types. If a CHECK constraint exists, STOP and surface it (it would force a migration, contradicting CONTEXT/RESEARCH A2). Then extend packages/atlas-core/tests/test_schemas.py with a test asserting that AuditEvent accepts event_type values `tool_requested`, `tool_completed`, `tool_failed`. The test MUST currently FAIL (the Literal does not yet include the three verbs — added in Plan 01). Use a parametrized test or three asserts; reference SC4. Do NOT add a fenced implementation of AuditEvent — only the test.
  </action>
  <verify>
    <automated>cd packages/atlas-core && python -m pytest tests/test_schemas.py -q 2>&1 | grep -E "tool_requested|tool_completed|tool_failed|fail|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 0001_core.sql confirmed: event_type is TEXT NOT NULL, no CHECK on enum (or STOP raised if present)
    - test_schemas.py contains a test for the three new event_type verbs
    - That test FAILS against current AuditEvent (RED) — proving Plan 01 must extend the Literal
  </acceptance_criteria>
  <done>A2 confirmed; new event_type test present and red.</done>
</task>

<task type="auto">
  <name>Task 2: Create tools test conftest (temp-DB fixture + mocks)</name>
  <read_first>
    - services/agent-runtime/tests/conftest.py (existing Phase C / runtime conftest — the temp-DB + migrations fixture pattern to mirror)
    - services/agent-runtime/atlas_runtime/db.py (connect, apply_migrations signatures — lines 85-109)
    - services/agent-runtime/atlas_runtime/mission_service.py (ensure_operator_run — fixture may pre-seed the operator run)
  </read_first>
  <action>
    Create services/agent-runtime/tests/tools/conftest.py with: (1) a `tool_db` fixture that calls `db.connect(tmp_path / "t.db")` then `db.apply_migrations(conn)` so ALL migrations including 0013 apply (0013 does not exist yet — fixture will fail until Plan 03; that is the intended RED for the migration), returning `(conn, lock)`; mirror the existing runtime conftest fixture. (2) A `mock_gh` helper that patches `subprocess.run` to return a CompletedProcess with canned `--json` stdout and returncode 0. (3) A `mock_urlopen` helper that patches `urllib.request.urlopen` to return a context manager yielding a `.read(n)` of canned bytes. Create services/agent-runtime/tests/tools/__init__.py if the package layout requires it. NEVER import or call the `atlas` CLI `_get_connection` (Pitfall 2 — it hits the real DB).
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -c "import ast; ast.parse(open('tests/tools/conftest.py').read()); print('conftest parses')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/tools/conftest.py exists and parses
    - `tool_db` fixture builds a temp DB via db.connect + db.apply_migrations (no live ~/.atlas path)
    - mock_gh and mock_urlopen helpers present
  </acceptance_criteria>
  <done>Temp-DB fixture and subprocess/urllib mocks scaffolded; no live-DB access.</done>
</task>

<task type="auto">
  <name>Task 3: Create the four failing test stubs (manifest, registry, service, adapters)</name>
  <read_first>
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the exact test names and SC mapping)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md (Validation Architecture: honest-failure cases, what to mock vs exercise)
    - services/agent-runtime/tests/test_discord_service.py (Phase C service-test analog: temp-DB usage, audit-event assertions, atomic-claim concurrency test)
  </read_first>
  <action>
    Create four pytest stub files, each importing the not-yet-existing modules so they collect as RED (use `pytest.importorskip` or a direct import that errors — prefer real import so the failure is explicit). Map tests to VALIDATION.md task IDs and SC references in docstrings:
    (a) packages/atlas-core/tests/test_tool_manifest.py (SC2): test ToolManifest accepts a valid dict; rejects unknown risk_level (ValidationError); rejects missing required `name`/`risk_level`. Import from `atlas_core.schemas.tool`.
    (b) services/agent-runtime/tests/test_tool_registry.py (SC2): test registry loads shipped manifests, binds name→adapter, `resolve(unknown)` raises ValueError, an invalid manifest fails at load. Import from `atlas_runtime.tools.registry`.
    (c) services/agent-runtime/tests/test_tool_service.py (SC1/SC4): use the `tool_db` fixture; test read-class invoke emits tool_requested then tool_completed; adapter raise emits tool_failed; write/shell short-circuits to a pending tool_approvals row (no inline exec); approve of pending executes + emits tool_completed; concurrent approve → only one executes (atomic claim); secret in args is `[REDACTED]` in stored rows. Import from `atlas_runtime.tool_service`.
    (d) services/agent-runtime/tests/test_tool_adapters.py (SC1): test web_fetch `_assert_safe` raises for `http://localhost`, `http://127.0.0.1`, `http://10.0.0.1`, `http://169.254.1.1`, `file://x` (patch getaddrinfo for the IP cases); web_fetch oversize body raises (size cap); workspace adapter rejects a CWD-escaping path via check_workspace_boundary; github adapter returns ToolResult(ok=False) when `gh` exits non-zero (mock_gh returncode!=0). Import from `atlas_runtime.tools.adapters.*`.
    Do NOT implement the production modules — these tests must fail on import or assertion (RED). No fenced production code in any test.
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_tool_registry.py tests/test_tool_service.py tests/test_tool_adapters.py --co -q 2>&1 | tail -5; cd ../../packages/atlas-core && python -m pytest tests/test_tool_manifest.py --co -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist with tests mapped to SC1/SC2/SC4 and VALIDATION.md task IDs
    - Each fails RED on missing-module import or unmet assertion (collection error is acceptable RED state pre-implementation)
    - test_tool_adapters.py covers all five SSRF cases + size cap + workspace escape + gh-failure honest case
  </acceptance_criteria>
  <done>Four RED test files scaffolded; downstream waves have explicit targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → SQLite | Tests must write only to a temp DB, never the operator's live ~/.atlas/atlas.db |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.0.4-00-01 | Tampering | test DB isolation | mitigate | conftest uses db.connect(tmp_path/"t.db"); tests never call CLI `_get_connection` (Pitfall 2) |
| T-10.0.4-SC | Tampering | npm/pip installs | accept | No package installs in this plan; pyyaml declaration deferred to Plan 01. RESEARCH Package Legitimacy Audit shows no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Wave 0 test files exist and are RED (fail on not-yet-implemented imports/assertions)
- A2 confirmed: audit_events.event_type is TEXT, no CHECK
- conftest fixture isolates to a temp DB
</verification>

<success_criteria>
- [ ] 5 test files + tools/conftest.py created
- [ ] All map to SC1/SC2/SC4 with VALIDATION.md task IDs in docstrings
- [ ] No test touches the live ~/.atlas/atlas.db
- [ ] A2 (no CHECK on event_type) confirmed
</success_criteria>

<output>
Create `.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-00-SUMMARY.md` when done
</output>
