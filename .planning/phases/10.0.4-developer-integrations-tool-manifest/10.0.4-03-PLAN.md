---
phase: 10.0.4-developer-integrations-tool-manifest
plan: 03
type: execute
wave: 3
depends_on: [10.0.4-01, 10.0.4-02]
files_modified:
  - services/agent-runtime/atlas_runtime/tools/registry.py
  - services/agent-runtime/atlas_runtime/tool_service.py
  - infra/migrations/0013_tool_approvals.sql
autonomous: true
requirements: [SC1, SC2, SC4]
must_haves:
  truths:
    - "registry loads each manifest, binds name→adapter, raises ValueError on unknown name, fails fast on invalid manifest"
    - "tool_service.invoke is the single chokepoint: emits tool_requested, calls policy.decide, runs read-class now or short-circuits write/shell to a pending approval"
    - "read-class invoke emits tool_completed; adapter raise emits tool_failed"
    - "approve runs the deferred adapter and emits tool_completed/tool_failed; reject leaves no execution"
    - "concurrent approve of one approval executes exactly once (atomic UPDATE...WHERE status='pending')"
    - "tool args/results are secret-redacted once at the audit boundary before persistence"
    - "0013_tool_approvals migration applies via db.apply_migrations"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/tool_service.py"
      provides: "invoke / propose / approve / reject / list (the state machine + chokepoint)"
      contains: "def invoke"
    - path: "services/agent-runtime/atlas_runtime/tools/registry.py"
      provides: "manifest load + name→adapter binding; resolve(name)"
      contains: "def resolve"
    - path: "infra/migrations/0013_tool_approvals.sql"
      provides: "tool_approvals table (gated write/shell calls)"
      contains: "CREATE TABLE IF NOT EXISTS tool_approvals"
  key_links:
    - from: "tool_service.invoke"
      to: "policy.decide"
      via: "the single policy decision point"
      pattern: "policy\\.decide"
    - from: "tool_service"
      to: "audit_service.emit"
      via: "tool_requested/tool_completed/tool_failed emission"
      pattern: "tool_(requested|completed|failed)"
    - from: "tool_service.approve"
      to: "tool_approvals"
      via: "atomic UPDATE ... WHERE status='pending'"
      pattern: "status\\s*=\\s*'pending'"
---

<objective>
Build the tool registry (manifest load + name→adapter binding) and `tool_service` — the single policy chokepoint and approval state machine — plus the additive `0013_tool_approvals` migration. This is a near-verbatim generalization of the Phase C `discord_service` pipeline into `tool_*`.

Purpose: SC1 (read auto-runs; write/shell gated), SC2 (registry binds manifests→adapters), SC4 (the three audit verbs). The redact-once discipline, operator-run FK bootstrap, and atomic TOCTOU claim are COPIED from discord_service, not reinvented.
Output: registry.py, tool_service.py, 0013_tool_approvals.sql.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-PATTERNS.md

<interfaces>
<!-- Primary analog: discord_service.py. Clone its shape verbatim, rename discord_* → tool_*. -->

From services/agent-runtime/atlas_runtime/discord_service.py (THE template):
- propose (112-167): redact ONCE — `params_json = _redact(json.dumps(params))` (line 127); `run_id = mission_service.ensure_operator_run(conn, lock)` (line 138).
- approve (229-290): atomic claim `UPDATE discord_approvals SET status='executing' WHERE id=? AND status='pending'`; proceed only if cur.rowcount==1, else raise *ApprovalError. On success _set_terminal "executed" + emit; on failure _set_terminal "failed" + emit.
- _set_terminal (329-343), _load/_row_to_approval helpers, custom DiscordApprovalError(ValueError).

From services/agent-runtime/atlas_runtime/audit_service.py:
- emit(conn, lock, *, run_id, event_type, tool_name=None, data=..., policy_result=None, tool_call_kwargs=None) (63-130): Pydantic-validates event_type before SQL; tool_call_kwargs writes a linked ToolCall row in the same txn. Never raw-INSERT.
- _redact (36-55): round-trip-safe, idempotent on already-redacted text.

From services/agent-runtime/atlas_runtime/mission_service.py:
- ensure_operator_run(conn, lock) -> "operator" (23-60): idempotent synthetic mission+run so audit_events.run_id REFERENCES runs(id) holds.

From services/agent-runtime/atlas_runtime/policy.py (Plan 01):
- decide(manifest, mode="read_only") -> PolicyDecision(allowed, reason, requires_approval).

From services/agent-runtime/atlas_runtime/agents/registry.py:
- resolve-or-raise pattern (8-31): _REGISTRY dict; get(name) raises ValueError "Unknown ... Known: {...}".

From packages/atlas-core/atlas_core/schemas/tool.py (Plan 01):
- ToolManifest, ToolResult, ToolApproval, ToolApprovalStatus (5 values), ToolRiskLevel.

From infra/migrations/0012_discord_approvals.sql (the DDL template to mirror).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 0013_tool_approvals migration + registry (manifest load + adapter binding)</name>
  <read_first>
    - infra/migrations/0012_discord_approvals.sql (clone exactly; swap columns)
    - services/agent-runtime/atlas_runtime/agents/registry.py (resolve-or-raise pattern 8-31)
    - services/agent-runtime/atlas_runtime/config_service.py (yaml.safe_load usage 97)
    - services/agent-runtime/atlas_runtime/tools/manifests/ (the four YAMLs from Plan 02)
    - services/agent-runtime/atlas_runtime/tools/adapters/ (the four adapter run() callables from Plan 02)
    - services/agent-runtime/tests/test_tool_registry.py (Plan 00 RED test)
  </read_first>
  <action>
    Create infra/migrations/0013_tool_approvals.sql mirroring 0012 (RESEARCH "0013 migration" + PATTERNS DDL): `CREATE TABLE IF NOT EXISTS tool_approvals (id TEXT PRIMARY KEY, tool_name TEXT NOT NULL, risk_level TEXT NOT NULL, args TEXT NOT NULL DEFAULT '{}', summary TEXT NOT NULL DEFAULT '', status TEXT NOT NULL DEFAULT 'pending', reason TEXT, result TEXT, run_id TEXT NOT NULL DEFAULT 'operator', requested_at TEXT NOT NULL, decided_at TEXT);` + `CREATE INDEX IF NOT EXISTS idx_tool_approvals_status ON tool_approvals(status, requested_at);`. Mirror the 0012 header comment block (additive; run_id stored but NOT FK-enforced — the audit_events row carries the runs(id) FK; args/result are JSON, secret-redacted before insert).
    Create tools/registry.py mirroring agents/registry.py: eager-load `manifests/*.yaml` via `yaml.safe_load`, validate each with `ToolManifest.model_validate` (a ValidationError is a fail-fast load error per CONTEXT), build `{name: (ToolManifest, adapter_run_callable)}` binding each manifest name to its adapter module's `run`; expose `resolve(name) -> (ToolManifest, run)` raising ValueError "Unknown tool {name!r}. Known: {...}" on miss, and `known_tools()` / `manifests()` accessors. The github adapter must NOT crash registry load if `gh` is absent (it only fails at call time) — do not probe gh during load.
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_tool_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 0013_tool_approvals.sql exists and applies via db.apply_migrations
    - registry loads all four manifests, binds name→adapter, resolve(unknown) raises ValueError, invalid manifest fails at load
    - registry load does not probe/require gh
    - test_tool_registry.py GREEN
  </acceptance_criteria>
  <done>Registry binds manifests→adapters (SC2); approval storage exists.</done>
</task>

<task type="auto">
  <name>Task 2: tool_service.invoke chokepoint + propose/approve/reject/list</name>
  <read_first>
    - services/agent-runtime/atlas_runtime/discord_service.py (FULL file — propose 112-167, approve 229-290, _set_terminal 329-343, _load/_row_to_approval, DiscordApprovalError)
    - services/agent-runtime/atlas_runtime/audit_service.py (emit + _redact)
    - services/agent-runtime/atlas_runtime/mission_service.py (ensure_operator_run)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md (Pattern 3 invoke() skeleton; Pitfalls 3/4 redaction+FK)
    - services/agent-runtime/tests/test_tool_service.py (Plan 00 RED tests)
  </read_first>
  <action>
    Create tool_service.py cloning discord_service.py, renaming discord_*→tool_*. Implement:
    `invoke(conn, lock, *, tool_name, args, mode="read_only", reason=None)` (RESEARCH Pattern 3 — the single chokepoint): resolve via registry (raises ValueError on unknown — never auto-run an unclassified tool); `run_id = mission_service.ensure_operator_run(conn, lock)`; `args_json = _redact(json.dumps(args or {}))` (redact ONCE — Pitfall 3; never redact a dict in place, never add a second redact path); emit `event_type="tool_requested"`; `decision = policy.decide(manifest, mode)`; if `decision.requires_approval` → INSERT a pending tool_approvals row (status='pending', args=args_json, risk_level from manifest), emit `event_type="approval"`, and return the ToolApproval (NO inline execution); else (read-class) run the adapter, emit `tool_completed` with tool_call_kwargs writing the linked ToolCall row ({tool_name, args=args_json, result=redacted output, policy_allowed=True, requires_approval=False}); on adapter exception emit `tool_failed` (policy_result="tool_failed") and re-raise.
    `approve(conn, lock, *, approval_id, reason=None)`: atomic claim `UPDATE tool_approvals SET status='executing' WHERE id=? AND status='pending'`, proceed only if rowcount==1 else raise ToolApprovalError; resolve adapter, run it, redact result, _set_terminal "executed" + emit `tool_completed`; on adapter exception _set_terminal "failed" + emit `tool_failed`; return the reloaded ToolApproval.
    `reject(conn, lock, *, approval_id, reason=None)`: atomic claim to status='rejected' (no execution); emit an audit event.
    `list_approvals(conn, *, status=None)` and helpers `_set_terminal`/`_load`/`_row_to_approval` cloned from discord_service. Define `ToolApprovalError(ValueError)`. Adapters are assumed authorized — tool_service is the ONLY caller of policy.decide.
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_tool_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - invoke emits tool_requested then tool_completed (read path); adapter raise → tool_failed
    - write/shell short-circuit to a pending tool_approvals row, no inline exec
    - approve executes the deferred adapter + emits tool_completed/tool_failed; reject leaves no execution
    - concurrent approve → exactly one executes (atomic claim, rowcount==1)
    - secret in args is [REDACTED] in stored tool_approvals.args and tool_calls.args
    - test_tool_service.py GREEN
  </acceptance_criteria>
  <done>Single chokepoint enforces SC1; the three audit verbs emit (SC4); redaction/FK/TOCTOU copied verbatim from Phase C.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool call → policy decision | every call must pass through policy.decide before execution |
| approval row → execution | a pending approval must execute at most once |
| tool args/results → persistence | secrets must be redacted before they hit SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.0.4-02 | Elevation of Privilege | invoke chokepoint | mitigate | single decision point: policy.decide before any adapter.run; write/shell never inline-execute; unknown tool → ValueError (test_tool_service) |
| T-10.0.4-03 | Elevation of Privilege (TOCTOU) | approve double-execute | mitigate | atomic UPDATE ... WHERE status='pending', rowcount==1 claim copied from discord_service 248-260; concurrency test asserts exactly one execution |
| T-10.0.4-09 | Information Disclosure | secret leakage into approval/audit rows | mitigate | _redact(json.dumps(args)) ONCE at the boundary (Pitfall 3); webhook tokens redacted before insert; test asserts [REDACTED] |
| T-10.0.4-10 | Repudiation | missing audit trail | mitigate | tool_requested/tool_completed/tool_failed emitted via audit_service.emit (append-only); operator run_id bootstrap keeps FK valid (Pitfall 4) |
</threat_model>

<verification>
- test_tool_service.py + test_tool_registry.py green
- 0013 migration applies in the temp-DB fixture
- full runtime suite green after this wave
</verification>

<success_criteria>
- [ ] registry binds manifests→adapters; resolve raises on unknown (SC2)
- [ ] invoke is the single chokepoint; read auto-runs, write/shell → pending (SC1)
- [ ] tool_requested/completed/failed emitted (SC4)
- [ ] atomic approve; redact-once; operator-run FK bootstrap
- [ ] 0013 migration additive and applied
</success_criteria>

<output>
Create `.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-03-SUMMARY.md` when done
</output>
