# Plan 10.0.4-00 — Summary

**Wave:** 0 (test scaffolding) · **Status:** complete · **Executed:** 2026-06-22 (inline)

## What shipped
Wave 0 establishes the Nyquist RED baseline for the tool layer, plus the A2 confirmation.

- **A2 CONFIRMED** — `infra/migrations/0001_core.sql` declares `audit_events.event_type` as
  `TEXT NOT NULL` with **no CHECK constraint** (validation is Pydantic-side, per audit_service
  HB-04-02). Adding the three `tool_*` verbs to the `AuditEvent` Literal is therefore code-only —
  no migration for the enum change. (The `tool_calls` table also already exists from 0001.)
- **`tests/conftest.py`** — added `mock_gh` (patches `subprocess.run` → canned CompletedProcess,
  records argv) and `mock_urlopen` (patches `urllib.request.urlopen` → chunked-`.read(n)` fake
  response) fixtures, visible to the tool tests at `tests/` level.
- **`tests/tools/__init__.py` + `tests/tools/conftest.py`** — canonical `tool_db` fixture using
  the real `db.connect` + `db.apply_migrations` runner (file-backed temp DB; exercises the
  `0013_tool_approvals` path once it exists). Never touches the live `~/.atlas/atlas.db`.
- **Five RED test files:**
  - `packages/atlas-core/tests/test_schemas.py` — parametrized test for the 3 new audit verbs (SC4).
  - `packages/atlas-core/tests/test_tool_manifest.py` — ToolManifest valid/invalid/frozen (SC2).
  - `services/agent-runtime/tests/test_tool_registry.py` — manifest load + name→adapter bind (SC2).
  - `services/agent-runtime/tests/test_tool_service.py` — invoke chokepoint, propose/approve/reject,
    atomic-claim guard, redaction, tool_requested/completed/failed emission (SC1/SC4).
  - `services/agent-runtime/tests/test_tool_adapters.py` — web_fetch SSRF (6 cases) + size cap,
    workspace boundary, github honest-failure + argv (SC1).

## RED state (intended)
- atlas-core: `8 failed` (3 audit verbs not yet in Literal + 5 ToolManifest on missing
  `atlas_core.schemas.tool`).
- agent-runtime: tool tests collect (imports are function-local) and fail at runtime on the
  not-yet-existing `atlas_runtime.tools.*` / `atlas_runtime.tool_service` modules.

## Deviations
- Executed **inline** (not via subagent) after repeated account session-limit interruptions on
  dispatched executors. Plan/wave structure, atomic commits, and SUMMARY preserved.
- `mock_gh`/`mock_urlopen` placed in the top-level `tests/conftest.py` (not `tests/tools/conftest.py`)
  because the tool test files live in `tests/` and pytest only applies a conftest to its own dir and
  below. `tests/tools/conftest.py` still ships the canonical `tool_db` (per the plan artifact) and
  references `apply_migrations`.

## Verification
- `pytest packages/atlas-core/tests/test_schemas.py::test_audit_event_accepts_tool_verbs
  packages/atlas-core/tests/test_tool_manifest.py -q` → 8 failed (RED, intended).
- `pytest services/agent-runtime/tests/test_tool_*.py --co -q` → 25 tests collected.

## Self-Check: PASSED
