---
phase: 10.0.4-developer-integrations-tool-manifest
plan: 04
type: execute
wave: 4
depends_on: [10.0.4-03]
files_modified:
  - services/agent-runtime/atlas_runtime/cli/tools.py
  - services/agent-runtime/atlas_runtime/cli/main.py
  - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs
  - services/agent-runtime/tests/cli/test_tools_cli.py
autonomous: true
requirements: [SC1, SC2, SC3, SC4]
must_haves:
  truths:
    - "atlas tools list|manifests|call|approvals|approve|reject each support --json"
    - "atlas tools call routes through tool_service.invoke; a failed-but-processed outcome exits 0, an un-processable one exits non-zero"
    - "gateway GET /v1/tools/manifests returns the manifest list"
    - "gateway tool/approval routes are dispatch-only (validate + shell to atlas CLI + parse JSON; no DB/state in Rust)"
    - "gateway rejects empty/invalid tool args via require_arg (400)"
  artifacts:
    - path: "services/agent-runtime/atlas_runtime/cli/tools.py"
      provides: "atlas tools Typer sub-app (list/manifests/call/approvals/approve/reject)"
      contains: "tools_app"
    - path: "native/atlas-core-rs/crates/atlas-gateway/src/lib.rs"
      provides: "/v1/tools/* dispatch-only handlers + route registration"
      contains: "/v1/tools/manifests"
  key_links:
    - from: "cli/main.py"
      to: "cli/tools.py tools_app"
      via: "app.add_typer(tools_app, name='tools')"
      pattern: "add_typer\\(tools_app"
    - from: "gateway /v1/tools/calls"
      to: "atlas tools call"
      via: "dispatch_atlas argv after `--` separator"
      pattern: "dispatch_atlas"
---

<objective>
Expose the tool layer through the `atlas tools` CLI group and the dispatch-only Rust gateway routes. The CLI mirrors `cli/discord.py`; the gateway mirrors the discord handlers (D-022: validate + shell to the `atlas` CLI + parse JSON — no business logic or DB writes in Rust).

Purpose: SC1/SC2/SC4 are reachable via CLI and HTTP; SC3's WebUI panels (Plan 05) consume these endpoints. The gateway argv-safety pattern (require_arg + argv vector + `--` separator) prevents command injection.
Output: cli/tools.py, cli/main.py wiring, lib.rs route handlers + registration, a CLI test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-PATTERNS.md

<interfaces>
<!-- CLI analog: cli/discord.py. Gateway analog: lib.rs discord handlers. Mirror exactly. -->

From services/agent-runtime/atlas_runtime/cli/discord.py:
- discord_app = typer.Typer(name="discord", ...); _LOCK = threading.Lock(); _get_lock() returns it (monkeypatch seam).
- _get_connection() hardcodes ~/.atlas/atlas.db (NOT ATLAS_HOME-aware — Pitfall 2). Copy for parity; tests use injected temp DB, never this.
- approve command (209-231): on *ApprovalError → echo err + raise typer.Exit(1); a PROCESSED outcome (status="failed") still exits 0 so the JSON reaches the gateway; only an un-processable approval (bad id/not pending) exits non-zero. --json prints model_dump().

From services/agent-runtime/atlas_runtime/cli/main.py (87-88):
- from atlas_runtime.cli.discord import discord_app; app.add_typer(discord_app, name="discord") — mirror for tools.

From services/agent-runtime/atlas_runtime/tool_service.py (Plan 03):
- invoke(conn, lock, *, tool_name, args, mode, reason); approve(...); reject(...); list_approvals(conn, status=None); ToolApprovalError.

From services/agent-runtime/atlas_runtime/tools/registry.py (Plan 03):
- manifests() / known_tools() / resolve(name).

From native/atlas-core-rs/crates/atlas-gateway/src/lib.rs:
- require_arg(value, msg) (469-477): rejects empty/whitespace and leading-`-`.
- dispatch_atlas(atlas_cmd, &arg_refs) (484): shells to the atlas CLI, returns stdout.
- discord_propose handler (683-717): argv vector, `--json` BEFORE `--`, user value AFTER `--`.
- route table (1764-1772): .route("/v1/discord/...", get/post(handler)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: atlas tools CLI sub-app + main.py wiring + CLI test</name>
  <read_first>
    - services/agent-runtime/atlas_runtime/cli/discord.py (FULL — sub-app, _get_connection/_get_lock seams, command shapes, exit-code convention)
    - services/agent-runtime/atlas_runtime/cli/main.py (87-88 add_typer wiring)
    - services/agent-runtime/atlas_runtime/tool_service.py + tools/registry.py (Plan 03 surfaces the CLI calls)
    - services/agent-runtime/tests/test_discord_*.py or tests/cli/ (a CLI test analog using CliRunner with an injected connection seam)
  </read_first>
  <action>
    Create cli/tools.py: `tools_app = typer.Typer(name="tools", help=...)`; copy the `_get_connection` (hardcoded ~/.atlas path, for parity) and `_get_lock` monkeypatch seams from discord.py. Commands, each with `--json`:
    - `list`: print known tool names (registry.known_tools()).
    - `manifests`: print all manifests (model_dump list) — used by gateway GET /v1/tools/manifests.
    - `call`: options `--mode` (default read_only), `--args` (JSON string), and the tool name as the argument AFTER `--` (matches gateway argv shape); parse --args JSON, call tool_service.invoke; print the ToolResult OR the ToolApproval (when write/shell short-circuits) as JSON.
    - `approvals`: list_approvals (optional --status); print JSON.
    - `approve` / `reject`: take approval_id argument + optional --reason; call tool_service.approve/reject. Preserve the exit-code convention EXACTLY: a processed outcome (status executed/failed/rejected) exits 0 so JSON reaches the gateway; an un-processable call (ToolApprovalError, bad id/not pending, unknown tool) echoes to stderr and raises typer.Exit(1).
    Wire in cli/main.py: `from atlas_runtime.cli.tools import tools_app; app.add_typer(tools_app, name="tools")` (mirror the discord try/except import wiring if main.py uses it).
    Create tests/cli/test_tools_cli.py using Typer CliRunner with `_get_connection` monkeypatched to an injected temp DB (NEVER the live ~/.atlas path — Pitfall 2): assert `tools manifests --json` returns the four manifests; assert `tools call --json -- web_fetch` with a write tool short-circuits to a pending approval JSON; assert an unknown tool exits non-zero.
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/cli/test_tools_cli.py -q && python -m atlas_runtime.cli.main tools --help >/dev/null 2>&1 && echo "tools group wired"</automated>
  </verify>
  <acceptance_criteria>
    - cli/tools.py provides list/manifests/call/approvals/approve/reject with --json
    - main.py registers the tools group (atlas tools --help works)
    - exit-code convention preserved (processed → 0, un-processable → non-zero)
    - test_tools_cli.py uses an injected temp DB, never the live CLI DB; passes
  </acceptance_criteria>
  <done>CLI surface for tools complete; gateway can shell to it.</done>
</task>

<task type="auto">
  <name>Task 2: Gateway /v1/tools/* dispatch-only routes</name>
  <read_first>
    - native/atlas-core-rs/crates/atlas-gateway/src/lib.rs (require_arg 469-477; dispatch_atlas 484; discord_propose 683-717; discord_approve 731-739; route table 1764-1772)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md (Gateway route example; D-022 dispatch-only; anti-pattern: no state in Rust)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-PATTERNS.md (the argv-vector + `--` separator template; new route list)
  </read_first>
  <action>
    Extend lib.rs with five dispatch-only handlers mirroring the discord handlers (validate → build argv vector → dispatch_atlas → parse JSON → return; NO DB, NO state, NO business logic — D-022 anti-pattern guard):
    - `tool_manifests` (GET /v1/tools/manifests): dispatch ["tools","manifests","--json"].
    - `tool_call` (POST /v1/tools/calls): body {tool, args?, mode?, reason?}; require_arg(&body.tool, "tool must be non-empty"); build args ["tools","call","--json"]; push --mode/--args options BEFORE the separator; push "--" then body.tool (user value AFTER `--`); dispatch + parse.
    - `tool_approvals` (GET /v1/tools/approvals): dispatch ["tools","approvals","--json"] (optional ?status forwarded as --status).
    - `tool_approve` (POST /v1/tools/approvals/{id}/approve): require_arg(id); args ["tools","approve","--json", "--", id] (id AFTER `--`); a failed-but-processed write returns 200 with status="failed" (CLI exits 0).
    - `tool_reject` (POST /v1/tools/approvals/{id}/reject): mirror approve with reject + optional --reason.
    Register all five in the route table next to the discord routes. Build the gateway after editing (per project memory the gateway must be rebuilt for it to take effect). Add or extend a `cargo test -p atlas-gateway` case asserting an empty `tool` body is rejected (require_arg → 400) — dispatch-only validation.
  </action>
  <verify>
    <automated>cd native/atlas-core-rs && cargo build -p atlas-gateway 2>&1 | tail -3 && cargo test -p atlas-gateway 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Five /v1/tools/* routes registered and dispatch-only (shell to atlas CLI, parse JSON)
    - require_arg guards tool name and approval id; empty → 400
    - user-controlled values passed AFTER `--`; no shell string building; no DB/state in Rust
    - gateway builds; cargo test green
  </acceptance_criteria>
  <done>HTTP surface for tools complete and injection-safe; cockpit (Plan 05) can call it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → gateway | request bodies are untrusted; must be validated before dispatch |
| gateway → atlas CLI | user values must be argv elements after `--`, never shell-parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.0.4-07 | Tampering (command/arg injection) | gateway /v1/tools/* | mitigate | require_arg rejects empty/leading-dash; argv vector + `--` separator (user value after `--`); never shell string (cargo test asserts empty body → 400) |
| T-10.0.4-11 | Elevation of Privilege | state/logic leaking into Rust | mitigate | D-022: handlers only validate + dispatch_atlas + parse JSON; no DB reads/writes, no policy in Rust (anti-pattern guard) |
| T-10.0.4-12 | Tampering | CLI test hits live DB | mitigate | test_tools_cli.py monkeypatches _get_connection to a temp DB (Pitfall 2) |
| T-10.0.4-SC | Tampering | cargo dependency | accept | No new Rust crates added; only handlers using existing axum/serde (RESEARCH: no package install this phase) |
</threat_model>

<verification>
- test_tools_cli.py green (temp DB)
- cargo build + cargo test -p atlas-gateway green
- atlas tools --help lists the six commands
</verification>

<success_criteria>
- [ ] atlas tools group with six commands, all --json (SC1/SC2/SC4 reachable via CLI)
- [ ] five gateway /v1/tools/* routes, dispatch-only (SC3 endpoints for cockpit)
- [ ] require_arg + `--` separator injection guard; cargo test asserts 400 on empty
- [ ] CLI test uses injected temp DB only
</success_criteria>

<output>
Create `.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-04-SUMMARY.md` when done
</output>
