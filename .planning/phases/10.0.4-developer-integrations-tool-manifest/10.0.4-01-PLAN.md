---
phase: 10.0.4-developer-integrations-tool-manifest
plan: 01
type: execute
wave: 1
depends_on: [10.0.4-00]
files_modified:
  - packages/atlas-core/atlas_core/schemas/tool.py
  - packages/atlas-core/atlas_core/schemas/core.py
  - services/agent-runtime/atlas_runtime/policy.py
  - services/agent-runtime/pyproject.toml
autonomous: true
requirements: [SC2, SC1, SC4]
must_haves:
  truths:
    - "AuditEvent accepts tool_requested/tool_completed/tool_failed event_types"
    - "ToolManifest validates a good manifest and rejects an unknown risk_level (fail-fast)"
    - "policy.decide() returns read=allow, write/shell=needs-approval"
    - "pyyaml is a declared dependency of the runtime package"
  artifacts:
    - path: "packages/atlas-core/atlas_core/schemas/tool.py"
      provides: "ToolManifest, ToolResult, ToolApproval, ToolRiskLevel, ToolApprovalStatus (frozen)"
      contains: "ToolManifest"
    - path: "services/agent-runtime/atlas_runtime/policy.py"
      provides: "PolicyDecision.requires_approval + decide(manifest, mode)"
      contains: "def decide"
    - path: "services/agent-runtime/pyproject.toml"
      provides: "explicit pyyaml dependency"
      contains: "pyyaml"
  key_links:
    - from: "atlas_core/schemas/tool.py"
      to: "atlas_core/schemas/core.py AuditEvent"
      via: "shared snake_case audit verb convention"
      pattern: "tool_requested"
    - from: "policy.decide"
      to: "ToolManifest.risk_level"
      via: "risk_level → allow/needs-approval mapping"
      pattern: "risk_level"
---

<objective>
Define the contracts for the tool layer: the frozen `ToolManifest`/`ToolResult`/`ToolApproval` models in atlas-core, the three new `AuditEvent.event_type` verbs, the `policy.decide()` aggregator with `requires_approval`, and an explicit `pyyaml` dependency. This is the interface-first wave — everything downstream implements against these contracts.

Purpose: Establish the data contracts before adapters/registry/service consume them (SC2 manifest contract; SC1 read/write/shell taxonomy; SC4 audit verbs). All atlas-core models are `frozen=True` (D-012/D-013).
Output: tool.py (new), core.py Literal extension, policy.py extension, pyproject.toml pyyaml declaration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md
@.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-PATTERNS.md

<interfaces>
<!-- Contracts the executor mirrors. Extracted from RESEARCH/PATTERNS. Use directly. -->

From packages/atlas-core/atlas_core/schemas/discord.py (the frozen-model + Literal taxonomy analog):
- DiscordApproval: model_config = ConfigDict(frozen=True, str_strip_whitespace=True); id default_factory uuid4; params: str = "{}" (JSON string, secret-redacted before persist); status Literal default "pending"; run_id: str = "operator"; requested_at/decided_at datetimes with field_serializer to isoformat; module has __all__.
- DiscordApprovalStatus = Literal["pending","executing","executed","rejected","failed"] — REUSE this exact 5-value set for ToolApprovalStatus.

From packages/atlas-core/atlas_core/schemas/core.py:
- AuditEvent.event_type Literal currently: "llm_call","tool_call","subagent_run","approval","artifact","wiki_update","memory_change","failure","discord_action" (core.py:255-265).
- ToolCall docstring (core.py:283-285) already promises a requires_approval field mapping to PolicyDecision.requires_approval.
- All schemas frozen=True (core.py:248).

From services/agent-runtime/atlas_runtime/policy.py:
- @dataclass PolicyDecision: allowed: bool; reason: str (policy.py:25-35) — plain dataclass, mutable, safe to extend.
- Existing reason strings are snake_case (e.g. within_workspace, tool_in_allowlist).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create atlas-core tool.py schemas + extend AuditEvent Literal</name>
  <read_first>
    - packages/atlas-core/atlas_core/schemas/discord.py (the exact frozen-model module to mirror: ConfigDict, Literal taxonomy, JSON-string field, field_serializer, __all__)
    - packages/atlas-core/atlas_core/schemas/core.py (AuditEvent.event_type Literal at 255-265 to extend; frozen-model convention at 248)
    - packages/atlas-core/tests/test_tool_manifest.py (the RED test from Plan 00 this must turn GREEN)
    - packages/atlas-core/tests/test_schemas.py (the RED event_type test from Plan 00 this must turn GREEN)
  </read_first>
  <behavior>
    - ToolManifest.model_validate accepts {name, description, risk_level:"read", permissions, inputs, outputs} → valid frozen instance
    - ToolManifest with risk_level not in {read,write,shell} → ValidationError
    - ToolManifest missing name or risk_level → ValidationError
    - ToolResult(tool_name, ok, output, error, exit_code) constructs; is frozen (mutation raises)
    - ToolApproval mirrors the 0013 DDL columns and reuses the 5-value status Literal
    - AuditEvent(event_type="tool_requested"|"tool_completed"|"tool_failed", ...) validates
  </behavior>
  <action>
    Create packages/atlas-core/atlas_core/schemas/tool.py mirroring discord.py. Define: `ToolRiskLevel = Literal["read","write","shell"]` (maps 1:1 to SC1: read=default-allow); `ToolApprovalStatus` reusing the exact 5-value set from DiscordApprovalStatus ("pending","executing","executed","rejected","failed"). `ToolManifest` (frozen, str_strip_whitespace) fields: name, description="", risk_level: ToolRiskLevel, permissions: list[str] default_factory list, inputs: dict[str,str] default_factory dict, outputs: dict[str,str] default_factory dict, audit_events: list[str] default_factory to the three verbs. `ToolResult` (frozen) fields: tool_name, ok: bool, output: str="" (JSON string, D-013, redacted at audit boundary), error: str|None=None, exit_code: int|None=None. `ToolApproval` (frozen) mirroring DiscordApproval column-for-column against the 0013 DDL: id (uuid default_factory), tool_name, risk_level: ToolRiskLevel, args: str="{}" (JSON, secret-redacted before persist), summary: str="", status: ToolApprovalStatus="pending", reason: str|None, result: str|None, run_id: str="operator", requested_at datetime default_factory utc, decided_at: datetime|None, with field_serializer to isoformat for both datetimes. Add `__all__`. THEN extend core.py AuditEvent.event_type Literal by adding `"tool_requested"`, `"tool_completed"`, `"tool_failed"` to the existing list (snake_case to match tool_call/discord_action; SC4 dotted form is the external label only — note this in a comment). Do not mutate any frozen instance; construct new ones.
  </action>
  <verify>
    <automated>cd packages/atlas-core && python -m pytest tests/test_tool_manifest.py tests/test_schemas.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tool.py exports ToolManifest, ToolResult, ToolApproval, ToolRiskLevel, ToolApprovalStatus via __all__
    - test_tool_manifest.py passes (good manifest valid; unknown risk_level + missing fields rejected)
    - test_schemas.py passes for the three new event_type verbs (SC4)
    - All new models frozen=True
  </acceptance_criteria>
  <done>Contracts exist and validated; Plan 00 RED tests for SC2/SC4 are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend policy.py — requires_approval + decide(manifest, mode)</name>
  <read_first>
    - services/agent-runtime/atlas_runtime/policy.py (PolicyDecision dataclass 25-35; check_workspace_boundary 38-67; reason-string convention)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md (Pattern 2: decide() mapping; Open Question 1: v0 mode is a code default "read_only")
    - services/agent-runtime/tests/test_policy.py (existing policy tests — add the decide() cases here)
  </read_first>
  <behavior>
    - decide(manifest with risk_level="read") → PolicyDecision(allowed=True, requires_approval=False, reason="read_class_allowed")
    - decide(manifest with risk_level="write") → PolicyDecision(allowed=False, requires_approval=True, reason="write_requires_approval")
    - decide(manifest with risk_level="shell") → allowed=False, requires_approval=True, reason="shell_requires_approval"
    - PolicyDecision still constructs with just (allowed, reason) — requires_approval defaults False (back-compat)
  </behavior>
  <action>
    Per RESEARCH Pattern 2: add `requires_approval: bool = False` to the existing `PolicyDecision` dataclass (back-compat default; it is a plain mutable dataclass — safe, unlike the frozen Pydantic models). Add `decide(manifest, mode: str = "read_only") -> PolicyDecision`: if `manifest.risk_level == "read"` return PolicyDecision(True, "read_class_allowed", requires_approval=False); otherwise return PolicyDecision(False, f"{manifest.risk_level}_requires_approval", requires_approval=True). For v0 the `mode` parameter is accepted but the default is "read_only" (Open Question 1 recommendation — write/shell never auto-run regardless of mode; defer a live config toggle). Use snake_case reason strings matching the existing convention. Add the decide() cases to test_policy.py. Do NOT let any adapter perform policy checks — policy lives only here and in tool_service.invoke (anti-pattern guard).
  </action>
  <verify>
    <automated>cd services/agent-runtime && python -m pytest tests/test_policy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - PolicyDecision has requires_approval (default False, back-compat preserved)
    - decide() returns the three-way read=allow / write|shell=needs-approval mapping
    - test_policy.py covers all three risk levels
  </acceptance_criteria>
  <done>policy.decide() is the single decision function for the chokepoint (SC1).</done>
</task>

<task type="auto">
  <name>Task 3: Declare pyyaml in runtime pyproject</name>
  <read_first>
    - services/agent-runtime/pyproject.toml (the `dependencies` array — where to add the pin)
    - .planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-RESEARCH.md (Standard Stack: pyyaml 6.0.3 already importable but transitive; add "pyyaml>=6,<7" to make the manifest-loader import explicit and supply-chain-honest)
  </read_first>
  <action>
    Add `"pyyaml>=6,<7"` to the `dependencies` array in services/agent-runtime/pyproject.toml. This makes the (currently transitive, verified 6.0.3) manifest-loader import explicit per RESEARCH; no new package is installed — it is already resolved in the venv. Re-run editable install so the dep is recorded. No other dependency changes (anti-bloat: no requests, no PyGithub).
  </action>
  <verify>
    <automated>cd services/agent-runtime && grep -n "pyyaml" pyproject.toml && python -c "import yaml; print('yaml', yaml.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml dependencies includes pyyaml>=6,<7
    - yaml imports and reports a 6.x version
    - No other third-party dependency added
  </acceptance_criteria>
  <done>Manifest loader's pyyaml import is declared, not transitive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest data → ToolManifest | Untrusted YAML must validate against the frozen schema before use |
| policy decision → execution | risk_level classification gates whether a tool auto-runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.0.4-01 | Elevation of Privilege | policy.decide | mitigate | write/shell always return requires_approval=True; never auto-run regardless of mode (test_policy.py covers all three risk levels) |
| T-10.0.4-06 | Tampering | ToolManifest validation | mitigate | unknown risk_level / missing required fields → ValidationError (frozen Pydantic; test_tool_manifest.py) |
| T-10.0.4-SC | Tampering | pip dependency (pyyaml) | accept | pyyaml is pre-installed v6.0.3, 18+yr-old package, ~300M/mo downloads (RESEARCH Package Legitimacy Audit: Approved). Declaration only — no registry install this plan |
</threat_model>

<verification>
- atlas-core test suite green for manifest + schemas
- runtime policy test green
- pyyaml declared and importable
</verification>

<success_criteria>
- [ ] tool.py with all five exports, all frozen
- [ ] AuditEvent Literal includes the three tool verbs (SC4)
- [ ] policy.decide() three-way mapping (SC1)
- [ ] pyyaml declared (SC2 loader honesty)
- [ ] Plan 00 RED tests for SC2/SC4 now GREEN
</success_criteria>

<output>
Create `.planning/phases/10.0.4-developer-integrations-tool-manifest/10.0.4-01-SUMMARY.md` when done
</output>
