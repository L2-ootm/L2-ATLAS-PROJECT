---
phase: 02-core-schemas
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/atlas-core/atlas_core/schemas/core.py
  - packages/atlas-core/atlas_core/schemas/__init__.py
  - packages/atlas-core/tests/test_schemas.py
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-03

must_haves:
  truths:
    - "7 Pydantic v2 models exist: Mission, Run, AuditEvent, ToolCall, Artifact, Source, WikiPage"
    - "All models use ConfigDict(frozen=True, str_strip_whitespace=True)"
    - "All datetime fields have @field_serializer returning ISO 8601 str"
    - "No Path objects in any model field — all path fields are str"
    - "No dict[str, Any] in any public model field — data/args/result fields are str"
    - "SECRET_PATTERNS constant defined at module level in core.py"
    - "from atlas_core.schemas.core import Mission succeeds in atlas-core venv"
    - "Mission.model_json_schema() returns dict with 'properties' key containing all 7 Mission fields"
    - "model_dump() on any model instance produces JSON-serializable dict (no datetime objects)"
  artifacts:
    - path: "packages/atlas-core/atlas_core/schemas/core.py"
      provides: "7 Pydantic v2 frozen models + SECRET_PATTERNS"
      contains: "class Mission(BaseModel)"
      exports: ["Mission", "Run", "AuditEvent", "ToolCall", "Artifact", "Source", "WikiPage", "SECRET_PATTERNS"]
    - path: "packages/atlas-core/atlas_core/schemas/__init__.py"
      provides: "public API re-exports for all 7 models"
      contains: "from atlas_core.schemas.core import"
    - path: "packages/atlas-core/tests/test_schemas.py"
      provides: "import + model_json_schema + serialization tests"
      contains: "def test_import"
  key_links:
    - from: "packages/atlas-core/atlas_core/schemas/__init__.py"
      to: "packages/atlas-core/atlas_core/schemas/core.py"
      via: "from atlas_core.schemas.core import Mission, Run, ..."
      pattern: "from atlas_core.schemas.core import"
    - from: "packages/atlas-core/tests/test_schemas.py"
      to: "packages/atlas-core/atlas_core/schemas/core.py"
      via: "from atlas_core.schemas.core import Mission"
      pattern: "from atlas_core.schemas.core import"
---

<objective>
Write all 7 Pydantic v2 domain models in core.py, update schemas/__init__.py with explicit re-exports, and write test_schemas.py covering SCHEMA-01 (import + instantiation + FK/Path/dict constraints) and SCHEMA-03 (model_json_schema emission).

Purpose: core.py is the single source of truth per D-012. Every downstream phase (4–7) imports from atlas_core.schemas.core. The serialization contract (frozen=True, field_serializer, str for paths/dicts) must be enforced now or silently breaks JSON insertion in Phase 4.

Output: core.py with 7 models, updated __init__.py, test_schemas.py with all SCHEMA-01 and SCHEMA-03 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-schemas/02-RESEARCH.md
@.planning/phases/02-core-schemas/02-PATTERNS.md
@.planning/phases/02-core-schemas/02-01-SUMMARY.md

<interfaces>
<!-- Definitive field tables — derived from 02-RESEARCH.md Canonical Field Design. Executor writes exactly these fields, no additions. -->

Mission fields:
  id: str = Field(default_factory=lambda: str(uuid.uuid4()))
  title: str
  intent: str = ""
  status: Literal["pending", "running", "succeeded", "failed", "cancelled"] = "pending"
  project: str = ""
  created_at: datetime.datetime  (UTC default_factory)
  updated_at: datetime.datetime  (UTC default_factory)

Run fields:
  id: str = Field(default_factory=...)
  mission_id: str
  session_id: Optional[str] = None
  status: Literal["running", "succeeded", "failed", "cancelled"] = "running"
  started_at: datetime.datetime  (UTC default_factory)
  finished_at: Optional[datetime.datetime] = None
  summary: str = ""

AuditEvent fields:
  id: str = Field(default_factory=...)
  run_id: str
  task_id: Optional[str] = None
  session_id: Optional[str] = None
  tool_call_id: Optional[str] = None
  event_type: Literal["llm_call", "tool_call", "subagent_run", "approval", "artifact", "wiki_update", "memory_change", "failure"]
  tool_name: Optional[str] = None
  timestamp: datetime.datetime  (UTC default_factory)
  duration_ms: Optional[int] = None
  data: str = "{}"
  policy_result: Optional[str] = None

ToolCall fields:
  id: str = Field(default_factory=...)
  audit_event_id: str
  run_id: str
  tool_name: str
  args: str = "{}"
  result: Optional[str] = None
  exit_code: Optional[int] = None
  stdout: Optional[str] = None
  stderr: Optional[str] = None
  duration_ms: Optional[int] = None
  policy_allowed: Optional[bool] = None
  requires_approval: Optional[bool] = None
  timestamp: datetime.datetime  (UTC default_factory)

Artifact fields:
  id: str = Field(default_factory=...)
  run_id: str
  audit_event_id: Optional[str] = None
  path: str
  artifact_type: Literal["file_write", "file_edit", "file_delete", "unknown"]
  sha256: Optional[str] = None
  size_bytes: Optional[int] = None
  created_at: datetime.datetime  (UTC default_factory)

Source fields:
  id: str = Field(default_factory=...)
  path: str
  sha256: str
  size_bytes: int
  mime_type: str = "text/plain"
  ingested_at: datetime.datetime  (UTC default_factory)
  title: str = ""

WikiPage fields:
  id: str = Field(default_factory=...)
  slug: str
  title: str
  body: str = ""
  source_id: Optional[str] = None
  created_at: datetime.datetime  (UTC default_factory)
  updated_at: datetime.datetime  (UTC default_factory)
  version: int = 1

Module-level constant (copy verbatim from jsonl_logger.py — do NOT modify patterns):
  import re
  SECRET_PATTERNS = (
      re.compile(r"(?i)\b(token|api[_-]?key|secret|password)=([^\s&]+)"),
      re.compile(r"(?i)\b(bearer)\s+([A-Za-z0-9._~+/=-]+)"),
  )
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write core.py with all 7 Pydantic v2 models</name>
  <files>
    packages/atlas-core/atlas_core/schemas/core.py
    packages/atlas-core/atlas_core/schemas/__init__.py
  </files>
  <read_first>
    - packages/atlas-core/atlas_core/schemas/__init__.py (current placeholder from plan 01)
    - .planning/phases/02-core-schemas/02-RESEARCH.md (Pattern 1: frozen model; Pattern 2: field_serializer; Canonical Field Design section; Common Pitfalls 1–3; Don't Hand-Roll section)
    - .planning/phases/02-core-schemas/02-PATTERNS.md (core.py pattern assignments — all code excerpts)
    - docs/decisions/D-013-language-strategy.md (frozen=True mandate; no dict[str, Any] rule)
    - docs/decisions/2026-06-04_D012_schema_source_of_truth.md (DDL mirrors models; model_json_schema bridge)
  </read_first>
  <behavior>
    - Test: from atlas_core.schemas.core import Mission, Run, AuditEvent, ToolCall, Artifact, Source, WikiPage, SECRET_PATTERNS — must not raise ImportError
    - Test: Mission(title="x").model_dump() returns dict with no datetime objects (all datetime values are ISO 8601 strings)
    - Test: AuditEvent(run_id="r1", event_type="llm_call").model_dump() — data field is "{}" (string, not dict)
    - Test: ToolCall(audit_event_id="a1", run_id="r1", tool_name="bash").model_dump() — args is "{}" (string); result is None; policy_allowed is None
    - Test: Artifact(run_id="r1", path="/tmp/f", artifact_type="file_write").model_dump() — path is str, not Path object; json.dumps(dumped) does not raise
    - Test: WikiPage(slug="test", title="Test").model_dump() — source_id is None; version is 1
    - Test: Mission.model_fields contains exactly 7 keys: id, title, intent, status, project, created_at, updated_at
    - Test: SECRET_PATTERNS is a tuple of 2 compiled regex patterns
  </behavior>
  <action>
    Write packages/atlas-core/atlas_core/schemas/core.py.

    File-level header imports (exact order per 02-PATTERNS.md):
      from __future__ import annotations
      import datetime
      import re
      import uuid
      from typing import Literal, Optional
      from pydantic import BaseModel, ConfigDict, Field, field_serializer

    After imports, define SECRET_PATTERNS constant (copy verbatim from 02-PATTERNS.md — do not alter the regex strings).

    Write all 7 models using exactly the fields listed in the <interfaces> section above. Apply to every model:
    - model_config = ConfigDict(frozen=True, str_strip_whitespace=True)
    - @field_serializer for every datetime field in that model, handling Optional[datetime] by returning None when dt is None

    For models with multiple datetime fields, you may list them in a single @field_serializer decorator:
      @field_serializer("created_at", "updated_at")
      def serialize_dt(self, dt: datetime.datetime | None) -> str | None:
          return None if dt is None else dt.isoformat()

    Enforcement rules (from D-013 + 02-RESEARCH.md pitfalls):
    - data field on AuditEvent: type is str, default = "{}" — NOT dict, NOT Any
    - args field on ToolCall: type is str, default = "{}" — NOT dict
    - result field on ToolCall: type is Optional[str] = None — NOT dict
    - path field on Artifact and Source: type is str — NOT pathlib.Path
    - policy_allowed and requires_approval on ToolCall: type is Optional[bool] = None

    Close the file with __all__ listing all 7 class names plus "SECRET_PATTERNS".

    Update packages/atlas-core/atlas_core/schemas/__init__.py:
    - Replace the placeholder with explicit re-exports of all 7 models and SECRET_PATTERNS from atlas_core.schemas.core
    - Set __all__ = ["Mission", "Run", "AuditEvent", "ToolCall", "Artifact", "Source", "WikiPage", "SECRET_PATTERNS"]
  </action>
  <verify>
    <automated>
      packages/atlas-core/.venv/Scripts/python.exe -c "from atlas_core.schemas.core import Mission, Run, AuditEvent, ToolCall, Artifact, Source, WikiPage, SECRET_PATTERNS; import json; m=Mission(title='t'); d=m.model_dump(); json.dumps(d); print('core.py OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/atlas-core/atlas_core/schemas/core.py exists
    - Running the verify command above prints "core.py OK" without exception
    - core.py contains "class Mission(BaseModel)" — grep confirms
    - core.py contains "class WikiPage(BaseModel)" — grep confirms
    - core.py contains "ConfigDict(frozen=True" — grep confirms all 7 models use it
    - core.py contains "@field_serializer" — grep confirms presence
    - core.py does NOT contain "dict[str, Any]" — grep must return 0 matches
    - core.py does NOT contain "pathlib.Path" as a field type — grep on "Path)" or ": Path" must return 0 matches on field definitions
    - schemas/__init__.py contains "from atlas_core.schemas.core import" — grep confirms
    - schemas/__init__.py __all__ contains all 7 model names and "SECRET_PATTERNS"
  </acceptance_criteria>
  <done>core.py with 7 models and SECRET_PATTERNS committed; schemas/__init__.py updated with re-exports; all behavior tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test_schemas.py covering SCHEMA-01 and SCHEMA-03</name>
  <files>
    packages/atlas-core/tests/test_schemas.py
  </files>
  <read_first>
    - packages/atlas-core/atlas_core/schemas/core.py (just written — read to know exact field names before writing tests)
    - packages/atlas-core/tests/conftest.py (fixture signatures to reuse)
    - .planning/phases/02-core-schemas/02-RESEARCH.md (Validation Architecture / Phase Requirements → Test Map; test naming convention)
    - .planning/phases/02-core-schemas/02-PATTERNS.md (test file pattern from Hermes analog section)
  </read_first>
  <behavior>
    - test_import: "from atlas_core.schemas.core import Mission" does not raise ImportError
    - test_all_models_importable: importing all 7 model names from atlas_core.schemas.core does not raise
    - test_model_instantiation_mission: Mission(title="hello") produces instance with id (non-empty string), status == "pending", intent == ""
    - test_model_instantiation_audit_event: AuditEvent(run_id="r1", event_type="llm_call") produces instance with data == "{}", timestamp is a datetime
    - test_serialization_no_datetime_objects: Mission(title="t").model_dump() — isinstance check on created_at value must be str, not datetime
    - test_serialization_json_safe: json.dumps(Mission(title="t").model_dump()) does not raise TypeError
    - test_serialization_no_dict_types: AuditEvent(run_id="r", event_type="llm_call").model_dump()["data"] is an instance of str
    - test_path_is_str: Artifact(run_id="r", path="/tmp/f", artifact_type="file_write").model_dump()["path"] is an instance of str (not Path)
    - test_json_schema_valid_mission: Mission.model_json_schema() returns dict containing "properties" key
    - test_json_schema_all_fields_present: set(Mission.model_json_schema()["properties"].keys()) == {"id", "title", "intent", "status", "project", "created_at", "updated_at"}
    - test_json_schema_status_enum: Mission.model_json_schema()["properties"]["status"]["enum"] contains "pending"
    - test_json_schema_all_models: model_json_schema() call on each of the 7 models returns a dict with "properties"
    - test_secret_patterns: SECRET_PATTERNS is a tuple of length 2; each element has a .match method
    - test_frozen_model: attempting to mutate Mission(title="t").title raises an exception (ValidationError or TypeError)
  </behavior>
  <action>
    Write packages/atlas-core/tests/test_schemas.py.

    File structure:
    - Module docstring: """Tests for atlas_core.schemas.core — SCHEMA-01 (import, instantiation, serialization) and SCHEMA-03 (model_json_schema)."""
    - Import section: import json, datetime, pytest, and any schema imports needed per test
    - Group tests into logical blocks with comments: # --- Import tests --- / # --- Instantiation tests --- / # --- Serialization tests --- / # --- JSON Schema tests ---
    - Use the test naming convention from Hermes analog: test_<subject>_<condition>

    Implement every test listed in the <behavior> block above. Each test must be a standalone function with a single, clear assert or try/except. Do not bundle multiple unrelated assertions into one test function — each behavior item maps to one test function.

    For test_frozen_model: use pytest.raises(Exception) since the exact exception type (ValidationError vs TypeError) depends on pydantic internals — catching Exception is acceptable here.

    Run the full test file with:
      packages/atlas-core/.venv/Scripts/python.exe -m pytest packages/atlas-core/tests/test_schemas.py -v

    All tests must pass (green). Fix any failures before marking done.
  </action>
  <verify>
    <automated>
      packages/atlas-core/.venv/Scripts/python.exe -m pytest packages/atlas-core/tests/test_schemas.py -v --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/atlas-core/tests/test_schemas.py exists
    - pytest exits with code 0 (all tests pass)
    - At minimum 13 test functions present (one per behavior item above)
    - test_json_schema_all_fields_present asserts the exact set {"id", "title", "intent", "status", "project", "created_at", "updated_at"}
    - test_json_schema_status_enum asserts "pending" is in the enum array
    - test_serialization_no_datetime_objects asserts created_at value in model_dump() is isinstance str
    - No test uses subjective checks like "looks correct" — all asserts are isinstance, ==, in, or pytest.raises
  </acceptance_criteria>
  <done>test_schemas.py written; pytest -v reports all tests passing; SCHEMA-01 and SCHEMA-03 verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external input → AuditEvent.data | Phase 4 will write LLM/tool output into AuditEvent.data; secrets may be present |
| model_dump() → SQLite TEXT | Serialized data must be str (ISO 8601 for datetimes, JSON str for payloads) to insert cleanly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | AuditEvent.data field carries secrets | mitigate | SECRET_PATTERNS constant defined in core.py; Phase 4 must apply redaction before populating data field; boundary documented |
| T-02-05 | Tampering | Schema drift — model field added without DDL update | mitigate | test_column_names_match_fields in plan 02-03 enforces 1:1 mapping at test time |
| T-02-06 | Tampering | dict[str, Any] leaks into JSON Schema (breaks TS/Rust codegen) | mitigate | D-013 rule enforced; test_serialization_no_dict_types asserts data field is str; grep gate in acceptance_criteria catches any dict[str, Any] in source |
| T-02-07 | Tampering | pathlib.Path in model field breaks cross-platform serialization | mitigate | test_path_is_str asserts Artifact.path is str; grep gate in Task 1 acceptance_criteria catches ": Path" field definitions |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — atlas-core[dev] installed in plan 01; all 4 packages slopcheck [OK] |
</threat_model>

<verification>
After both tasks complete:

1. packages/atlas-core/.venv/Scripts/python.exe -c "from atlas_core.schemas.core import Mission; print(Mission.model_json_schema()['properties'].keys())" — output contains id, title, intent, status, project, created_at, updated_at
2. packages/atlas-core/.venv/Scripts/python.exe -m pytest packages/atlas-core/tests/test_schemas.py -v — exits 0, all tests green
3. grep -c "dict\[str, Any\]" packages/atlas-core/atlas_core/schemas/core.py — output is 0
4. grep -c "pathlib" packages/atlas-core/atlas_core/schemas/core.py — output is 0 (no Path imports in model file)
</verification>

<success_criteria>
- All 7 models importable from atlas_core.schemas.core
- model_json_schema() on all 7 models returns valid JSON Schema (verified by test)
- model_dump() returns JSON-serializable dict with ISO 8601 strings for all datetime fields (verified by test)
- No dict[str, Any] or pathlib.Path in model fields (verified by grep + test)
- pytest packages/atlas-core/tests/test_schemas.py exits 0
</success_criteria>

<output>
Create .planning/phases/02-core-schemas/02-02-SUMMARY.md when done
</output>
