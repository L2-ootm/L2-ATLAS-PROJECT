---
phase: 02-core-schemas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/atlas-core/pyproject.toml
  - packages/atlas-core/atlas_core/__init__.py
  - packages/atlas-core/atlas_core/schemas/__init__.py
  - packages/atlas-core/tests/__init__.py
  - packages/atlas-core/tests/conftest.py
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03

must_haves:
  truths:
    - "atlas-core package directory uses atlas_core/ (not src/) per D-011"
    - "pyproject.toml specifies hatchling backend, pydantic>=2.0 dep, pytest+ruff dev extras"
    - "uv venv created at packages/atlas-core/.venv targeting Python 3.11"
    - "pip install -e packages/atlas-core[dev] installs into atlas-core venv, not Hermes venv"
    - "from atlas_core import __version__ succeeds in the atlas-core venv"
    - "conftest.py db fixture applies 0001_core.sql on :memory: SQLite with WAL + FK on"
  artifacts:
    - path: "packages/atlas-core/pyproject.toml"
      provides: "hatchling build config, pydantic dep, pytest+ruff dev extras, pytest testpaths"
      contains: "[tool.hatch.build.targets.wheel]"
    - path: "packages/atlas-core/atlas_core/__init__.py"
      provides: "package version marker"
      contains: "__version__"
    - path: "packages/atlas-core/atlas_core/schemas/__init__.py"
      provides: "re-exports placeholder (updated in plan 02)"
    - path: "packages/atlas-core/tests/conftest.py"
      provides: ":memory: SQLite fixture for migration tests"
      contains: "PRAGMA journal_mode=WAL"
  key_links:
    - from: "packages/atlas-core/pyproject.toml"
      to: "packages/atlas-core/atlas_core/"
      via: "[tool.hatch.build.targets.wheel] packages = [\"atlas_core\"]"
      pattern: "packages.*atlas_core"
    - from: "packages/atlas-core/.venv"
      to: "packages/atlas-core/atlas_core/"
      via: "uv pip install -e packages/atlas-core[dev] --python packages/atlas-core/.venv/Scripts/python.exe"
      pattern: "editable install targets .venv not Hermes venv"
---

<objective>
Bootstrap the atlas-core Python package: create the directory tree, write pyproject.toml with hatchling backend, create package __init__.py files, set up a project-scoped venv, install the package in editable mode with dev extras, and write the pytest conftest with the :memory: SQLite migration fixture.

Purpose: All downstream plans (02-02, 02-03) require an installable atlas-core package in a correctly targeted venv. Without this wave, editable installs silently target the Hermes venv and imports fail.

Output: Installable Python package scaffold with a working :memory: test fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-schemas/02-RESEARCH.md
@.planning/phases/02-core-schemas/02-PATTERNS.md

<interfaces>
<!-- Key constraints the executor must honor. Extracted from locked decisions. -->

From docs/decisions/2026-06-04_D011_repo_layout.md:
  Schemas live at: packages/atlas-core/atlas_core/schemas/
  (NOT packages/atlas-core/src/schemas/ — the src/ stub exists and must NOT be used)

From docs/decisions/D-013-language-strategy.md:
  Build backend: hatchling (setuptools not confirmed in uv env)
  Dependencies: pydantic>=2.0 only in [project.dependencies]
  Dev-only: pytest>=9.0, ruff>=0.15 in [project.optional-dependencies.dev]

From 02-RESEARCH.md (Environment Availability):
  uv 0.11.16 available
  Python 3.11.15 available
  Target venv: packages/atlas-core/.venv (NOT C:\Users\Davi\AppData\Local\hermes\hermes-agent\venv)
  Install command: uv venv packages/atlas-core/.venv --python 3.11
                   uv pip install -e "packages/atlas-core[dev]" --python packages/atlas-core/.venv/Scripts/python.exe

From 02-RESEARCH.md (Validation Architecture / Wave 0 Gaps):
  conftest.py must:
    1. connect to :memory: SQLite
    2. execute PRAGMA journal_mode=WAL
    3. execute PRAGMA foreign_keys = ON
    4. read infra/migrations/0001_core.sql and executescript() it
    5. yield connection, then close

  MIGRATION_PATH must be computed relative to conftest.py:
    pathlib.Path(__file__).parent.parent.parent.parent / "infra" / "migrations" / "0001_core.sql"
    (conftest.py is at packages/atlas-core/tests/ — 4 parents up = project root)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create atlas-core package tree and pyproject.toml</name>
  <files>
    packages/atlas-core/pyproject.toml
    packages/atlas-core/atlas_core/__init__.py
    packages/atlas-core/atlas_core/schemas/__init__.py
    packages/atlas-core/tests/__init__.py
  </files>
  <read_first>
    - packages/atlas-core/ (entire directory tree — currently has only src/schemas/ stub)
    - docs/decisions/2026-06-04_D011_repo_layout.md (canonical paths)
    - docs/decisions/D-013-language-strategy.md (dependency budget, hatchling requirement)
    - .planning/phases/02-core-schemas/02-RESEARCH.md (pyproject.toml pattern in Code Examples section)
    - .planning/phases/02-core-schemas/02-PATTERNS.md (pyproject.toml pattern and __init__.py patterns)
  </read_first>
  <action>
    Create the Python package tree under packages/atlas-core/. The directory packages/atlas-core/src/schemas/ exists as a stub from an earlier plan — do NOT put any files there; all new files go into atlas_core/ (Python package convention per D-011).

    Write packages/atlas-core/pyproject.toml with:
    - [build-system]: requires = ["hatchling"], build-backend = "hatchling.build"
    - [project]: name = "atlas-core", version = "0.1.0", description = "ATLAS domain schemas and shared contracts", requires-python = ">=3.11", dependencies = ["pydantic>=2.0"]
    - [project.optional-dependencies]: dev = ["pytest>=9.0", "ruff>=0.15"]
    - [tool.hatch.build.targets.wheel]: packages = ["atlas_core"]
    - [tool.pytest.ini_options]: testpaths = ["tests"]
    - [tool.ruff]: target-version = "py311", line-length = 100
    - [tool.ruff.lint]: select = ["E", "F", "I", "UP", "ANN"]

    Write packages/atlas-core/atlas_core/__init__.py:
    - Module docstring: """ATLAS core domain schemas and shared contracts."""
    - Single line: __version__ = "0.1.0"
    - Nothing else — models are exported from schemas/__init__.py only

    Write packages/atlas-core/atlas_core/schemas/__init__.py:
    - Module docstring: """Atlas-core domain schemas — public API."""
    - Placeholder comment: # Re-exports added in plan 02-02 when core.py is written
    - __all__ = [] (empty for now; plan 02-02 fills this)

    Write packages/atlas-core/tests/__init__.py:
    - Empty file (pytest discovery requires it)
  </action>
  <verify>
    <automated>
      python -c "import tomllib; d = tomllib.loads(open('packages/atlas-core/pyproject.toml').read()); assert d['build-system']['build-backend'] == 'hatchling.build'; assert 'pydantic>=2.0' in d['project']['dependencies']; assert d['tool']['hatch']['build']['targets']['wheel']['packages'] == ['atlas_core']; print('pyproject.toml valid')"
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/atlas-core/pyproject.toml exists and contains [tool.hatch.build.targets.wheel]
    - packages/atlas-core/pyproject.toml contains packages = ["atlas_core"]
    - packages/atlas-core/pyproject.toml contains [tool.pytest.ini_options] with testpaths = ["tests"]
    - packages/atlas-core/atlas_core/__init__.py contains __version__ = "0.1.0"
    - packages/atlas-core/atlas_core/schemas/__init__.py exists (may have empty __all__)
    - packages/atlas-core/tests/__init__.py exists (empty)
    - packages/atlas-core/src/ directory left untouched (not used)
  </acceptance_criteria>
  <done>pyproject.toml written with hatchling backend; all four __init__.py files created; no files written into src/</done>
</task>

<task type="auto">
  <name>Task 2: Create venv, install editable, write conftest.py</name>
  <files>
    packages/atlas-core/.venv (created by uv)
    packages/atlas-core/tests/conftest.py
  </files>
  <read_first>
    - packages/atlas-core/pyproject.toml (must exist from Task 1 before this runs)
    - .planning/phases/02-core-schemas/02-RESEARCH.md (Pitfall 6: uv environment trap; Code Examples: pytest fixture; Validation Architecture section)
    - .planning/phases/02-core-schemas/02-PATTERNS.md (test fixture pattern from Hermes analog)
  </read_first>
  <action>
    Create the project-scoped venv and install atlas-core in editable mode.

    Run in order (PowerShell-compatible):
    1. uv venv packages/atlas-core/.venv --python 3.11
    2. uv pip install -e "packages/atlas-core[dev]" --python packages/atlas-core/.venv/Scripts/python.exe

    Verify the install targeted the atlas-core venv, NOT the Hermes venv at C:\Users\Davi\AppData\Local\hermes\hermes-agent\venv. Check by running:
      packages/atlas-core/.venv/Scripts/python.exe -c "import atlas_core; print(atlas_core.__version__)"
    Expected output: 0.1.0

    Write packages/atlas-core/tests/conftest.py:
    - Import sqlite3, pathlib, pytest
    - Define MIGRATION_PATH = pathlib.Path(__file__).parent.parent.parent.parent / "infra" / "migrations" / "0001_core.sql"
    - Define a @pytest.fixture(name="db") that:
        * opens sqlite3.connect(":memory:")
        * executes PRAGMA journal_mode=WAL
        * executes PRAGMA foreign_keys = ON
        * reads MIGRATION_PATH.read_text(encoding="utf-8") and calls conn.executescript(sql) IF the migration file exists, otherwise skips (migration file is written in plan 02-03)
        * yields conn
        * calls conn.close() in teardown
    - Include a module-level comment explaining the MIGRATION_PATH depth: "4 parents up from tests/ = project root"
    - The fixture must NOT raise an error when 0001_core.sql does not yet exist — use: if MIGRATION_PATH.exists(): conn.executescript(...)
  </action>
  <verify>
    <automated>
      packages/atlas-core/.venv/Scripts/python.exe -c "import atlas_core; print(atlas_core.__version__)"
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/atlas-core/.venv/ directory exists with Scripts/python.exe present
    - Running packages/atlas-core/.venv/Scripts/python.exe -c "import atlas_core; print(atlas_core.__version__)" outputs "0.1.0" without error
    - packages/atlas-core/tests/conftest.py exists
    - conftest.py contains PRAGMA journal_mode=WAL and PRAGMA foreign_keys = ON
    - conftest.py MIGRATION_PATH resolves to infra/migrations/0001_core.sql (4 parent hops from tests/)
    - conftest.py fixture does not raise FileNotFoundError when 0001_core.sql is absent
    - The installed atlas_core is NOT present in C:\Users\Davi\AppData\Local\hermes\hermes-agent\venv (verify by checking that the Hermes venv site-packages does not contain atlas_core after this step)
  </acceptance_criteria>
  <done>atlas-core venv created at packages/atlas-core/.venv; editable install confirmed importable; conftest.py written with guarded migration fixture</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uv → local filesystem | uv creates venv and installs packages from PyPI; package legitimacy must be verified |
| pyproject.toml → installed package | hatchling reads pyproject.toml to determine which directories are included in the wheel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | uv pip install targets wrong venv | mitigate | Explicitly pass --python packages/atlas-core/.venv/Scripts/python.exe; verify import succeeds in atlas-core venv |
| T-02-02 | Tampering | pyproject.toml packages = wrong path | mitigate | packages = ["atlas_core"] (not "src" or "atlas-core"); verified by import test |
| T-02-03 | Information Disclosure | test conftest.py reads migration SQL from filesystem | accept | Migration SQL is non-sensitive DDL; no secrets in schema files |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Package legitimacy audit in 02-RESEARCH.md — all 4 packages [OK] by slopcheck 0.6.1; no [ASSUMED] or [SUS] packages |
</threat_model>

<verification>
After both tasks complete:

1. packages/atlas-core/pyproject.toml exists with hatchling backend and packages = ["atlas_core"]
2. packages/atlas-core/.venv/Scripts/python.exe -c "import atlas_core; print(atlas_core.__version__)" outputs "0.1.0"
3. packages/atlas-core/tests/conftest.py contains PRAGMA journal_mode=WAL and PRAGMA foreign_keys = ON
4. packages/atlas-core/src/ directory is untouched (no new files added there)
</verification>

<success_criteria>
- atlas-core is importable from packages/atlas-core/.venv Python interpreter
- pyproject.toml uses hatchling backend with packages = ["atlas_core"] (not src/)
- conftest.py db fixture handles absent migration file gracefully
- No pollution of Hermes venv
</success_criteria>

<output>
Create .planning/phases/02-core-schemas/02-01-SUMMARY.md when done
</output>
